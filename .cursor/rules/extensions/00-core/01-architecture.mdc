---
alwaysApply: ${cursor.file.path.includes("layer/") || cursor.file.path.includes("image/")}
---


# Правило 01: Архитектурные принципы расширений rpi-image-gen

## Назначение правила
Определяет фундаментальные архитектурные принципы разработки расширений rpi-image-gen.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Core Concepts rpi-image-gen

### Основные концепции
Согласно официальной документации rpi-image-gen использует три ключевые концепции:

#### 1. **Configuration** - Декларативные переменные и атрибуты сборки
- Простые файлы для чтения и понимания
- Переменные окружения с префиксами IGconf_*
- Валидация и проверка типов

#### 2. **Layers** - Модульные, компонуемые компоненты
- Автоматическая проверка переменных
- Политики валидации
- Полная документация для каждого слоя
- Метаданные X-Env-* для описания

#### 3. **Hooks** - Скрипты, выполняющиеся на определенных этапах сборки
- Shell скрипты для интеграции
- Определенные точки выполнения (setup, customize, cleanup)
- Возможность модификации поведения сборки

### Архитектура системы сборки

#### Компоненты системы сборки
```yaml
rpi-image-gen использует:
├── bdebstrap     - модульное применение слоев
├── mmdebstrap    - создание файловой системы
├── genimage      - генерация образов дисков
├── podman unshare - безопасное выполнение без root
└── YAML          - конфигурационные файлы
```

#### Поддерживаемые платформы
Согласно официальной документации rpi-image-gen:

##### Основная платформа
- **Raspberry Pi OS** - оптимизировано для работы на Raspberry Pi OS
- **Debian Bookworm arm64** - рекомендуемая среда разработки
- **64-bit архитектура** - предпочтительно для лучших результатов

##### Альтернативные платформы
- **x86_64** - поддержка через QEMU эмуляцию
- **Контейнерные среды** - поддержка изолированных окружений
- **Non-arm64 платформы** - ограниченная поддержка, без гарантий

#### Этапы сборки (5 стадий)
1. **Parameter Assembly** - Сборка параметров и валидация
2. **Layer Processing** - Обработка слоев и разрешение зависимостей
3. **Build Preparation** - Подготовка окружения и bdebstrap конфигурации
4. **Filesystem Generation** - Создание ФС + хуки + overlay
5. **Image Generation** - Создание финального образа

### Архитектурные принципы

### Модульность и разделение ответственности

#### Принцип единственной ответственности
```yaml
# ❌ НЕПРАВИЛЬНО - слой делает слишком много вещей
# my-monolith-layer.yaml
mmdebstrap:
  packages:
    - nginx apache2 mysql-server php  # Разные сервисы в одном слое
    - docker.io podman                # Контейнеризация
    - monitoring tools...            # Мониторинг
  customize-hooks:
    - |  # Слишком много логики в одном месте
      setup_web_servers
      configure_database
      setup_containers
      configure_monitoring

# ✅ ПРАВИЛЬНО - разделение на специализированные слои
# web-server-layer.yaml - только веб-сервер
# database-layer.yaml - только база данных
# container-layer.yaml - только контейнеризация
# monitoring-layer.yaml - только мониторинг
```

#### Модульная архитектура
```
extension/
├── layer/
│   ├── base-setup.yaml       # Базовая настройка
│   ├── web-server.yaml       # Веб-сервер
│   ├── database.yaml         # База данных
│   ├── monitoring.yaml       # Мониторинг
│   └── security.yaml         # Безопасность
├── config/
│   ├── development.yaml      # Конфигурация разработки
│   ├── staging.yaml          # Конфигурация тестирования
│   └── production.yaml       # Продакшн конфигурация
└── hooks/
    ├── setup/                # Хуки настройки
    ├── customize/            # Хуки кастомизации
    └── cleanup/              # Хуки очистки
```

### Независимость и переиспользуемость

#### Независимые компоненты
```yaml
# Слой должен быть самодостаточным
# METABEGIN
# X-Env-Layer-Name: nginx-web-server
# X-Env-Layer-Requires: essential,net-misc  # Минимальные зависимости
# X-Env-VarPrefix: web
# METAEND

# ✅ Хорошо - минимальные зависимости, четкая ответственность
```

#### Переиспользуемые шаблоны
```yaml
# Шаблон для создания веб-приложений
# web-app-template.yaml
mmdebstrap:
  packages:
    - nginx
    - ${IGconf_web_app_packages:-}
  customize-hooks:
    - |
      # Конфигурируемый веб-сервер
      setup_nginx "${IGconf_web_domain:-localhost}" "${IGconf_web_port:-80}"
      deploy_app "${IGconf_web_app_path:-/var/www/html}"
```

### Иерархическая организация

#### Уровни абстракции
```
┌─────────────────────────────────────────┐
│ 4. Прикладные расширения (Application)  │
│    - Конкретные приложения и сервисы    │
├─────────────────────────────────────────┤
│ 3. Инфраструктурные расширения (Infra)  │
│    - Мониторинг, безопасность, сеть     │
├─────────────────────────────────────────┤
│ 2. Платформенные расширения (Platform)  │
│    - Базовые сервисы и инструменты      │
├─────────────────────────────────────────┤
│ 1. Системные расширения (System)        │
│    - Базовая ОС и драйверы              │
└─────────────────────────────────────────┘
```

#### Зависимости между уровнями
```yaml
# Системный уровень - минимальные зависимости
layer:
  base: bookworm-minbase

# Платформенный уровень - зависит от системного
layer:
  base: bookworm-minbase
  platform: essential-services

# Инфраструктурный уровень - зависит от платформенного
layer:
  base: bookworm-minbase
  platform: essential-services
  infra: monitoring-suite

# Прикладной уровень - зависит от всех нижних
layer:
  base: bookworm-minbase
  platform: essential-services
  infra: monitoring-suite
  app: my-web-application
```

### Конфигурационная иерархия

#### Переопределение настроек
```yaml
# Базовая конфигурация (в слое)
# X-Env-Var-loglevel: info
# X-Env-Var-port: 8080

# Переопределение в конфигурации образа
layer:
  app: my-app
  # Переопределяем настройки через переменные окружения
  IGconf_app_loglevel=debug
  IGconf_app_port=9000
```

#### Наследование конфигураций
```yaml
# development.yaml наследует от base.yaml
# base.yaml
device:
  layer: pi5
image:
  layer: image-rpios
layer:
  base: bookworm-minbase

# development.yaml
<<: *base_config  # Наследование
layer:
  <<: *base_layer
  debug: debug-tools  # Дополнительные слои
```

## Типы расширений и их архитектура

### Device Extensions (Расширения устройств)
```yaml
# Специфичны для конкретного оборудования
# METABEGIN
# X-Env-Layer-Name: pi5-optimization
# X-Env-Layer-Category: device
# X-Env-Layer-Description: Оптимизации для Raspberry Pi 5
# METAEND

mmdebstrap:
  packages:
    - raspberrypi-bootloader-pi5
    - linux-firmware-raspi
  customize-hooks:
    - |
      # Настройка специфичная для Pi 5
      configure_pi5_hardware
      optimize_pi5_performance
```

### Application Extensions (Расширения приложений)
```yaml
# Контейнеризованные приложения
# METABEGIN
# X-Env-Layer-Name: web-application
# X-Env-Layer-Category: extension
# METAEND

mmdebstrap:
  packages:
    - docker.io
    - docker-compose
  customize-hooks:
    - |
      # Развертывание приложения в контейнерах
      deploy_container_app
      configure_reverse_proxy
```

### Infrastructure Extensions (Инфраструктурные расширения)
```yaml
# Системные сервисы и инструменты
# METABEGIN
# X-Env-Layer-Name: monitoring-stack
# X-Env-Layer-Category: extension
# METAEND

mmdebstrap:
  packages:
    - prometheus
    - grafana
    - node-exporter
  customize-hooks:
    - |
      # Настройка стека мониторинга
      configure_prometheus
      configure_grafana
      setup_alerting
```

## Принципы совместимости

### Версионирование API
```yaml
# Семантическое версионирование
# X-Env-Layer-Version: 1.2.3
# MAJOR.MINOR.PATCH

# 1.2.3 -> 1.3.0 - новая функциональность, обратная совместимость
# 1.2.3 -> 2.0.0 - breaking changes
# 1.2.3 -> 1.2.4 - bug fixes
```

### Обратная совместимость
```yaml
# Поддержка старых версий API
customize-hooks:
  - |
    # Проверка версии и адаптация
    if [[ "${IGconf_api_version:-v1}" == "v1" ]]; then
      # Старый API
      legacy_configuration
    else
      # Новый API
      modern_configuration
    fi
```

## Архитектурные паттерны

### Strategy Pattern (Шаблон стратегия)
```bash
# Разные стратегии для разных сценариев
configure_database() {
  case "${IGconf_db_type:-sqlite}" in
    "postgresql")
      configure_postgresql
      ;;
    "mysql")
      configure_mysql
      ;;
    "sqlite"|*)
      configure_sqlite
      ;;
  esac
}
```

### Builder Pattern (Шаблон строитель)
```bash
# Построение конфигурации по шагам
build_application() {
  setup_environment
  install_dependencies
  configure_services
  deploy_application
  verify_deployment
}

setup_environment() {
  # Шаг 1: Подготовка окружения
}

install_dependencies() {
  # Шаг 2: Установка зависимостей
}

configure_services() {
  # Шаг 3: Настройка сервисов
}

deploy_application() {
  # Шаг 4: Развертывание приложения
}

verify_deployment() {
  # Шаг 5: Проверка развертывания
}
```

### Facade Pattern (Шаблон фасад)
```bash
# Упрощенный интерфейс для сложных операций
deploy_full_stack_app() {
  # Скрываем сложность за простым интерфейсом
  deploy_database
  deploy_backend
  deploy_frontend
  configure_networking
  setup_monitoring
}
```

## Качество архитектуры

### Метрики качества
- **Связность (Cohesion)**: модули должны иметь четкую ответственность
- **Связанность (Coupling)**: минимальные зависимости между модулями
- **Удобство поддержки**: легкость модификации и расширения
- **Переиспользуемость**: возможность использования в разных контекстах

### Антипаттерны (что избегать)
```bash
# ❌ God Object - один слой делает все
# ❌ Tight Coupling - сильная зависимость между компонентами
# ❌ Shotgun Surgery - изменение требует правок в многих местах
# ❌ Feature Envy - класс использует данные другого класса
```

## Архитектурное ревью

### Контрольные вопросы
- [ ] Является ли архитектура модульной?
- [ ] Есть ли четкое разделение ответственности?
- [ ] Минимальны ли зависимости между компонентами?
- [ ] Легко ли тестировать отдельные компоненты?
- [ ] Возможна ли переиспользуемость компонентов?
- [ ] Поддерживается ли обратная совместимость?

### Архитектурные решения
```yaml
# Документирование архитектурных решений
# ADR (Architecture Decision Records)

# Title: Выбор контейнеризации для приложений
# Status: Accepted
# Context: Нужно изолировать приложения в системе
# Decision: Использовать Docker для контейнеризации
# Consequences: Увеличение размера образа, но лучшая изоляция
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано на основе архитектурных принципов разработки расширений rpi-image-gen