---
alwaysApply: ${cursor.file.path.includes("layer/") || cursor.file.path.includes("config/") || cursor.file.path.includes("image/")}
---

# Правило 01-2: Архитектура системы сборки rpi-image-gen

## Назначение правила
Определяет принципы работы с системой сборки rpi-image-gen, включая bdebstrap, mmdebstrap, genimage и архитектуру выполнения. Устанавливает стандарты для создания и организации конфигурационных файлов.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Архитектура системы сборки

### Основные компоненты
Согласно официальной документации rpi-image-gen использует следующую архитектуру:

#### 1. **bdebstrap** - Модульное применение слоев
- **Назначение**: Агрегирует и объединяет конфигурации слоев
- **Вход**: YAML файлы слоев с метаданными X-Env-*
- **Выход**: Единая конфигурация для mmdebstrap
- **Особенности**: Поддерживает зависимости между слоями
- **Алгоритм работы**: Читает все слои, разрешает зависимости, объединяет конфигурации
- **Оптимизации**: Кэширование результатов, параллельная обработка независимых слоев
- **Отладка**: `--verbose` режим показывает порядок применения слоев

#### 2. **mmdebstrap** - Создание файловой системы
- **Назначение**: Создает корневую файловую систему
- **Вход**: Конфигурация от bdebstrap + репозитории Debian
- **Выход**: Корневая ФС в целевом каталоге
- **Особенности**: Без root прав, использует namespaces

#### 3. **genimage** - Генерация образов дисков
- **Назначение**: Создает загрузочные образы дисков
- **Вход**: Файловая система + конфигурация разделов
- **Выход**: .img файлы для записи на носители
- **Особенности**: Поддержка различных форматов разделов (MBR, GPT, hybrid)
- **Алгоритм**: Анализирует конфигурацию разделов, создает layout, копирует файлы
- **Форматы образов**: raw, qcow2, vmdk, tar для различных платформ
- **Оптимизации**: Sparse файлы, compression, разделение на части
- **Интеграция**: С rpi-sb-provisioner для signed boot и encrypted storage

#### 4. **podman unshare** - Безопасное выполнение
- **Назначение**: Обработка прав доступа к файлам
- **Особенности**: Linux namespaces для корректного chown
- **Безопасность**: Позволяет работать без root привилегий
- **Альтернативы**: bubblewrap, unshare для других дистрибутивов

### Этапы сборки

#### Этап 1: Parameter Assembly (Сборка параметров)
```bash
# Валидация входных аргументов
# Парсинг конфигурационных файлов
# Установление начального окружения
rpi-image-gen build -c config.yaml -S /source/dir
```

#### Этап 2: Layer Processing (Обработка слоев)
```bash
# Сбор слоев из указанных директорий
# Валидация зависимостей
# Разрешение порядка применения слоев
# Расширение переменных окружения
```

#### Этап 3: Build Preparation (Подготовка сборки)
```bash
# Настройка окружения
# Генерация конфигурации bdebstrap
# Проверка доступности ресурсов
```

#### Этап 4: Filesystem Generation (Генерация ФС)
```bash
# Создание ФС через mmdebstrap
# Выполнение хуков (setup, customize, cleanup)
# Применение rootfs-overlay
# Генерация SBOM
```

#### Этап 5: Image Generation (Генерация образа)
```bash
# Создание образа через genimage
# Применение пост-обработки
# Финализация образа
```

### Структура конфигурации

#### Рекомендуемая структура конфигурационного файла
```yaml
# Полная структура конфигурации согласно обновленным правилам
---

# ============================================================================
# ПЕРЕМЕННЫЕ СЛОЕВ (вынесены из блока layer для лучшей читаемости)
# ============================================================================

# Настройки слоев в короткой форме (без префиксов IGconf_)
sysctl_kernel:
  ptrace_restrict: y
  aslr_enable: y
  kptr_restrict: 1

apparmor_core:
  enable: y
  mode: enforce
  auditd: y

# ... остальные переменные слоев

# ============================================================================
# КОНФИГУРАЦИЯ УСТРОЙСТВА И ОБРАЗА
# ============================================================================

device:                   # Конфигурация устройства
  layer: pi5             # Слой устройства
  hostname: my-device    # Имя хоста

image:                    # Конфигурация образа
  layer: hybrid-raid-luks # Слой образа
  boot_part_size: 200%   # Размер boot раздела
  root_part_size: 400%   # Размер root раздела
  name: my-custom-image  # Имя образа

# ============================================================================
# СЛОИ СИСТЕМЫ (только ссылки на слои)
# ============================================================================

layer:                    # Слои расширения
  base: bookworm-minbase # Базовый слой
  security: apparmor-core # Слой безопасности
  firewall: ufw-core-basic # Файрвол
  # ... дополнительные слои
```

#### Принципы организации конфигурации

##### 1. **Разделение на блоки**
- **Блок переменных**: Все настройки слоев в начале файла
- **Блок device/image**: Конфигурация устройства и образа
- **Блок layer**: Только ссылки на слои (без переменных)

##### 2. **Короткие имена переменных**
- Использовать короткие имена без префиксов `IGconf_`
- rpi-image-gen автоматически добавляет правильные префиксы
- Пример: `ptrace_restrict: y` → `IGconf_sysctl_kernel_ptrace_restrict=y`

##### 3. **Иерархическая организация слоев**
```yaml
# Группировка по уровням абстракции
layer:
  # Уровень 1: Базовая система
  base: bookworm-minbase
  build_base: sys-build-base

  # Уровень 2: Инфраструктура
  sysctl_kernel: sysctl-kernel
  firewall: ufw-core-basic

  # Уровень 3: Сервисы безопасности
  security: apparmor-core
  audit: auditd-core

  # Уровень 4: Приложения
  containers: docker-debian-bookworm
```

### Система переменных окружения

#### Правила именования переменных
```bash
# В конфигурационном файле - короткие имена (рекомендуется)
sysctl_kernel:
  ptrace_restrict: y        # → IGconf_sysctl_kernel_ptrace_restrict=y

apparmor_core:
  mode: enforce             # → IGconf_apparmor_core_mode=enforce

# Полные имена IGconf_* используются только при переопределении
IGconf_device_class=pi5
IGconf_device_hostname=my-rpi
IGconf_image_compression=zstd

# Передача через командную строку
rpi-image-gen build -c config.yaml -- IGconf_device_class=pi5 IGconf_image_compression=gzip
```

#### Источники переменных (по приоритету)
1. **Командная строка** (высший приоритет)
2. **Переменные окружения shell**
3. **Конфигурационный файл** (короткие имена автоматически преобразуются)
4. **Значения по умолчанию из метаданных слоя**

#### Преобразование коротких имен
rpi-image-gen автоматически преобразует переменные из конфигурационного файла:
```yaml
sysctl_kernel:
  ptrace_restrict: y
  aslr_enable: y
```
становится:
```bash
IGconf_sysctl_kernel_ptrace_restrict=y
IGconf_sysctl_kernel_aslr_enable=y
```

### Создание конфигураций

#### Шаги создания полной конфигурации

##### 1. **Определение требований**
- Определить целевое устройство (Raspberry Pi 5, Radxa и т.д.)
- Выбрать базовую систему (Debian Bookworm/Trixie)
- Определить требуемые функции (безопасность, контейнеры, мониторинг)

##### 2. **Выбор и настройка слоев**
```bash
# Посмотреть доступные слои
./rpi-image-gen layer --list

# Изучить переменные конкретного слоя
./rpi-image-gen layer --describe apparmor-core
```

##### 3. **Создание структуры конфигурации**
```yaml
# 1. Переменные слоев (в начале файла)
sysctl_kernel:
  ptrace_restrict: y
  aslr_enable: y

apparmor_core:
  enable: y
  mode: enforce

# 2. Конфигурация устройства и образа
device:
  layer: pi5
  hostname: my-device

image:
  layer: hybrid-raid-luks
  boot_part_size: 200%
  root_part_size: 400%

# 3. Слои системы
layer:
  base: bookworm-minbase
  security: apparmor-core
```

##### 4. **Тестирование и валидация**
```bash
# Валидация конфигурации
./rpi-image-gen build -c config.yaml --dry-run

# Тест сборки filesystem
./rpi-image-gen build -c config.yaml -f -- IGconf_sys_apt_keydir=/usr/share/keyrings
```

##### 5. **Оптимизация и настройка**
- Настроить переменные для конкретного использования
- Проверить зависимости между слоями
- Оптимизировать размеры разделов

#### Лучшие практики создания конфигураций

##### Организация по уровням абстракции
```yaml
layer:
  # Уровень 1: Базовая система
  base: bookworm-minbase
  build_base: sys-build-base

  # Уровень 2: Системная инфраструктура
  kernel_security: sysctl-kernel
  system_security: sysctl-security
  firewall: ufw-core-basic

  # Уровень 3: Сервисы и безопасность
  authentication: pam-core
  access_control: apparmor-core
  intrusion_detection: fail2ban-core

  # Уровень 4: Прикладные сервисы
  containers: docker-debian-bookworm
  development: distrobox

  # Уровень 5: Специфично для устройства
  device_optimization: rpi5-server-config
  hardware_support: radxa-sata-penta-hat
```

##### Группировка переменных
```yaml
# Группировать переменные по функциональности
security_settings:
  apparmor_core:
    enable: y
    mode: enforce
  fail2ban_core:
    enable: y
    loglevel: INFO

container_settings:
  docker:
    trust_user1: n
  distrobox:
    enable_host_docker: y
    container_name: development
```

##### Документирование конфигураций
```yaml
# Всегда добавлять комментарии
# ============================================================================
# ПРОДАКШН КОНФИГУРАЦИЯ RASPBERRY PI 5 С ПОЛНОЙ БЕЗОПАСНОСТЬЮ
# ============================================================================
#
# Цель: Защищенный сервер для разработки с контейнерами
# Аппаратное обеспечение: Raspberry Pi 5 + Radxa SATA HAT
# Базовая система: Debian Bookworm
# ============================================================================
```

### Хуки и точки интеграции

