---
alwaysApply: ${cursor.file.path.includes("layer/") || cursor.file.path.includes("config/") || cursor.file.path.includes("image/")}
---

# Правило 01-3: Практические паттерны и инструменты системы сборки

## Назначение правила
Определяет практические паттерны, инструменты и стандарты качества для работы с системой сборки rpi-image-gen.

## Хуки и точки интеграции

#### Типы хуков в mmdebstrap
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # setup: выполняется до установки пакетов
      echo "Setup phase"
      prepare_environment

    - |
      # customize: выполняется после установки пакетов
      echo "Customize phase"
      configure_services

    - |
      # cleanup: финальная очистка
      echo "Cleanup phase"
      remove_build_artifacts
```

#### Порядок выполнения хуков
1. **setup-hooks**: Подготовка окружения
2. **essential-hooks**: Установка essential пакетов
3. **customize-hooks**: Кастомизация системы
4. **cleanup-hooks**: Очистка и финализация

### Rootfs-overlay механизм

#### Структура overlay
```yaml
mmdebstrap:
  rootfs-overlay:
    # Файлы добавляются в корень ФС
    etc/hostname: |
      my-custom-hostname

    etc/network/interfaces: |
      auto eth0
      iface eth0 inet dhcp

    usr/local/bin/my-script: |
      #!/bin/bash
      echo "Custom script"
```

#### Правила overlay
- Файлы копируются с сохранением прав доступа
- Относительные пути разрешаются от корня ФС
- Поддерживаются как текстовые, так и бинарные файлы

### Зависимости и совместимость

#### Управление зависимостями
```yaml
# В метаданных слоя
# METABEGIN
# X-Env-Layer-Name: my-layer
# X-Env-Layer-Requires: base-layer, essential
# X-Env-Layer-Provides: my-functionality
# X-Env-Layer-Version: 1.0.0
# METAEND
```

#### Категории слоев
- **device**: Специфичны для оборудования (Pi5, Radxa, etc.)
- **extension**: Общие расширения (security, monitoring, apps)
- **image**: Определяют layout образа (mbr, gpt, raid, etc.)

### Практические паттерны

#### Базовая конфигурация для разработки
```yaml
# config/development.yaml
device:
  layer: pi5
  hostname: dev-rpi

image:
  layer: standard-rpios
  boot_part_size: 200%
  root_part_size: 300%

layer:
  base: bookworm-minbase
  debug: debug-tools
```

#### Продакшн конфигурация
```yaml
# config/production.yaml
device:
  layer: pi5
  hostname: prod-server

image:
  layer: hybrid-raid-luks
  compression: zstd
  boot_part_size: 200%
  root_part_size: 400%

layer:
  base: bookworm-minbase
  security: hardened-security-suite
  monitoring: prometheus-stack
  backup: automated-backup
```

### Детальное описание архитектурных компонентов

#### Углубленный анализ bdebstrap
**bdebstrap** является ключевым компонентом модульной архитектуры rpi-image-gen:
- **Модульность**: Каждый слой обрабатывается независимо, затем объединяется
- **Зависимости**: Автоматическое разрешение через X-Env-Layer-Requires
- **Переопределение**: Позволяет слоям переопределять настройки предыдущих
- **Валидация**: Проверка совместимости и корректности метаданных
- **Производительность**: Параллельная обработка независимых слоев

#### Продвинутые возможности mmdebstrap
**mmdebstrap** обеспечивает надежное создание файловой системы:
- **Chroot окружение**: Безопасная изоляция процесса установки
- **Mirror управление**: Поддержка множественных репозиториев
- **Hook система**: setup-hooks, customize-hooks, cleanup-hooks
- **Variant поддержка**: minbase, buildd, important для разных сценариев
- **Filesystem overlay**: Применение дополнительных файлов и настроек

#### Конфигурация genimage для различных сценариев
**genimage** предоставляет гибкую генерацию образов:
- **Partition layout**: MBR, GPT, hybrid для разных загрузчиков
- **Filesystem типы**: ext4, btrfs, squashfs в зависимости от требований
- **Boot разделы**: FAT32 для UEFI, специальные разделы для Raspberry Pi
- **Compression**: gzip, xz, zstd для оптимизации размера
- **Sparse файлы**: Экономия дискового пространства

#### Интеграция с podman для безопасности
**podman unshare** обеспечивает безопасное выполнение:
- **User namespaces**: Корректное отображение UID/GID
- **Filesystem права**: Сохранение ownership без root
- **Sandboxing**: Изоляция от системных изменений
- **Compatibility**: Работает на системах без systemd

### Инструменты и команды

#### Основные команды rpi-image-gen
```bash
# Сборка образа
rpi-image-gen build -c config.yaml

# С указанием исходной директории
rpi-image-gen build -S /path/to/project -c config.yaml

# Список доступных слоев
rpi-image-gen layer --list

# Описание слоя
rpi-image-gen layer --describe layer-name

# Линтинг слоя
rpi-image-gen metadata --lint layer.yaml

# Справка
rpi-image-gen --help
```

#### Отладка и разработка
```bash
# Подробный вывод
rpi-image-gen build -c config.yaml -v

# Сохранение промежуточных результатов
rpi-image-gen build -c config.yaml --keep-tmp

# Только валидация конфигурации
rpi-image-gen build -c config.yaml --dry-run
```

### Качество и надежность

#### Проверки перед сборкой
- [ ] Валидность YAML конфигурации
- [ ] Доступность всех слоев
- [ ] Разрешение зависимостей
- [ ] Проверка переменных окружения
- [ ] Валидность метаданных слоев

#### Идемпотентность
```bash
# Скрипты должны быть идемпотентными
configure_service() {
  if ! systemctl is-enabled my-service; then
    systemctl enable my-service
  fi
}
```

#### Обработка ошибок
```bash
# Всегда использовать set -euo pipefail
set -euo pipefail

error_handler() {
  echo "ERROR: Build failed" >&2
  exit 1
}

trap error_handler ERR
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила
- [ ] Все новые конфигурации используют рекомендованную структуру с вынесенными переменными
- [ ] Переменные слоев используют короткие имена без префиксов IGconf_
- [ ] Конфигурации организованы по уровням абстракции

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила
- [ ] Все конфигурации проходят валидацию rpi-image-gen
- [ ] Конфигурации документированы и содержат описания назначения

### Проверки качества конфигураций
- [ ] Переменные слоев вынесены в отдельный блок в начале файла
- [ ] Используются короткие имена переменных (ptrace_restrict вместо IGconf_sysctl_kernel_ptrace_restrict)
- [ ] Конфигурации разделены на логические блоки с комментариями
- [ ] Слои организованы иерархически по уровням абстракции
- [ ] Зависимости между слоями корректно разрешены

### Источник
Создано на основе изучения README.adoc и технической документации rpi-image-gen. Обновлено для поддержки нового подхода к организации конфигураций с вынесенными переменными слоев.