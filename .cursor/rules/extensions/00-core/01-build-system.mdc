---
alwaysApply: ${cursor.file.path.includes("layer/") || cursor.file.path.includes("config/") || cursor.file.path.includes("image/")}
---

# Правило 01: Система сборки rpi-image-gen

## Назначение правила
Определяет принципы работы с системой сборки rpi-image-gen, включая bdebstrap, mmdebstrap, genimage и архитектуру выполнения.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Архитектура системы сборки

### Основные компоненты
Согласно официальной документации rpi-image-gen использует следующую архитектуру:

#### 1. **bdebstrap** - Модульное применение слоев
- **Назначение**: Агрегирует и объединяет конфигурации слоев
- **Вход**: YAML файлы слоев с метаданными X-Env-*
- **Выход**: Единая конфигурация для mmdebstrap
- **Особенности**: Поддерживает зависимости между слоями

#### 2. **mmdebstrap** - Создание файловой системы
- **Назначение**: Создает корневую файловую систему
- **Вход**: Конфигурация от bdebstrap + репозитории Debian
- **Выход**: Корневая ФС в целевом каталоге
- **Особенности**: Без root прав, использует namespaces

#### 3. **genimage** - Генерация образов дисков
- **Назначение**: Создает загрузочные образы дисков
- **Вход**: Файловая система + конфигурация разделов
- **Выход**: .img файлы для записи на носители
- **Особенности**: Поддержка различных форматов разделов

#### 4. **podman unshare** - Безопасное выполнение
- **Назначение**: Обработка прав доступа к файлам
- **Особенности**: Linux namespaces для корректного chown

### Этапы сборки

#### Этап 1: Parameter Assembly (Сборка параметров)
```bash
# Валидация входных аргументов
# Парсинг конфигурационных файлов
# Установление начального окружения
rpi-image-gen build -c config.yaml -S /source/dir
```

#### Этап 2: Layer Processing (Обработка слоев)
```bash
# Сбор слоев из указанных директорий
# Валидация зависимостей
# Разрешение порядка применения слоев
# Расширение переменных окружения
```

#### Этап 3: Build Preparation (Подготовка сборки)
```bash
# Настройка окружения
# Генерация конфигурации bdebstrap
# Проверка доступности ресурсов
```

#### Этап 4: Filesystem Generation (Генерация ФС)
```bash
# Создание ФС через mmdebstrap
# Выполнение хуков (setup, customize, cleanup)
# Применение rootfs-overlay
# Генерация SBOM
```

#### Этап 5: Image Generation (Генерация образа)
```bash
# Создание образа через genimage
# Применение пост-обработки
# Финализация образа
```

### Структура конфигурации

#### Основные секции конфигурационного файла
```yaml
# Полная структура конфигурации согласно README.adoc
---
include:
  file: base-config.yaml  # Включение других конфигов

device:                   # Конфигурация устройства
  layer: pi5             # Слой устройства
  hostname: my-device    # Имя хоста
  class: pi5            # Класс устройства

image:                    # Конфигурация образа
  layer: hybrid-raid-luks # Слой образа
  compression: zstd      # Сжатие
  boot_part_size: 200%   # Размер boot раздела
  root_part_size: 400%   # Размер root раздела

layer:                    # Слои расширения
  base: bookworm-minbase # Базовый слой
  security: apparmor-core # Слой безопасности
  monitoring: prometheus  # Слой мониторинга
  # ... дополнительные слои
```

### Система переменных окружения

#### Правила именования переменных
```bash
# IGconf_<section>_<key> - основной паттерн
IGconf_device_class=pi5
IGconf_device_hostname=my-rpi
IGconf_image_compression=zstd

# Передача через командную строку
rpi-image-gen build -c config.yaml -- IGconf_device_class=pi5 IGconf_image_compression=gzip
```

#### Источники переменных (по приоритету)
1. **Командная строка** (высший приоритет)
2. **Переменные окружения shell**
3. **Конфигурационный файл**
4. **Значения по умолчанию из метаданных слоя**

### Хуки и точки интеграции

#### Типы хуков в mmdebstrap
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # setup: выполняется до установки пакетов
      echo "Setup phase"
      prepare_environment

    - |
      # customize: выполняется после установки пакетов
      echo "Customize phase"
      configure_services

    - |
      # cleanup: финальная очистка
      echo "Cleanup phase"
      remove_build_artifacts
```

#### Порядок выполнения хуков
1. **setup-hooks**: Подготовка окружения
2. **essential-hooks**: Установка essential пакетов
3. **customize-hooks**: Кастомизация системы
4. **cleanup-hooks**: Очистка и финализация

### Rootfs-overlay механизм

#### Структура overlay
```yaml
mmdebstrap:
  rootfs-overlay:
    # Файлы добавляются в корень ФС
    etc/hostname: |
      my-custom-hostname

    etc/network/interfaces: |
      auto eth0
      iface eth0 inet dhcp

    usr/local/bin/my-script: |
      #!/bin/bash
      echo "Custom script"
```

#### Правила overlay
- Файлы копируются с сохранением прав доступа
- Относительные пути разрешаются от корня ФС
- Поддерживаются как текстовые, так и бинарные файлы

### Зависимости и совместимость

#### Управление зависимостями
```yaml
# В метаданных слоя
# METABEGIN
# X-Env-Layer-Name: my-layer
# X-Env-Layer-Requires: base-layer, essential
# X-Env-Layer-Provides: my-functionality
# X-Env-Layer-Version: 1.0.0
# METAEND
```

#### Категории слоев
- **device**: Специфичны для оборудования (Pi5, Radxa, etc.)
- **extension**: Общие расширения (security, monitoring, apps)
- **image**: Определяют layout образа (mbr, gpt, raid, etc.)

### Практические паттерны

#### Базовая конфигурация для разработки
```yaml
# config/development.yaml
device:
  layer: pi5
  hostname: dev-rpi

image:
  layer: standard-rpios
  boot_part_size: 200%
  root_part_size: 300%

layer:
  base: bookworm-minbase
  debug: debug-tools
```

#### Продакшн конфигурация
```yaml
# config/production.yaml
device:
  layer: pi5
  hostname: prod-server

image:
  layer: hybrid-raid-luks
  compression: zstd
  boot_part_size: 200%
  root_part_size: 400%

layer:
  base: bookworm-minbase
  security: hardened-security-suite
  monitoring: prometheus-stack
  backup: automated-backup
```

### Инструменты и команды

#### Основные команды rpi-image-gen
```bash
# Сборка образа
rpi-image-gen build -c config.yaml

# С указанием исходной директории
rpi-image-gen build -S /path/to/project -c config.yaml

# Список доступных слоев
rpi-image-gen layer --list

# Описание слоя
rpi-image-gen layer --describe layer-name

# Линтинг слоя
rpi-image-gen metadata --lint layer.yaml

# Справка
rpi-image-gen --help
```

#### Отладка и разработка
```bash
# Подробный вывод
rpi-image-gen build -c config.yaml -v

# Сохранение промежуточных результатов
rpi-image-gen build -c config.yaml --keep-tmp

# Только валидация конфигурации
rpi-image-gen build -c config.yaml --dry-run
```

### Качество и надежность

#### Проверки перед сборкой
- [ ] Валидность YAML конфигурации
- [ ] Доступность всех слоев
- [ ] Разрешение зависимостей
- [ ] Проверка переменных окружения
- [ ] Валидность метаданных слоев

#### Идемпотентность
```bash
# Скрипты должны быть идемпотентными
configure_service() {
  if ! systemctl is-enabled my-service; then
    systemctl enable my-service
  fi
}
```

#### Обработка ошибок
```bash
# Всегда использовать set -euo pipefail
set -euo pipefail

error_handler() {
  echo "ERROR: Build failed" >&2
  exit 1
}

trap error_handler ERR
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано на основе изучения README.adoc и технической документации rpi-image-gen