---
alwaysApply: ${cursor.file.path.includes("layer/") || cursor.file.path.includes("hooks/")}
---

# Правило 01: Хуки и точки интеграции

## Назначение правила
Определяет правила работы с хуками и точками интеграции в системе сборки rpi-image-gen.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Архитектура хуков

### Типы хуков в rpi-image-gen

#### 1. **mmdebstrap customize-hooks**
Основной механизм интеграции в процесс сборки ФС:

```yaml
mmdebstrap:
  customize-hooks:
    - |
      # Shell скрипт, выполняющийся на этапе customize
      set -euo pipefail
      echo "Customizing system..."

      # Ваша логика конфигурации
      configure_service
```

#### 2. **Layer-specific hooks**
Хуки, определенные в отдельных файлах слоя:

```
layer/
├── my-layer.yaml          # Основной файл слоя
└── hooks/
    ├── setup.sh          # Хук настройки
    ├── customize.sh      # Хук кастомизации
    └── cleanup.sh        # Хук очистки
```

#### 3. **Image-specific hooks**
Хуки для конкретных типов образов:

```yaml
image/
├── mbr/
│   └── hybrid-raid-luks/
│       └── device/
│           └── rootfs-overlay/
│               └── etc/
│                   └── systemd/
│                       └── system/
│                           └── disk-expansion.service
```

### Этапы выполнения хуков

#### Порядок выполнения в mmdebstrap
1. **setup-hooks** - Выполняются до установки пакетов
2. **essential-hooks** - После установки essential пакетов
3. **customize-hooks** - После установки всех пакетов (основной этап)
4. **cleanup-hooks** - Финальная очистка

#### Практический порядок в rpi-image-gen
```
1. Parameter Assembly     (сбор параметров)
2. Layer Processing       (обработка слоев)
3. Build Preparation      (подготовка)
4. ├── mmdebstrap setup
5. ├── Install essential packages
6. ├── mmdebstrap customize-hooks ← ОСНОВНОЙ ЭТАП
7. ├── rootfs-overlay application
8. ├── Additional layer processing
9. Image Generation       (создание образа)
```

### Структура customize-hooks

#### Базовый шаблон хука
```bash
#!/bin/bash
# rpi-image-gen customize hook for my-layer
set -euo pipefail

# Функция для обработки ошибок
error_handler() {
  echo "ERROR: Hook failed in my-layer" >&2
  exit 1
}
trap error_handler ERR

# Основная логика
main() {
  echo "🔧 Configuring my-layer..."

  # Проверка переменных окружения
  validate_environment

  # Резервное копирование
  backup_existing_config

  # Конфигурация
  apply_configuration

  # Валидация
  validate_configuration

  echo "✅ my-layer configured successfully"
}

# Вспомогательные функции
validate_environment() {
  # Проверка необходимых условий
  if [[ ! -d /etc ]]; then
    echo "ERROR: /etc directory not found" >&2
    exit 1
  fi
}

backup_existing_config() {
  # Безопасное резервное копирование
  local config_file="/etc/my-service.conf"
  if [[ -f "$config_file" ]] && [[ ! -f "${config_file}.backup" ]]; then
    cp "$config_file" "${config_file}.backup"
    echo "📋 Backed up existing configuration"
  fi
}

apply_configuration() {
  # Применение конфигурации
  cat > /etc/my-service.conf << EOF
# Generated by my-layer v${IGconf_my_layer_version:-1.0.0}
setting1=${IGconf_my_layer_setting1:-default}
setting2=${IGconf_my_layer_setting2:-default}
EOF
}

validate_configuration() {
  # Проверка корректности конфигурации
  if [[ ! -f /etc/my-service.conf ]]; then
    echo "ERROR: Configuration file not created" >&2
    exit 1
  fi
}

# Выполнение
main "$@"
```

### Rootfs-overlay механизм

#### Структура overlay
```yaml
mmdebstrap:
  rootfs-overlay:
    # Конфигурационные файлы
    etc/my-service/config.conf: |
      # Конфигурация сервиса
      key1=value1
      key2=value2

    # Скрипты
    usr/local/bin/my-status: |
      #!/bin/bash
      echo "Service status"
      systemctl status my-service

    # Systemd сервисы
    etc/systemd/system/my-service.service: |
      [Unit]
      Description=My Service
      After=network.target

      [Service]
      Type=simple
      ExecStart=/usr/local/bin/my-service
      Restart=always

      [Install]
      WantedBy=multi-user.target

    # Profile скрипты (автозагрузка)
    etc/profile.d/my-status.sh: |
      # Показывать статус при логине
      if [[ -x /usr/local/bin/my-status ]]; then
        echo "Service Status:"
        /usr/local/bin/my-status | head -5
        echo
      fi
```

#### Правила работы с overlay
- **Пути**: Относительные от корня ФС (`/` = корень целевой системы)
- **Права доступа**: Сохраняются как указано (используйте `install` или `chmod` в хуках)
- **Перезапись**: Новые файлы перезаписывают существующие
- **Создание директорий**: Автоматически создаются необходимые директории

### Идемпотентность хуков

#### Принципы идемпотентности
```bash
# ❌ НЕИДЕМПОТЕНТНО - добавляет дубликаты
echo "nameserver 8.8.8.8" >> /etc/resolv.conf

# ✅ ИДЕМПОТЕНТНО - проверка перед добавлением
add_nameserver() {
  local nameserver="8.8.8.8"
  if ! grep -q "^nameserver $nameserver$" /etc/resolv.conf; then
    echo "nameserver $nameserver" >> /etc/resolv.conf
    echo "✅ Added nameserver $nameserver"
  else
    echo "⚪ Nameserver $nameserver already configured"
  fi
}
```

#### Шаблоны идемпотентных операций
```bash
# Добавление в файл (с проверкой)
add_to_file() {
  local file="$1"
  local content="$2"
  local marker="$3"

  if ! grep -q "$marker" "$file" 2>/dev/null; then
    echo "$content" >> "$file"
    echo "✅ Added to $file"
  else
    echo "⚪ Already in $file"
  fi
}

# Создание файла (с бэкапом)
create_config_file() {
  local file="$1"
  local content="$2"

  # Бэкап существующего файла
  if [[ -f "$file" ]] && [[ ! -f "${file}.backup" ]]; then
    cp "$file" "${file}.backup"
  fi

  # Создание/перезапись
  cat > "$file" << EOF
$content
EOF
  echo "✅ Created $file"
}

# Управление systemd
manage_systemd_service() {
  local service="$1"
  local action="$2"  # enable/disable

  case "$action" in
    enable)
      if ! systemctl is-enabled "$service" 2>/dev/null; then
        systemctl enable "$service"
        echo "✅ Enabled $service"
      fi
      ;;
    disable)
      if systemctl is-enabled "$service" 2>/dev/null; then
        systemctl disable "$service"
        echo "✅ Disabled $service"
      fi
      ;;
  esac
}
```

### Обработка ошибок

#### Безопасные паттерны
```bash
#!/bin/bash
set -euo pipefail  # Обязательно!

# Централизованная обработка ошибок
error_handler() {
  local exit_code=$?
  echo "ERROR: Hook failed with exit code $exit_code" >&2
  echo "ERROR: Failed command: $BASH_COMMAND" >&2
  echo "ERROR: Line: $LINENO" >&2
  exit $exit_code
}
trap error_handler ERR

# Функция восстановления
cleanup_on_error() {
  echo "🧹 Cleaning up after error..."
  # Восстановление из бэкапа
  if [[ -f /etc/my-service.conf.backup ]]; then
    mv /etc/my-service.conf.backup /etc/my-service.conf
    echo "✅ Restored backup configuration"
  fi
}
trap cleanup_on_error EXIT

# Валидация входных данных
validate_inputs() {
  if [[ -z "${IGconf_my_setting:-}" ]]; then
    echo "ERROR: IGconf_my_setting is required" >&2
    exit 1
  fi

  case "${IGconf_my_mode:-default}" in
    default|advanced|minimal)
      ;;
    *)
      echo "ERROR: Invalid mode: ${IGconf_my_mode}" >&2
      exit 1
  esac
}
```

### Мониторинг и служба восстановления

#### Systemd с автоматическим перезапуском
```ini
# etc/systemd/system/my-service.service
[Unit]
Description=My Service
After=network.target
Requires=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/my-service
Restart=always          # Автоматический перезапуск
RestartSec=10          # Задержка перед перезапуском
StartLimitInterval=0   # Без ограничений на перезапуски

[Install]
WantedBy=multi-user.target
```

#### Скрипты статуса
```bash
#!/bin/bash
# /usr/local/bin/my-status
echo "=== My Service Status ==="
echo

# Проверка сервиса
if systemctl is-active --quiet my-service 2>/dev/null; then
  echo "✅ Service: running"
else
  echo "❌ Service: not running"
fi

# Проверка конфигурации
if [[ -f /etc/my-service.conf ]]; then
  echo "✅ Configuration: present"
  echo "   Modified: $(stat -c '%y' /etc/my-service.conf)"
else
  echo "❌ Configuration: missing"
fi

# Проверка логов
if [[ -f /var/log/my-service.log ]]; then
  echo "✅ Logs: present"
  echo "   Last entries:"
  tail -3 /var/log/my-service.log | sed 's/^/   /'
else
  echo "⚠️  Logs: not found"
fi
```

### Интеграция с переменными окружения

#### Использование IGconf_* переменных
```bash
#!/bin/bash
set -euo pipefail

# Значения с fallback
SERVICE_PORT="${IGconf_my_service_port:-8080}"
SERVICE_MODE="${IGconf_my_service_mode:-production}"
DEBUG_ENABLED="${IGconf_my_service_debug:-false}"

# Валидация значений
case "$SERVICE_MODE" in
  development|staging|production)
    ;;
  *)
    echo "ERROR: Invalid service mode: $SERVICE_MODE" >&2
    exit 1
    ;;
esac

# Применение конфигурации
cat > /etc/my-service.conf << EOF
port=$SERVICE_PORT
mode=$SERVICE_MODE
debug=$DEBUG_ENABLED
EOF
```

### Порядок выполнения слоев

#### Зависимости между слоями
```yaml
# METABEGIN
# X-Env-Layer-Name: my-application
# X-Env-Layer-Requires: base-system, networking, security
# X-Env-Layer-Provides: my-app
# METAEND

mmdebstrap:
  customize-hooks:
    - |
      # Этот хук выполнится ПОСЛЕ хуков зависимостей
      # base-system, networking, security уже настроены
      configure_my_application
```

#### Фазы применения слоев
1. **Base layers** - Базовая система (Debian, essential пакеты)
2. **Infrastructure layers** - Сеть, безопасность, мониторинг
3. **Platform layers** - Платформенные сервисы
4. **Application layers** - Конкретные приложения

### Тестирование хуков

#### Модульное тестирование
```bash
#!/bin/bash
# tests/test-hooks.sh

# Тест идемпотентности
test_idempotency() {
  echo "Testing hook idempotency..."

  # Первый запуск
  ./hook.sh
  local first_run=$?

  # Второй запуск
  ./hook.sh
  local second_run=$?

  if [[ $first_run -eq 0 ]] && [[ $second_run -eq 0 ]]; then
    echo "✅ Idempotency test passed"
  else
    echo "❌ Idempotency test failed"
    return 1
  fi
}

# Тест валидации
test_validation() {
  echo "Testing input validation..."

  # Тест с корректными данными
  IGconf_test_setting=valid ./hook.sh
  if [[ $? -eq 0 ]]; then
    echo "✅ Valid input test passed"
  else
    echo "❌ Valid input test failed"
    return 1
  fi

  # Тест с некорректными данными
  IGconf_test_setting=invalid ./hook.sh 2>/dev/null
  if [[ $? -ne 0 ]]; then
    echo "✅ Invalid input test passed"
  else
    echo "❌ Invalid input test failed"
    return 1
  fi
}
```

#### Отладка хуков
```bash
# Включение отладки в хуке
set -x  # Показывать выполняемые команды
set -v  # Показывать прочитанные строки

# Логирование
exec > >(tee -a /tmp/hook-debug.log) 2>&1
echo "Starting hook execution at $(date)"

# Проверка окружения
echo "Environment variables:"
env | grep "^IGconf_" | sort

echo "Current directory: $(pwd)"
echo "Target root: ${1:-not-set}"
```

### Лучшие практики

#### Структура сложных хуков
```bash
#!/bin/bash
# Хорошо структурированный хук

set -euo pipefail

# Константы
readonly CONFIG_FILE="/etc/my-service/config"
readonly SERVICE_NAME="my-service"
readonly BACKUP_SUFFIX=".backup.$(date +%Y%m%d_%H%M%S)"

# Функции
validate_prerequisites() {
  # Проверка зависимостей
}

setup_environment() {
  # Подготовка окружения
}

backup_configuration() {
  # Резервное копирование
}

apply_configuration() {
  # Применение настроек
}

configure_service() {
  # Настройка сервиса
}

enable_service() {
  # Включение и запуск
}

cleanup() {
  # Очистка
}

main() {
  echo "🔧 Configuring $SERVICE_NAME..."

  validate_prerequisites
  setup_environment
  backup_configuration
  apply_configuration
  configure_service
  enable_service

  echo "✅ $SERVICE_NAME configured successfully"
}

# Обработчики сигналов
trap cleanup EXIT
trap 'echo "ERROR: Hook interrupted" >&2; exit 1' INT TERM

# Выполнение
main "$@"
```

#### Документирование хуков
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # Hook: Configure My Service
      # Purpose: Install and configure my-service with high availability
      # Dependencies: network, systemd
      # Variables:
      #   - IGconf_my_service_port: Service port (default: 8080)
      #   - IGconf_my_service_mode: Operation mode (development/staging/production)
      # Author: Team Name
      # Version: 1.2.0
      # Date: 2024-01-15

      set -euo pipefail
      # ... implementation ...
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано на основе изучения механизма хуков и точек интеграции в rpi-image-gen