---
alwaysApply: ${cursor.file.path.includes("layer/") || cursor.file.path.includes("config/") || cursor.file.path.includes("scripts/")}
---

# Правило 01: Система переменных окружения IGconf_*

## Назначение правила
Определяет правила работы с переменными окружения IGconf_* в системе rpi-image-gen.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Архитектура переменных

### Правила именования
Согласно документации rpi-image-gen, все переменные конфигурации используют префикс `IGconf_`:

```bash
IGconf_<section>_<key>=<value>
```

#### Структура имени переменной
- **IGconf_**: Обязательный префикс (фиксированный)
- **section**: Логическая группа (device, image, layer-specific)
- **key**: Конкретный параметр конфигурации
- **value**: Значение переменной

#### Примеры корректных имен
```bash
# Переменные устройства
IGconf_device_class=pi5
IGconf_device_hostname=my-rpi
IGconf_device_storage_type=nvme

# Переменные образа
IGconf_image_compression=zstd
IGconf_image_boot_part_size=200%
IGconf_image_root_part_size=400%
IGconf_image_postbuild=${DIRECTORY}/post-build.sh  # Post-build hook для слоя

# Переменные слоев (с префиксом слоя)
IGconf_apparmor_core_mode=enforce
IGconf_fail2ban_core_maxretry=3
IGconf_ufw_core_default_policy=deny
```

### Источники переменных

#### Приоритет источников (от высшего к низшему)
1. **Командная строка** - переменные, переданные через `--`
2. **Переменные окружения shell** - установленные в текущем shell
3. **Конфигурационные файлы** - значения из YAML/INI файлов
4. **Метаданные слоев** - значения по умолчанию из X-Env-Var полей

#### Пример приоритетов
```bash
# Конфигурационный файл (низший приоритет)
device:
  hostname: default-host

# Переменная окружения (средний приоритет)
export IGconf_device_hostname=env-host

# Командная строка (высший приоритет)
rpi-image-gen build -c config.yaml -- IGconf_device_hostname=cmd-host

# Результат: hostname = "cmd-host"
```

### Декларация переменных в слоях

#### Структура метаданных переменных
```yaml
# METABEGIN
# X-Env-Layer-Name: my-layer
# X-Env-VarPrefix: mylayer
#
# X-Env-Var-setting1: default_value
# X-Env-Var-setting1-Desc: Description of setting1
# X-Env-Var-setting1-Required: false
# X-Env-Var-setting1-Valid: bool
# X-Env-Var-setting1-Set: lazy
#
# X-Env-Var-setting2: 42
# X-Env-Var-setting2-Desc: Numeric setting
# X-Env-Var-setting2-Required: true
# X-Env-Var-setting2-Valid: int:1-100
# METAEND
```

#### Поля метаданных переменных

##### X-Env-Var-<name>
- **Назначение**: Значение по умолчанию
- **Тип**: Строка, число, булев и т.д.
- **Обязательность**: Опционально (если Required=true, то обязательно)

##### X-Env-Var-<name>-Desc
- **Назначение**: Человеко-читаемое описание
- **Тип**: Строка
- **Обязательность**: Рекомендуется

##### X-Env-Var-<name>-Required
- **Назначение**: Обязательность переменной
- **Значения**: `true`/`false` или `y`/`n`
- **Обязательность**: Обязательно

##### X-Env-Var-<name>-Valid
- **Назначение**: Правила валидации
- **Форматы**:
  - `bool` - true/false, y/n, 1/0
  - `int:min-max` - целое число в диапазоне
  - `keywords:value1,value2,value3` - список допустимых значений
  - `pattern:regex` - регулярное выражение

##### X-Env-Var-<name>-Set
- **Назначение**: Политика присваивания
- **Значения**:
  - `lazy` - присваивать только если не установлено
  - `force` - всегда присваивать значение по умолчанию

### Типы валидации переменных

#### Булевы переменные
```yaml
# X-Env-Var-enable: y
# X-Env-Var-enable-Valid: bool
```
Допустимые значения: `true`, `false`, `y`, `n`, `1`, `0`, `yes`, `no`

#### Целочисленные переменные
```yaml
# X-Env-Var-max_retry: 3
# X-Env-Var-max_retry-Valid: int:1-10
```
Допустимые значения: целые числа от 1 до 10 включительно

#### Перечисления (keywords)
```yaml
# X-Env-Var-compression: zstd
# X-Env-Var-compression-Valid: keywords:gzip,lz4,zstd,none
```
Допустимые значения: `gzip`, `lz4`, `zstd`, `none`

#### Регулярные выражения
```yaml
# X-Env-Var-hostname: myhost
# X-Env-Var-hostname-Valid: pattern:^[a-z][a-z0-9-]*$
```
Допустимые значения: соответствуют регулярному выражению

### Передача переменных в сборку

#### Через командную строку
```bash
# Передача отдельных переменных
rpi-image-gen build -c config.yaml -- IGconf_device_class=pi5 IGconf_image_compression=zstd

# Передача из файла переменных
env $(cat variables.env | xargs) rpi-image-gen build -c config.yaml

# Использование скрипта-обертки
#!/bin/bash
source variables.env
rpi-image-gen build -c config.yaml
```

#### Через переменные окружения
```bash
# Установка в shell
export IGconf_device_class=pi5
export IGconf_image_compression=zstd
rpi-image-gen build -c config.yaml
```

#### Через конфигурационные файлы
```yaml
# config/my-config.yaml
device:
  class: ${IGconf_device_class}
  hostname: ${IGconf_device_hostname}

image:
  compression: ${IGconf_image_compression}
```

### Лучшие практики

#### Организация переменных по префиксам
```yaml
# Группировка по функциональности
# apparmor_core_*
IGconf_apparmor_core_mode=enforce
IGconf_apparmor_core_auditd_integration=y
IGconf_apparmor_core_desktop_notifications=n

# fail2ban_core_*
IGconf_fail2ban_core_maxretry=3
IGconf_fail2ban_core_bantime=3600
IGconf_fail2ban_core_log_level=INFO

# ufw_core_*
IGconf_ufw_core_default_input_policy=drop
IGconf_ufw_core_default_output_policy=accept
IGconf_ufw_core_logging=low
```

#### Документирование переменных
```yaml
# Всегда добавлять описания
# X-Env-Var-mode-Desc: AppArmor enforcement mode (enforce/complain)
# X-Env-Var-maxretry-Desc: Maximum retry attempts before ban
# X-Env-Var-bantime-Desc: Ban time in seconds for violations

# Использовать понятные значения по умолчанию
# X-Env-Var-mode: enforce  # Безопасное по умолчанию
# X-Env-Var-maxretry: 3    # Разумное значение
# X-Env-Var-bantime: 3600  # 1 час - баланс между безопасностью и удобством
```

#### Валидация в скриптах
```bash
#!/bin/bash
set -euo pipefail

# Проверка обязательных переменных
if [[ -z "${IGconf_required_setting:-}" ]]; then
  echo "ERROR: IGconf_required_setting is required" >&2
  exit 1
fi

# Валидация значений
case "${IGconf_mode:-enforce}" in
  enforce|complain)
    ;;
  *)
    echo "ERROR: Invalid mode: ${IGconf_mode}" >&2
    exit 1
    ;;
esac
```

### Распространенные ошибки

#### ❌ Неправильные имена переменных
```bash
# Неправильно - нет префикса IGconf_
device_class=pi5

# Неправильно - неправильный разделитель
IGconf_device-class=pi5

# Неправильно - заглавные буквы в section/key
IGconf_Device_Class=pi5
```

#### ❌ Игнорирование приоритетов
```yaml
# Конфигурационный файл
device:
  class: pi4  # Будет переопределено

# Правильно: использовать переменные для переопределения
device:
  class: ${IGconf_device_class:-pi5}
```

#### ❌ Отсутствие валидации
```yaml
# Плохо - нет проверки типов
max_retry="${IGconf_max_retry:-3}"

# Хорошо - с валидацией
max_retry="${IGconf_max_retry:-3}"
if ! [[ "$max_retry" =~ ^[0-9]+$ ]] || (( max_retry < 1 || max_retry > 10 )); then
  echo "ERROR: max_retry must be integer 1-10" >&2
  exit 1
fi
```

### Интеграция с конфигурационными файлами

#### Рекомендуемая структура конфигурации (новый подход)
```yaml
# Полная конфигурация с вынесенными переменными слоев
---

# ============================================================================
# ПЕРЕМЕННЫЕ СЛОЕВ (вынесены из блока layer для лучшей читаемости)
# ============================================================================

# Настройки в короткой форме (без префиксов IGconf_)
sysctl_kernel:
  ptrace_restrict: y
  aslr_enable: y
  kptr_restrict: 1
  dmesg_restrict: y

apparmor_core:
  enable: y
  mode: enforce
  auditd: y

docker:
  trust_user1: n

# ============================================================================
# КОНФИГУРАЦИЯ УСТРОЙСТВА И ОБРАЗА
# ============================================================================

device:
  layer: pi5
  hostname: rpi-complete-system

image:
  layer: hybrid-raid-luks
  boot_part_size: 200%
  root_part_size: 400%
  name: complete-system

# ============================================================================
# СЛОИ СИСТЕМЫ (только ссылки)
# ============================================================================

layer:
  base: bookworm-minbase
  security: apparmor-core
  sysctl_kernel: sysctl-kernel
  containers: docker-debian-bookworm
```

#### Legacy подходы (для совместимости)
```yaml
# Inline переменные (прямые значения) - устарело
device:
  hostname: prod-server  # Прямое значение, не переменная
  class: pi5            # Прямое значение

# Переменные окружения (IGconf_*) - для переопределения
device:
  hostname: ${IGconf_device_hostname:-default-host}
  class: ${IGconf_device_class:-pi5}

# Смешанный подход - устарело
device:
  layer: pi5                    # Прямое значение (статичное)
  hostname: ${IGconf_device_hostname:-rpi-system}  # Переменная с fallback
```

### Отладка переменных

#### Проверка установленных переменных
```bash
# Вывести все IGconf_* переменные
env | grep "^IGconf_"

# Проверить конкретную переменную
echo "Device class: ${IGconf_device_class:-not-set}"

# Дебаг в скриптах
set -x  # Включить трассировку
echo "Using hostname: $IGconf_device_hostname"
```

#### Линтинг и валидация
```bash
# Проверить слой на корректность переменных
rpi-image-gen metadata --lint layer.yaml

# Проверить конфигурацию
rpi-image-gen build -c config.yaml --dry-run
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Проверки качества переменных
- [ ] Переменные слоев используют короткие имена без префиксов IGconf_
- [ ] Переменные правильно группируются по блокам слоев
- [ ] rpi-image-gen корректно преобразует короткие имена в полные IGconf_* переменные
- [ ] Переменные имеют валидные значения согласно документации слоев

### Источник
Создано на основе изучения системы переменных в документации rpi-image-gen. Обновлено для поддержки нового подхода с короткими именами переменных в конфигурациях.