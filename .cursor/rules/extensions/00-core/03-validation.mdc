---
alwaysApply: ${cursor.file.path.includes("layer/") || cursor.file.path.includes("config/") || cursor.file.path.endsWith(".yaml")}
---

# Правило 03: Валидация конфигураций расширений rpi-image-gen

## Назначение правила
Определяет стандарты валидации конфигураций расширений с автоматической проверкой в Cursor IDE.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Структура конфигурации

### Обязательные секции
```yaml
# 🔴 ОБЯЗАТЕЛЬНЫЕ секции для всех конфигураций

device:           # Конфигурация устройства
  layer: string  # Ссылка на слой устройства (обязательно)

image:            # Конфигурация образа
  layer: string  # Ссылка на слой образа (обязательно)

layer:            # Конфигурация слоев
  base: string   # Базовый слой (обязательно)
  # Дополнительные слои опциональны
```

### Пример валидной конфигурации
```yaml
device:
  layer: pi5
  hostname: rpi-extension-${HOST_SUFFIX}

image:
  layer: image-rpios
  boot_part_size: 200%
  root_part_size: 300%

layer:
  base: bookworm-minbase
  security: security-minimal
  network: net-misc
  extension: my-custom-extension
```

## Метаданные слоя

### Обязательные поля метаданных
```yaml
# METABEGIN
# X-Env-Layer-Name: my-extension                    # 🔴 ОБЯЗАТЕЛЬНО
# X-Env-Layer-Category: extension                  # 🔴 ОБЯЗАТЕЛЬНО
# X-Env-Layer-Description: Описание расширения     # 🔴 ОБЯЗАТЕЛЬНО
# X-Env-Layer-Version: 1.0.0                       # 🔴 ОБЯЗАТЕЛЬНО
# X-Env-Layer-Requires: essential                  # 🔴 ОБЯЗАТЕЛЬНО
# X-Env-VarPrefix: extension                       # 🟡 РЕКОМЕНДУЕТСЯ
# METAEND
```

### Категории слоев
```yaml
# Допустимые значения X-Env-Layer-Category
X-Env-Layer-Category: device      # Слой устройства (Pi 4, Pi 5, etc.)
X-Env-Layer-Category: extension   # Расширение приложения
X-Env-Layer-Category: suite       # Комплексное решение
X-Env-Layer-Category: base        # Базовый слой
```

## Переменные окружения

### Префиксы переменных
```yaml
# Стандартные префиксы для разных типов расширений
IGconf_extension_*    # Переменные расширения
IGconf_device_*       # Переменные устройства
IGconf_image_*        # Переменные образа
IGconf_app_*          # Переменные приложения
IGconf_infra_*        # Инфраструктурные переменные
```

### Структура переменных
```yaml
# X-Env-Var-{name}: {default_value}
# X-Env-Var-{name}-Description: {description}
# X-Env-Var-{name}-Required: {true|false}
# X-Env-Var-{name}-Valid: {validation_rule}
# X-Env-Var-{name}-Set: {immediate|lazy}

# Пример полной переменной
X-Env-Var-database_url: postgresql://localhost:5432/app
X-Env-Var-database_url-Description: URL подключения к базе данных
X-Env-Var-database_url-Required: true
X-Env-Var-database_url-Valid: regex|^postgresql://.+
X-Env-Var-database_url-Set: immediate
```

### Правила валидации
```yaml
# Типы валидации
X-Env-Var-port-Valid: int:1024-65535          # Целое число в диапазоне
X-Env-Var-loglevel-Valid: debug,info,warn,error # Список допустимых значений
X-Env-Var-hostname-Valid: regex:^[a-zA-Z0-9.-]+$ # Регулярное выражение
X-Env-Var-enabled-Valid: true,false           # Булево значение
```

## Автоматическая валидация в Cursor

### Проверка синтаксиса YAML
- [ ] Корректный YAML синтаксис
- [ ] Согласованность отступов (2 пробела)
- [ ] Отсутствие табуляций
- [ ] Корректность типов значений

### Проверка структуры
- [ ] Наличие обязательных секций (device, image, layer)
- [ ] Корректность ссылок на слои
- [ ] Валидность метаданных X-Env-*
- [ ] Соответствие категорий слоев

### Проверка переменных
- [ ] Корректность префиксов IGconf_*
- [ ] Наличие описаний для переменных
- [ ] Валидность правил валидации
- [ ] Соответствие типов значений

## Расширенная валидация

### Проверка зависимостей
```yaml
# Валидация зависимостей между слоями
X-Env-Layer-Requires: essential,net-misc,security-minimal

# Проверка существования зависимых слоев
# Cursor автоматически проверит наличие указанных слоев
```

### Семантическая валидация
```yaml
# Проверка семантической корректности
device:
  layer: pi5                    # Должен существовать слой pi5
  hostname: rpi-${HOST_SUFFIX}  # Переменная должна быть определена

image:
  layer: image-rpios           # Должен существовать слой image-rpios
  boot_part_size: 200%         # Корректный формат размера

layer:
  base: bookworm-minbase       # Должен существовать базовый слой
  extension: my-extension      # Слой должен быть определен в метаданных
```

### Кросс-валидация
```yaml
# Проверка соответствия между секциями
# Например: если device.layer = pi5, то должны быть определены
# соответствующие переменные IGconf_device_*
```

## Инструменты валидации

### Команды rpi-image-gen
```bash
# Валидация конфигурации
rpi-image-gen metadata --lint config.yaml
rpi-image-gen metadata --lint layer/my-layer.yaml

# Проверка зависимостей
rpi-image-gen layer --describe my-layer
rpi-image-gen layer --list --depends my-layer

# Валидация переменных
rpi-image-gen metadata --validate-vars layer/my-layer.yaml
```

### Автоматизированные проверки
```bash
#!/bin/bash
# Скрипт комплексной валидации

validate_config() {
    local config_file="$1"

    echo "Валидация конфигурации: $config_file"

    # Синтаксическая валидация
    if ! python3 -c "import yaml; yaml.safe_load(open('$config_file'))"; then
        echo "❌ Ошибка синтаксиса YAML"
        return 1
    fi

    # Валидация через rpi-image-gen
    if ! rpi-image-gen metadata --lint "$config_file"; then
        echo "❌ Ошибка валидации rpi-image-gen"
        return 1
    fi

    # Проверка обязательных секций
    if ! yq '.device.layer and .image.layer and .layer.base' "$config_file" >/dev/null; then
        echo "❌ Отсутствуют обязательные секции"
        return 1
    fi

    echo "✅ Конфигурация валидна"
    return 0
}

# Пример использования
validate_config "config/my-config.yaml"
```

## Типичные ошибки и исправления

### Отсутствующие обязательные секции
```yaml
# ❌ Неправильно - отсутствует секция device
image:
  layer: image-rpios
layer:
  base: bookworm-minbase

# ✅ Правильно - все обязательные секции
device:
  layer: pi5
image:
  layer: image-rpios
layer:
  base: bookworm-minbase
```

### Некорректные метаданные
```yaml
# ❌ Неправильно - отсутствуют обязательные поля
# METABEGIN
# X-Env-Layer-Name: my-extension
# METAEND

# ✅ Правильно - все обязательные поля
# METABEGIN
# X-Env-Layer-Name: my-extension
# X-Env-Layer-Category: extension
# X-Env-Layer-Description: My custom extension
# X-Env-Layer-Version: 1.0.0
# X-Env-Layer-Requires: essential
# METAEND
```

### Неправильные переменные окружения
```yaml
# ❌ Неправильно - неправильный префикс
X-Env-Var-my_setting: value

# ✅ Правильно - корректный префикс
X-Env-Var-extension_my_setting: value
X-Env-VarPrefix: extension
```

## Интеграция с CI/CD

### GitHub Actions пример
```yaml
name: Validate Configuration
on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-yaml jq

      - name: Validate YAML syntax
        run: |
          find . -name "*.yaml" -exec python3 -c "import yaml; yaml.safe_load(open('{}'))" \;

      - name: Validate rpi-image-gen configs
        run: |
          find layer/ config/ -name "*.yaml" -exec rpi-image-gen metadata --lint {} \;

      - name: Check required sections
        run: |
          for file in layer/*.yaml config/*.yaml; do
            if [ -f "$file" ]; then
              if ! yq '.device.layer and .image.layer and .layer.base' "$file" >/dev/null; then
                echo "Missing required sections in $file"
                exit 1
              fi
            fi
          done
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано на основе стандартов валидации конфигураций rpi-image-gen с интеграцией в Cursor IDE