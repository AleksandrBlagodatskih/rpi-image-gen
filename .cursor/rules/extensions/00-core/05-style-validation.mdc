---
alwaysApply: ${cursor.file.path.includes("layer/") || cursor.file.path.includes("config/") || cursor.file.path.endsWith(".yaml") || cursor.file.path.endsWith(".sh")}
---

# Правило 05: Автоматическая валидация стиля кода

## Назначение правила
Обеспечивает автоматическую проверку соответствия кода стандартам стиля, определенным в руководстве по стилю кодирования.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Проверки стиля для Bash скриптов

### Основные проверки безопасности
```bash
# 🔴 ОБЯЗАТЕЛЬНО: set -euo pipefail в начале скрипта
validate_bash_safety() {
    local file="$1"

    # Проверка shebang
    if ! head -1 "$file" | grep -q "^#!/bin/bash"; then
        echo "ERROR: Missing or incorrect shebang in $file"
        return 1
    fi

    # Проверка set -euo pipefail в первых 5 строках
    if ! head -5 "$file" | grep -q "set -euo pipefail"; then
        echo "ERROR: Missing 'set -euo pipefail' in $file"
        return 1
    fi

    # Проверка наличия die функции
    if ! grep -q "^die()" "$file"; then
        echo "ERROR: Missing die() function in $file"
        return 1
    fi

    echo "✅ Bash safety checks passed for $file"
}

# 🔴 ОБЯЗАТЕЛЬНО: Валидация переменных окружения
validate_igconf_usage() {
    local file="$1"

    # Поиск использования IGconf_ переменных
    if grep -q "IGconf_" "$file"; then
        # Проверка что переменные валидируются
        if ! grep -q "IGconf_[a-zA-Z_]*:?[^?]" "$file"; then
            echo "WARNING: IGconf_ variables found but may not be validated in $file"
        fi
    fi

    echo "✅ IGconf usage validation passed for $file"
}
```

### Структурные проверки
```bash
# 🔴 ОБЯЗАТЕЛЬНО: Проверка структуры скрипта
validate_bash_structure() {
    local file="$1"

    # Проверка наличия main функции
    if ! grep -q "^main()" "$file"; then
        echo "WARNING: No main() function found in $file"
    fi

    # Проверка наличия trap для cleanup
    if ! grep -q "trap.*EXIT" "$file"; then
        echo "WARNING: No EXIT trap found in $file"
    fi

    # Проверка документирования функций
    local undocumented_functions
    undocumented_functions=$(grep -n "^[a-zA-Z_][a-zA-Z0-9_]*()" "$file" | \
        while IFS=: read -r line func; do
            # Проверка что предыдущая строка содержит комментарий
            if ! sed -n "$((line-1))p" "$file" | grep -q "^#"; then
                echo "$func"
            fi
        done)

    if [[ -n "$undocumented_functions" ]]; then
        echo "WARNING: Undocumented functions in $file: $undocumented_functions"
    fi

    echo "✅ Bash structure validation passed for $file"
}
```

## Проверки стиля для YAML конфигураций

### Структурные проверки YAML
```bash
# 🔴 ОБЯЗАТЕЛЬНО: Проверка структуры конфигурации
validate_yaml_structure() {
    local file="$1"

    # Проверка наличия обязательных секций
    if ! grep -q "^layer:" "$file"; then
        echo "ERROR: Missing 'layer:' section in $file"
        return 1
    fi

    # Проверка иерархической организации
    if grep -q "^  [a-zA-Z_].*:" "$file" && ! grep -q "^#.*переменные.*слоев" "$file"; then
        echo "WARNING: Variables found but no section comment in $file"
    fi

    echo "✅ YAML structure validation passed for $file"
}

# 🔴 ОБЯЗАТЕЛЬНО: Проверка комментариев
validate_yaml_comments() {
    local file="$1"

    # Проверка наличия заголовочных комментариев
    if ! grep -q "^# ============================================================================" "$file"; then
        echo "WARNING: No section headers found in $file"
    fi

    # Проверка документирования переменных
    local total_lines
    total_lines=$(wc -l < "$file")
    local comment_lines
    comment_lines=$(grep -c "^#" "$file")

    local comment_ratio=$(( comment_lines * 100 / total_lines ))
    if [[ $comment_ratio -lt 10 ]]; then
        echo "WARNING: Low comment ratio in $file: $comment_ratio%"
    fi

    echo "✅ YAML comments validation passed for $file"
}
```

## Проверки метаданных слоев

### Валидация X-Env метаданных
```bash
# 🔴 ОБЯЗАТЕЛЬНО: Проверка метаданных слоя
validate_layer_metadata() {
    local file="$1"

    # Проверка наличия METABEGIN/METAEND
    if ! grep -q "^# METABEGIN" "$file" || ! grep -q "^# METAEND" "$file"; then
        echo "ERROR: Missing METABEGIN/METAEND markers in $file"
        return 1
    fi

    # Извлечение метаданных
    local metadata
    metadata=$(sed -n '/^# METABEGIN/,/^# METAEND/p' "$file")

    # Проверка обязательных полей
    local required_fields=("X-Env-Layer-Name" "X-Env-Layer-Category" "X-Env-Layer-Version")
    for field in "${required_fields[@]}"; do
        if ! grep -q "$field:" <<< "$metadata"; then
            echo "ERROR: Missing required field '$field' in $file"
            return 1
        fi
    done

    # Проверка документирования переменных
    if grep -q "X-Env-Var-" <<< "$metadata"; then
        # Проверка что все переменные документированы
        local undoc_vars
        undoc_vars=$(grep "X-Env-Var-[^:]*:" <<< "$metadata" | \
            grep -v -A 3 "Description:" | tail -1)
        if [[ -n "$undoc_vars" ]]; then
            echo "WARNING: Undocumented variables found in $file"
        fi
    fi

    echo "✅ Layer metadata validation passed for $file"
}
```

## Комплексная валидация файлов

### Основная функция валидации
```bash
# 🔴 ОБЯЗАТЕЛЬНО: Комплексная проверка файла
validate_file() {
    local file="$1"
    local file_type
    file_type=$(get_file_type "$file")

    echo "🔍 Validating $file (type: $file_type)"

    case "$file_type" in
        "bash")
            validate_bash_safety "$file"
            validate_igconf_usage "$file"
            validate_bash_structure "$file"
            ;;
        "yaml")
            validate_yaml_structure "$file"
            validate_yaml_comments "$file"
            validate_layer_metadata "$file"
            ;;
        "mdc")
            # Правила имеют свою валидацию
            echo "✅ MDC file validation skipped (rules have their own validation)"
            ;;
        *)
            echo "⚠️  Unknown file type for $file"
            ;;
    esac

    echo "✅ File validation completed for $file"
}

# Определение типа файла
get_file_type() {
    local file="$1"

    case "$file" in
        *.sh) echo "bash" ;;
        *.yaml) echo "yaml" ;;
        *.mdc) echo "mdc" ;;
        *) echo "unknown" ;;
    esac
}
```

## Интеграция с pre-commit хуками

### Pre-commit конфигурация
```yaml
# .pre-commit-config.yaml
repos:
  - repo: local
    hooks:
      - id: rpi-image-gen-style-check
        name: rpi-image-gen style validation
        entry: ./scripts/validate-style.sh
        language: system
        files: \.(sh|yaml)$
        pass_filenames: true

  - repo: https://github.com/shellcheck-py/shellcheck-py
    rev: v0.9.0.5
    hooks:
      - id: shellcheck
        args: [--external-sources]

  - repo: https://github.com/adrienverge/yamllint
    rev: v1.32.0
    hooks:
      - id: yamllint
        args: [--config-file=.yamllint.yaml]
```

### Скрипт валидации стиля
```bash
#!/bin/bash
# scripts/validate-style.sh
set -euo pipefail

# Function for error handling
die() {
    echo "ERROR: $*" >&2
    exit 1
}

# Основная функция валидации
main() {
    local files=("$@")
    local failed_files=()

    for file in "${files[@]}"; do
        if [[ -f "$file" ]]; then
            echo "Validating: $file"
            if ! validate_file "$file"; then
                failed_files+=("$file")
            fi
        fi
    done

    if [[ ${#failed_files[@]} -gt 0 ]]; then
        echo "❌ Style validation failed for: ${failed_files[*]}"
        exit 1
    else
        echo "✅ All files passed style validation"
    fi
}

# Валидация файла (упрощенная версия для CI)
validate_file() {
    local file="$1"
    local errors=0

    # Базовые проверки для всех файлов
    if [[ ! -s "$file" ]]; then
        echo "ERROR: Empty file: $file"
        return 1
    fi

    # Тип-специфичные проверки
    case "$file" in
        *.sh)
            # Проверка set -euo pipefail
            if ! head -5 "$file" | grep -q "set -euo pipefail"; then
                echo "ERROR: Missing 'set -euo pipefail' in $file"
                ((errors++))
            fi

            # Проверка die функции
            if ! grep -q "^die()" "$file"; then
                echo "ERROR: Missing die() function in $file"
                ((errors++))
            fi
            ;;
        *.yaml)
            # Проверка наличия layer секции
            if ! grep -q "^layer:" "$file"; then
                echo "ERROR: Missing 'layer:' section in $file"
                ((errors++))
            fi
            ;;
    esac

    return $errors
}

# Выполнение
main "$@"
```

## Интеграция с Cursor IDE

### Автоматические проверки в редакторе
```json
// .cursor/settings.json
{
    "editor.codeActionsOnSave": {
        "source.fixAll": true,
        "source.organizeImports": true
    },
    "editor.formatOnSave": true,
    "files.associations": {
        "*.mdc": "markdown"
    },
    "emmet.includeLanguages": {
        "mdc": "markdown"
    }
}
```

### Расширения Cursor для валидации
```json
// .cursor/extensions.json
{
    "recommendations": [
        "timonwong.shellcheck",
        "redhat.vscode-yaml",
        "ms-vscode.vscode-json",
        "ms-vscode.powershell",
        "streetsidesoftware.code-spell-checker"
    ]
}
```

## Метрики качества и отчетность

### Генерация отчетов о качестве
```bash
# scripts/generate-quality-report.sh
#!/bin/bash
set -euo pipefail

generate_quality_report() {
    local output_file="${1:-quality-report.md}"

    cat > "$output_file" << 'EOF'
# Отчет о качестве кода rpi-image-gen

## Общая статистика

EOF

    # Подсчет файлов
    local bash_files
    bash_files=$(find . -name "*.sh" -type f | wc -l)
    local yaml_files
    yaml_files=$(find . -name "*.yaml" -type f | wc -l)

    echo "- Bash скриптов: $bash_files" >> "$output_file"
    echo "- YAML конфигураций: $yaml_files" >> "$output_file"
    echo "" >> "$output_file"

    # Проверка стиля
    echo "## Результаты проверки стиля" >> "$output_file"
    echo "" >> "$output_file"

    local style_errors=0
    local style_warnings=0

    # Проверка Bash файлов
    while IFS= read -r file; do
        if ! validate_bash_style "$file" 2>/dev/null; then
            ((style_errors++))
        fi
    done < <(find . -name "*.sh" -type f)

    # Проверка YAML файлов
    while IFS= read -r file; do
        if ! validate_yaml_style "$file" 2>/dev/null; then
            ((style_errors++))
        fi
    done < <(find . -name "*.yaml" -type f)

    echo "- Ошибок стиля: $style_errors" >> "$output_file"
    echo "- Предупреждений стиля: $style_warnings" >> "$output_file"
    echo "" >> "$output_file"

    # Рекомендации
    echo "## Рекомендации" >> "$output_file"
    echo "" >> "$output_file"

    if [[ $style_errors -gt 0 ]]; then
        echo "- Исправить ошибки стиля в затронутых файлах" >> "$output_file"
    fi

    echo "- Регулярно запускать проверки стиля" >> "$output_file"
    echo "- Обновлять правила стиля при изменениях архитектуры" >> "$output_file"

    echo "✅ Отчет о качестве сгенерирован: $output_file"
}
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Автоматическая валидация включена в CI/CD пайплайн
- [ ] Pre-commit хуки настроены для всех разработчиков
- [ ] Правила стиля документированы и доступны
- [ ] Нарушения стиля блокируют мердж в основную ветку

### Качественные показатели
- [ ] 0 ошибок стиля в основной ветке
- [ ] Все новые файлы проходят валидацию стиля
- [ ] Среднее время проверки стиля < 30 секунд
- [ ] Автоматизированная генерация отчетов о качестве

### Автоматизированная проверка
- [ ] Валидация при каждом коммите
- [ ] Проверка в pull request
- [ ] Еженедельные отчеты о качестве
- [ ] Мониторинг трендов качества кода

### Источник
Создано для обеспечения автоматического контроля качества кода в rpi-image-gen проекте.