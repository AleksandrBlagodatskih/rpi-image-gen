---
alwaysApply: ${cursor.file.path.includes("layer/") || cursor.file.path.includes("config/")}
---

# Правило 08: Архитектурные компоненты rpi-image-gen

## Назначение правила
Детальное описание и рекомендации по использованию ключевых архитектурных компонентов rpi-image-gen: bdebstrap, mmdebstrap, genimage, podman unshare.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Детальная спецификация компонентов

### bdebstrap - Модульный оркестратор слоев

#### Архитектура и алгоритм работы
**bdebstrap** реализует паттерн "Composite Configuration Builder":

1. **Discovery Phase**: Сканирование директорий на наличие слоев
2. **Dependency Resolution**: Топологическая сортировка зависимостей
3. **Variable Expansion**: Применение X-Env-* метаданных
4. **Merge Phase**: Объединение конфигураций слоев
5. **Validation**: Финальная проверка совместимости

#### Оптимизации производительности
```yaml
# Параллельная обработка независимых слоев
bdebstrap:
  parallel: true
  max_workers: 4

# Кэширование результатов валидации
cache:
  validation_results: true
  dependency_graph: true
```

#### Расширенные возможности
- **Conditional Layers**: Слои применяются только при определенных условиях
- **Override Semantics**: Переопределение настроек с сохранением истории
- **Plugin Architecture**: Возможность расширения через custom hooks

### mmdebstrap - Конструктор файловой системы

#### Продвинутые режимы работы
```bash
# Различные варианты базовой системы
mmdebstrap --variant=minbase     # Минимальная система
mmdebstrap --variant=buildd      # Система для сборки пакетов
mmdebstrap --variant=important   # Важные пакеты для embedded

# Множественные зеркала для отказоустойчивости
--mirror=http://deb.debian.org/debian/
--mirror=http://mirror.example.com/debian/
```

#### Hook система - детальный разбор
```yaml
mmdebstrap:
  # Выполняются ДО установки пакетов
  setup-hooks:
    - |
      #!/bin/bash
      echo "Pre-installation setup"
      # Настройка apt sources
      # Создание пользователей
      # Базовая конфигурация

  # Выполняются ПОСЛЕ установки пакетов (основной этап)
  customize-hooks:
    - |
      #!/bin/bash
      echo "Post-installation customization"
      # Установка сервисов
      # Конфигурация приложений
      # Настройка безопасности

  # Финальная очистка
  cleanup-hooks:
    - |
      #!/bin/bash
      echo "Final cleanup"
      # Удаление кэша
      # Очистка временных файлов
      # Финализация системы
```

#### Filesystem Overlay механизм
```yaml
mmdebstrap:
  rootfs-overlay:
    # Конфигурационные файлы
    etc/hostname: |
      my-custom-hostname

    etc/network/interfaces: |
      auto eth0
      iface eth0 inet dhcp

    # Скрипты с правами выполнения
    usr/local/bin/custom-script: |
      #!/bin/bash
      echo "Custom startup script"

    # Systemd сервисы
    etc/systemd/system/custom.service: |
      [Unit]
      Description=Custom Service
      After=network.target

      [Service]
      ExecStart=/usr/local/bin/custom-script
      Restart=always

      [Install]
      WantedBy=multi-user.target
```

### genimage - Генератор дисковых образов

#### Конфигурационные шаблоны для различных сценариев

##### Стандартный Raspberry Pi образ
```ini
# config/genimage-rpi.cfg
image boot.vfat {
    vfat {
        files = {
            "bcm2711-rpi-4-b.dtb",
            "kernel8.img",
            "overlays/",
            "start4.elf",
            "fixup4.dat"
        }
    }
    size = 256M
}

image root.ext4 {
    ext4 {
        label = "root"
    }
    size = 2G
}
```

##### Hybrid RAID + LUKS шифрование
```ini
# config/genimage-raid-luks.cfg
image raid-luks.img {
    hdimage {
        partition-table-type = "gpt"
        gpt-location = 0
    }

    partition boot {
        partition-type-uuid = U
        image = "boot.vfat"
        offset = 1M
    }

    partition root-luks {
        partition-type-uuid = L
        image = "root-luks.ext4"
        size = 4G
    }

    partition raid-data {
        partition-type-uuid = L
        image = "raid-data.btrfs"
    }
}
```

##### Embedded система с squashfs
```ini
# config/genimage-embedded.cfg
image embedded.img {
    hdimage {
        disk-signature = 0x12345678
    }

    partition boot {
        in-partition-table = false
        image = "boot.vfat"
        offset = 4K
    }

    partition root {
        image = "root.squashfs"
        size = 512M
    }
}
```

#### Расширенные опции genimage
```ini
# Продвинутые настройки
image advanced.img {
    hdimage {
        # Hybrid MBR для совместимости
        partition-table-type = "hybrid"
        mbr = true
        gpt = true

        # Sparse файлы для экономии места
        fill = false

        # Выравнивание разделов
        align = 1M
    }
}
```

### podman unshare - Система безопасности

#### Детальный анализ namespace изоляции
```bash
# Структура namespace изоляции
podman unshare bash -c "
  # User namespace: UID/GID mapping
  # Mount namespace: Изоляция файловой системы
  # Network namespace: Опционально для сервисов
  # IPC namespace: Изоляция межпроцессного взаимодействия

  # Корректное применение прав
  chown -R app:app /target/directory
  chmod 755 /target/directory
"
```

#### Интеграция с различными инструментами
```yaml
# Альтернативы для разных сред
security_tools:
  # Для Ubuntu/Debian
  - podman unshare

  # Для Arch Linux
  - bubblewrap --unshare-all

  # Для RHEL/CentOS
  - unshare --user --map-root-user

  # Для минимальных систем
  - chroot с manual chown
```

## Расширенные паттерны использования

### Многоэтапная сборка с кэшированием
```bash
# Этап 1: Базовая система (кешируется)
rpi-image-gen build --stage base -c config.yaml

# Этап 2: Приложения (быстрее, использует кэш)
rpi-image-gen build --stage apps -c config.yaml --use-cache

# Этап 3: Финализация образа
rpi-image-gen build --stage image -c config.yaml
```

### Параллельная сборка для мультиархитектуры
```bash
# Одновременная сборка для разных платформ
rpi-image-gen build -c arm64-config.yaml &
rpi-image-gen build -c armhf-config.yaml &
wait
```

### Инкрементальная сборка с overlay
```yaml
# Базовый образ + инкрементальные изменения
base_image: debian-base.img

overlays:
  - security-patches.overlay
  - custom-apps.overlay
  - user-config.overlay

result: final-custom.img
```

## Оптимизации производительности

### Кэширование на уровне компонентов
- **bdebstrap**: Кэширование dependency resolution
- **mmdebstrap**: Reuse of downloaded packages
- **genimage**: Incremental image building
- **podman**: Layer caching для повторяющихся операций

### Параллелизация процессов
```yaml
# Конфигурация параллельной обработки
build:
  parallel_jobs: 4
  max_download_workers: 8
  concurrent_layer_processing: true

# Оптимизация для CI/CD
ci_optimization:
  disable_progress_bars: true
  use_package_cache: true
  skip_documentation: true
```

## Диагностика и отладка

### Детальная трассировка компонентов
```bash
# bdebstrap отладка
rpi-image-gen build --debug-bdebstrap -c config.yaml

# mmdebstrap verbose режим
rpi-image-gen build --mmdebstrap-verbose -c config.yaml

# genimage debugging
rpi-image-gen build --genimage-debug -c config.yaml
```

### Анализ производительности по компонентам
```bash
# Профилирование каждого этапа
rpi-image-gen build --profile-components -c config.yaml

# Вывод:
# bdebstrap: 2.3s
# mmdebstrap: 45.7s (80% пакетов из кэша)
# genimage: 8.1s
# Total: 56.1s
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано на основе детального анализа архитектурных компонентов rpi-image-gen