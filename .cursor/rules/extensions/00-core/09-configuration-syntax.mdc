---
alwaysApply: ${cursor.file.path.endsWith(".yaml") && cursor.file.path.includes("config/")}
---

# Правило 09: Синтаксис конфигурации YAML

## Назначение правила
Определяет стандарты синтаксиса и структуру конфигурационных файлов YAML для системы rpi-image-gen. Обеспечивает единообразие и читаемость конфигураций.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Структура конфигурации

### Основные секции (обязательные)
Каждая конфигурация должна содержать минимум три секции:

```yaml
# Устройство (обязательно)
device:
  layer: <device_layer>

# Образ (обязательно)
image:
  layer: <image_layer>
  name: <image_name>

# Базовый слой (обязательно)
layer:
  base: <base_layer>
```

### Расширенные секции (опциональные)
```yaml
# Переменные слоев (рекомендуется для сложных конфигураций)
<layer_name>:
  <parameter>: <value>

# Дополнительные слои
layer:
  <layer_type>: <layer_name>
```

## Паттерны конфигурации

### Простая конфигурация (test.yaml)
```yaml
device:
  layer: rpi5

image:
  layer: hybrid-raid-luks
  name: test-hybrid-raid-luks

hybrid_raid_luks:  # Параметры конкретного слоя
  rootfs_type: ext4
  encryption_enabled: y
  key_method: file
  key_size: 512
  ssd_devices: /dev/sda,/dev/sdb
  boot_part_size: 200M
  root_part_size: 2G
  pmap: crypt
  disk_expansion_enabled: y

layer:
  base: bookworm-minbase
```

### Комплексная конфигурация (complete-system.yaml)
```yaml
# Переменные слоев (группировка по уровням абстракции)
sysctl_kernel:
  ptrace_restrict: y
  aslr_enable: y

sysctl_security:
  core_dump_restrict: y
  perf_event_paranoid: 2

# ... другие переменные слоев

# Конфигурация устройства и образа
device:
  layer: rpi5
  hostname: rpi-complete-system

image:
  layer: hybrid-raid-luks
  boot_part_size: 200%
  root_part_size: 400%
  name: complete-system

# Слои системы
layer:
  base: bookworm-minbase
  build_base: sys-build-base
  sysctl_kernel: sysctl-kernel
  sysctl_security: sysctl-security
  # ... другие слои
```

## Правила именования

### Имена секций
- Использовать snake_case для имен секций
- Избегать дефисов в именах переменных окружения
- Для параметров слоев использовать префикс из `X-Env-VarPrefix`

### Имена параметров
- snake_case для всех параметров
- Булевы значения: `y`/`n`
- Размеры: с единицами измерения (`200M`, `2G`, `100%`)

## Организация по уровням абстракции

### Уровень 1: Базовая система
```yaml
layer:
  base: <debian_suite>-minbase
  build_base: sys-build-base
```

### Уровень 2: Инфраструктурные расширения
```yaml
# Ядро и безопасность
sysctl_kernel: {ptrace_restrict: y, aslr_enable: y}
sysctl_security: {core_dump_restrict: y}
audit_core: {enable: y, buffer_size: 1024}
firewall_basic: {enable: y, default_input_policy: DROP}
```

### Уровень 3: Сервисные расширения
```yaml
# Аутентификация и политика
pam_core: {enable: y, minlen: 12}
password_policies_core: {enable: y, min_length: 12}
fail2ban_core: {enable: y, loglevel: INFO}
apparmor_core: {enable: y, mode: enforce}
```

### Уровень 4: Прикладные расширения
```yaml
# Инструменты и сервисы
docker: {trust_user1: n}
distrobox: {container_name: development, container_image: ubuntu:22.04}
cockpit_web: {enable: y}
```

### Уровень 5: Устройственные расширения
```yaml
# Специфично для оборудования
rpi5_server_config: {disable_hdmi: y, pcie_max_speed: y}
radxa_sata_hat: {pcie_gen3: y, initramfs_sata: y}
```

## Правила корректного написания YAML

### Структура отступов
```yaml
# ❌ НЕПРАВИЛЬНО - смешанные отступы
device:
 layer: rpi5    # 1 пробел
image:
  layer: hybrid-raid-luks  # 2 пробела

# ✅ ПРАВИЛЬНО - consistent отступы (2 пробела)
device:
  layer: rpi5
image:
  layer: hybrid-raid-luks
```

### Вложение блоков
```yaml
# ✅ ПРАВИЛЬНО - правильная иерархия
mmdebstrap:
  packages:
    - package1
  customize-hooks:
    - hook1

# ❌ НЕПРАВИЛЬНО - дублирование блоков
mmdebstrap:
  customize-hooks:
    - hook1
  customize-hooks:  # Дублирование!
    - hook2

# ❌ НЕПРАВИЛЬНО - недопустимый блок rootfs-overlay
mmdebstrap:
  packages: [...]
  rootfs-overlay:  # НЕДОПУСТИМЫЙ БЛОК!
    files: [...]
```

### Префиксы переменных слоев
```yaml
# ✅ ПРАВИЛЬНО - префиксы без дефисов
hybrid_raid_luks:
  encryption_enabled: y

# ❌ НЕПРАВИЛЬНО - дефисы в префиксах
hybrid-raid-luks:  # Bash не поддерживает дефисы в именах переменных!
  encryption_enabled: y
```

### Работа с overlays
```yaml
# ❌ НЕПРАВИЛЬНО - overlays НЕ определяются в YAML!
mmdebstrap:
  rootfs-overlay:  # НЕДОПУСТИМЫЙ БЛОК!
    etc/hostname: |
      my-hostname

# ✅ ПРАВИЛЬНО - overlays в директориях
# Файлы размещаются в: layer/name/device/rootfs-overlay/
# Структура директорий:
layer/name/device/
└── rootfs-overlay/
    ├── etc/
    │   └── hostname
    └── usr/local/bin/
        └── script.sh

# Система автоматически применит overlays из директорий
```

### Типичные ошибки и исправления

#### 1. Дублирование блоков
```yaml
# ❌ Ошибка
layer:
  base: bookworm-minbase
  raid_luks: raid-luks
layer:  # Дублирование!
  docker: docker-debian

# ✅ Исправление
layer:
  base: bookworm-minbase
  raid_luks: raid-luks
  docker: docker-debian
```

#### 2. Неправильная структура слоев
```yaml
# ❌ Ошибка - mmdebstrap в image слое
image:
  layer: hybrid-raid-luks
mmdebstrap:  # Не должно быть в image слое!
  packages: [...]

# ✅ Исправление - разделить на image и storage слои
image:
  layer: hybrid-raid-luks

layer:
  raid_luks: raid-luks  # Отдельный storage слой
```

#### 3. Неправильные типы данных
```yaml
# ❌ Ошибка
layer:
  base: bookworm-minbase  # Строка
  count: "5"             # Строка вместо числа
  enabled: "y"           # Строка вместо boolean

# ✅ Исправление
layer:
  base: bookworm-minbase  # Строка
  count: 5               # Число
  enabled: y             # Boolean (без кавычек)
```

#### 4. Использование недопустимого блока rootfs-overlay
```yaml
# ❌ Ошибка - блок rootfs-overlay в YAML
mmdebstrap:
  packages: [...]
  rootfs-overlay:  # НЕДОПУСТИМЫЙ БЛОК!
    etc/hostname: |
      my-hostname

# ✅ Исправление - overlays через директории
# Создать файл: layer/name/device/rootfs-overlay/etc/hostname
# Содержимое файла: my-hostname
# Система автоматически применит overlay
```

#### 5. Неправильные отступы в многострочном тексте
```yaml
# ❌ Ошибка
customize-hooks:
  - |
  echo "hello"  # Неправильный отступ

# ✅ Исправление
customize-hooks:
  - |
    echo "hello"  # Правильный отступ (минимум 1 пробел после |)
```

### Лучшие практики

#### Группировка переменных
- Группировать переменные по уровням абстракции
- Добавлять комментарии с описанием уровней
- Использовать пустые строки для разделения групп

#### Читабельность
- Использовать inline синтаксис для простых объектов
- Выносить сложные конфигурации на отдельные строки
- Добавлять комментарии для неочевидных параметров
- Избегать чрезмерно длинных строк

#### Валидация
- Проверять соответствие типов данных
- Валидировать допустимые значения параметров
- Тестировать конфигурацию перед сборкой
- Использовать YAML линтеры для проверки синтаксиса

### Инструменты валидации

#### Проверка синтаксиса
```bash
# Python YAML validator
python3 -c "import yaml; yaml.safe_load(open('config.yaml'))"

# yamllint
yamllint config.yaml
```

#### Проверка структуры
```bash
# Проверка обязательных секций
grep -E "^(device|image|layer):" config.yaml

# Проверка отступов (должны быть кратны 2)
grep -n "^ " config.yaml | grep -v "^[0-9]*:  " | grep -v "^[0-9]*:    "
```

## Примеры использования

### Минимальная конфигурация
```yaml
device: {layer: rpi5}
image: {layer: image-rpios, name: minimal}
layer: {base: bookworm-minbase}
```

### Полная конфигурация с переменными
```yaml
# Переменные слоев
docker: {trust_user1: n}
sysctl_kernel: {ptrace_restrict: y}

# Основная конфигурация
device: {layer: rpi5}
image: {layer: hybrid-raid-luks, name: full-system}
layer:
  base: bookworm-minbase
  docker: docker-debian-bookworm
  sysctl_kernel: sysctl-kernel
```

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Синтаксические требования
- [ ] **YAML синтаксис валиден** - проходит парсинг без ошибок
- [ ] **Отступы кратны 2 пробелам** - consistent indentation
- [ ] **Отсутствие дублирования блоков** - каждый блок определен единожды
- [ ] **Правильная иерархия вложенности** - блоки на соответствующих уровнях

### Структурные требования
- [ ] **Обязательные секции присутствуют** - device, image, layer
- [ ] **Переменные слоев без дефисов** - snake_case префиксы
- [ ] **Отсутствие недопустимых блоков** - нет rootfs-overlay в YAML
- [ ] **Overlays через директории** - файлы в device/rootfs-overlay/
- [ ] **Соответствие типов данных** - boolean, string, number корректно

### Качественные показатели
- [ ] **Логическая группировка** - переменные по уровням абстракции
- [ ] **Читабельность кода** - понятные комментарии и структура
- [ ] **Отсутствие антипаттернов** - нет дублирования, неправильных отступов
- [ ] **Соответствие паттернам** - следование примерам из правила

### Автоматизированная проверка
```bash
# Проверка YAML синтаксиса
python3 -c "import yaml; yaml.safe_load(open('config.yaml'))"

# Проверка отступов (только 2 или 4 пробела)
grep -n "^ " config.yaml | grep -v "^[0-9]*:  " | grep -v "^[0-9]*:    "

# Проверка обязательных секций
grep -E "^(device|image|layer):" config.yaml

# Проверка на дублирование блоков
grep -E "^(device|image|layer|mmdebstrap|customize-hooks|rootfs-overlay):" config.yaml | sort | uniq -d
```

### Инструменты и команды
- **yamllint** - для проверки стиля и синтаксиса
- **yaml-validator** - для проверки корректности структуры
- **rpi-image-gen metadata --lint** - для проверки метаданных слоев
- **rpi-image-gen layer --validate-vars** - для проверки переменных

### Источник
Создано на основе анализа конфигураций test.yaml и complete-system.yaml с исправлением выявленных ошибок в структуре YAML файлов rpi-image-gen.