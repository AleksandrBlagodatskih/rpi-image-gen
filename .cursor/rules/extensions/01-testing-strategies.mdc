---
alwaysApply: ${cursor.file.path.includes("test/") || cursor.file.path.includes("layer/")}
---


# Правило 01: Руководство по тестированию

## Назначение правила
Определяет стандарты тестирования расширений rpi-image-gen, обеспечивая быструю разработку с использованием pre-built пакетов и production-ready компонентов.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Руководство по тестированию

### Стратегии тестирования

#### 1. Модульное тестирование
```bash
# Тестирование отдельного слоя
rpi-image-gen layer --describe your-layer-name

# Проверка зависимостей
rpi-image-gen layer --list --depends your-layer-name

# Валидация переменных
rpi-image-gen metadata --lint layer/your-layer.yaml
```

#### 2. Интеграционное тестирование
```bash
# Тестовая сборка с минимальной конфигурацией
rpi-image-gen build --dry-run -c test-config.yaml

# Полная сборка с тестовыми данными
export IGconf_extension_test_mode=true
export IGconf_extension_debug=true
rpi-image-gen build -c test-config.yaml
```

#### 3. Нагрузочное тестирование
```bash
# Мониторинг ресурсов во время сборки
time rpi-image-gen build -c config.yaml

# Проверка размера образа
du -h work/image-*/image-*.img

# Анализ производительности через измерение времени
time rpi-image-gen build -c config.yaml
```

### Автоматизированные тесты

#### Структура тестового проекта:
```
tests/
├── unit/
│   ├── test_layer_validation.py
│   ├── test_variable_expansion.py
│   └── test_dependency_resolution.py
├── integration/
│   ├── test_full_build.py
│   ├── test_custom_hooks.py
│   └── test_image_boot.py
└── fixtures/
    ├── test_configs/
    └── expected_outputs/
```

#### Пример тестового сценария:
```python
#!/usr/bin/env python3
import subprocess
import sys

def test_extension_build():
    """Тестирование сборки расширения"""
    cmd = [
        'rpi-image-gen', 'build',
        '-c', 'tests/fixtures/test-config.yaml',
        '--dry-run'
    ]

    result = subprocess.run(cmd, capture_output=True, text=True)

    if result.returncode != 0:
        print(f"Ошибка сборки: {result.stderr}")
        return False

    return True

if __name__ == "__main__":
    success = test_extension_build()
    sys.exit(0 if success else 1)
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано из раздела документации: `02_руководство_по_тестированию.mdc`
