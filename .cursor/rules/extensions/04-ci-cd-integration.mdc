---
alwaysApply: ${cursor.file.path.includes(".github/") || cursor.file.path.includes("scripts/")}
---


# Правило 04: Интеграция с CI/CD системами

## Назначение правила
Определяет обязательные практики интеграции расширений с системами непрерывной интеграции и доставки.

## Обязательные CI/CD конфигурации 🔴 ОВЯЗАТЕЛЬНЫЕ

### GitHub Actions Workflow 🔴 ОВЯЗАТЕЛЬНЫЕ
```yaml
name: Build and Test Extension
on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y qemu-user-static binfmt-support

    - name: Lint extension
      run: |
        rpi-image-gen metadata --lint layer/*.yaml
        rpi-image-gen layer --lint layer/*.yaml

    - name: Run unit tests
      run: |
        python3 -m pytest tests/unit/ -v

    - name: Build test image
      run: |
        rpi-image-gen build -c tests/test-config.yaml --dry-run

    - name: Generate SBOM
      run: |
        rpi-image-gen build -c config.yaml --sbom
```

### GitLab CI Pipeline 🔴 ОВЯЗАТЕЛЬНЫЕ
```yaml
stages:
  - validate
  - test
  - build
  - deploy

validate_extension:
  stage: validate
  script:
    - rpi-image-gen metadata --lint layer/*.yaml
    - rpi-image-gen layer --describe $CI_EXTENSION_LAYER

test_extension:
  stage: test
  script:
    - python3 -m pytest tests/ --junitxml=report.xml
  artifacts:
    reports:
      junit: report.xml

build_image:
  stage: build
  script:
    - rpi-image-gen build -c config.yaml
  artifacts:
    paths:
      - work/image-*/image-*.img
    expire_in: 1 week

security_scan:
  stage: build
  script:
    - rpi-image-gen build -c config.yaml --sbom --cve-check
  allow_failure: true
```

## Расширенные CI/CD конфигурации ⚠️ РЕКОМЕНДУЕМЫЕ

### Jenkins Pipeline
```groovy
pipeline {
    agent any

    stages {
        stage('Validate') {
            steps {
                sh 'rpi-image-gen metadata --lint layer/*.yaml'
                sh 'rpi-image-gen layer --describe ${EXTENSION_LAYER}'
            }
        }

        stage('Test') {
            steps {
                sh 'python3 -m pytest tests/ -v --junitxml=results.xml'
                junit 'results.xml'
            }
        }

        stage('Build') {
            steps {
                sh 'rpi-image-gen build -c config.yaml'
                archiveArtifacts artifacts: 'work/image-*/image-*.img', fingerprint: true
            }
        }

        stage('Security Scan') {
            steps {
                sh 'rpi-image-gen build -c config.yaml --sbom --cve-check'
                publishHTML(target: [
                    allowMissing: false,
                    alwaysLinkToLastBuild: true,
                    keepAll: true,
                    reportDir: 'security-reports',
                    reportFiles: 'sbom-report.html',
                    reportName: 'SBOM Security Report'
                ])
            }
        }

        stage('Deploy') {
            when {
                branch 'main'
            }
            steps {
                sh './deploy-extension.sh'
            }
        }
    }

    post {
        always {
            cleanWs()
        }
        failure {
            mail to: 'team@company.com',
                 subject: "Failed Pipeline: ${currentBuild.fullDisplayName}",
                 body: "Something is wrong with ${env.BUILD_URL}"
        }
    }
}
```

### Azure DevOps Pipeline
```yaml
trigger:
  branches:
    include:
    - main
    - develop

pool:
  vmImage: 'ubuntu-latest'

variables:
  EXTENSION_LAYER: 'your-extension-name'

stages:
- stage: Validate
  jobs:
  - job: Lint
    steps:
    - script: rpi-image-gen metadata --lint layer/*.yaml
      displayName: 'Lint extension metadata'

- stage: Test
  dependsOn: Validate
  jobs:
  - job: UnitTests
    steps:
    - script: python3 -m pytest tests/unit/ -v
      displayName: 'Run unit tests'

  - job: IntegrationTests
    steps:
    - script: rpi-image-gen build -c tests/test-config.yaml --dry-run
      displayName: 'Integration test build'

- stage: Build
  dependsOn: Test
  jobs:
  - job: BuildImage
    steps:
    - script: rpi-image-gen build -c config.yaml
      displayName: 'Build extension image'

    - publish: $(System.DefaultWorkingDirectory)/work/image-*/image-*.img
      artifact: extension-image

- stage: Deploy
  dependsOn: Build
  condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
  jobs:
  - deployment: DeployToRegistry
    environment: production
    strategy:
      runOnce:
        deploy:
          steps:
          - script: ./deploy-to-registry.sh
            displayName: 'Deploy to extension registry'
```

## Лучшие практики для CI/CD 🔴 ОВЯЗАТЕЛЬНЫЕ

### Структура этапов CI/CD
1. **Validate** - Проверка корректности кода и метаданных
2. **Test** - Запуск модульных и интеграционных тестов
3. **Build** - Сборка образа с расширением
4. **Security** - Сканирование безопасности и генерация SBOM
5. **Deploy** - Развертывание в реестр расширений

### Обязательные проверки на каждом этапе

#### Этап Validate 🔴 ОВЯЗАТЕЛЬНЫЕ
```bash
# Проверка метаданных расширения
rpi-image-gen metadata --lint layer/*.yaml

# Валидация структуры слоев
rpi-image-gen layer --describe your-extension-name

# Проверка синтаксиса скриптов
find . -name "*.sh" -exec bash -n {} \;

# Валидация YAML файлов
find . -name "*.yaml" -exec python3 -c "import yaml; yaml.safe_load(open('{}'))" \;
```

#### Этап Test 🔴 ОВЯЗАТЕЛЬНЫЕ
```bash
# Модульные тесты
python3 -m pytest tests/unit/ -v --cov=extension --cov-report=xml

# Интеграционные тесты
rpi-image-gen build -c tests/integration-config.yaml --dry-run

# Тесты производительности
timeout 1800 rpi-image-gen build -c tests/perf-config.yaml

# Тесты совместимости
for config in tests/compatibility/*.yaml; do
    rpi-image-gen build -c "$config" --dry-run
done
```

#### Этап Security 🔴 ОВЯЗАТЕЛЬНЫЕ
```bash
# Генерация SBOM
rpi-image-gen build -c config.yaml --sbom

# Сканирование уязвимостей
rpi-image-gen build -c config.yaml --sbom --cve-check

# Проверка зависимостей
rpi-image-gen layer --list --depends your-extension-name --security

# Сканирование секретов
gitleaks detect --verbose --redact --config .gitleaks.toml
```

### Управление артефактами 🔴 ОВЯЗАТЕЛЬНЫЕ

#### Сохранение результатов сборки
```yaml
# GitHub Actions
- name: Upload build artifacts
  uses: actions/upload-artifact@v3
  with:
    name: extension-image
    path: work/image-*/image-*.img
    retention-days: 7

# GitLab CI
build_image:
  artifacts:
    paths:
      - work/image-*/image-*.img
    expire_in: 1 week
    reports:
      sbom: sbom-report.json
```

#### Управление версиями артефактов
```bash
# Генерация версии на основе git
EXTENSION_VERSION=$(git describe --tags --always)
BUILD_DATE=$(date -u +"%Y%m%dT%H%M%SZ")
ARTIFACT_NAME="extension-${EXTENSION_VERSION}-${BUILD_DATE}.img"

# Сохранение метаданных сборки
cat > build-metadata.json << EOF
{
  "version": "${EXTENSION_VERSION}",
  "build_date": "${BUILD_DATE}",
  "git_commit": "$(git rev-parse HEAD)",
  "git_branch": "$(git branch --show-current)",
  "build_host": "$(hostname)"
}
EOF
```

## Мониторинг и оповещения 🔴 ОВЯЗАТЕЛЬНЫЕ

### Метрики CI/CD
```yaml
# Отслеживание ключевых метрик
ci_metrics:
  build_time: "<время сборки>"
  test_coverage: "<покрытие тестами>"
  security_score: "<рейтинг безопасности>"
  artifact_size: "<размер артефакта>"

# Оповещения о проблемах
notifications:
  on_failure: true
  on_security_issue: true
  on_performance_regression: true
```

### Автоматические отчеты
```bash
#!/bin/bash
# Генерация отчета о CI/CD запуске

generate_ci_report() {
    local build_status="$1"
    local report_file="ci-report-$(date +%Y%m%d-%H%M%S).md"

    cat > "$report_file" << EOF
# Отчет о CI/CD запуске

## Общая информация
- **Дата и время**: $(date)
- **Статус сборки**: $build_status
- **Коммит**: $(git rev-parse HEAD)
- **Ветка**: $(git branch --show-current)

## Результаты тестирования
\`\`\`
$(python3 -m pytest tests/ --tb=short 2>&1)
\`\`\`

## Метрики безопасности
- Уязвимости: $(grep -c "CRITICAL\|HIGH" security-scan.log 2>/dev/null || echo "0")
- SBOM сгенерирован: $(test -f sbom-report.json && echo "Да" || echo "Нет")

## Производительность
- Время сборки: $(cat build-time.log 2>/dev/null || echo "Не измерено")
- Размер образа: $(du -h work/image-*/image-*.img 2>/dev/null | cut -f1 || echo "Не найден")

## Рекомендации
$(generate_recommendations "$build_status")
EOF

    echo "Отчет сохранен: $report_file"
}

generate_recommendations() {
    local status="$1"

    if [[ "$status" != "success" ]]; then
        echo "- Исправьте выявленные ошибки"
        echo "- Проверьте логи сборки"
        echo "- Обновите тесты при необходимости"
    else
        echo "- Сборка прошла успешно"
        echo "- Рассмотрите возможность оптимизации времени сборки"
        echo "- Проверьте метрики безопасности"
    fi
}
```

## Развертывание расширений 🔴 ОВЯЗАТЕЛЬНЫЕ

### Публикация в реестр
```bash
#!/bin/bash
# Скрипт публикации расширения

publish_extension() {
    local extension_name="$1"
    local version="$2"
    local registry_url="$3"

    # Валидация перед публикацией
    if ! rpi-image-gen metadata --lint "layer/${extension_name}.yaml"; then
        echo "Ошибка: метаданные невалидны"
        exit 1
    fi

    # Создание архива расширения
    tar -czf "${extension_name}-${version}.tar.gz" \
        layer/ \
        config/ \
        tests/ \
        README.md \
        CHANGELOG.md

    # Загрузка в реестр
    curl -X POST "${registry_url}/api/extensions" \
        -H "Authorization: Bearer ${REGISTRY_TOKEN}" \
        -F "extension=@${extension_name}-${version}.tar.gz" \
        -F "metadata=$(cat layer/${extension_name}.yaml)"

    echo "Расширение опубликовано: ${extension_name}@${version}"
}
```

### Автоматическое развертывание
```yaml
# Автоматическая публикация при релизе
on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Publish to Extension Registry
      run: |
        VERSION=${{ github.event.release.tag_name }}
        ./scripts/publish-extension.sh ${{ github.event.repository.name }} $VERSION ${{ secrets.REGISTRY_URL }}
```

## Безопасность в CI/CD ⚠️ РЕКОМЕНДУЕМЫЕ

### Защита секретов
```yaml
# Безопасное управление секретами
secrets:
  registry_token:
    source: vault
    path: secret/ci/registry-token
  signing_key:
    source: aws-secretsmanager
    key: extension-signing-key
```

### Проверка подлинности
```bash
# Проверка подписи артефактов
verify_artifact_signature() {
    local artifact="$1"
    local signature="$2"
    local public_key="$3"

    if ! openssl dgst -sha256 -verify "$public_key" -signature "$signature" "$artifact"; then
        echo "Ошибка: подпись артефакта недействительна"
        exit 1
    fi

    echo "Подпись артефакта проверена успешно"
}
```

## Метрики соблюдения правила 🔴 ОВЯЗАТЕЛЬНЫЕ

### Обязательные элементы CI/CD
- [ ] Автоматический запуск тестов при каждом коммите
- [ ] Валидация метаданных расширения
- [ ] Генерация SBOM для каждой сборки
- [ ] Сканирование безопасности
- [ ] Сохранение артефактов сборки
- [ ] Автоматические отчеты о статусе

### Качественные показатели
- [ ] Время сборки в CI/CD < 30 минут
- [ ] Успешность сборок > 95%
- [ ] Полное покрытие тестами критических функций
- [ ] Автоматическая публикация релизов
- [ ] Интеграция с системами мониторинга

### Непрерывное улучшение
- [ ] Регулярный анализ метрик производительности
- [ ] Оптимизация времени сборки
- [ ] Улучшение качества тестов
- [ ] Автоматизация рутинных задач
- [ ] Мониторинг тенденций качества