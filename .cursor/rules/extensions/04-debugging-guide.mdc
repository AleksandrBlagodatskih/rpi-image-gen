---
alwaysApply: ${cursor.file.path.includes("debug/") || cursor.file.path.includes("logs/")}
---


# Правило 04: Руководство по отладке

## Назначение правила
Определяет стандарты и требования для руководство по отладке в разработке расширений rpi-image-gen.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Руководство по отладке

### Методы диагностики проблем

#### 1. Включение отладочного режима
```bash
# Детальное логирование через переменные окружения
export VERBOSE=1
rpi-image-gen build -c config.yaml

# Сохранение временных файлов (если поддерживается)
rpi-image-gen build -c config.yaml

# Трассировка выполнения хуков через логи
rpi-image-gen build -c config.yaml 2>&1 | tee build.log
```

#### 2. Анализ логов
```bash
# Поиск ошибок в логах
tail -f /tmp/rpi-image-gen-*/build.log | grep -i error

# Анализ времени выполнения этапов
time rpi-image-gen build -c config.yaml

# Проверка переменных окружения
env | grep IGconf_ | sort
```

#### 3. Интерактивная отладка
```bash
# Пошаговое выполнение с анализом этапов
# Разделить сборку на этапы для пошаговой отладки
rpi-image-gen layer --list  # Проверить доступные слои
rpi-image-gen layer --describe problematic_layer  # Проанализировать слой

# Ручная проверка конфигурации
rpi-image-gen metadata --lint config.yaml

# Проверка зависимостей пакетов
apt-cache depends package-name
```

### Распространенные проблемы и решения

#### Проблема: Не разрешаются зависимости слоев
**Симптомы**: Ошибка "Layer not found" или "Dependency resolution failed"
**Решение**:
```bash
# Проверка доступных слоев
rpi-image-gen layer --list

# Валидация зависимостей через анализ слоя
rpi-image-gen layer --describe layer-name

# Проверка путей поиска
find . -name "*.yaml" -path "*/layer/*" | head -10
```

#### Проблема: Не работают переменные конфигурации
**Симптомы**: Переменные не расширяются или имеют неправильные значения
**Решение**:
```bash
# Проверка синтаксиса переменных
rpi-image-gen metadata --lint layer.yaml

# Тестирование расширения переменных
echo "IGconf_extension_test=${IGconf_extension_test}"

# Валидация правил валидации через linting
rpi-image-gen metadata --lint layer/layer-name.yaml
```

#### Проблема: Ошибки в хуках
**Симптомы**: Скрипты завершаются с ошибкой или не выполняются
**Решение**:
```bash
# Проверка синтаксиса скриптов
bash -n hook-script.sh

# Тестирование в изолированной среде
uchroot /tmp/test-chroot 'bash -c "your-command"'

# Анализ прав доступа
ls -la /path/to/script.sh
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано из раздела документации: `05_руководство_по_отладке.mdc`
