---
alwaysApply: ${cursor.file.path.includes("troubleshoot") || cursor.file.content.includes("debug")}
---


# Правило 06: Руководство по отладке расширений

## Назначение правила
Определяет систематический подход к диагностике и устранению проблем в расширениях rpi-image-gen.

## Методы диагностики 🔴 ОВЯЗАТЕЛЬНЫЕ

### 1. Включение отладочного режима 🔴 ОВЯЗАТЕЛЬНЫЕ
```bash
# Детальное логирование
rpi-image-gen build -c config.yaml -v --debug

# Сохранение временных файлов для анализа
rpi-image-gen build -c config.yaml --keep-tmp

# Трассировка выполнения хуков
rpi-image-gen build -c config.yaml --trace-hooks

# Профилирование производительности
rpi-image-gen build -c config.yaml --perf-report

# Анализ зависимостей
rpi-image-gen build -c config.yaml --deps-report
```

### 2. Анализ логов 🔴 ОВЯЗАТЕЛЬНЫЕ
```bash
# Мониторинг логов в реальном времени
tail -f /tmp/rpi-image-gen-*/build.log | grep -i error

# Анализ времени выполнения этапов
rpi-image-gen build -c config.yaml --timing-report

# Проверка переменных окружения
env | grep IGconf_ | sort

# Поиск паттернов ошибок
grep -r "ERROR\|WARNING\|FAILED" /tmp/rpi-image-gen-*/ | head -20

# Анализ размера логов
du -sh /tmp/rpi-image-gen-*/logs/
```

### 3. Интерактивная отладка 🔴 ОВЯЗАТЕЛЬНЫЕ
```bash
# Запуск в интерактивном режиме
rpi-image-gen build -c config.yaml --interactive

# Шаговая отладка хуков
rpi-image-gen build -c config.yaml --step-through-hooks

# Проверка зависимостей пакетов
apt-cache depends package-name
apt-cache rdepends package-name

# Валидация конфигурации без сборки
rpi-image-gen config --validate config.yaml

# Тестирование отдельных компонентов
rpi-image-gen layer --test layer-name
```

## Систематический подход к отладке 🔴 ОВЯЗАТЕЛЬНЫЕ

### Этап 1: Сбор информации
```bash
#!/bin/bash
# Скрипт сбора диагностической информации

collect_diagnostic_info() {
    local output_dir="/tmp/extension-diagnostic-$(date +%Y%m%d-%H%M%S)"
    mkdir -p "$output_dir"

    echo "Сбор диагностической информации в $output_dir"

    # Системная информация
    uname -a > "$output_dir/system-info.txt"
    lsb_release -a >> "$output_dir/system-info.txt" 2>/dev/null || true

    # Информация о rpi-image-gen
    rpi-image-gen --version > "$output_dir/rpi-image-gen-version.txt" 2>&1 || true

    # Переменные окружения
    env | grep -E "(IGconf_|RPI_|DOCKER_)" | sort > "$output_dir/environment-vars.txt"

    # Список установленных пакетов
    dpkg -l | grep -E "(rpi|raspberry|mmdebstrap)" > "$output_dir/installed-packages.txt"

    # Информация о диске
    df -h > "$output_dir/disk-usage.txt"
    du -sh /tmp/rpi-image-gen-* 2>/dev/null >> "$output_dir/disk-usage.txt" || true

    # Логи последних операций
    find /tmp -name "rpi-image-gen-*" -type d -mtime -1 -exec find {} -name "*.log" \; | head -10 | xargs cp -t "$output_dir/" 2>/dev/null || true

    echo "Диагностическая информация собрана в: $output_dir"
    echo "Для анализа создайте архив: tar -czf diagnostic.tar.gz $output_dir"
}
```

### Этап 2: Анализ проблемы
```bash
#!/bin/bash
# Анализатор проблем расширений

analyze_extension_issue() {
    local extension_name="$1"
    local issue_type="$2"

    echo "Анализ проблемы: $issue_type для расширения $extension_name"

    case "$issue_type" in
        "dependency")
            echo "=== Анализ проблем зависимостей ==="
            rpi-image-gen layer --describe "$extension_name" --deps

            # Проверка наличия зависимых слоев
            for dep in $(rpi-image-gen layer --list-deps "$extension_name"); do
                if ! rpi-image-gen layer --exists "$dep"; then
                    echo "ОШИБКА: Зависимость не найдена: $dep"
                fi
            done
            ;;

        "variable")
            echo "=== Анализ проблем переменных ==="
            rpi-image-gen metadata --lint "layer/${extension_name}.yaml"

            # Проверка расширения переменных
            grep -o 'IGconf_[a-zA-Z_][a-zA-Z0-9_]*' "layer/${extension_name}.yaml" | sort -u | while read -r var; do
                if [[ -z "${!var:-}" ]]; then
                    echo "ПРЕДУПРЕЖДЕНИЕ: Переменная $var не установлена"
                fi
            done
            ;;

        "hook")
            echo "=== Анализ проблем хуков ==="
            find . -name "*hook*" -path "*/${extension_name}/*" -type f | while read -r hook; do
                echo "Проверка хука: $hook"
                if ! bash -n "$hook"; then
                    echo "ОШИБКА: Синтаксическая ошибка в $hook"
                fi
            done
            ;;

        "build")
            echo "=== Анализ проблем сборки ==="
            # Проверка размера временных файлов
            du -sh /tmp/rpi-image-gen-* 2>/dev/null || echo "Временные файлы не найдены"

            # Анализ последних логов
            find /tmp -name "rpi-image-gen-*" -type d -mtime -1 | head -1 | xargs find -name "*.log" 2>/dev/null | head -5 | xargs tail -50 2>/dev/null || true
            ;;

        *)
            echo "Неизвестный тип проблемы. Доступные типы: dependency, variable, hook, build"
            ;;
    esac
}
```

### Этап 3: Устранение проблемы
```bash
#!/bin/bash
# Инструмент исправления распространенных проблем

fix_common_issues() {
    local extension_name="$1"
    local issue_type="$2"

    echo "Попытка автоматического исправления проблемы: $issue_type"

    case "$issue_type" in
        "dependency")
            echo "Исправление проблем зависимостей..."

            # Добавление отсутствующих зависимостей
            for dep in $(rpi-image-gen layer --list-deps "$extension_name" 2>/dev/null); do
                if ! rpi-image-gen layer --exists "$dep"; then
                    echo "Добавление зависимости: $dep"
                    # Логика добавления зависимости
                fi
            done
            ;;

        "permission")
            echo "Исправление проблем с правами доступа..."

            # Исправление прав на файлы хуков
            find "layer/${extension_name}" -name "*.sh" -exec chmod +x {} \;

            # Проверка прав на конфигурационные файлы
            find "layer/${extension_name}" -name "*.yaml" -exec chmod 644 {} \;
            ;;

        "syntax")
            echo "Исправление синтаксических ошибок..."

            # Исправление распространенных ошибок в bash скриптах
            find "layer/${extension_name}" -name "*.sh" -exec sed -i 's/==/=/g' {} \;

            # Добавление shebang если отсутствует
            find "layer/${extension_name}" -name "*.sh" | xargs grep -L "^#!/bin/bash" | xargs sed -i '1i#!/bin/bash'
            ;;

        "cleanup")
            echo "Очистка временных файлов и кэша..."

            # Очистка временных файлов rpi-image-gen
            rm -rf /tmp/rpi-image-gen-*

            # Очистка кэша apt
            apt-get clean

            # Очистка Docker образов (если используется)
            docker system prune -f 2>/dev/null || true
            ;;

        *)
            echo "Автоматическое исправление для типа '$issue_type' не реализовано"
            echo "Рекомендуется ручное вмешательство"
            ;;
    esac
}
```

## Распространенные проблемы и решения 🔴 ОВЯЗАТЕЛЬНЫЕ

### Проблема: Не разрешаются зависимости слоев
**Симптомы**: Ошибка "Layer not found" или "Dependency resolution failed"
**Решение**:
```bash
# Проверка доступных слоев
rpi-image-gen layer --list

# Валидация зависимостей
rpi-image-gen layer --describe layer-name --deps

# Проверка путей поиска
find . -name "*.yaml" -path "*/layer/*" | head -10

# Исправление: добавление отсутствующих зависимостей
echo "# X-Env-Layer-Requires: essential,missing-dependency" >> layer/your-layer.yaml
```

### Проблема: Не работают переменные конфигурации
**Симптомы**: Переменные не расширяются или имеют неправильные значения
**Решение**:
```bash
# Проверка синтаксиса переменных
rpi-image-gen metadata --lint layer.yaml

# Тестирование расширения переменных
echo "IGconf_extension_test=${IGconf_extension_test}"

# Валидация правил валидации
rpi-image-gen layer --describe layer-name --validate-vars

# Исправление: проверка корректности METABEGIN блока
grep -A 20 "^# METABEGIN" layer/your-layer.yaml
```

### Проблема: Ошибки в хуках
**Симптомы**: Скрипты завершаются с ошибкой или не выполняются
**Решение**:
```bash
# Проверка синтаксиса скриптов
bash -n hook-script.sh

# Тестирование в изолированной среде
uchroot /tmp/test-chroot 'bash -c "your-command"'

# Анализ прав доступа
ls -la /path/to/script.sh

# Исправление: добавление обработки ошибок
sed -i '1a set -euo pipefail' hook-script.sh
```

### Проблема: Недостаточно места на диске
**Симптомы**: Сборка прерывается из-за нехватки места
**Решение**:
```bash
# Проверка использования диска
df -h

# Очистка временных файлов
rm -rf /tmp/rpi-image-gen-*

# Очистка кэша пакетов
apt-get clean && apt-get autoclean

# Проверка размера work директории
du -sh work/

# Исправление: увеличение места или оптимизация конфигурации
echo "Уменьшите размер образа или очистите диск"
```

### Проблема: Конфликты пакетов
**Симптомы**: Ошибки зависимостей пакетов во время установки
**Решение**:
```bash
# Проверка конфликтов
apt-cache depends conflicting-package

# Анализ установленных пакетов
dpkg -l | grep conflicting

# Исправление: разрешение конфликтов в mmdebstrap секции
# Удаление конфликтующих пакетов или изменение порядка установки
```

### Проблема: Проблемы с сетью
**Симптомы**: Ошибки загрузки пакетов или подключения к репозиториям
**Решение**:
```bash
# Проверка сетевых настроек
ip addr show
ip route show

# Тестирование DNS
nslookup deb.debian.org

# Проверка прокси настроек
env | grep -i proxy

# Исправление: настройка сети или использование локального mirror
echo "Настройте сетевые параметры или используйте локальный кэш"
```

## Расширенные методы отладки ⚠️ РЕКОМЕНДУЕМЫЕ

### Отладка в изолированной среде
```bash
#!/bin/bash
# Создание изолированной среды для отладки

create_debug_environment() {
    local extension_name="$1"
    local debug_dir="/tmp/debug-${extension_name}"

    echo "Создание отладочной среды: $debug_dir"

    # Создание минимальной chroot среды
    mkdir -p "$debug_dir"
    debootstrap --foreign --arch=arm64 bullseye "$debug_dir" http://deb.debian.org/debian/

    # Копирование расширения для тестирования
    cp -r "layer/${extension_name}" "$debug_dir/tmp/"

    # Настройка qemu для эмуляции
    cp /usr/bin/qemu-aarch64-static "$debug_dir/usr/bin/"

    echo "Отладочная среда готова. Вход: chroot $debug_dir /bin/bash"
}
```

### Профилирование производительности
```bash
#!/bin/bash
# Профилирование операций расширения

profile_extension_performance() {
    local extension_name="$1"
    local profile_file="/tmp/profile-${extension_name}.log"

    echo "Профилирование расширения: $extension_name"

    # Мониторинг системных ресурсов
    {
        echo "Timestamp,CPU%,Memory%,Disk_IO,Network_IO"
        while ps aux | grep -q "rpi-image-gen.*build"; do
            timestamp=$(date +%s)
            cpu=$(top -bn1 | grep "Cpu(s)" | awk '{print $2 + $4}')
            mem=$(free | awk 'NR==2{printf "%.2f", $3*100/$2}')
            disk=$(iostat -x sda 1 1 2>/dev/null | awk 'NR==4{print $4 + $5}' || echo "0")
            net=$(cat /proc/net/dev | awk 'NR==3{print $2 + $10}')

            echo "${timestamp},${cpu},${mem},${disk},${net}"
            sleep 2
        done
    } > "$profile_file"

    echo "Профиль сохранен: $profile_file"
}
```

### Автоматизированное тестирование отладки
```bash
#!/bin/bash
# Автоматизированный тестер отладочных сценариев

run_debug_test_suite() {
    local extension_name="$1"
    local test_results="/tmp/debug-test-results-${extension_name}.json"

    echo "Запуск набора тестов отладки для: $extension_name"

    # Тест 1: Синтаксическая проверка
    syntax_check=$(check_syntax_errors "$extension_name")
    echo "Синтаксис: $syntax_check"

    # Тест 2: Проверка зависимостей
    deps_check=$(check_dependencies "$extension_name")
    echo "Зависимости: $deps_check"

    # Тест 3: Тест сборки
    build_test=$(test_build_process "$extension_name")
    echo "Сборка: $build_test"

    # Тест 4: Проверка переменных
    vars_test=$(check_variables "$extension_name")
    echo "Переменные: $vars_test"

    # Сохранение результатов
    cat > "$test_results" << EOF
    {
        "extension": "$extension_name",
        "timestamp": "$(date -Iseconds)",
        "tests": {
            "syntax": "$syntax_check",
            "dependencies": "$deps_check",
            "build": "$build_test",
            "variables": "$vars_test"
        }
    }
    EOF

    echo "Результаты сохранены: $test_results"
}
```

## Инструменты отладки 🔴 ОВЯЗАТЕЛЬНЫЕ

### Основные инструменты
- `strace` - трассировка системных вызовов
- `gdb` - отладчик для C/C++ компонентов
- `valgrind` - анализ памяти и производительности
- `tcpdump` - анализ сетевых проблем
- `iotop` - мониторинг I/O операций

### Специфические для rpi-image-gen
```bash
# Отладочные команды rpi-image-gen
rpi-image-gen build --debug-mode          # Расширенная отладка
rpi-image-gen build --verbose 3           # Максимальная детализация
rpi-image-gen build --log-level DEBUG     # Детальное логирование
rpi-image-gen build --trace-all           # Трассировка всех операций
rpi-image-gen build --profile             # Профилирование производительности
```

## Профилактика проблем 🔴 ОВЯЗАТЕЛЬНЫЕ

### Статический анализ кода
```bash
#!/bin/bash
# Автоматический анализ кода расширения

static_code_analysis() {
    local extension_path="$1"

    echo "Статический анализ кода: $extension_path"

    # Проверка bash скриптов
    find "$extension_path" -name "*.sh" -exec shellcheck {} \;

    # Проверка Python кода (если есть)
    find "$extension_path" -name "*.py" -exec pylint {} \;

    # Проверка YAML файлов
    find "$extension_path" -name "*.yaml" -exec yamllint {} \;

    # Проверка JSON файлов (если есть)
    find "$extension_path" -name "*.json" -exec jq . {} \; >/dev/null

    echo "Анализ завершен"
}
```

### Мониторинг качества кода
```bash
#!/bin/bash
# Мониторинг качества кода расширения

monitor_code_quality() {
    local extension_name="$1"

    # Подсчет метрик качества
    files_count=$(find "layer/${extension_name}" -type f | wc -l)
    scripts_count=$(find "layer/${extension_name}" -name "*.sh" | wc -l)
    lines_count=$(find "layer/${extension_name}" -name "*.sh" -exec wc -l {} \; | awk '{sum += $1} END {print sum}')
    functions_count=$(grep -r "^function " "layer/${extension_name}" | wc -l)
    error_handling_count=$(grep -r "set -euo pipefail\|trap\|die\|error_exit" "layer/${extension_name}" | wc -l)

    # Расчет показателей
    avg_lines_per_file=$((lines_count / scripts_count))
    error_handling_ratio=$((error_handling_count * 100 / scripts_count))

    echo "Метрики качества для $extension_name:"
    echo "  Файлов: $files_count"
    echo "  Скриптов: $scripts_count"
    echo "  Строк кода: $lines_count"
    echo "  Функций: $functions_count"
    echo "  Среднее строк на файл: $avg_lines_per_file"
    echo "  Процент обработки ошибок: ${error_handling_ratio}%"

    # Рекомендации
    if [[ $avg_lines_per_file -gt 100 ]]; then
        echo "РЕКОМЕНДАЦИЯ: Разбейте большие файлы на модули"
    fi

    if [[ $error_handling_ratio -lt 50 ]]; then
        echo "РЕКОМЕНДАЦИЯ: Улучшите обработку ошибок"
    fi
}
```

## Метрики соблюдения правила 🔴 ОВЯЗАТЕЛЬНЫЕ

### Эффективность отладки
- [ ] Быстрое выявление проблем
- [ ] Полный сбор диагностической информации
- [ ] Систематический подход к анализу
- [ ] Эффективное устранение проблем

### Качество отладочной инфраструктуры
- [ ] Наличие отладочных инструментов
- [ ] Документирование процедур отладки
- [ ] Автоматизация повторяющихся задач
- [ ] Профилактика распространенных проблем

### Непрерывное улучшение
- [ ] Анализ паттернов ошибок
- [ ] Улучшение инструментов отладки
- [ ] Обновление руководств
- [ ] Профилактические меры