---
alwaysApply: ${cursor.file.path.match(/\.(yaml|sh|py|md)$/) && !cursor.file.path.includes("test/")}
---


# Правило 08: Стандарты качества кода расширений

## Назначение правила
Определяет обязательные стандарты качества кода для разработки надежных и поддерживаемых расширений rpi-image-gen.

## Обязательные стандарты 🔴 ОВЯЗАТЕЛЬНЫЕ

### 1. Документирование кода 🔴 ОВЯЗАТЕЛЬНЫЕ
```bash
#!/bin/bash
# Описание: Краткое описание функции скрипта
# Назначение: Для чего предназначен этот скрипт
# Параметры:
#   $1 - первый параметр (тип: string, обязательный)
#   $2 - второй параметр (тип: integer, опциональный, по умолчанию: 60)
# Возвращает:
#   0 - успех, операция завершена успешно
#   1 - ошибка, проблемы с выполнением
#   2 - предупреждение, частичное выполнение
# Примеры:
#   script.sh "параметр1" 30
# Зависимости: curl, jq
# Автор: Имя Разработчика
# Версия: 1.0.0
# Дата создания: 2024-01-01

set -euo pipefail

SCRIPT_NAME="$(basename "${BASH_SOURCE[0]}")"
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
SCRIPT_VERSION="1.0.0"

# Глобальные константы
readonly MAX_RETRIES=3
readonly TIMEOUT=30

# Функция логирования с уровнями
log_info() { echo "[$SCRIPT_NAME] INFO: $*" >&2; }
log_warn() { echo "[$SCRIPT_NAME] WARN: $*" >&2; }
log_error() { echo "[$SCRIPT_NAME] ERROR: $*" >&2; }
log_debug() { [[ "${DEBUG:-false}" = "true" ]] && echo "[$SCRIPT_NAME] DEBUG: $*" >&2; }

# Функция отображения справки
show_help() {
    cat << EOF
Использование: $SCRIPT_NAME [ОПЦИИ] АРГУМЕНТЫ

ОПЦИИ:
    -h, --help          Показать эту справку
    -v, --version       Показать версию
    --debug             Включить отладочный режим
    --dry-run           Тестовый запуск без изменений

АРГУМЕНТЫ:
    PARAM1              Первый обязательный параметр
    PARAM2              Второй опциональный параметр

ПРИМЕРЫ:
    $SCRIPT_NAME --debug "value1" 42
    $SCRIPT_NAME --dry-run "test"

EOF
}

# Функция отображения версии
show_version() {
    echo "$SCRIPT_NAME версия $SCRIPT_VERSION"
}

# Основная функция с полным описанием
main() {
    local param1=""
    local param2=""
    local debug=false
    local dry_run=false

    # Разбор аргументов командной строки
    while [[ $# -gt 0 ]]; do
        case $1 in
            -h|--help)
                show_help
                exit 0
                ;;
            -v|--version)
                show_version
                exit 0
                ;;
            --debug)
                debug=true
                shift
                ;;
            --dry-run)
                dry_run=true
                shift
                ;;
            -*)
                log_error "Неизвестная опция: $1"
                show_help
                exit 1
                ;;
            *)
                if [[ -z "$param1" ]]; then
                    param1="$1"
                elif [[ -z "$param2" ]]; then
                    param2="$1"
                else
                    log_error "Слишком много аргументов"
                    show_help
                    exit 1
                fi
                shift
                ;;
        esac
    done

    # Валидация обязательных параметров
    [[ -n "$param1" ]] || {
        log_error "Первый параметр обязателен"
        show_help
        exit 1
    }

    # Настройка отладки
    if [[ "$debug" = "true" ]]; then
        export DEBUG=true
        set -x
    fi

    log_info "Начало выполнения скрипта"
    log_debug "Param1: $param1, Param2: ${param2:-'не задан'}"

    # Реализация основной логики
    if [[ "$dry_run" = "true" ]]; then
        log_info "Тестовый запуск - изменения не будут применены"
        return 0
    fi

    # Выполнение операций
    if execute_operations "$param1" "$param2"; then
        log_info "Операция завершена успешно"
        return 0
    else
        log_error "Операция завершилась с ошибкой"
        return 1
    fi
}

# Функция выполнения операций с подробным описанием
execute_operations() {
    local param1="$1"
    local param2="$2"

    log_debug "Выполнение операций с параметрами: $param1, ${param2:-'default'}"

    # Этап 1: Подготовка
    prepare_environment || return 1

    # Этап 2: Валидация
    validate_parameters "$param1" "$param2" || return 1

    # Этап 3: Выполнение
    perform_main_operation "$param1" "$param2" || return 1

    # Этап 4: Верификация
    verify_results || return 1

    # Этап 5: Очистка
    cleanup_resources || return 1

    return 0
}

# Подробно документированные вспомогательные функции
prepare_environment() {
    # Описание: Подготовка окружения для выполнения операций
    # Возвращает: 0 при успехе, 1 при ошибке
    log_debug "Подготовка окружения"

    # Создание временных директорий
    if ! mkdir -p "/tmp/${SCRIPT_NAME}"; then
        log_error "Не удалось создать временную директорию"
        return 1
    fi

    # Проверка зависимостей
    local deps=("curl" "jq")
    for dep in "${deps[@]}"; do
        if ! command -v "$dep" >/dev/null 2>&1; then
            log_error "Требуется установить: $dep"
            return 1
        fi
    done

    return 0
}

validate_parameters() {
    local param1="$1"
    local param2="$2"

    # Валидация param1
    if [[ ! "$param1" =~ ^[a-zA-Z0-9_-]+$ ]]; then
        log_error "Param1 должен содержать только буквы, цифры, дефисы и подчеркивания"
        return 1
    fi

    # Валидация param2 (если задан)
    if [[ -n "$param2" ]] && [[ ! "$param2" =~ ^[0-9]+$ ]]; then
        log_error "Param2 должен быть числом"
        return 1
    fi

    return 0
}

perform_main_operation() {
    local param1="$1"
    local param2="$2"

    log_info "Выполнение основной операции"

    # Реализация с подробным логированием
    log_debug "Обработка: $param1"

    # Имитация длительной операции с прогрессом
    for i in {1..10}; do
        log_debug "Прогресс: $i/10"
        sleep 0.1
    done

    log_debug "Операция завершена"
    return 0
}

verify_results() {
    log_debug "Верификация результатов"

    # Проверка результатов выполнения
    if [[ -f "/tmp/${SCRIPT_NAME}/result" ]]; then
        log_debug "Результат найден"
        return 0
    else
        log_error "Результат не найден"
        return 1
    fi
}

cleanup_resources() {
    log_debug "Очистка ресурсов"

    # Удаление временных файлов
    if [[ -d "/tmp/${SCRIPT_NAME}" ]]; then
        rm -rf "/tmp/${SCRIPT_NAME}"
        log_debug "Временные файлы удалены"
    fi

    return 0
}

# Функция обработки сигналов
cleanup_on_exit() {
    local exit_code=$?

    log_debug "Очистка при выходе (код: $exit_code)"

    # Экстренная очистка
    if [[ -d "/tmp/${SCRIPT_NAME}" ]]; then
        rm -rf "/tmp/${SCRIPT_NAME}"
    fi

    exit "$exit_code"
}

# Установка обработчиков сигналов
trap cleanup_on_exit EXIT
trap 'log_error "Получен сигнал SIGINT"; exit 130' INT
trap 'log_error "Получен сигнал SIGTERM"; exit 143' TERM

# Запуск основной функции
main "$@"
```

### 2. Обработка ошибок 🔴 ОВЯЗАТЕЛЬНЫЕ
```bash
#!/bin/bash
set -euo pipefail

# Глобальные настройки обработки ошибок
readonly MAX_RETRIES=3
readonly RETRY_DELAY=5

# Стандартизированные функции обработки ошибок
die() {
    # Описание: Немедленное завершение с сообщением об ошибке
    # Параметры: $1 - сообщение об ошибке
    log_error "Критическая ошибка: $1"
    exit 1
}

warn() {
    # Описание: Предупреждение без завершения
    # Параметры: $1 - сообщение предупреждения
    log_warn "Предупреждение: $1"
}

error_exit() {
    # Описание: Грамотное завершение с кодом ошибки
    # Параметры: $1 - сообщение, $2 - код ошибки (опционально, по умолчанию 1)
    local message="$1"
    local code="${2:-1}"

    log_error "$message"
    exit "$code"
}

# Функция повторных попыток
retry_operation() {
    # Описание: Повтор операции при неудаче
    # Параметры: $1 - максимальное количество попыток, остальные - команда
    local max_attempts="$1"
    shift

    local attempt=1
    while [[ $attempt -le $max_attempts ]]; do
        log_debug "Попытка $attempt из $max_attempts"

        if "$@"; then
            log_debug "Операция успешна с попытки $attempt"
            return 0
        else
            log_warn "Попытка $attempt завершилась неудачей"

            if [[ $attempt -lt $max_attempts ]]; then
                log_info "Повтор через $RETRY_DELAY секунд..."
                sleep $RETRY_DELAY
            fi
        fi

        ((attempt++))
    done

    log_error "Операция завершилась неудачей после $max_attempts попыток"
    return 1
}

# Функция валидации с подробными сообщениями
validate_or_die() {
    # Описание: Валидация условия с завершением при неудаче
    # Параметры: $1 - условие, $2 - сообщение об ошибке
    local condition="$1"
    local error_message="$2"

    if ! eval "$condition"; then
        die "$error_message"
    fi
}

# Функция обработки частичных сбоев
handle_partial_failure() {
    # Описание: Обработка ситуаций, когда часть операций успешна
    # Параметры: $1 - код возврата, $2 - сообщение
    local exit_code="$1"
    local message="$2"

    case "$exit_code" in
        0)
            log_info "Операция полностью успешна"
            ;;
        1)
            warn "$message: частичная неудача, продолжаем"
            ;;
        2)
            error_exit "$message: критическая ошибка"
            ;;
        *)
            error_exit "$message: неизвестная ошибка (код: $exit_code)"
            ;;
    esac
}

# Безопасное выполнение команд
safe_execute() {
    # Описание: Безопасное выполнение команд с обработкой ошибок
    # Параметры: команда и аргументы
    local command_output
    local command_error

    log_debug "Выполнение: $*"

    if command_output=$( "$@" 2>&1 ); then
        log_debug "Команда выполнена успешно"
        echo "$command_output"
        return 0
    else
        local exit_code=$?
        log_error "Команда завершилась с кодом $exit_code: $*"
        log_error "Вывод: $command_output"
        return $exit_code
    fi
}

# Проверка зависимостей с понятными сообщениями
check_dependencies() {
    # Описание: Проверка наличия необходимых инструментов
    local missing_deps=()

    for dep in "$@"; do
        if ! command -v "$dep" >/dev/null 2>&1; then
            missing_deps+=("$dep")
        fi
    done

    if [[ ${#missing_deps[@]} -gt 0 ]]; then
        error_exit "Отсутствуют необходимые зависимости: ${missing_deps[*]}"
    fi
}

# Примеры использования
validate_system() {
    # Проверка системных требований
    validate_or_die '[[ -w /tmp ]] ' "Нет прав записи в /tmp"
    validate_or_die '[[ $(id -u) -eq 0 ]]' "Требуются права root"

    # Проверка зависимостей
    check_dependencies curl jq wget

    # Безопасное выполнение команд
    if ! safe_execute mkdir -p /opt/extension; then
        handle_partial_failure $? "Не удалось создать директорию"
    fi

    return 0
}
```

### 3. Безопасность 🔴 ОВЯЗАТЕЛЬНЫЕ
```bash
#!/bin/bash
set -euo pipefail

# Функции безопасного выполнения
safe_chroot() {
    # Описание: Безопасное выполнение в chroot окружении
    # Параметры: $1 - путь к chroot, остальные - команда
    local chroot_path="$1"
    shift

    # Валидация пути chroot
    [[ -d "$chroot_path" ]] || die "Chroot директория не существует: $chroot_path"
    [[ -r "$chroot_path" ]] || die "Нет прав чтения chroot директории"

    # Использование uchroot для безопасности
    uchroot "$chroot_path" sh -c "$*"
}

secure_file_operations() {
    # Описание: Безопасные операции с файлами
    local file_path="$1"
    local operation="$2"

    # Валидация пути
    if [[ "$file_path" =~ \.\. ]]; then
        die "Небезопасный путь: $file_path"
    fi

    # Проверка прав доступа
    case "$operation" in
        read)
            [[ -r "$file_path" ]] || die "Нет прав чтения: $file_path"
            ;;
        write)
            [[ -w "$(dirname "$file_path")" ]] || die "Нет прав записи в директорию: $(dirname "$file_path")"
            ;;
        execute)
            [[ -x "$file_path" ]] || die "Файл не исполняемый: $file_path"
            ;;
    esac
}

# Защита от инъекций команд
sanitize_input() {
    # Описание: Очистка входных данных от опасных символов
    # Параметры: $1 - входная строка
    local input="$1"

    # Удаление опасных символов
    input=$(printf '%s\n' "$input" | sed 's/[;&|`$()<>]//g')

    # Ограничение длины
    input=$(printf '%.100s' "$input")

    echo "$input"
}

secure_variable_expansion() {
    # Описание: Безопасное расширение переменных
    local var_name="$1"
    local default_value="${2:-}"

    # Проверка корректности имени переменной
    if [[ ! "$var_name" =~ ^[A-Z_][A-Z0-9_]*$ ]]; then
        die "Некорректное имя переменной: $var_name"
    fi

    # Безопасное получение значения
    local value="${!var_name:-$default_value}"

    # Санитизация значения
    sanitize_input "$value"
}

# Управление привилегиями
drop_privileges() {
    # Описание: Сброс привилегий до безопасного уровня
    local target_user="${1:-nobody}"
    local target_group="${2:-nogroup}"

    # Проверка возможности смены пользователя
    if [[ $(id -u) -eq 0 ]]; then
        log_info "Сброс привилегий до $target_user:$target_group"

        # Смена группы
        if ! sg "$target_group" -c "true"; then
            warn "Не удалось сменить группу на $target_group"
        fi

        # Смена пользователя
        if ! su -s /bin/sh "$target_user"; then
            warn "Не удалось сменить пользователя на $target_user"
        fi
    else
        warn "Нет прав для сброса привилегий"
    fi
}

# Безопасное создание временных файлов
create_secure_temp() {
    # Описание: Создание безопасных временных файлов/директорий
    local type="$1"  # file или directory
    local template="${2:-tempXXXXXX}"

    local temp_path

    case "$type" in
        file)
            temp_path=$(mktemp "/tmp/${template}")
            ;;
        directory)
            temp_path=$(mktemp -d "/tmp/${template}")
            ;;
        *)
            die "Неизвестный тип: $type. Используйте 'file' или 'directory'"
            ;;
    esac

    # Установка безопасных прав
    chmod 600 "$temp_path" 2>/dev/null || true

    # Очистка при выходе
    trap "rm -rf '$temp_path'" EXIT

    echo "$temp_path"
}

# Валидация SSL/TLS сертификатов
validate_certificate() {
    # Описание: Проверка валидности SSL сертификатов
    local cert_file="$1"
    local key_file="$2"

    # Проверка существования файлов
    [[ -f "$cert_file" ]] || die "Сертификат не найден: $cert_file"
    [[ -f "$key_file" ]] || die "Ключ не найден: $key_file"

    # Проверка прав доступа
    [[ -r "$cert_file" ]] || die "Нет прав чтения сертификата"
    [[ -r "$key_file" ]] || die "Нет прав чтения ключа"

    # Валидация с помощью openssl
    if ! openssl x509 -in "$cert_file" -text -noout >/dev/null 2>&1; then
        die "Некорректный сертификат: $cert_file"
    fi

    if ! openssl rsa -in "$key_file" -check >/dev/null 2>&1; then
        die "Некорректный ключ: $key_file"
    fi

    # Проверка соответствия ключа и сертификата
    local cert_modulus
    local key_modulus

    cert_modulus=$(openssl x509 -in "$cert_file" -modulus -noout | openssl md5)
    key_modulus=$(openssl rsa -in "$key_file" -modulus -noout | openssl md5)

    if [[ "$cert_modulus" != "$key_modulus" ]]; then
        die "Ключ не соответствует сертификату"
    fi

    log_info "Сертификат и ключ валидны"
}

# Мониторинг безопасности
security_audit() {
    # Описание: Проверка безопасности системы
    log_info "Запуск аудита безопасности"

    # Проверка открытых портов
    local open_ports
    open_ports=$(netstat -tln 2>/dev/null | grep LISTEN | wc -l)
    if [[ $open_ports -gt 10 ]]; then
        warn "Обнаружено $open_ports открытых портов"
    fi

    # Проверка прав на системные файлы
    local critical_files=("/etc/passwd" "/etc/shadow" "/etc/sudoers")
    for file in "${critical_files[@]}"; do
        if [[ -f "$file" ]] && [[ $(stat -c %a "$file") != "600" ]]; then
            warn "Небезопасные права на $file: $(stat -c %a "$file")"
        fi
    done

    # Проверка запущенных процессов root
    local root_processes
    root_processes=$(ps -U root -u root u | wc -l)
    if [[ $root_processes -gt 20 ]]; then
        warn "Много процессов запущено от root: $root_processes"
    fi

    log_info "Аудит безопасности завершен"
}

# Примеры безопасного кода
secure_example() {
    # Безопасная обработка пользовательского ввода
    local user_input
    read -r user_input

    # Санитизация
    local safe_input
    safe_input=$(sanitize_input "$user_input")

    # Безопасное использование в команде
    printf '%s\n' "$safe_input" | grep "pattern"

    # Безопасное создание временного файла
    local temp_file
    temp_file=$(create_secure_temp file)

    # Безопасная запись
    printf '%s\n' "$safe_input" > "$temp_file"

    # Безопасное выполнение в chroot
    safe_chroot "/chroot/path" "command with args"

    # Сброс привилегий
    drop_privileges "appuser" "appgroup"
}
```

### 4. Переносимость 🔴 ОВЯЗАТЕЛЬНЫЕ
```bash
#!/bin/bash

# Определение платформы и архитектуры
detect_platform() {
    # Описание: Определение текущей платформы
    local os_name
    local os_version
    local arch

    # Определение ОС
    if [[ -f /etc/os-release ]]; then
        os_name=$(grep '^ID=' /etc/os-release | cut -d= -f2 | tr -d '"')
        os_version=$(grep '^VERSION_ID=' /etc/os-release | cut -d= -f2 | tr -d '"')
    elif [[ -f /etc/redhat-release ]]; then
        os_name="rhel"
        os_version=$(rpm -q --qf '%{VERSION}' redhat-release)
    elif [[ -f /etc/debian_version ]]; then
        os_name="debian"
        os_version=$(cat /etc/debian_version)
    else
        os_name="unknown"
        os_version="unknown"
    fi

    # Определение архитектуры
    case $(uname -m) in
        x86_64)
            arch="amd64"
            ;;
        aarch64)
            arch="arm64"
            ;;
        armv7l)
            arch="armhf"
            ;;
        *)
            arch=$(uname -m)
            ;;
    esac

    echo "PLATFORM_OS=$os_name"
    echo "PLATFORM_VERSION=$os_version"
    echo "PLATFORM_ARCH=$arch"
}

# Кросс-платформенные утилиты
cross_platform_tools() {
    # Поиск команд в разных местах
    find_command() {
        local cmd="$1"
        local alternatives=("${@:2}")

        if command -v "$cmd" >/dev/null 2>&1; then
            echo "$cmd"
            return 0
        fi

        for alt in "${alternatives[@]}"; do
            if command -v "$alt" >/dev/null 2>&1; then
                echo "$alt"
                return 0
            fi
        done

        return 1
    }

    # Определение менеджера пакетов
    detect_package_manager() {
        if command -v apt-get >/dev/null 2>&1; then
            echo "apt"
        elif command -v yum >/dev/null 2>&1; then
            echo "yum"
        elif command -v dnf >/dev/null 2>&1; then
            echo "dnf"
        elif command -v pacman >/dev/null 2>&1; then
            echo "pacman"
        else
            echo "unknown"
        fi
    }

    # Универсальная установка пакетов
    install_package() {
        local package="$1"
        local pkg_manager
        pkg_manager=$(detect_package_manager)

        case "$pkg_manager" in
            apt)
                apt-get update && apt-get install -y "$package"
                ;;
            yum)
                yum install -y "$package"
                ;;
            dnf)
                dnf install -y "$package"
                ;;
            pacman)
                pacman -S --noconfirm "$package"
                ;;
            *)
                die "Неподдерживаемый менеджер пакетов: $pkg_manager"
                ;;
        esac
    }

    # Экспорт функций для использования
    export -f find_command
    export -f detect_package_manager
    export -f install_package
}

# Платформо-независимые пути
platform_independent_paths() {
    # Определение стандартных директорий
    get_config_dir() {
        if [[ -n "${XDG_CONFIG_HOME:-}" ]]; then
            echo "$XDG_CONFIG_HOME"
        elif [[ -n "${HOME:-}" ]]; then
            echo "$HOME/.config"
        else
            echo "/etc"
        fi
    }

    get_cache_dir() {
        if [[ -n "${XDG_CACHE_HOME:-}" ]]; then
            echo "$XDG_CACHE_HOME"
        elif [[ -n "${HOME:-}" ]]; then
            echo "$HOME/.cache"
        else
            echo "/tmp"
        fi
    }

    get_data_dir() {
        if [[ -n "${XDG_DATA_HOME:-}" ]]; then
            echo "$XDG_DATA_HOME"
        elif [[ -n "${HOME:-}" ]]; then
            echo "$HOME/.local/share"
        else
            echo "/usr/local/share"
        fi
    }

    # Нормализация путей
    normalize_path() {
        local path="$1"

        # Преобразование в абсолютный путь
        if [[ "$path" != /* ]]; then
            path="$(cd "$(dirname "$path")" && pwd)/$(basename "$path")"
        fi

        # Удаление двойных слэшей и ./
        path=$(echo "$path" | sed 's|//|/|g; s|^\./||')

        echo "$path"
    }

    # Проверка существования пути с учетом платформы
    path_exists() {
        local path="$1"

        if [[ -e "$path" ]] || [[ -h "$path" ]]; then
            return 0
        else
            return 1
        fi
    }
}

# Обработка текстовых кодировок
handle_text_encoding() {
    # Определение кодировки файла
    detect_encoding() {
        local file="$1"

        if command -v file >/dev/null 2>&1; then
            file --mime-type "$file" | cut -d= -f2 | tr -d ' '
        elif command -v uchardet >/dev/null 2>&1; then
            uchardet "$file"
        else
            echo "unknown"
        fi
    }

    # Конвертация в UTF-8
    convert_to_utf8() {
        local input_file="$1"
        local output_file="$2"
        local source_encoding
        source_encoding=$(detect_encoding "$input_file")

        case "$source_encoding" in
            "text/plain; charset=iso-8859-1"|"iso-8859-1")
                iconv -f iso-8859-1 -t utf-8 "$input_file" > "$output_file"
                ;;
            "text/plain; charset=windows-1251"|"windows-1251")
                iconv -f windows-1251 -t utf-8 "$input_file" > "$output_file"
                ;;
            "utf-8"|"text/plain; charset=utf-8")
                cp "$input_file" "$output_file"
                ;;
            *)
                warn "Неизвестная кодировка $source_encoding, копирование как есть"
                cp "$input_file" "$output_file"
                ;;
        esac
    }

    # Безопасная обработка текстовых файлов
    safe_read_file() {
        local file="$1"
        local encoding
        encoding=$(detect_encoding "$file")

        if [[ "$encoding" != "utf-8" ]] && [[ "$encoding" != "text/plain; charset=utf-8" ]]; then
            local temp_file
            temp_file=$(mktemp)
            convert_to_utf8 "$file" "$temp_file"
            cat "$temp_file"
            rm -f "$temp_file"
        else
            cat "$file"
        fi
    }
}

# Атомарные операции с файлами
atomic_file_operations() {
    # Атомарная запись файла
    atomic_write() {
        local file="$1"
        local content="$2"

        local temp_file
        temp_file=$(mktemp "$(dirname "$file")/tmp.XXXXXX")

        printf '%s\n' "$content" > "$temp_file"

        if mv "$temp_file" "$file"; then
            return 0
        else
            rm -f "$temp_file"
            return 1
        fi
    }

    # Атомарное обновление файла
    atomic_update() {
        local file="$1"
        local update_function="$2"

        local temp_file
        temp_file=$(mktemp "$(dirname "$file")/tmp.XXXXXX")

        # Применение функции обновления
        if "$update_function" "$file" > "$temp_file"; then
            if mv "$temp_file" "$file"; then
                return 0
            fi
        fi

        rm -f "$temp_file"
        return 1
    }
}

# Мониторинг ресурсов с учетом платформы
platform_aware_monitoring() {
    # Получение информации о памяти
    get_memory_info() {
        if command -v free >/dev/null 2>&1; then
            free -h
        elif [[ -f /proc/meminfo ]]; then
            grep -E "(MemTotal|MemFree|Buffers|Cached)" /proc/meminfo
        else
            echo "Информация о памяти недоступна"
        fi
    }

    # Получение информации о дисках
    get_disk_info() {
        if command -v df >/dev/null 2>&1; then
            df -h
        else
            echo "Информация о дисках недоступна"
        fi
    }

    # Получение загрузки системы
    get_system_load() {
        if [[ -f /proc/loadavg ]]; then
            cat /proc/loadavg
        elif command -v uptime >/dev/null 2>&1; then
            uptime
        else
            echo "Информация о загрузке недоступна"
        fi
    }
}

# Инициализация кросс-платформенного окружения
init_cross_platform() {
    log_info "Инициализация кросс-платформенного окружения"

    # Определение платформы
    eval "$(detect_platform)"

    log_info "Платформа: $PLATFORM_OS $PLATFORM_VERSION ($PLATFORM_ARCH)"

    # Настройка кросс-платформенных инструментов
    cross_platform_tools

    # Настройка путей
    platform_independent_paths

    # Настройка обработки текста
    handle_text_encoding

    # Настройка атомарных операций
    atomic_file_operations

    # Настройка мониторинга
    platform_aware_monitoring

    log_info "Кросс-платформенное окружение инициализировано"
}
```

## Рекомендуемые практики ⚠️ РЕКОМЕНДУЕМЫЕ

### 1. Модульность функций
```bash
# Принципы модульного дизайна
modular_design_principles() {
    # Единая ответственность
    single_responsibility() {
        # Каждая функция делает только одну вещь
        validate_input() { ... }
        process_data() { ... }
        save_results() { ... }
    }

    # Принцип открытости/закрытости
    open_closed_principle() {
        # Функции открыты для расширения, закрыты для модификации
        generic_processor() {
            local processor_type="$1"
            shift

            case "$processor_type" in
                json)
                    process_json "$@"
                    ;;
                xml)
                    process_xml "$@"
                    ;;
                yaml)
                    process_yaml "$@"
                    ;;
                *)
                    die "Неподдерживаемый тип процессора: $processor_type"
                    ;;
            esac
        }
    }

    # Принцип инверсии зависимостей
    dependency_injection() {
        # Зависимости передаются извне
        process_with_dependencies() {
            local logger="${1:-log_info}"
            local validator="${2:-validate_input}"
            local processor="${3:-process_data}"

            "$logger" "Начало обработки"

            if "$validator" "$input_data"; then
                "$processor" "$input_data"
                "$logger" "Обработка завершена"
            else
                "$logger" "Валидация завершилась неудачей"
                return 1
            fi
        }
    }
}

# Шаблоны проектирования для bash
design_patterns() {
    # Шаблон "Фабрика"
    create_object_factory() {
        create_logger() {
            local type="$1"

            case "$type" in
                file)
                    file_logger "$@"
                    ;;
                syslog)
                    syslog_logger "$@"
                    ;;
                console)
                    console_logger "$@"
                    ;;
            esac
        }

        create_validator() {
            local type="$1"

            case "$type" in
                email)
                    validate_email "$@"
                    ;;
                url)
                    validate_url "$@"
                    ;;
                json)
                    validate_json "$@"
                    ;;
            esac
        }
    }

    # Шаблон "Стратегия"
    strategy_pattern() {
        define_backup_strategy() {
            local strategy="$1"

            case "$strategy" in
                full)
                    perform_full_backup "$@"
                    ;;
                incremental)
                    perform_incremental_backup "$@"
                    ;;
                differential)
                    perform_differential_backup "$@"
                    ;;
            esac
        }
    }

    # Шаблон "Наблюдатель"
    observer_pattern() {
        add_event_listener() {
            local event="$1"
            local callback="$2"

            # Регистрация обработчика события
            eval "event_${event}_handlers+=('$callback')"
        }

        trigger_event() {
            local event="$1"
            shift

            # Вызов всех обработчиков события
            local handlers_var="event_${event}_handlers[@]"
            local handlers=("${!handlers_var}")

            for handler in "${handlers[@]}"; do
                "$handler" "$@"
            done
        }
    }
}

# Система плагинов
plugin_system() {
    # Загрузка плагинов
    load_plugins() {
        local plugin_dir="$1"

        for plugin in "$plugin_dir"/*.sh; do
            if [[ -f "$plugin" ]]; then
                log_info "Загрузка плагина: $(basename "$plugin")"
                source "$plugin"
            fi
        done
    }

    # Регистрация хуков плагинов
    register_plugin_hooks() {
        local hook_name="$1"
        local hook_function="$2"

        eval "${hook_name}_hooks+=('$hook_function')"
    }

    # Выполнение хуков
    execute_hooks() {
        local hook_name="$1"
        shift

        local hooks_var="${hook_name}_hooks[@]"
        local hooks=("${!hooks_var}")

        for hook in "${hooks[@]}"; do
            if command -v "$hook" >/dev/null 2>&1; then
                "$hook" "$@"
            fi
        done
    }
}
```

### 2. Валидация входных данных
```bash
# Комплексная система валидации
advanced_validation() {
    # Валидация типов данных
    validate_type() {
        local value="$1"
        local expected_type="$2"

        case "$expected_type" in
            string)
                [[ -n "$value" ]] || return 1
                ;;
            integer)
                [[ "$value" =~ ^-?[0-9]+$ ]] || return 1
                ;;
            float)
                [[ "$value" =~ ^-?[0-9]*\.?[0-9]+$ ]] || return 1
                ;;
            boolean)
                [[ "$value" =~ ^(true|false|0|1|yes|no)$ ]] || return 1
                ;;
            email)
                [[ "$value" =~ ^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$ ]] || return 1
                ;;
            url)
                [[ "$value" =~ ^https?:// ]] || return 1
                ;;
            ip)
                [[ "$value" =~ ^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$ ]] || return 1
                ;;
            json)
                echo "$value" | jq . >/dev/null 2>&1 || return 1
                ;;
            yaml)
                echo "$value" | python3 -c "import yaml, sys; yaml.safe_load(sys.stdin)" >/dev/null 2>&1 || return 1
                ;;
        esac
    }

    # Валидация диапазонов
    validate_range() {
        local value="$1"
        local min_val="$2"
        local max_val="$3"

        if [[ "$value" =~ ^[0-9]+(\.[0-9]+)?$ ]]; then
            awk -v val="$value" -v min="$min_val" -v max="$max_val" \
                'BEGIN { if (val >= min && val <= max) exit 0; else exit 1 }'
        else
            return 1
        fi
    }

    # Валидация длины
    validate_length() {
        local value="$1"
        local min_len="$2"
        local max_len="${3:-}"

        local length=${#value}

        if [[ -n "$max_len" ]]; then
            [[ $length -ge $min_len ]] && [[ $length -le $max_len ]]
        else
            [[ $length -ge $min_len ]]
        fi
    }

    # Валидация списков
    validate_in_list() {
        local value="$1"
        shift
        local allowed_values=("$@")

        for allowed in "${allowed_values[@]}"; do
            if [[ "$value" = "$allowed" ]]; then
                return 0
            fi
        done

        return 1
    }

    # Комплексная валидация
    validate_complex() {
        local value="$1"
        local rules="$2"  # JSON с правилами валидации

        # Парсинг правил и применение валидации
        local type
        type=$(echo "$rules" | jq -r '.type // "string"')

        if ! validate_type "$value" "$type"; then
            return 1
        fi

        # Валидация диапазона
        local min_val max_val
        min_val=$(echo "$rules" | jq -r '.min // empty')
        max_val=$(echo "$rules" | jq -r '.max // empty')

        if [[ -n "$min_val" ]] && ! validate_range "$value" "$min_val" "$max_val"; then
            return 1
        fi

        # Валидация длины
        local min_len max_len
        min_len=$(echo "$rules" | jq -r '.minLength // empty')
        max_len=$(echo "$rules" | jq -r '.maxLength // empty')

        if [[ -n "$min_len" ]] && ! validate_length "$value" "$min_len" "$max_len"; then
            return 1
        fi

        # Валидация списка допустимых значений
        local allowed
        allowed=$(echo "$rules" | jq -r '.enum[] // empty' 2>/dev/null)
        if [[ -n "$allowed" ]]; then
            local allowed_array=($allowed)
            if ! validate_in_list "$value" "${allowed_array[@]}"; then
                return 1
            fi
        fi

        return 0
    }
}

# Система обработки ошибок валидации
validation_error_handling() {
    # Структурированные сообщения об ошибках
    validation_error() {
        local field="$1"
        local value="$2"
        local error_type="$3"
        local expected="$4"

        case "$error_type" in
            type)
                log_error "Поле '$field' имеет некорректный тип. Ожидалось: $expected, получено: $value"
                ;;
            range)
                log_error "Поле '$field' вне допустимого диапазона. Ожидалось: $expected, получено: $value"
                ;;
            length)
                log_error "Поле '$field' имеет некорректную длину. Ожидалось: $expected, получено: ${#value} символов"
                ;;
            enum)
                log_error "Поле '$field' содержит недопустимое значение. Разрешенные значения: $expected, получено: $value"
                ;;
            required)
                log_error "Обязательное поле '$field' не указано"
                ;;
            *)
                log_error "Ошибка валидации поля '$field': $error_type"
                ;;
        esac
    }

    # Сбор всех ошибок валидации
    collect_validation_errors() {
        local errors=()

        # Пример сбора ошибок
        if ! validate_type "$input1" "string"; then
            errors+=("input1:type:string")
        fi

        if ! validate_range "$input2" 1 100; then
            errors+=("input2:range:1-100")
        fi

        # Возврат массива ошибок
        printf '%s\n' "${errors[@]}"
    }

    # Форматированный вывод ошибок
    display_validation_errors() {
        local errors=("$@")

        if [[ ${#errors[@]} -gt 0 ]]; then
            log_error "Найдены ошибки валидации:"

            for error in "${errors[@]}"; do
                IFS=':' read -r field type expected <<< "$error"
                validation_error "$field" "${!field}" "$type" "$expected"
            done

            return 1
        fi

        return 0
    }
}
```

## Автоматизация контроля качества 🔴 ОВЯЗАТЕЛЬНЫЕ

### Система линтинга
```bash
#!/bin/bash
# Автоматизированный линтер для расширений

extension_linter() {
    local extension_path="$1"
    local lint_errors=0

    log_info "Запуск линтера для расширения: $extension_path"

    # Проверка shell скриптов
    while IFS= read -r -d '' script; do
        log_debug "Проверка скрипта: $script"

        # Синтаксическая проверка
        if ! bash -n "$script"; then
            log_error "Синтаксическая ошибка в $script"
            ((lint_errors++))
        fi

        # Проверка с shellcheck (если установлен)
        if command -v shellcheck >/dev/null 2>&1; then
            if ! shellcheck -x "$script"; then
                log_warn "Предупреждения shellcheck в $script"
            fi
        fi

        # Проверка документирования
        if ! grep -q "^# Описание:" "$script"; then
            log_error "Отсутствует документация в $script"
            ((lint_errors++))
        fi

        # Проверка обработки ошибок
        if ! grep -q "set -euo pipefail" "$script"; then
            log_warn "Отсутствует 'set -euo pipefail' в $script"
        fi

    done < <(find "$extension_path" -name "*.sh" -print0)

    # Проверка YAML файлов
    while IFS= read -r -d '' yaml_file; do
        log_debug "Проверка YAML: $yaml_file"

        if command -v yamllint >/dev/null 2>&1; then
            if ! yamllint "$yaml_file"; then
                log_error "Ошибка yamllint в $yaml_file"
                ((lint_errors++))
            fi
        fi

        # Проверка синтаксиса YAML
        if ! python3 -c "import yaml; yaml.safe_load(open('$yaml_file'))" 2>/dev/null; then
            log_error "Некорректный YAML синтаксис в $yaml_file"
            ((lint_errors++))
        fi

    done < <(find "$extension_path" -name "*.yaml" -print0)

    # Проверка структуры проекта
    check_project_structure "$extension_path" || ((lint_errors++))

    if [[ $lint_errors -gt 0 ]]; then
        log_error "Найдено ошибок линтера: $lint_errors"
        return 1
    else
        log_info "Линтер прошел успешно"
        return 0
    fi
}

check_project_structure() {
    local extension_path="$1"

    # Обязательные файлы
    local required_files=("layer/extension.yaml" "README.md")

    for file in "${required_files[@]}"; do
        if [[ ! -f "$extension_path/$file" ]]; then
            log_error "Отсутствует обязательный файл: $file"
            return 1
        fi
    done

    # Рекомендуемая структура директорий
    local recommended_dirs=("tests" "scripts" "config")

    for dir in "${recommended_dirs[@]}"; do
        if [[ ! -d "$extension_path/$dir" ]]; then
            log_warn "Рекомендуется создать директорию: $dir"
        fi
    done

    return 0
}
```

### Система метрик качества
```bash
#!/bin/bash
# Сбор метрик качества кода

collect_quality_metrics() {
    local extension_path="$1"
    local metrics_file="/tmp/quality-metrics.json"

    log_info "Сбор метрик качества для: $extension_path"

    # Метрики файлов
    local file_count
    file_count=$(find "$extension_path" -type f | wc -l)

    local script_count
    script_count=$(find "$extension_path" -name "*.sh" | wc -l)

    local yaml_count
    yaml_count=$(find "$extension_path" -name "*.yaml" | wc -l)

    # Метрики кода
    local total_lines
    total_lines=$(find "$extension_path" -name "*.sh" -exec wc -l {} \; | awk '{sum += $1} END {print sum}')

    local avg_lines_per_script
    avg_lines_per_script=$((total_lines / script_count))

    # Метрики качества
    local documented_functions
    documented_functions=$(grep -r "^# Описание:" "$extension_path" | wc -l)

    local error_handling_count
    error_handling_count=$(grep -r "set -euo pipefail\|die\|error_exit" "$extension_path" | wc -l)

    local function_count
    function_count=$(grep -r "^[a-zA-Z_][a-zA-Z0-9_]*()" "$extension_path" | wc -l)

    # Метрики сложности
    local complexity_score
    complexity_score=$(calculate_complexity "$extension_path")

    # Формирование отчета
    cat > "$metrics_file" << EOF
    {
        "extension": "$(basename "$extension_path")",
        "timestamp": "$(date -Iseconds)",
        "files": {
            "total": $file_count,
            "scripts": $script_count,
            "yaml": $yaml_count
        },
        "code": {
            "total_lines": $total_lines,
            "avg_lines_per_script": $avg_lines_per_script,
            "functions": $function_count,
            "documented_functions": $documented_functions
        },
        "quality": {
            "error_handling": $error_handling_count,
            "complexity_score": $complexity_score,
            "documentation_ratio": $((documented_functions * 100 / function_count))
        }
    }
EOF

    log_info "Метрики сохранены: $metrics_file"
    cat "$metrics_file"
}

calculate_complexity() {
    local extension_path="$1"
    local complexity=0

    # Простая оценка сложности на основе количества ветвлений
    while IFS= read -r -d '' script; do
        local if_count
        local case_count
        local loop_count

        if_count=$(grep -c "^[[:space:]]*if " "$script")
        case_count=$(grep -c "^[[:space:]]*case " "$script")
        loop_count=$(grep -c "^[[:space:]]*\(for\|while\) " "$script")

        complexity=$((complexity + if_count + case_count + loop_count))
    done < <(find "$extension_path" -name "*.sh" -print0)

    echo "$complexity"
}
```

## Метрики соблюдения правила 🔴 ОВЯЗАТЕЛЬНЫЕ

### Обязательные стандарты качества
- [ ] Полная документация всех функций и скриптов
- [ ] Корректная обработка ошибок во всех скриптах
- [ ] Безопасное выполнение всех операций
- [ ] Переносимость кода между платформами
- [ ] Модульная структура с единой ответственностью

### Рекомендуемые практики
- [ ] Использование шаблонов проектирования
- [ ] Комплексная валидация входных данных
- [ ] Автоматизированный контроль качества
- [ ] Сбор и анализ метрик качества
- [ ] Непрерывное улучшение качества кода

### Пороги качества
- **Документированность**: > 90% функций документированы
- **Обработка ошибок**: > 95% скриптов имеют корректную обработку ошибок
- **Сложность**: Средняя сложность функций < 10
- **Линтер**: 0 критических ошибок, < 5 предупреждений
- **Тестовое покрытие**: > 80% основных функций протестированы