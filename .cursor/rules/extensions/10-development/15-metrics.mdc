---
alwaysApply: ${cursor.file.path.includes("prometheus/") || cursor.file.path.includes("grafana/")}
---


# Правило 14: Метрики и мониторинг

## Назначение правила
Определяет стандарты и требования для метрики и мониторинг в разработке расширений rpi-image-gen.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Метрики и мониторинг

### Метрики качества расширений:

#### 1. Метрики разработки:
```bash
# Время разработки
# Сложность кода (цикломатическая сложность)
# Покрытие тестами
# Количество дефектов на 1000 строк кода
```

#### 2. Метрики производительности:
```bash
# Время сборки образа
# Размер результирующего образа
# Использование памяти во время сборки
# Время загрузки системы
```

#### 3. Метрики безопасности:
```bash
# Количество уязвимостей (CVE)
# Рейтинг безопасности SBOM
# Количество пакетов с известными уязвимостями
# Время реакции на обновления безопасности
```

### Инструменты мониторинга:

#### 1. Мониторинг сборки:
```bash
#!/bin/bash
# Скрипт мониторинга процесса сборки

BUILD_START=$(date +%s)
echo "Начало сборки: $(date)"

# Мониторинг использования ресурсов
while ps aux | grep -q rpi-image-gen; do
    echo "$(date): CPU $(top -bn1 | grep rpi-image-gen | awk '{print $9}')% MEM $(top -bn1 | grep rpi-image-gen | awk '{print $10}')%"
    sleep 10
done

BUILD_END=$(date +%s)
echo "Время сборки: $((BUILD_END - BUILD_START)) секунд"
```

#### 2. Мониторинг развернутых систем:
```yaml
# Prometheus метрики для расширения
- name: extension_uptime_seconds
  type: counter
  help: Время работы расширения в секундах

- name: extension_requests_total
  type: counter
  help: Общее количество запросов к расширению

- name: extension_errors_total
  type: counter
  help: Общее количество ошибок расширения
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано из раздела документации: `15_метрики_и_мониторинг.mdc`
