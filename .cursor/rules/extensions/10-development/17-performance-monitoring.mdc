---
alwaysApply: ${cursor.file.path.includes("layer/") || cursor.file.path.includes("config/")}
---

# Правило 17-2: Мониторинг и лучшие практики оптимизации

## Назначение правила
Определяет стандарты мониторинга производительности и лучшие практики оптимизации сборки расширений rpi-image-gen.

## Производительность сборки

### Метрики производительности 🔴 ОБЯЗАТЕЛЬНЫЕ

#### Целевые показатели времени сборки
```bash
# Максимальное время сборки для разных типов расширений
MAX_BUILD_TIME_MINIMAL="10 minutes"      # Минимальное расширение
MAX_BUILD_TIME_STANDARD="30 minutes"     # Стандартное расширение
MAX_BUILD_TIME_COMPLEX="60 minutes"      # Сложное расширение

# Измерение времени сборки
measure_build_time() {
    local start_time=$(date +%s)
    rpi-image-gen build -c config.yaml "$@"
    local end_time=$(date +%s)
    local duration=$((end_time - start_time))

    echo "Время сборки: $duration секунд"

    if [[ $duration -gt 1800 ]]; then  # 30 минут
        echo "ПРЕДУПРЕЖДЕНИЕ: Сборка занимает слишком много времени"
        return 1
    fi

    return 0
}
```

#### Оптимизация размера образа
```yaml
# Минимизация размера образа
mmdebstrap:
  variant: minbase  # Использовать минимальную базу
  packages:
    - essential    # Только необходимые пакеты
    - --no-install-recommends  # Без рекомендуемых пакетов

  customize-hooks:
    - |
      # Очистка кэша пакетов
      apt-get clean
      rm -rf /var/lib/apt/lists/*

      # Удаление ненужных файлов
      rm -rf /usr/share/doc/*
      rm -rf /usr/share/man/*
      rm -rf /usr/share/locale/*  # Если не нужны локали
```

### Кэширование для ускорения сборки

#### Кэширование apt пакетов
```bash
# Настройка локального apt-cacher
setup_apt_cache() {
    # Установка apt-cacher-ng для локального кэширования
    apt-get install -y apt-cacher-ng

    # Настройка клиента
    echo 'Acquire::http::Proxy "http://localhost:3142";' > /etc/apt/apt.conf.d/01proxy

    # Запуск сервиса
    systemctl enable apt-cacher-ng
    systemctl start apt-cacher-ng
}
```

#### Кэширование Docker образов
```yaml
# Использование предварительно собранных базовых образов
mmdebstrap:
  variant: docker  # Использовать Docker-like слои
  packages:
    - base-files
    - base-passwd

# Кэширование слоев для повторных сборок
build-cache:
  enable: true
  layers:
    - base-system
    - development-tools
    - runtime-dependencies
```

## Оптимизация ресурсов

### Управление памятью и CPU
```bash
# Ограничение ресурсов для сборки
build_with_limits() {
    local mem_limit="2G"
    local cpu_limit="2"

    # Использование systemd-run для ограничения ресурсов
    systemd-run --scope \
        --property=MemoryLimit="$mem_limit" \
        --property=CPUQuota="$((cpu_limit * 100))%" \
        rpi-image-gen build -c config.yaml
}
```

### Параллельная обработка
```bash
# Параллельная установка пакетов
parallel_package_install() {
    local packages=("nginx" "php" "mysql-server" "redis-server")

    # Параллельная установка с помощью xargs
    printf '%s\n' "${packages[@]}" | \
        xargs -n 1 -P 4 apt-get install -y --no-install-recommends
}
```

## Мониторинг производительности

### Автоматическое профилирование
```bash
#!/bin/bash
# Скрипт профилирования сборки

profile_build() {
    local config_file="$1"
    local profile_log="/tmp/build-profile-$(date +%s).log"

    echo "Профилирование сборки: $config_file" > "$profile_log"

    # Мониторинг системных ресурсов
    {
        echo "Timestamp,CPU%,Memory%,Disk_IO,Build_Step"
        local step="init"

        while ps aux | grep -q "rpi-image-gen.*build"; do
            local timestamp=$(date +%s)
            local cpu=$(top -bn1 | grep -E "(rpi-image-gen|mmdebstrap)" | awk '{sum += $9} END {print sum}' || echo "0")
            local mem=$(free | awk 'NR==2{printf "%.2f", $3*100/$2}' || echo "0")
            local disk_io=$(iostat -x 1 1 2>/dev/null | awk 'NR==4{print $4 + $5}' || echo "0")

            # Определение текущего этапа сборки
            if ps aux | grep -q "mmdebstrap.*packages"; then
                step="packages"
            elif ps aux | grep -q "customize.*hook"; then
                step="customize"
            elif ps aux | grep -q "update-initramfs"; then
                step="initramfs"
            fi

            echo "${timestamp},${cpu},${mem},${disk_io},${step}"
            sleep 5
        done
    } >> "$profile_log"

    echo "Профиль сохранен: $profile_log"
}
```

### Метрики оптимизации
```yaml
# Ключевые метрики для мониторинга
performance_metrics:
  build_time:
    target: "< 30 min"
    current: "25 min"
    status: "✅ OK"

  image_size:
    target: "< 2GB"
    current: "1.8GB"
    status: "✅ OK"

  package_checks:
    target: "0 redundant checks"
    current: "0"
    status: "✅ OK"

  initramfs_updates:
    target: "1 update per build"
    current: "1"
    status: "✅ OK"
```

## Лучшие практики оптимизации

### Предварительная подготовка
1. **Анализ зависимостей** - проверить все зависимости заранее
2. **Кэширование** - настроить локальное кэширование пакетов
3. **Базовые образы** - использовать предварительно подготовленные базовые образы
4. **Многоэтапная сборка** - разделить сборку на этапы для лучшего кэширования

### Оптимизация конфигурации
```yaml
# Оптимизированная конфигурация сборки
device:
  layer: pi5
  # Минимальные настройки устройства

image:
  layer: image-rpios
  boot_part_size: 256M  # Минимальный размер загрузочного раздела
  root_part_size: 4G    # Оптимальный размер корневого раздела

layer:
  base: bookworm-minbase  # Минимальная база
  # Только необходимые слои
```

### Мониторинг и улучшение
```bash
# Автоматический анализ производительности
analyze_performance() {
    local build_log="$1"

    echo "Анализ производительности сборки..."

    # Анализ времени по этапам
    grep "duration\|time" "$build_log" | while read -r line; do
        echo "Этап: $line"
    done

    # Рекомендации по оптимизации
    if grep -q "update-initramfs" "$build_log"; then
        local initramfs_count=$(grep -c "update-initramfs" "$build_log")
        if [[ $initramfs_count -gt 1 ]]; then
            echo "ПРЕДУПРЕЖДЕНИЕ: Множественные обновления initramfs ($initramfs_count)"
            echo "РЕКОМЕНДАЦИЯ: Обновляйте initramfs только один раз в конце"
        fi
    fi
}
```

