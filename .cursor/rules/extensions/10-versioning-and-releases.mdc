---
alwaysApply: ${cursor.file.content.includes("version") || cursor.file.content.includes("Version")}
---


# Правило 10: Управление версиями и релизы расширений

## Назначение правила
Определяет стандарты семантического версионирования, жизненного цикла релизов и процессов управления версиями для расширений rpi-image-gen.

## Стратегия версионирования 🔴 ОВЯЗАТЕЛЬНЫЕ

### 1. Семантическое версионирование (SemVer) 🔴 ОВЯЗАТЕЛЬНЫЕ
```bash
# Формат версии: MAJOR.MINOR.PATCH
# Пример: 2.1.3

MAJOR=2  # Несовместимые изменения API
MINOR=1  # Добавление функциональности (обратная совместимость)
PATCH=3  # Исправление ошибок (обратная совместимость)

# Правила обновления версий:
#
# MAJOR (X.y.z):
# - Несовместимые изменения API
# - Изменения архитектуры расширения
# - Удаление функциональности
# - Изменения обязательных переменных конфигурации
#
# MINOR (x.Y.z):
# - Добавление новой функциональности
# - Улучшения существующих функций
# - Добавление опциональных переменных
# - Расширение API без breaking changes
#
# PATCH (x.y.Z):
# - Исправление ошибок
# - Безопасные улучшения
# - Обновление зависимостей (patch level)
# - Исправление документации
```

### Система управления версиями
```bash
#!/bin/bash
# Система управления версиями расширения

version_manager() {
    local action="$1"
    local extension_name="$2"
    shift 2

    case "$action" in
        "init")
            init_versioning "$extension_name"
            ;;
        "bump")
            bump_version "$extension_name" "$@"
            ;;
        "release")
            create_release "$extension_name" "$@"
            ;;
        "check")
            check_version_consistency "$extension_name"
            ;;
        "validate")
            validate_version_format "$extension_name"
            ;;
        *)
            echo "Использование: $0 {init|bump|release|check|validate} extension-name [args...]"
            exit 1
            ;;
    esac
}

init_versioning() {
    local extension_name="$1"
    local version_file="VERSION"
    local changelog_file="CHANGELOG.md"

    # Создание файла версии
    echo "1.0.0" > "$version_file"

    # Создание CHANGELOG.md
    cat > "$changelog_file" << 'EOF'
# Changelog

Все изменения в расширении документируются в этом файле.

Формат версий соответствует [Semantic Versioning](https://semver.org/).

## [Unreleased]

### Added
- Начальная версия расширения

### Changed
- Ничего

### Deprecated
- Ничего

### Removed
- Ничего

### Fixed
- Ничего

### Security
- Ничего
EOF

    # Обновление метаданных расширения
    if [[ -f "layer/${extension_name}.yaml" ]]; then
        sed -i "s/X-Env-Layer-Version:.*/X-Env-Layer-Version: 1.0.0/" "layer/${extension_name}.yaml"
    fi

    git add VERSION CHANGELOG.md "layer/${extension_name}.yaml"
    git commit -m "feat: initialize versioning system"

    echo "Система версионирования инициализирована для $extension_name"
}

bump_version() {
    local extension_name="$1"
    local bump_type="$2"  # major, minor, patch
    local current_version
    local new_version

    # Получение текущей версии
    if [[ -f "VERSION" ]]; then
        current_version=$(cat VERSION)
    else
        echo "Файл VERSION не найден. Инициализируйте версионирование: $0 init $extension_name"
        exit 1
    fi

    # Расчет новой версии
    new_version=$(calculate_new_version "$current_version" "$bump_type")

    echo "Обновление версии: $current_version -> $new_version"

    # Обновление файла версии
    echo "$new_version" > VERSION

    # Обновление метаданных расширения
    if [[ -f "layer/${extension_name}.yaml" ]]; then
        sed -i "s/X-Env-Layer-Version:.*/X-Env-Layer-Version: ${new_version}/" "layer/${extension_name}.yaml"
    fi

    # Создание записи в CHANGELOG
    update_changelog "$new_version" "$bump_type"

    # Git коммит
    git add VERSION CHANGELOG.md "layer/${extension_name}.yaml"
    git commit -m "chore: bump version to $new_version"

    echo "Версия успешно обновлена до $new_version"
}

calculate_new_version() {
    local current_version="$1"
    local bump_type="$2"

    # Разбор версии
    IFS='.' read -ra VERSION_PARTS <<< "$current_version"
    local major="${VERSION_PARTS[0]}"
    local minor="${VERSION_PARTS[1]}"
    local patch="${VERSION_PARTS[2]}"

    case "$bump_type" in
        major)
            major=$((major + 1))
            minor=0
            patch=0
            ;;
        minor)
            minor=$((minor + 1))
            patch=0
            ;;
        patch)
            patch=$((patch + 1))
            ;;
        *)
            echo "Неверный тип обновления: $bump_type" >&2
            exit 1
            ;;
    esac

    echo "${major}.${minor}.${patch}"
}

update_changelog() {
    local new_version="$1"
    local bump_type="$2"
    local date
    date=$(date +%Y-%m-%d)

    # Создание новой секции в CHANGELOG
    local temp_changelog
    temp_changelog=$(mktemp)

    # Заголовок новой версии
    cat > "$temp_changelog" << EOF
# Changelog

Все изменения в расширении документируются в этом файле.

Формат версий соответствует [Semantic Versioning](https://semver.org/).

## [${new_version}] - ${date}

### Added
- Ничего

### Changed
- Ничего

### Deprecated
- Ничего

### Removed
- Ничего

### Fixed
- Ничего

### Security
- Ничего

EOF

    # Добавление существующего содержимого CHANGELOG
    if [[ -f "CHANGELOG.md" ]]; then
        # Пропуск заголовка и добавление остального
        tail -n +5 CHANGELOG.md >> "$temp_changelog"
    fi

    mv "$temp_changelog" CHANGELOG.md

    echo "CHANGELOG.md обновлен для версии $new_version"
}

create_release() {
    local extension_name="$1"
    local version="$2"
    local release_notes="${3:-}"

    echo "Создание релиза $extension_name версии $version"

    # Проверка готовности к релизу
    if ! check_release_readiness "$extension_name" "$version"; then
        echo "Релиз не готов. Исправьте проблемы и повторите."
        exit 1
    fi

    # Создание git тега
    git tag -a "v$version" -m "Release version $version"

    # Push тега
    git push origin "v$version"

    # Создание GitHub релиза
    if command -v gh >/dev/null 2>&1; then
        local notes_file
        if [[ -n "$release_notes" ]] && [[ -f "$release_notes" ]]; then
            gh release create "v$version" \
                --title "Release v$version" \
                --notes-file "$release_notes" \
                --latest
        else
            gh release create "v$version" \
                --title "Release v$version" \
                --generate-notes \
                --latest
        fi
    fi

    # Создание архива релиза
    create_release_archive "$extension_name" "$version"

    echo "Релиз $version успешно создан"
}

check_release_readiness() {
    local extension_name="$1"
    local version="$2"

    local issues=0

    echo "Проверка готовности к релизу..."

    # Проверка версии в метаданных
    if [[ -f "layer/${extension_name}.yaml" ]]; then
        local metadata_version
        metadata_version=$(grep "X-Env-Layer-Version:" "layer/${extension_name}.yaml" | sed 's/.*: //' | tr -d ' ')

        if [[ "$metadata_version" != "$version" ]]; then
            echo "ОШИБКА: Версия в метаданных ($metadata_version) не соответствует версии релиза ($version)"
            ((issues++))
        fi
    fi

    # Проверка CHANGELOG
    if ! grep -q "^## \[${version}\]" CHANGELOG.md; then
        echo "ОШИБКА: Версия $version не найдена в CHANGELOG.md"
        ((issues++))
    fi

    # Проверка тестов
    if [[ -d "tests" ]] && ! run_tests; then
        echo "ОШИБКА: Тесты не проходят"
        ((issues++))
    fi

    # Проверка линтера
    if ! run_linter "$extension_name"; then
        echo "ОШИБКА: Линтер выявил проблемы"
        ((issues++))
    fi

    return $issues
}

create_release_archive() {
    local extension_name="$1"
    local version="$2"

    local archive_name="${extension_name}-${version}.tar.gz"
    local temp_dir
    temp_dir=$(mktemp -d)

    echo "Создание архива релиза: $archive_name"

    # Копирование файлов
    cp -r layer/ "$temp_dir/"
    cp -r config/ "$temp_dir/" 2>/dev/null || true
    cp -r scripts/ "$temp_dir/" 2>/dev/null || true
    cp -r docs/ "$temp_dir/" 2>/dev/null || true
    cp README.md CHANGELOG.md LICENSE VERSION "$temp_dir/" 2>/dev/null || true

    # Создание архива
    tar -czf "$archive_name" -C "$temp_dir" .

    # Очистка
    rm -rf "$temp_dir"

    echo "Архив релиза создан: $archive_name"
}

check_version_consistency() {
    local extension_name="$1"

    echo "Проверка согласованности версий для $extension_name"

    local file_version=""
    local metadata_version=""
    local git_tag=""

    # Версия из файла
    if [[ -f "VERSION" ]]; then
        file_version=$(cat VERSION)
    fi

    # Версия из метаданных
    if [[ -f "layer/${extension_name}.yaml" ]]; then
        metadata_version=$(grep "X-Env-Layer-Version:" "layer/${extension_name}.yaml" | sed 's/.*: //' | tr -d ' ')
    fi

    # Последний git тег
    if git rev-parse --git-dir >/dev/null 2>&1; then
        git_tag=$(git describe --tags --abbrev=0 2>/dev/null || echo "none")
        git_tag=${git_tag#v}  # Удаление префикса v
    fi

    echo "Версия в файле VERSION: $file_version"
    echo "Версия в метаданных: $metadata_version"
    echo "Последний git тег: $git_tag"

    if [[ "$file_version" != "$metadata_version" ]]; then
        echo "ПРЕДУПРЕЖДЕНИЕ: Несоответствие версии в файле и метаданных"
        return 1
    fi

    if [[ -n "$git_tag" && "$git_tag" != "none" && "$git_tag" != "$file_version" ]]; then
        echo "ПРЕДУПРЕЖДЕНИЕ: Несоответствие git тега и версии файла"
        return 1
    fi

    echo "Версии согласованы"
    return 0
}

validate_version_format() {
    local extension_name="$1"

    echo "Валидация формата версий для $extension_name"

    local version_regex='^[0-9]+\.[0-9]+\.[0-9]+$'

    # Проверка файла VERSION
    if [[ -f "VERSION" ]]; then
        local file_version
        file_version=$(cat VERSION)

        if [[ ! "$file_version" =~ $version_regex ]]; then
            echo "ОШИБКА: Неверный формат версии в файле VERSION: $file_version"
            echo "Ожидается формат: MAJOR.MINOR.PATCH (например: 1.2.3)"
            return 1
        fi
    fi

    # Проверка метаданных
    if [[ -f "layer/${extension_name}.yaml" ]]; then
        local metadata_version
        metadata_version=$(grep "X-Env-Layer-Version:" "layer/${extension_name}.yaml" | sed 's/.*: //' | tr -d ' ')

        if [[ ! "$metadata_version" =~ $version_regex ]]; then
            echo "ОШИБКА: Неверный формат версии в метаданных: $metadata_version"
            return 1
        fi
    fi

    echo "Формат версий корректен"
    return 0
}
```

### 2. Жизненный цикл версий 🔴 ОВЯЗАТЕЛЬНЫЕ
```markdown
# Пример жизненного цикла версий расширения

## v2.1.0 (Current) - Active Development
- Добавлена новая функциональность
- Улучшена производительность
- Исправлены известные ошибки

## v2.0.0 - Major Release
- Полностью переписана архитектура
- Изменен API переменных
- Добавлена поддержка новых устройств

## v1.5.3 - Patch Release
- Исправлена критическая ошибка безопасности
- Обновлены зависимости
- Улучшена стабильность

## v1.0.0 - Initial Release
- Первая стабильная версия
- Полная документация
- Тестовое покрытие > 80%

## Стадии жизненного цикла:

### 1. Development (Разработка)
- Активная разработка новых функций
- Добавление экспериментальных возможностей
- Версия: x.y.z-dev или x.y.z-alpha.x

### 2. Release Candidate (Кандидат на релиз)
- Код стабилизирован
- Основные тесты пройдены
- Версия: x.y.z-rc.x

### 3. Stable Release (Стабильный релиз)
- Полностью протестирован
- Документация обновлена
- Версия: x.y.z

### 4. Maintenance (Поддержка)
- Только исправления ошибок и security патчи
- Версия: x.y.(z+1), x.y.(z+2), ...

### 5. End of Life (Завершение поддержки)
- Релиз больше не поддерживается
- Пользователям рекомендуется обновление
```

## Процесс релиза 🔴 ОВЯЗАТЕЛЬНЫЕ

### 1. Подготовка релиза 🔴 ОВЯЗАТЕЛЬНЫЕ
```bash
#!/bin/bash
# Подготовка расширения к релизу

prepare_release() {
    local extension_name="$1"
    local version="$2"

    echo "Подготовка релиза $extension_name версии $version"

    # 1. Обновление версии в метаданных
    echo "Обновление версии в метаданных..."
    sed -i "s/X-Env-Layer-Version:.*/X-Env-Layer-Version: ${version}/" "layer/${extension_name}.yaml"

    # 2. Обновление CHANGELOG.md
    echo "Обновление CHANGELOG..."
    local date
    date=$(date +%Y-%m-%d)

    # Создание временного файла с новой записью
    local temp_changelog
    temp_changelog=$(mktemp)

    cat > "$temp_changelog" << EOF
# Changelog

Все изменения в расширении документируются в этом файле.

Формат версий соответствует [Semantic Versioning](https://semver.org/).

## [${version}] - ${date}

### Added
- Добавьте новые функции

### Changed
- Изменения в существующих функциях

### Deprecated
- Устаревшие функции

### Removed
- Удаленные функции

### Fixed
- Исправленные ошибки

### Security
- Исправления безопасности

EOF

    # Добавление существующего CHANGELOG (если есть)
    if [[ -f "CHANGELOG.md" ]]; then
        tail -n +5 CHANGELOG.md >> "$temp_changelog"
    fi

    mv "$temp_changelog" CHANGELOG.md

    # 3. Запуск полного тестового набора
    echo "Запуск тестов..."
    if ! run_full_test_suite; then
        echo "ОШИБКА: Тесты не пройдены"
        exit 1
    fi

    # 4. Финальная проверка линтинга
    echo "Проверка линтера..."
    if ! rpi-image-gen metadata --lint "layer/${extension_name}.yaml"; then
        echo "ОШИБКА: Линтер выявил проблемы"
        exit 1
    fi

    # 5. Создание тестового образа
    echo "Создание тестового образа..."
    if ! rpi-image-gen build -c "config/${extension_name}.yaml" --dry-run; then
        echo "ОШИБКА: Сборка тестового образа завершилась неудачей"
        exit 1
    fi

    # 6. Генерация SBOM
    echo "Генерация SBOM..."
    rpi-image-gen build -c "config/${extension_name}.yaml" --sbom

    echo "Подготовка к релизу завершена"
}

run_full_test_suite() {
    # Запуск всех типов тестов
    echo "Запуск модульных тестов..."
    python3 -m pytest tests/unit/ -v --tb=short || return 1

    echo "Запуск интеграционных тестов..."
    rpi-image-gen build -c tests/integration-config.yaml --dry-run || return 1

    echo "Запуск тестов производительности..."
    timeout 600 rpi-image-gen build -c tests/perf-config.yaml || return 1

    echo "Все тесты пройдены"
    return 0
}
```

### 2. Создание релиза 🔴 ОВЯЗАТЕЛЬНЫЕ
```bash
#!/bin/bash
# Создание релиза расширения

create_release_process() {
    local extension_name="$1"
    local version="$2"

    echo "Создание релиза $extension_name версии $version"

    # Тегирование релиза
    echo "Создание git тега..."
    git add .
    git commit -m "chore: prepare release v$version" 2>/dev/null || true
    git tag -a "v$version" -m "Release version $version"

    # Push изменений и тегов
    echo "Отправка изменений в репозиторий..."
    git push origin main
    git push origin "v$version"

    # Создание релиза на GitHub
    echo "Создание GitHub релиза..."
    if command -v gh >/dev/null 2>&1; then
        gh release create "v$version" \
            --title "Release v$version" \
            --notes-file CHANGELOG.md \
            --latest
    fi

    # Загрузка дополнительных ассетов
    upload_release_assets "$extension_name" "$version"

    echo "Релиз v$version успешно создан"
}

upload_release_assets() {
    local extension_name="$1"
    local version="$2"

    echo "Загрузка ассетов релиза..."

    # Создание архива исходников
    git archive --format=tar.gz --prefix="${extension_name}-${version}/" \
        -o "${extension_name}-${version}-source.tar.gz" "v$version"

    # Загрузка через GitHub CLI
    if command -v gh >/dev/null 2>&1; then
        gh release upload "v$version" \
            "${extension_name}-${version}-source.tar.gz" \
            "sbom-report.json" \
            "CHANGELOG.md"
    fi

    echo "Ассеты релиза загружены"
}
```

### 3. Пост-релизная активность 🔴 ОВЯЗАТЕЛЬНЫЕ
```bash
#!/bin/bash
# Пост-релизная активность

post_release_activities() {
    local extension_name="$1"
    local version="$2"

    echo "Начало пост-релизной активности для $extension_name v$version"

    # 1. Мониторинг использования новой версии
    echo "Настройка мониторинга использования..."
    setup_usage_monitoring "$extension_name" "$version"

    # 2. Сбор отзывов пользователей
    echo "Настройка сбора отзывов..."
    setup_feedback_collection "$extension_name" "$version"

    # 3. Мониторинг ошибок
    echo "Настройка мониторинга ошибок..."
    setup_error_monitoring "$extension_name" "$version"

    # 4. Планирование следующего релиза
    echo "Планирование следующего релиза..."
    plan_next_release "$extension_name" "$version"

    # 5. Обновление документации
    echo "Проверка необходимости обновления документации..."
    check_documentation_updates "$extension_name" "$version"

    echo "Пост-релизная активность настроена"
}

setup_usage_monitoring() {
    local extension_name="$1"
    local version="$2"

    # Настройка отслеживания загрузок
    echo "Отслеживание загрузок релиза v$version..."

    # Настройка метрик использования
    cat > "usage-monitoring.json" << EOF
    {
        "extension": "$extension_name",
        "version": "$version",
        "release_date": "$(date -Iseconds)",
        "metrics": {
            "downloads": 0,
            "active_users": 0,
            "error_reports": 0,
            "feedback_count": 0
        }
    }
EOF

    echo "Мониторинг использования настроен"
}

setup_feedback_collection() {
    local extension_name="$1"
    local version="$2"

    # Создание issue шаблонов для отзывов
    cat > ".github/ISSUE_TEMPLATE/release-feedback.md" << EOF
    ---
    name: Release Feedback
    about: Оставьте отзыв о релизе v$version
    title: "[Feedback v$version] "
    labels: feedback, release
    ---

    ## Информация о версии
    - **Версия расширения**: $version
    - **Дата релиза**: $(date +%Y-%m-%d)
    - **Ваша ОС**: 
    - **Версия rpi-image-gen**:

    ## Отзыв о релизе

    ### Что работает хорошо
    <!-- Опишите, что вам понравилось в этом релизе -->

    ### Проблемы и улучшения
    <!-- Опишите найденные проблемы или предложения по улучшению -->

    ### Предложения для следующих релизов
    <!-- Что бы вы хотели видеть в следующих версиях -->
EOF

    echo "Сбор отзывов настроен"
}

setup_error_monitoring() {
    local extension_name="$1"
    local version="$2"

    # Настройка сбора отчетов об ошибках
    cat > "error-monitoring-config.json" << EOF
    {
        "extension": "$extension_name",
        "version": "$version",
        "error_tracking": {
            "github_issues": true,
            "error_log_analysis": true,
            "user_reports": true
        },
        "alerts": {
            "critical_errors_threshold": 5,
            "error_rate_threshold": 0.1
        }
    }
EOF

    echo "Мониторинг ошибок настроен"
}

plan_next_release() {
    local extension_name="$1"
    local version="$2"

    # Создание roadmap для следующего релиза
    local next_version
    next_version=$(calculate_next_version "$version")

    cat > "ROADMAP-${next_version}.md" << EOF
    # Roadmap для версии $next_version

    ## Планируемые изменения

    ### Major Changes
    - [ ] Крупные архитектурные изменения

    ### Minor Features
    - [ ] Добавление новой функциональности
    - [ ] Улучшения пользовательского опыта

    ### Patches
    - [ ] Исправление известных ошибок
    - [ ] Улучшение производительности
    - [ ] Обновление зависимостей

    ## Временная шкала
    - **Начало разработки**: $(date +%Y-%m-%d -d "+1 month")
    - **Release Candidate**: $(date +%Y-%m-%d -d "+2 months")
    - **Stable Release**: $(date +%Y-%m-%d -d "+3 months")

    ## Критерии готовности
    - [ ] Все запланированные функции реализованы
    - [ ] Тестовое покрытие > 85%
    - [ ] Документация обновлена
    - [ ] Производительность не ухудшилась
    - [ ] Нет критических уязвимостей
EOF

    echo "Планирование следующего релиза: $next_version"
}

check_documentation_updates() {
    local extension_name="$1"
    local version="$2"

    echo "Проверка необходимости обновления документации..."

    # Проверка актуальности README
    if ! grep -q "$version" README.md; then
        echo "ПРЕДУПРЕЖДЕНИЕ: README.md не содержит информацию о версии $version"
    fi

    # Проверка CHANGELOG
    if ! grep -q "$version" CHANGELOG.md; then
        echo "ОШИБКА: CHANGELOG.md не содержит информацию о версии $version"
    fi

    # Проверка примеров
    if [[ -d "examples" ]]; then
        local outdated_examples
        outdated_examples=$(find examples -name "*.yaml" -exec grep -l "version.*:" {} \; | xargs grep -L "$version" 2>/dev/null || true)

        if [[ -n "$outdated_examples" ]]; then
            echo "ПРЕДУПРЕЖДЕНИЕ: Следующие примеры могут содержать устаревшую версию:"
            echo "$outdated_examples"
        fi
    fi

    echo "Проверка документации завершена"
}

calculate_next_version() {
    local current_version="$1"
    local next_type="${2:-minor}"  # major, minor, patch

    # Разбор текущей версии
    IFS='.' read -ra VERSION_PARTS <<< "$current_version"
    local major="${VERSION_PARTS[0]}"
    local minor="${VERSION_PARTS[1]}"
    local patch="${VERSION_PARTS[2]}"

    case "$next_type" in
        major)
            echo "$((major + 1)).0.0"
            ;;
        minor)
            echo "${major}.$((minor + 1)).0"
            ;;
        patch)
            echo "${major}.${minor}.$((patch + 1))"
            ;;
        *)
            echo "${major}.$((minor + 1)).0"
            ;;
    esac
}
```

## Автоматизация управления версиями ⚠️ РЕКОМЕНДУЕМЫЕ

### CI/CD для версионирования
```yaml
# .github/workflows/version-management.yml
name: Version Management

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  version-check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Check version consistency
      run: |
        ./scripts/version-manager.sh check ${{ github.event.repository.name }}

    - name: Validate version format
      run: |
        ./scripts/version-manager.sh validate ${{ github.event.repository.name }}

  automated-versioning:
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: Automated version bump
      run: |
        # Определение типа изменения
        if git log --oneline -1 | grep -q "feat:"; then
            BUMP_TYPE="minor"
        elif git log --oneline -1 | grep -q "fix:"; then
            BUMP_TYPE="patch"
        elif git log --oneline -1 | grep -q "BREAKING CHANGE"; then
            BUMP_TYPE="major"
        else
            BUMP_TYPE="patch"
        fi

        ./scripts/version-manager.sh bump ${{ github.event.repository.name }} $BUMP_TYPE

    - name: Create release
      if: env.BUMP_TYPE != 'patch'
      run: |
        VERSION=$(cat VERSION)
        ./scripts/version-manager.sh release ${{ github.event.repository.name }} $VERSION

    - name: Push changes
      run: |
        git push origin main --tags
```

### Автоматическое обновление зависимостей
```bash
#!/bin/bash
# Автоматическое обновление зависимостей

update_dependencies() {
    local extension_name="$1"

    echo "Проверка обновлений зависимостей для $extension_name..."

    # Обновление пакетов Debian
    if [[ -f "layer/${extension_name}.yaml" ]]; then
        # Извлечение списка пакетов
        local packages
        packages=$(grep -A 50 "^packages:" "layer/${extension_name}.yaml" | grep "^  -" | sed 's/  - //' | tr '\n' ' ')

        if [[ -n "$packages" ]]; then
            echo "Обновление пакетов: $packages"

            # Создание временного скрипта обновления
            cat > "update-dependencies.sh" << EOF
#!/bin/bash
set -e

# Обновление списка пакетов
apt-get update

# Обновление указанных пакетов
apt-get install --only-upgrade -y $packages

# Очистка
apt-get autoremove -y
apt-get autoclean

echo "Зависимости обновлены"
EOF

            chmod +x "update-dependencies.sh"
        fi
    fi

    echo "Обновление зависимостей завершено"
}
```

## Метрики соблюдения правила 🔴 ОВЯЗАТЕЛЬНЫЕ

### Обязательные элементы версионирования
- [ ] Использование семантического версионирования
- [ ] Наличие файла VERSION
- [ ] Ведение CHANGELOG.md
- [ ] Согласованность версий во всех файлах
- [ ] Создание git тегов для релизов

### Качественные показатели релизов
- [ ] Полный набор тестов перед релизом
- [ ] Валидация линтера перед релизом
- [ ] Наличие SBOM для каждого релиза
- [ ] Документация обновлена для релиза
- [ ] Архивы релизов созданы и подписаны

### Непрерывное улучшение
- [ ] Регулярный анализ обратной связи
- [ ] Мониторинг использования релизов
- [ ] Автоматизация повторяющихся задач
- [ ] Планирование следующих релизов
- [ ] Улучшение процессов релиза