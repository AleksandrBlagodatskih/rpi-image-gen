---
alwaysApply: ${cursor.file.path.includes("docs/") || cursor.file.path.includes("README")}
---


# Правило 11: Организация кода и очередность операций

## Назначение правила
Определяет принципы структурной организации кода и детерминированной очередности выполнения операций в расширениях rpi-image-gen.

## Организация кода и очередность операций 🔴 ОВЯЗАТЕЛЬНЫЕ

### Иерархия приоритетов в коде 🔴 ОВЯЗАТЕЛЬНЫЕ
```
┌─────────────────────────────────────────────────────────────┐
│ 1. Безопасность и обработка ошибок (высший приоритет)     │
├─────────────────────────────────────────────────────────────┤
│ 2. Валидация входных данных и зависимостей                │
├─────────────────────────────────────────────────────────────┤
│ 3. Подготовка окружения и ресурсов                         │
├─────────────────────────────────────────────────────────────┤
│ 4. Основная логика операции                                │
├─────────────────────────────────────────────────────────────┤
│ 5. Верификация результатов и очистка                       │
├─────────────────────────────────────────────────────────────┤
│ 6. Логирование и мониторинг (низший приоритет)            │
└─────────────────────────────────────────────────────────────┘
```

### Стандарты именования операций 🔴 ОВЯЗАТЕЛЬНЫЕ

#### Префиксы функций по назначению
```bash
# Рекомендуемые префиксы для функций по назначению:
setup_operation_name()      # Подготовка ресурсов
validate_operation_name()   # Валидация данных
execute_operation_name()    # Основная логика
verify_operation_name()     # Проверка результатов
cleanup_operation_name()    # Очистка ресурсов
monitor_operation_name()    # Мониторинг процесса

# Примеры конкретных операций:
install_package_dependencies()  # Установка зависимостей
configure_network_settings()    # Настройка сети
generate_security_keys()        # Генерация ключей
deploy_application_files()      # Развертывание файлов
backup_critical_data()          # Резервное копирование
```

#### Семантическое именование по контексту
```bash
# Контекстные префиксы для ясности:
pre_build_operation()      # До сборки
post_build_operation()     # После сборки
pre_deploy_operation()     # До развертывания
post_deploy_operation()    # После развертывания

# Специфические контексты:
device_specific_operation()    # Специфично для устройства
image_specific_operation()     # Специфично для образа
security_critical_operation()  # Критично для безопасности
```

## Управление зависимостями между операциями 🔴 ОВЯЗАТЕЛЬНЫЕ

### Матрица зависимостей операций
```bash
declare -A OPERATION_DEPENDENCIES=(
    ["install_dependencies"]="validate_system,setup_package_manager"
    ["configure_network"]="install_dependencies,validate_network_config"
    ["setup_security"]="configure_network,install_security_tools"
    ["deploy_application"]="setup_security,validate_deployment_config"
    ["cleanup_resources"]="deploy_application,backup_logs"
)

# Функция проверки и выполнения зависимостей
execute_with_dependencies() {
    local operation="$1"
    local -a deps=(${OPERATION_DEPENDENCIES[$operation]:-})

    for dep in "${deps[@]}"; do
        msg "Выполнение зависимости: $dep"
        if ! declare -f "$dep" >/dev/null 2>&1; then
            die "Зависимость не найдена: $dep"
        fi

        if ! "$dep"; then
            die "Ошибка выполнения зависимости: $dep"
        fi
    done

    msg "Выполнение основной операции: $operation"
    "$operation"
}
```

### Граф зависимостей для сложных операций
```bash
# Визуализация графа зависимостей:
#
#       A ──┐
#           ├──> C ──> E
#       B ──┤        │
#           └──> D ──┘
#
# Последовательность выполнения: A, B → C, D → E

declare -A COMPLEX_OPERATION_ORDER=(
    ["network_setup"]="A:validate_config,B:check_interfaces,C:configure_dhcp,D:setup_routing,E:restart_services"
    ["security_hardening"]="A:backup_configs,B:install_tools,C:update_packages,D:configure_firewall,E:audit_system"
    ["deployment_pipeline"]="A:validate_environment,B:prepare_resources,C:deploy_application,D:configure_services,E:health_check"
)
```

## Синхронизация операций между компонентами 🔴 ОВЯЗАТЕЛЬНЫЕ

### Координация между этапами сборки
```bash
# Координация между этапами сборки
coordinate_build_stages() {
    local build_id=$(generate_build_id)

    # Этап 1: Подготовка (setup hooks)
    execute_stage_with_sync "setup" "$build_id"

    # Этап 2: Установка (essential hooks)
    execute_stage_with_sync "essential" "$build_id"

    # Этап 3: Кастомизация (customize hooks)
    execute_stage_with_sync "customize" "$build_id"

    # Этап 4: Очистка (cleanup hooks)
    execute_stage_with_sync "cleanup" "$build_id"
}

execute_stage_with_sync() {
    local stage="$1"
    local build_id="$2"

    # Создание семафора для синхронизации
    local sync_file="/tmp/build_${build_id}_${stage}.sync"

    # Выполнение всех хуков этапа параллельно
    execute_parallel_hooks "$stage" &

    # Ожидание завершения всех хуков этапа
    wait_for_stage_completion "$sync_file"

    # Проверка результатов всех хуков
    validate_stage_results "$stage" "$build_id"
}
```

### Синхронизация между слоями
```bash
# Управление очередностью между слоями
manage_layer_execution_order() {
    local -a execution_order=()

    # Определение порядка выполнения слоев
    while IFS=: read -r layer priority; do
        case "$priority" in
            "critical")   execution_order=("critical" "$layer" "${execution_order[@]}") ;;
            "high")       execution_order+=("$layer") ;;
            "normal")     execution_order+=("$layer") ;;
            "low")        execution_order+=("$layer") ;;
        esac
    done < <(get_layer_priorities)

    # Выполнение в правильном порядке
    for layer in "${execution_order[@]}"; do
        execute_layer_with_context "$layer"
    done
}
```

## Обработка ошибок и восстановление 🔴 ОВЯЗАТЕЛЬНЫЕ

### Стратегии обработки ошибок по приоритету
```bash
# Иерархия стратегий обработки ошибок:
#
# 1. Критические ошибки (немедленное завершение)
#    - Нарушение безопасности
#    - Повреждение системных файлов
#    - Критические зависимости не удовлетворены
#
# 2. Восстановимые ошибки (попытка исправления)
#    - Временные сетевые проблемы
#    - Недостаток дискового пространства
#    - Конфликты пакетов
#
# 3. Предупреждения (продолжение с уведомлением)
#    - Неоптимальные настройки
#    - Устаревшие зависимости
#    - Несущественные предупреждения

handle_error_by_priority() {
    local error_code="$1"
    local operation_context="$2"

    case "$error_code" in
        "CRITICAL_SECURITY_BREACH")
            emergency_shutdown "Нарушение безопасности в $operation_context"
            ;;
        "SYSTEM_FILE_CORRUPTION")
            attempt_system_recovery "$operation_context"
            ;;
        "DEPENDENCY_MISSING")
            attempt_dependency_resolution "$operation_context"
            ;;
        "NETWORK_TEMPORARY_FAILURE")
            retry_with_backoff "$operation_context"
            ;;
        "DEPRECATED_WARNING")
            log_warning_and_continue "$operation_context"
            ;;
        *)
            log_error_and_fail "Неизвестная ошибка в $operation_context: $error_code"
            ;;
    esac
}
```

### Автоматическое восстановление
```bash
# Система автоматического восстановления
implement_recovery_mechanisms() {
    # Резервное копирование перед критическими операциями
    create_recovery_point() {
        local operation="$1"
        local recovery_id=$(generate_recovery_id)

        cp -r /etc /etc.backup.${recovery_id}
        cp -r /var /var.backup.${recovery_id}

        echo "$recovery_id:$operation:$(date)" >> /var/log/recovery.log
        return "$recovery_id"
    }

    # Восстановление из точки восстановления
    restore_from_recovery_point() {
        local recovery_id="$1"

        if [[ -d "/etc.backup.${recovery_id}" ]]; then
            msg "Восстановление из точки: $recovery_id"
            cp -r "/etc.backup.${recovery_id}"/* /etc/
            cp -r "/var.backup.${recovery_id}"/* /var/
            return 0
        fi

        return 1
    }

    # Мониторинг и автоматическое восстановление
    monitor_and_recover() {
        while true; do
            if check_system_health; then
                sleep 60
            else
                msg "Обнаружены проблемы со здоровьем системы"
                latest_recovery=$(tail -1 /var/log/recovery.log | cut -d: -f1)
                if restore_from_recovery_point "$latest_recovery"; then
                    msg "Система восстановлена из точки: $latest_recovery"
                else
                    emergency_shutdown "Критическая ошибка системы"
                fi
            fi
        done
    } &
}
```

## Производительность и оптимизация порядка операций 🔴 ОВЯЗАТЕЛЬНЫЕ

### Анализ узких мест производительности
```bash
# Профилирование производительности операций
profile_operation_performance() {
    local operation="$1"

    # Создание профиля операции
    exec 3>&2 2> >(tee "/tmp/perf_${operation}.log")

    local start_time=$(timestamp_ms)
    local cpu_start=$(get_cpu_usage)
    local mem_start=$(get_memory_usage)

    # Выполнение операции с профилированием
    "$operation"

    local end_time=$(timestamp_ms)
    local cpu_end=$(get_cpu_usage)
    local mem_end=$(get_memory_usage)

    # Анализ результатов
    local duration=$((end_time - start_time))
    local cpu_used=$((cpu_end - cpu_start))
    local mem_used=$((mem_end - mem_start))

    # Сохранение метрик
    cat >> /var/log/performance.log << EOF
    $(date): $operation - Duration: ${duration}ms, CPU: ${cpu_used}%, Mem: ${mem_used}KB
    EOF

    exec 2>&3 3>&-
}
```

### Оптимизация порядка выполнения
```bash
# Оптимизация порядка операций на основе анализа
optimize_operation_order() {
    local operations=("$@")

    # Анализ зависимостей и ресурсов
    for operation in "${operations[@]}"; do
        analyze_operation_requirements "$operation"
    done

    # Оптимизация порядка:
    # 1. Группировка по ресурсам (CPU-интенсивные вместе)
    # 2. Параллельное выполнение независимых операций
    # 3. Последовательное выполнение операций с зависимостями
    # 4. Минимизация переключений контекста

    local optimized_order=()

    # CPU-интенсивные операции первыми
    for op in "${operations[@]}"; do
        if is_cpu_intensive "$op"; then
            optimized_order+=("$op")
        fi
    done

    # I/O операции параллельно с CPU
    for op in "${operations[@]}"; do
        if is_io_operation "$op" && ! is_in_list "$op" "${optimized_order[@]}"; then
            optimized_order+=("$op")
        fi
    done

    # Зависимые операции в конце
    for op in "${operations[@]}"; do
        if has_dependencies "$op" && ! is_in_list "$op" "${optimized_order[@]}"; then
            optimized_order+=("$op")
        fi
    done

    echo "${optimized_order[@]}"
}
```

## Мониторинг и аудит очередности операций 🔴 ОВЯЗАТЕЛЬНЫЕ

### Система аудита операций
```bash
# Подробное логирование всех операций
audit_operation_execution() {
    local operation="$1"
    local audit_log="/var/log/operation_audit.log"

    # Создание записи аудита
    cat >> "$audit_log" << EOF
=== ОПЕРАЦИЯ: $operation ===
Время начала: $(date -Iseconds)
Пользователь: $(whoami)
PID: $$
Контекст: $(get_operation_context)
Параметры: $(get_operation_parameters)
Ожидаемый результат: $(get_expected_outcome)
EOF

    # Выполнение операции с аудитом
    local start_time=$(date +%s)
    if "$operation"; then
        local status="SUCCESS"
        local exit_code=0
    else
        local status="FAILED"
        local exit_code=$?
    fi
    local end_time=$(date +%s)

    # Завершение записи аудита
    cat >> "$audit_log" << EOF
Статус: $status
Код завершения: $exit_code
Длительность: $((end_time - start_time)) сек
Время завершения: $(date -Iseconds)
=============================================
EOF

    return $exit_code
}
```

### Мониторинг в реальном времени
```bash
# Мониторинг выполнения операций
monitor_operation_progress() {
    local operation="$1"
    local progress_file="/tmp/operation_${operation}_progress"

    # Инициализация мониторинга
    echo "0: Начало операции $operation" > "$progress_file"

    # Мониторинг этапов
    track_progress() {
        local stage="$1"
        local percentage="$2"

        echo "$percentage: Этап $stage операции $operation" >> "$progress_file"

        # Отправка уведомлений при достижении ключевых этапов
        case "$percentage" in
            "25") notify_progress "Операция $operation: 25% завершено" ;;
            "50") notify_progress "Операция $operation: 50% завершено" ;;
            "75") notify_progress "Операция $operation: 75% завершено" ;;
            "100") notify_progress "Операция $operation завершена успешно" ;;
        esac
    }

    # Интеграция мониторинга в операцию
    "$operation" && track_progress "complete" "100" || track_progress "failed" "0"
}
```

## Правила и паттерны кода для каждого каталога 🔴 ОВЯЗАТЕЛЬНЫЕ

### Структура проекта и назначение каталогов 🔴 ОВЯЗАТЕЛЬНЫЕ
```
rpi-image-gen/
├── bin/                    # Исполняемые файлы и утилиты
├── config/                 # Конфигурационные файлы сборки
├── device/                 # Специфичные настройки устройств
├── docs/                   # Документация проекта
├── examples/               # Примеры расширений и использования
├── image/                  # Специфичные настройки образов
├── layer/                  # Библиотека слоев системы
├── layer-hooks/           # Хуки для слоев
├── lib/                    # Библиотеки и вспомогательные скрипты
├── scripts/               # Скрипты для различных этапов сборки
├── site/                  # Python модули проекта
├── templates/             # Шаблоны для генерации документации
├── test/                  # Тесты проекта
└── work/                  # Рабочая директория сборки
```

### bin/ - Исполняемые файлы и утилиты 🔴 ОВЯЗАТЕЛЬНЫЕ
```bash
#!/bin/bash
# Стандарты кодирования для bin/

# Обязательные переменные окружения
readonly SCRIPT_NAME="$(basename "${BASH_SOURCE[0]}")"
readonly SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Функции логирования
log_info() { echo "[$SCRIPT_NAME] INFO: $*"; }
log_warn() { echo "[$SCRIPT_NAME] WARN: $*" >&2; }
log_error() { echo "[$SCRIPT_NAME] ERROR: $*" >&2; }

# Функция завершения с ошибкой
die() {
    log_error "$*"
    exit 1
}

# Проверка зависимостей
check_dependencies() {
    local deps=("tool1" "tool2" "tool3")
    for dep in "${deps[@]}"; do
        command -v "$dep" >/dev/null 2>&1 || die "Требуется установить: $dep"
    done
}

# Основная логика
main() {
    check_dependencies

    # Реализация основной функциональности
    log_info "Запуск операции"

    # Возврат кода успеха
    return 0
}

# Запуск главной функции
main "$@"
```

### config/ - Конфигурационные файлы 🔴 ОВЯЗАТЕЛЬНЫЕ
```yaml
# Стандарты кодирования для config/

# Метаданные конфигурации (комментарии в начале файла)
# Назначение: Минимальная конфигурация Debian Bookworm
# Версия: 1.0.0
# Требования: Должна выполняться первой в цепочке сборки

# Основная секция конфигурации
device:
  layer: pi5                    # Слой устройства (обязательно)
  hostname: rpi-${HOST_SUFFIX}  # Шаблон имени хоста
  user: pi                     # Пользователь системы

image:
  layer: image-rpios           # Слой образа (обязательно)
  boot_part_size: 200%         # Размер загрузочного раздела
  root_part_size: 300%         # Размер корневого раздела
  name: deb12-arm64-min        # Имя результирующего образа

layer:
  base: bookworm-minbase       # Базовый слой (обязательно)
  # Дополнительные слои перечисляются здесь
  security: security-minimal
  network: net-misc
```

## Метрики соблюдения правила 🔴 ОВЯЗАТЕЛЬНЫЕ

### Структурная организация
- [ ] Иерархия приоритетов соблюдается во всех скриптах
- [ ] Стандарты именования функций применяются
- [ ] Зависимости между операциями документированы
- [ ] Синхронизация между компонентами реализована

### Качество кода
- [ ] Функции имеют единую ответственность
- [ ] Обработка ошибок соответствует иерархии
- [ ] Производительность операций оптимизирована
- [ ] Мониторинг и аудит реализованы

### Организация проекта
- [ ] Структура каталогов соответствует стандартам
- [ ] Файлы размещены в правильных каталогах
- [ ] Кодирование соответствует правилам каждого каталога
- [ ] Документация структуры поддерживается в актуальном состоянии