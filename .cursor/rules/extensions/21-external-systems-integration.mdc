---
alwaysApply: ${cursor.file.path.includes("integration/") || cursor.file.content.includes("api")}
---


# Правило 21: Интеграция с внешними системами

## Назначение правила
Определяет стандарты интеграции расширений rpi-image-gen с внешними системами, используя возможности хуков, внешних директорий и переиспользуемых компонентов.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Интеграция с внешними системами

### Docker и контейнеризация

#### Создание Docker-образа с расширением:

```dockerfile
FROM debian:bookworm-slim

# Установка зависимостей rpi-image-gen
RUN apt-get update && apt-get install -y \
    qemu-user-static \
    binfmt-support \
    dosfstools \
    e2fsprogs \
    parted \
    wget \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Копирование расширения
COPY extension/ /opt/rpi-image-gen-extension/

# Скрипт сборки
COPY build.sh /build.sh
RUN chmod +x /build.sh

CMD ["/build.sh"]
```

```bash
#!/bin/bash
# build.sh - Скрипт сборки образа с расширением
set -euo pipefail

cd /opt/rpi-image-gen-extension

# Клонирование rpi-image-gen (если нужно)
if [ ! -d "rpi-image-gen" ]; then
    git clone https://github.com/raspberrypi/rpi-image-gen.git
fi

cd rpi-image-gen

# Сборка образа с расширением
export IGconf_extension_docker_mode=true
./rpi-image-gen build -c ../config/extension-config.yaml

# Копирование результата
cp work/image-*/*.img /output/
```

#### Kubernetes интеграция:

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rpi-image-builder
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rpi-image-builder
  template:
    metadata:
      labels:
        app: rpi-image-builder
    spec:
      containers:
      - name: builder
        image: your-registry/rpi-image-builder:latest
        env:
        - name: IGconf_extension_target_device
          value: "pi4"
        - name: IGconf_extension_custom_firmware
          value: "true"
        volumeMounts:
        - name: output
          mountPath: /output
        - name: config
          mountPath: /config
      volumes:
      - name: output
        emptyDir: {}
      - name: config
        configMap:
          name: extension-config
```

### Облачная интеграция

#### AWS S3 для хранения образов:

```bash
#!/bin/bash
# Загрузка образа в S3
upload_to_s3() {
    local image_file="$1"
    local s3_bucket="$2"
    local s3_key="images/$(basename "$image_file")"

    # Проверка наличия AWS CLI
    command -v aws >/dev/null 2>&1 || {
        echo "Ошибка: AWS CLI не установлен" >&2
        return 1
    }

    # Загрузка в S3
    aws s3 cp "$image_file" "s3://$s3_bucket/$s3_key" \
        --storage-class STANDARD_IA \
        --metadata "built=$(date -Iseconds),extension=$IGconf_extension_name"

    # Создание публичной ссылки (если нужно)
    if [ "${IGconf_extension_public_download:-false}" = "true" ]; then
        aws s3api put-object-acl \
            --bucket "$s3_bucket" \
            --key "$s3_key" \
            --acl public-read
    fi

    echo "Образ загружен: https://$s3_bucket.s3.amazonaws.com/$s3_key"
}
```

#### GitHub Releases API:

```python
#!/usr/bin/env python3
import os
import requests
import mimetypes
from pathlib import Path

def upload_to_github_releases(image_path, release_tag, repo_owner, repo_name):
    """Загрузка образа в GitHub Releases"""

    # Параметры аутентификации
    github_token = os.getenv('GITHUB_TOKEN')
    if not github_token:
        raise ValueError("Не установлен GITHUB_TOKEN")

    # Поиск release
    release_url = f"https://api.github.com/repos/{repo_owner}/{repo_name}/releases/tags/{release_tag}"
    headers = {
        'Authorization': f'token {github_token}',
        'Accept': 'application/vnd.github.v3+json'
    }

    response = requests.get(release_url, headers=headers)
    response.raise_for_status()
    release_data = response.json()

    # Загрузка файла
    upload_url = release_data['upload_url'].replace('{?name,label}', '')
    image_file = Path(image_path)

    with open(image_file, 'rb') as f:
        files = {'file': (image_file.name, f, mimetypes.guess_type(image_file)[0])}
        response = requests.post(
            f"{upload_url}?name={image_file.name}&label={image_file.name}",
            headers=headers,
            files=files
        )

    response.raise_for_status()
    asset_data = response.json()

    print(f"Файл загружен: {asset_data['browser_download_url']}")
    return asset_data['browser_download_url']
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано из раздела документации: `22_интеграция_с_внешними_системами.mdc`
