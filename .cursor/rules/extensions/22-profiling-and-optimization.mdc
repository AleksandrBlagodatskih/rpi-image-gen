---
alwaysApply: ${cursor.file.path.includes("perf") || cursor.file.content.includes("performance")}
---


# Правило 22: Профилирование и оптимизация

## Назначение правила
Определяет стандарты и требования для профилирование и оптимизация в разработке расширений rpi-image-gen.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Профилирование и оптимизация

### Анализ производительности расширений

#### Инструменты профилирования:

```bash
#!/bin/bash
# Скрипт профилирования сборки расширения

EXTENSION_NAME="your-extension"
LOG_FILE="/tmp/extension-profile.log"

# Включение детального логирования
export IGconf_extension_debug=true
export IGconf_extension_profile=true

echo "=== Профилирование сборки расширения: $(date) ===" | tee -a "$LOG_FILE"

# Мониторинг ресурсов во время сборки
{
    echo "Время;CPU%;Память%;Диск I/O;Сетевой I/O"
    while ps aux | grep -q "rpi-image-gen.*build"; do
        # Сбор метрик системы
        CPU=$(top -bn1 | grep "Cpu(s)" | awk '{print $2 + $4}')
        MEM=$(free | awk 'NR==2{printf "%.2f", $3*100/$2 }')
        DISK=$(iostat -x sda 1 1 | awk 'NR==4{print $4 + $5}')
        NET=$(cat /proc/net/dev | awk 'NR==3{print $2 + $10}')

        echo "$(date +%s);$CPU;$MEM;$DISK;$NET"
        sleep 5
    done
} >> "$LOG_FILE" &

PROFILE_PID=$!

# Запуск сборки с замером времени
time rpi-image-gen build -c config.yaml 2>&1 | tee -a "$LOG_FILE"

# Остановка мониторинга
kill $PROFILE_PID

# Анализ результатов
echo "=== Анализ производительности ===" | tee -a "$LOG_FILE"
echo "Общее время сборки: $(grep "real" "$LOG_FILE" | tail -1)" | tee -a "$LOG_FILE"
echo "Максимальное использование CPU: $(awk -F';' 'NR>1{print $2}' "$LOG_FILE" | sort -nr | head -1)%" | tee -a "$LOG_FILE"
echo "Максимальное использование памяти: $(awk -F';' 'NR>1{print $3}' "$LOG_FILE" | sort -nr | head -1)%" | tee -a "$LOG_FILE"
```

#### Оптимизация размера образа:

```bash
#!/bin/bash
# Анализ и оптимизация размера образа

IMAGE_FILE="$1"
OUTPUT_DIR="${2:-optimized}"

# Монтирование образа для анализа
TEMP_DIR=$(mktemp -d)
mount -o loop,ro "$IMAGE_FILE" "$TEMP_DIR"

echo "=== Анализ содержимого образа ==="
echo "Общий размер: $(du -sh "$IMAGE_FILE" | cut -f1)"

# Анализ размеров директорий
echo -e "\nТоп-10 самых больших директорий:"
du -S "$TEMP_DIR" 2>/dev/null | sort -nr | head -10

# Поиск редко используемых файлов
echo -e "\nПотенциальные кандидаты для удаления:"
find "$TEMP_DIR" -name "*.log" -o -name "*~" -o -name ".bash_history" | head -10

# Поиск дублированных библиотек
echo -e "\nДублированные библиотеки:"
find "$TEMP_DIR/usr/lib" -name "*.so.*" -exec basename {} \; | sort | uniq -d | head -5

# Размонтирование
umount "$TEMP_DIR"
rmdir "$TEMP_DIR"
```

### Оптимизация расширений

#### Минимизация зависимостей:

```yaml
# Оптимизированный слой с минимальными зависимостями
mmdebstrap:
  packages:
    # Только необходимые пакеты
    - essential-package-1
    - essential-package-2
    # Условные зависимости через переменные
    - ${IGconf_extension_optional_packages:-}
  setup-hooks:
    - |
      # Условная установка опциональных пакетов
      if [ -n "${IGconf_extension_optional_packages:-}" ]; then
        apt-get update && apt-get install -y ${IGconf_extension_optional_packages}
      fi
```

#### Параллельная обработка:

```bash
#!/bin/bash
# Параллельное выполнение хуков для ускорения сборки

parallel_hook() {
    local hook_script="$1"
    local hook_name="$2"

    echo "Выполнение хука: $hook_name"
    uchroot "$1" bash -c "$hook_script" &
}

# Запуск хуков параллельно
parallel_hook "$1" "network-setup" &
parallel_hook "$1" "user-creation" &
parallel_hook "$1" "package-cleanup" &

# Ожидание завершения всех хуков
wait

echo "Все хуки выполнены параллельно"
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано из раздела документации: `23_профилирование_и_оптимизация.mdc`
