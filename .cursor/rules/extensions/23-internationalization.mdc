---
alwaysApply: ${cursor.file.path.includes("locale/") || cursor.file.path.includes("i18n")}
---


# Правило 23: Международная поддержка

## Назначение правила
Определяет стандарты и требования для международная поддержка в разработке расширений rpi-image-gen.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Международная поддержка

### Локализация расширений

#### Структура локализации:

```
extension/
├── layer/
│   └── extension-layer.yaml
├── locales/
│   ├── en/
│   │   ├── LC_MESSAGES/
│   │   │   └── extension.mo
│   │   └── description.md
│   ├── ru/
│   │   ├── LC_MESSAGES/
│   │   │   └── extension.mo
│   │   └── description.md
│   └── es/
│       └── ...
└── scripts/
    └── install-locales.sh
```

#### Генерация переводов:

```bash
#!/bin/bash
# Генерация файлов локализации

LOCALES=("en" "ru" "es" "fr" "de")
DOMAIN="extension"

for locale in "${LOCALES[@]}"; do
    echo "Генерация переводов для $locale"

    # Создание структуры директорий
    mkdir -p "locales/$locale/LC_MESSAGES"

    # Извлечение строк для перевода
    xgettext --language=Shell --keyword=gettext --output="locales/$locale/$DOMAIN.pot" \
        layer/*.yaml scripts/*.sh hooks/*/*.sh

    # Компиляция переводов
    if [ -f "locales/$locale/$DOMAIN.po" ]; then
        msgfmt "locales/$locale/$DOMAIN.po" -o "locales/$locale/LC_MESSAGES/$DOMAIN.mo"
    fi
done
```

#### Использование локализации в скриптах:

```bash
#!/bin/bash
# Локализованный скрипт установки

# Загрузка локализации
export TEXTDOMAIN=extension
export TEXTDOMAINDIR=/usr/share/locale

echo "$(gettext "Installing extension...")"
echo "$(gettext "Configuration completed")"
echo "$(gettext "Installation successful")"

# Локализованные сообщения об ошибках
error_exit() {
    echo "$(gettext "Error"): $1" >&2
    exit 1
}

# Использование в коде
[[ -f "$REQUIRED_FILE" ]] || error_exit "$(gettext "Required file not found")"
```

### Поддержка различных регионов

#### Настройка часовых поясов:

```yaml
# Переменные для настройки региональных параметров
mmdebstrap:
  customize-hooks:
    - |
      # Настройка локали и часового пояса
      if [ -n "${IGconf_extension_timezone:-}" ]; then
        ln -sf "/usr/share/zoneinfo/${IGconf_extension_timezone}" /etc/localtime
        dpkg-reconfigure -f noninteractive tzdata
      fi

      # Настройка локали
      if [ -n "${IGconf_extension_locale:-}" ]; then
        locale-gen "${IGconf_extension_locale}"
        update-locale LANG="${IGconf_extension_locale}"
      fi
```

#### Поддержка различных кодировок:

```bash
#!/bin/bash
# Обработка файлов с различными кодировками

detect_encoding() {
    local file="$1"
    file --mime-type "$file" | cut -d= -f2 | tr -d ' '
}

convert_encoding() {
    local input_file="$1"
    local output_file="$2"
    local target_encoding="${3:-UTF-8}"

    case $(detect_encoding "$input_file") in
        "text/plain; charset=iso-8859-1")
            iconv -f iso-8859-1 -t "$target_encoding" "$input_file" > "$output_file"
            ;;
        "text/plain; charset=windows-1251")
            iconv -f windows-1251 -t "$target_encoding" "$input_file" > "$output_file"
            ;;
        *)
            cp "$input_file" "$output_file"
            ;;
    esac
}
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано из раздела документации: `24_международная_поддержка.mdc`
