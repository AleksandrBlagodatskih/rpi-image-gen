---
alwaysApply: ${cursor.file.content.includes("breaking") || cursor.file.content.includes("BREAKING")}
---


# Правило 27: Критические изменения (Breaking Changes)

## Назначение правила
Определяет стандарты и требования для критические изменения (breaking changes) в разработке расширений rpi-image-gen.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Критические изменения (Breaking Changes)

### 1. Изменения в API переменных
```yaml
# Старый формат (v1.x)
extension:
  old_setting: value

# Новый формат (v2.0)
extension:
  new_category:
    setting: value
```

### 2. Миграционный скрипт
```bash
#!/bin/bash
# migrate-v1-to-v2.sh

migrate_config() {
    local config_file="$1"

    # Резервное копирование
    cp "$config_file" "$config_file.backup"

    # Преобразование старого формата в новый
    sed -i 's/^extension:$/extension:\n  new_category:/' "$config_file"
    sed -i 's/^  old_setting:/    setting:/' "$config_file"

    echo "Миграция завершена. Создан бэкап: $config_file.backup"
}
```

#### Автоматическая миграция:

```yaml
# Детекция версии и автоматическая миграция
mmdebstrap:
  setup-hooks:
    - |
      # Проверка версии расширения
      if [ -f "/etc/extension-version" ]; then
        INSTALLED_VERSION=$(cat /etc/extension-version)
        if [ "$INSTALLED_VERSION" != "${IGconf_extension_version}" ]; then
          echo "Выполнение миграции с версии $INSTALLED_VERSION на ${IGconf_extension_version}"

          # Запуск миграционного скрипта
          /usr/local/bin/migrate-extension.sh "$INSTALLED_VERSION" "${IGconf_extension_version}"

          # Обновление версии
          echo "${IGconf_extension_version}" > /etc/extension-version
        fi
      else
        # Первая установка
        echo "${IGconf_extension_version}" > /etc/extension-version
      fi
```

### Обратная совместимость

#### Поддержка legacy режимов:

```yaml
# Условная поддержка старых переменных
customize-hooks:
  - |
    # Проверка legacy переменных
    if [ -n "${IGconf_extension_legacy_mode:-}" ] && [ "${IGconf_extension_legacy_mode}" = "true" ]; then
      # Использование старого API
      LEGACY_SETTING="${IGconf_extension_old_setting:-default_value}"
    else
      # Использование нового API
      NEW_SETTING="${IGconf_extension_new_category_setting:-default_value}"
    fi
```

#### Документирование миграции:

```markdown
<!-- MIGRATION_GUIDE.md -->

# Руководство по миграции на v2.0

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано из раздела документации: `28_критические_изменения_breaking_changes.mdc`
