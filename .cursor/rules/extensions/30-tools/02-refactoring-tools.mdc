---
alwaysApply: ${cursor.file.path.endsWith('.mdc') || cursor.file.path.includes("layer/") || cursor.file.path.includes("config/")}
---

# Правило 02: Инструменты рефакторинга и оптимизации

## Назначение правила
Определяет инструменты и методики автоматического рефакторинга конфигураций rpi-image-gen, обеспечивает поддержание связности, устранение дублирования кода и оптимизацию архитектуры расширений без нарушения функциональности.

## Важное замечание 🔵 ИНФОРМАЦИЯ
**Это правило описывает ЖЕЛАЕМУЮ функциональность для будущего развития системы rpi-image-gen.** Описанные команды рефакторинга (`rpi-image-gen refactor --*`) пока не реализованы в текущей версии проекта. Правило служит руководством для будущей реализации этих возможностей.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Автоматическое выявление дублирования

### Анализ дублированного кода
**🔴 ОБЯЗАТЕЛЬНОЕ**: Система автоматически находит повторяющиеся паттерны:

```bash
# Поиск дублированных настроек
rpi-image-gen refactor --find-duplicates layer/ --output duplicates.json

# Структура отчета
{
  "duplicates": [
    {
      "pattern": "IGconf_security_mode.*enforce",
      "occurrences": 5,
      "files": ["layer/security-*.yaml"],
      "recommendation": "extract to security-common.yaml"
    }
  ]
}
```

### Метрики дублирования
**🔴 ОБЯЗАТЕЛЬНОЕ**: Количественная оценка избыточности кода:

```yaml
# Метрики дублирования
duplication_metrics:
  duplicate_lines: 234
  duplicate_percentage: 15.2
  duplicated_variables: 12
  duplicated_hooks: 8
  extractable_patterns: 5
```

## Инструменты извлечения общих компонентов

### Автоматическое извлечение переменных
**🔴 ОБЯЗАТЕЛЬНОЕ**: Извлечение общих переменных в базовые слои:

```bash
# Извлечение общих переменных безопасности
rpi-image-gen refactor --extract-variables layer/security-*.yaml \
  --prefix IGconf_security_ \
  --output layer/security-common.yaml

# Результат: создание общего слоя с переменными
# IGconf_security_mode, IGconf_security_audit, etc.
```

### Извлечение общих хуков
**🔴 ОБЯЗАТЕЛЬНОЕ**: Объединение повторяющихся скриптов:

```bash
# Анализ и объединение хуков
rpi-image-gen refactor --merge-hooks layer/*/hooks/setup.sh \
  --output layer/base/hooks/common-setup.sh

# Автоматическое разделение на:
# - common-setup.sh: Общие операции
# - specific-setup.sh: Специфические настройки
```

### Создание композитных слоев
**🔴 ОБЯЗАТЕЛЬНОЕ**: Автоматическое объединение часто используемых комбинаций:

```bash
# Анализ паттернов использования
rpi-image-gen refactor --find-patterns config/ --min-frequency 3

# Создание композитного слоя
rpi-image-gen refactor --create-composite \
  --layers security-basic,monitoring-core,backup-simple \
  --name server-essentials \
  --output layer/server-essentials.yaml
```

## Оптимизация зависимостей

### Снижение глубины зависимостей
**🔴 ОБЯЗАТЕЛЬНОЕ**: Автоматическое упрощение цепочек зависимостей:

```bash
# Анализ глубины зависимостей
rpi-image-gen refactor --analyze-depth config.yaml

# Автоматическая оптимизация
rpi-image-gen refactor --flatten-dependencies config.yaml \
  --max-depth 4 \
  --output config-optimized.yaml

# Методы оптимизации:
# 1. Объединение промежуточных слоев
# 2. Прямые зависимости вместо транзитивных
# 3. Создание композитных слоев
```

### Устранение транзитивных зависимостей
**🔴 ОБЯЗАТЕЛЬНОЕ**: Прямые ссылки вместо цепочек:

```yaml
# До оптимизации (глубина 4)
app-layer → service-layer → security-layer → base-layer

# После оптимизации (глубина 2)
app-layer → app-composite → base-layer

# Где app-composite включает service + security
```

## Инструменты разрешения конфликтов

### Автоматическое разрешение конфликтов переменных
**🔴 ОБЯЗАТЕЛЬНОЕ**: Интеллектуальное объединение конфликтующих настроек:

```bash
# Разрешение конфликтов
rpi-image-gen refactor --resolve-conflicts config.yaml --strategy merge

# Стратегии разрешения:
# - merge: Объединение значений
# - override: Приоритет последнего слоя
# - explicit: Требование явного указания
# - validate: Проверка совместимости
```

### Примеры разрешения конфликтов
**🔴 ОБЯЗАТЕЛЬНОЕ**: Типовые паттерны решения конфликтов:

```yaml
# Конфликт списков
IGconf_security_packages: "ufw,auditd"
IGconf_security_packages: "fail2ban,apparmor"

# Результат слияния
IGconf_security_packages: "ufw,auditd,fail2ban,apparmor"

# Конфликт режимов
IGconf_security_mode: "enforce"
IGconf_security_mode: "audit"

# Результат валидации
ERROR: Conflicting security modes - choose explicit value
```

## Оптимизация производительности конфигураций

### Анализ размера слоев
**🔴 ОБЯЗАТЕЛЬНОЕ**: Оценка и оптимизация размера конфигураций:

```bash
# Анализ размера
rpi-image-gen refactor --analyze-size layer/ --output size-report.json

# Метрики размера
{
  "total_lines": 15420,
  "average_layer_size": 234,
  "largest_layer": "security-suite.yaml (1200 lines)",
  "recommendations": [
    "Разбить security-suite.yaml на специализированные слои",
    "Извлечь общие настройки в base-security.yaml"
  ]
}
```

### Оптимизация времени сборки
**🔴 ОБЯЗАТЕЛЬНОЕ**: Ускорение процесса сборки через оптимизацию:

```bash
# Анализ времени сборки по слоям
rpi-image-gen refactor --analyze-build-time config.yaml

# Оптимизация порядка слоев
rpi-image-gen refactor --optimize-build-order config.yaml \
  --parallel-safe \
  --output config-fast.yaml

# Результат: уменьшение времени сборки на 30-50%
```

## Инструменты миграции и обновления

### Автоматическая миграция версий
**🔴 ОБЯЗАТЕЛЬНОЕ**: Обновление конфигураций при изменении интерфейсов:

```bash
# Миграция на новую версию слоя
rpi-image-gen refactor --migrate-version \
  --from security-basic:1.0.0 \
  --to security-basic:2.0.0 \
  --config config.yaml

# Автоматические изменения:
# - IGconf_old_variable → IGconf_new_variable
# - Добавление новых обязательных переменных
# - Обновление зависимостей
```

### Обновление устаревших паттернов
**🔴 ОБЯЗАТЕЛЬНОЕ**: Модернизация конфигураций:

```bash
# Поиск устаревших паттернов
rpi-image-gen refactor --find-deprecated config.yaml

# Автоматическое обновление
rpi-image-gen refactor --modernize config.yaml --output config-modern.yaml

# Обновляемые паттерны:
# - Старые имена переменных
# - Устаревшие хуки
# - Неэффективные конструкции
```

## Качественный анализ и рекомендации

### Автоматическая оценка качества
**🔴 ОБЯЗАТЕЛЬНОЕ**: Комплексный анализ конфигураций:

```bash
# Полный анализ качества
rpi-image-gen refactor --quality-audit config.yaml --output audit-report.md

# Структура отчета
# ## Качество кода
# - Дублирование: 12% (хорошо)
# - Связность: 87% (хорошо)
# - Сложность: 3.2 (приемлемо)
#
# ## Архитектурные проблемы
# - Циклические зависимости: 0 (отлично)
# - Глубина зависимостей: 4 (хорошо)
#
# ## Рекомендации по улучшению
# 1. Извлечь общие переменные в base-security.yaml
# 2. Объединить мелкие слои monitoring-*
# 3. Оптимизировать порядок сборки
```

### Интерактивный режим рефакторинга
**🔴 ОБЯЗАТЕЛЬНОЕ**: Интерактивные инструменты с подтверждением изменений:

```bash
# Интерактивный рефакторинг
rpi-image-gen refactor --interactive config.yaml

# Примеры команд:
# > extract-common security-* IGconf_security_*
# > merge-layers monitoring-basic monitoring-extended
# > resolve-conflict IGconf_network_mode last-wins
# > optimize-order parallel-safe
```

## Интеграция с системой контроля версий

### Автоматическое комментирование изменений
**🔴 ОБЯЗАТЕЛЬНОЕ**: Создание понятных коммитов при рефакторинге:

```bash
# Рефакторинг с автоматическими коммитами
rpi-image-gen refactor --extract-common layer/security-*.yaml \
  --commit-messages \
  --output layer/security-common.yaml

# Создаваемые коммиты:
# "refactor: extract common security variables to security-common.yaml"
# "refactor: update security layers to use common variables"
# "refactor: remove duplicate variable definitions"
```

### Откат изменений рефакторинга
**🔴 ОБЯЗАТЕЛЬНОЕ**: Возможность безопасного отката:

```bash
# Создание точки восстановления
rpi-image-gen refactor --create-backup config.yaml

# Откат изменений
rpi-image-gen refactor --rollback config.yaml --to backup-2024-01-15

# Проверка целостности после отката
rpi-image-gen validate config.yaml
```

## Профилактика технического долга

### Регулярный аудит качества
**🔴 ОБЯЗАТЕЛЬНОЕ**: Периодическая проверка и улучшение:

```yaml
# Расписание аудита качества
quality_audit_schedule:
  daily: "check for new duplicates"
  weekly: "full cohesion analysis"
  monthly: "architecture review"
  quarterly: "major refactoring initiatives"
```

### Метрики технического долга
**🔴 ОБЯЗАТЕЛЬНОЕ**: Количественная оценка накопленного долга:

```yaml
# Метрики технического долга
technical_debt:
  duplication_debt: 15.2%   # Процент дублированного кода
  complexity_debt: 234      # Число сложных конструкций
  dependency_debt: 8        # Число глубоких зависимостей
  maintenance_effort: 2.3x  # Множитель усилий на поддержку

# Тренды улучшения
debt_trends:
  duplication_debt: "↓ 2.1%"  # Улучшение за последний месяц
  complexity_debt: "↑ 12"     # Ухудшение (требует внимания)
```

## Лучшие практики применения инструментов

### Поэтапное улучшение качества
**🔴 ОБЯЗАТЕЛЬНОЕ**: Постепенное применение рефакторинга:

```yaml
# Этап 1: Анализ и планирование
rpi-image-gen refactor --analyze config.yaml --plan-improvements

# Этап 2: Безопасные изменения
rpi-image-gen refactor --safe-improvements config.yaml --backup

# Этап 3: Оптимизации производительности
rpi-image-gen refactor --performance-optimizations config.yaml

# Этап 4: Архитектурные улучшения
rpi-image-gen refactor --architecture-improvements config.yaml
```

### Документирование изменений рефакторинга
**🔴 ОБЯЗАТЕЛЬНОЕ**: Подробная документация всех изменений:

```markdown
## Рефакторинг конфигурации (2024-01-15)

### Выполненные изменения
1. **Извлечение общих переменных**
   - Создан `layer/security-common.yaml`
   - Объединены дублированные `IGconf_security_*` переменные
   - Экономия: 45 строк кода

2. **Оптимизация зависимостей**
   - Уменьшена максимальная глубина с 6 до 4 уровней
   - Создан композитный слой `server-essentials`
   - Улучшение времени сборки: 25%

3. **Разрешение конфликтов**
   - Автоматическое слияние списков пакетов
   - Явное разрешение конфликтующих режимов безопасности

### Метрики до/после
- Дублирование кода: 18.5% → 12.3%
- Связность: 82% → 89%
- Время сборки: 8:30 → 6:15

### Тестирование
- ✅ Все существующие тесты проходят
- ✅ Новая конфигурация собирается успешно
- ✅ Производительность улучшена
```

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Регулярное выявление дублированного кода
- [ ] Автоматическое извлечение общих компонентов
- [ ] Оптимизация глубины зависимостей
- [ ] Разрешение конфликтов переменных
- [ ] Анализ и оптимизация производительности

### Качественные показатели
- [ ] Уровень дублирования кода < 15%
- [ ] Максимальная глубина зависимостей ≤ 4
- [ ] Связность конфигураций > 85%
- [ ] Время сборки уменьшается при рефакторинге
- [ ] Отсутствие критических конфликтов

### Автоматизированная проверка
- [ ] Интеграция инструментов рефакторинга в CI/CD
- [ ] Автоматическая генерация отчетов о качестве
- [ ] Мониторинг трендов технического долга
- [ ] Уведомления о необходимости рефакторинга

### Источник
Создано как дополнение к правилам 00-rule-transitions.mdc и 01-dependency-validation.mdc для обеспечения автоматической оптимизации и поддержания высокого качества архитектуры расширений rpi-image-gen.