---
alwaysApply: ${cursor.file.path.match(/\.(yaml|sh)$/) && !cursor.file.path.includes("test/")}
---


# Правило 32: Автоматическая валидация кода расширений

## Назначение правила
Обеспечивает автоматическую проверку и валидацию кода расширений rpi-image-gen в Cursor IDE.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Автоматическая валидация YAML конфигураций

### Структура конфигурации
```yaml
# Проверка обязательных секций
device:  # ОБЯЗАТЕЛЬНО
  layer: string  # Ссылка на слой устройства

image:   # ОБЯЗАТЕЛЬНО
  layer: string  # Ссылка на слой образа

layer:   # ОБЯЗАТЕЛЬНО
  base: string  # Базовый слой
  # Дополнительные слои опциональны
```

### Валидация переменных окружения
```yaml
# Корректные префиксы
IGconf_extension_*    # Переменные расширения
IGconf_device_*       # Переменные устройства
IGconf_image_*        # Переменные образа

# Примеры правильных имен
IGconf_extension_name: "my-extension"
IGconf_device_hostname: "rpi-device"
IGconf_image_boot_size: "200%"
```

### Проверка метаданных слоя
```yaml
# Обязательные поля метаданных
X-Env-Layer-Name: string          # Имя слоя
X-Env-Layer-Category: string      # Категория (extension|device|suite)
X-Env-Layer-Description: string   # Описание
X-Env-Layer-Version: semver       # Версия (1.0.0)
X-Env-Layer-Requires: string      # Зависимости через запятую

# Опциональные поля
X-Env-VarPrefix: string           # Префикс переменных
```

## Автоматическая валидация Bash скриптов

### Обязательные паттерны безопасности
```bash
#!/bin/bash
# shellcheck disable=SC2154,SC2086

set -euo pipefail  # ОБЯЗАТЕЛЬНО для всех скриптов

# Функции обработки ошибок
die() {
    echo "ERROR: $*" >&2
    exit 1
}

warn() {
    echo "WARN: $*" >&2
}
```

### Стандарты функций
```bash
# Правильное именование функций
setup_operation() { ... }     # Подготовка
validate_operation() { ... }  # Валидация
execute_operation() { ... }   # Выполнение
cleanup_operation() { ... }   # Очистка

# Функции с параметрами
configure_service() {
    local service_name="$1"
    local service_port="$2"

    # Валидация параметров
    [[ -n "$service_name" ]] || die "Service name required"
    [[ "$service_port" =~ ^[0-9]+$ ]] || die "Invalid port number"
}
```

### Безопасное использование переменных
```bash
# Правильное использование
IGconf_extension_name="${IGconf_extension_name:-default-name}"
IGconf_device_port="${IGconf_device_port:-8080}"

# Неправильное использование (предупреждение)
# $IGconf_variable (без кавычек)
# ${IGconf_variable} (лучше использовать двойные кавычки)
```

## Автоматическая валидация хуков

### Setup hooks валидация
```bash
# Корректная структура setup hook
#!/bin/bash
set -euo pipefail

# Проверка зависимостей
command -v required_tool >/dev/null 2>&1 || {
    echo "ERROR: required_tool not found" >&2
    exit 1
}

# Создание директорий
mkdir -p /var/lib/extension-data
chown extension-user:extension-group /var/lib/extension-data

exit 0
```

### Customize hooks валидация
```bash
# Корректная структура customize hook
#!/bin/bash
set -euo pipefail

# Использование переменных окружения
extension_name="${IGconf_extension_name:-unknown}"
device_type="${IGconf_device_type:-rpi}"

# Безопасные операции
case "$device_type" in
    "pi4"|"pi5")
        configure_pi_specifics
        ;;
    "cm4")
        configure_cm4_specifics
        ;;
    *)
        echo "WARN: Unknown device type: $device_type" >&2
        ;;
esac

exit 0
```

### Cleanup hooks валидация
```bash
# Корректная структура cleanup hook
#!/bin/bash
set -euo pipefail

# Очистка временных файлов
rm -rf /tmp/extension-build-*

# Остановка сервисов если нужно
if systemctl is-active --quiet extension-service; then
    systemctl stop extension-service
fi

# Удаление временных пользователей/групп
if id -u temp-user >/dev/null 2>&1; then
    userdel temp-user
fi

exit 0
```

## Правила линтинга

### YAML линтер правила
- [ ] Корректный синтаксис YAML
- [ ] Отсутствие табуляций (только пробелы)
- [ ] Согласованность отступов (2 пробела)
- [ ] Наличие комментариев для сложных секций
- [ ] Валидация ссылок на слои

### Shell линтер правила
- [ ] `set -euo pipefail` в начале скриптов
- [ ] Кавычки вокруг переменных
- [ ] Проверка наличия команд перед использованием
- [ ] Валидация параметров функций
- [ ] Отсутствие устаревших конструкций

### Общие правила качества
- [ ] Максимальная длина строки: 120 символов
- [ ] Наличие документации функций
- [ ] Согласованность именования
- [ ] Отсутствие закомментированного кода
- [ ] Минимализм: отсутствие избыточного кода
- [ ] Одна функция = одна задача (Single Responsibility)
- [ ] Отсутствие дублирования кода (DRY)

## Автоисправление типичных ошибок

### Автоматическое исправление YAML
```yaml
# До исправления
device:
layer:pi5
image:
  layer:image-rpios

# После исправления
device:
  layer: pi5
image:
  layer: image-rpios
```

### Автоматическое исправление Shell
```bash
# До исправления
echo $VAR
command arg1 arg2

# После исправления
echo "$VAR"
command arg1 arg2 || die "Command failed"
```

## Интеграция с системами контроля качества

### Pre-commit хуки
```bash
#!/bin/bash
# .pre-commit-config.yaml интеграция

repos:
  - repo: local
    hooks:
      - id: validate-rpi-extension
        name: Validate rpi-image-gen extension
        entry: validate-extension.py
        language: system
        files: ^(layer|config)/.*\.(yaml|sh)$
        pass_filenames: false
```

### CI/CD интеграция
```yaml
# .github/workflows/validate-extension.yml
name: Validate Extension
on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Validate extension
        run: |
          find layer/ config/ -name "*.yaml" -exec rpi-image-gen metadata --lint {} \;
          find layer-hooks/ -name "*.sh" -exec shellcheck {} \;

      - name: Check code simplicity
        run: |
          echo "=== Проверка принципа минимализма ==="

          # Подсчет строк кода
          total_lines=$(find layer/ -name "*.yaml" -exec wc -l {} \; | awk '{sum += $1} END {print sum}')
          echo "Общее количество строк: $total_lines"

          # Поиск дублированного кода (простая проверка)
          duplicates=$(find layer/ -name "*.yaml" -exec grep -h "apt-get install" {} \; | sort | uniq -c | awk '$1 > 1 {print $2 ": " $1 " раз"}')
          if [ -n "$duplicates" ]; then
            echo "⚠️  Найдено дублирование команд установки:"
            echo "$duplicates"
          fi

          # Проверка длины функций в bash скриптах
          find layer-hooks/ -name "*.sh" -exec bash -c '
            file="$1"
            echo "Проверка функций в $file:"
            awk "
            /^function / { func=substr(\$2, 1, index(\$2, \"(\")-1); start=NR }
            /^}/ { if (func) { lines=NR-start; if (lines > 50) print \"⚠️  Длинная функция \" func \": \" lines \" строк\"; func=\"\" } }
            " "$file"
          ' _ {} \;
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- - [ ] Непрерывное улучшение формулировок правила

### Источник
Создано для автоматизации контроля качества расширений rpi-image-gen