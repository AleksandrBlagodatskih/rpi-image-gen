---
alwaysApply: ${cursor.file.path.match(/\.(yaml|sh)$/) && cursor.file.content.length > 50}
---


# Правило 35: Интеллектуальные инструменты рефакторинга

## Назначение правила
Предоставляет интеллектуальные инструменты для автоматического рефакторинга и улучшения кода расширений rpi-image-gen.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Интеллектуальный анализ кода

### Автоматическое обнаружение проблем
```bash
# Анализ файла на проблемы
rpi-image-gen analyze --file layer.yaml --deep

# Поиск дублированного кода
rpi-image-gen analyze --duplicate-code --path layer/

# Анализ сложности кода
rpi-image-gen analyze --complexity --threshold 10
```

### Умные предложения по улучшению
```bash
# Предложения по рефакторингу
rpi-image-gen suggest --file layer.yaml

# Примеры вывода:
# ⚠️  Line 45: Consider using variables instead of hardcoded paths
# 💡 Suggestion: APPARMOR_DIR="/etc/apparmor.d"
# 💡 Suggestion: Replace 3 occurrences of "/etc/apparmor.d" with $APPARMOR_DIR

# 🔄 Line 78-85: Duplicate systemctl enable pattern
# 💡 Suggestion: Extract to function enable_service()
```

## Автоматизированный рефакторинг

### Рефакторинг паттернов
```bash
# Удаление дублированного кода
refactor-duplicate-code() {
    local file="$1"

    # Найти повторяющиеся блоки
    # Извлечь в функции
    # Заменить вызовы функций

    echo "✅ Refactored duplicate code in $file"
}

# Упрощение условных конструкций
refactor-conditionals() {
    local file="$1"

    # Преобразовать длинные if-else в case
    # Упростить вложенные условия
    # Использовать early returns

    echo "✅ Simplified conditionals in $file"
}

# Оптимизация переменных
refactor-variables() {
    local file="$1"

    # Удалить неиспользуемые переменные
    # Переименовать неясные имена
    # Группировать связанные переменные

    echo "✅ Optimized variables in $file"
}
```

### Безопасный рефакторинг
```bash
# Безопасное переименование с проверками
safe-rename() {
    local old_name="$1"
    local new_name="$2"
    local file="$3"

    # Проверить все использования
    local usages=$(grep -n "$old_name" "$file" | wc -l)

    # Создать backup
    cp "$file" "${file}.backup"

    # Выполнить замену
    sed -i "s/$old_name/$new_name/g" "$file"

    # Проверить корректность
    if validate-file "$file"; then
        echo "✅ Renamed $old_name -> $new_name ($usages occurrences)"
        rm "${file}.backup"
    else
        echo "❌ Rename failed, restoring backup"
        mv "${file}.backup" "$file"
    fi
}

# Пример использования
safe-rename "old_function" "new_function" "layer.yaml"
```

## Структурный рефакторинг

### Разделение больших файлов
```bash
# Автоматическое разделение больших файлов
split-large-file() {
    local file="$1"
    local max_lines="${2:-200}"

    local lines=$(wc -l < "$file")

    if [ "$lines" -gt "$max_lines" ]; then
        echo "🔄 Splitting large file: $(basename "$file") ($lines lines)"

        # Разделить на логические компоненты
        extract-functions "$file"
        extract-variables "$file"
        extract-hooks "$file"

        echo "✅ File split into smaller components"
    fi
}

# Извлечение функций
extract-functions() {
    local file="$1"

    # Найти все функции
    grep -n "^[a-zA-Z_][a-zA-Z0-9_]*()" "$file" | while read -r line; do
        local func_name=$(echo "$line" | cut -d: -f2 | cut -d'(' -f1)
        extract-function "$func_name" "$file" "functions.sh"
    done
}
```

### Оптимизация структуры
```bash
# Оптимизация структуры директорий
optimize-structure() {
    local layer_dir="$1"

    # Группировка связанных файлов
    # Создание поддиректорий по назначению
    # Перемещение шаблонов в отдельную директорию

    echo "✅ Structure optimized for $layer_dir"
}

# Консолидация конфигураций
consolidate-configs() {
    local layer_dir="$1"

    # Объединение связанных конфигураций
    # Удаление дублированных настроек
    # Создание общих конфигурационных файлов

    echo "✅ Configurations consolidated for $layer_dir"
}
```

## Семантический рефакторинг

### Улучшение читаемости
```bash
# Улучшение именования
improve-naming() {
    local file="$1"

    # Переименование неясных переменных
    sed -i 's/var1/config_file/g' "$file"
    sed -i 's/tmp/temp_dir/g' "$file"

    # Улучшение имен функций
    sed -i 's/do_something/configure_service/g' "$file"

    echo "✅ Naming improved in $file"
}

# Добавление комментариев
add-comments() {
    local file="$1"

    # Добавить комментарии к сложным конструкциям
    # Объяснить бизнес-логику
    # Документировать параметры функций

    echo "✅ Comments added to $file"
}

# Форматирование кода
format-code() {
    local file="$1"

    # Исправить отступы
    # Выровнять присваивания
    # Стандартизировать пробелы

    echo "✅ Code formatted in $file"
}
```

### Улучшение надежности
```bash
# Добавление error handling
add-error-handling() {
    local file="$1"

    # Добавить проверки ошибок
    if ! grep -q "set -euo pipefail" "$file"; then
        sed -i '1a set -euo pipefail' "$file"
    fi

    # Добавить die() функцию если отсутствует
    if ! grep -q "^die()" "$file"; then
        sed -i '/set -euo pipefail/a \
die() {\
    echo "ERROR: $*" >&2\
    exit 1\
}' "$file"
    fi

    echo "✅ Error handling added to $file"
}

# Добавление валидации
add-validation() {
    local file="$1"

    # Добавить валидацию входных параметров
    # Проверить существование файлов
    # Валидировать переменные окружения

    echo "✅ Validation added to $file"
}
```

## Производительность рефакторинга

### Оптимизация выполнения
```bash
# Оптимизация shell скриптов
optimize-shell-performance() {
    local file="$1"

    # Использовать более эффективные команды
    sed -i 's/grep | head/grep -m1/g' "$file"

    # Избегать subprocess в циклах
    # Использовать arrays вместо строк
    # Кэшировать результаты команд

    echo "✅ Shell performance optimized in $file"
}

# Оптимизация YAML
optimize-yaml() {
    local file="$1"

    # Удалить неиспользуемые anchors
    # Упростить структуры
    # Оптимизировать повторяющиеся паттерны

    echo "✅ YAML optimized in $file"
}
```

### Память и ресурсы
```bash
# Оптимизация использования памяти
optimize-memory() {
    local file="$1"

    # Избегать загрузки больших файлов в память
    # Использовать streaming для больших данных
    # Очищать временные переменные

    echo "✅ Memory usage optimized in $file"
}

# Оптимизация дискового ввода-вывода
optimize-io() {
    local file="$1"

    # Группировать операции чтения/записи
    # Использовать буферизацию
    # Минимизировать seeks

    echo "✅ I/O operations optimized in $file"
}
```

## Интеграция с CI/CD

### Автоматический рефакторинг в pipeline
```yaml
# .github/workflows/refactor.yml
name: Automatic Refactoring
on:
  schedule:
    - cron: '0 2 * * 0'  # Еженедельно по воскресеньям

jobs:
  refactor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run refactoring tools
        run: |
          find layer/ -name "*.yaml" -exec rpi-image-gen refactor --file {} --apply-safe \;

      - name: Create pull request
        if: success()
        uses: peter-evans/create-pull-request@v4
        with:
          title: "🤖 Automatic code refactoring"
          body: "Automatic refactoring improvements applied by AI assistant"
```

### Мониторинг качества кода
```bash
# Мониторинг метрик качества
monitor-code-quality() {
    echo "=== Code Quality Metrics ==="

    # Сложность кода
    echo "Cyclomatic complexity: $(find layer/ -name "*.yaml" -exec wc -l {} \; | awk '{sum+=$1} END {print sum/NR}') avg lines"

    # Дублирование кода
    echo "Code duplication: $(find-duplicate-lines layer/) lines"

    # Покрытие тестами
    echo "Test coverage: $(find layer/ -name "*test*" | wc -l) test files"

    # Время сборки
    echo "Build time: $(measure-build-time) seconds"
}
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано для интеллектуального рефакторинга расширений rpi-image-gen