---
alwaysApply: ${cursor.file.path.match(/\.(yaml|sh)$/) && !cursor.file.path.includes("test/")}
---


# Правило 13: Стандарты качества кода расширений

## Назначение правила
Определяет стандарты качества кода для расширений rpi-image-gen

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Стандарты качества кода

### Обязательные стандарты:

1. **Документирование кода**
   ```bash
   #!/bin/bash
   # Описание: Краткое описание функции скрипта
   # Параметры:
   #   $1 - первый параметр
   #   $2 - второй параметр
   # Возвращает:
   #   0 - успех
   #   1 - ошибка
   ```

2. **Обработка ошибок**
   ```bash
   set -euo pipefail

   error_exit() {
       echo "Ошибка: $1" >&2
       exit 1
   }

   # Проверка обязательных условий
   [[ -n "${REQUIRED_VAR:-}" ]] || error_exit "Не установлена REQUIRED_VAR"
   ```

3. **Безопасность**
   ```bash
   # Использование uchroot для безопасных операций
   uchroot "$1" sh -c "safe_command"

   # Минимизация привилегий
   chown -R "$USER:$USER" /home/"$USER"/restricted-dir

   # Защита от инъекций
   printf '%s\n' "$USER_INPUT" | tee file.txt
   ```

4. **Переносимость**
   ```bash
   # Использование стандартных команд
   command -v required_tool >/dev/null 2>&1 || {
       echo "Ошибка: Требуется установить required_tool" >&2
       exit 1
   }

   # Платформо-независимые пути
   SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
   ```

### Рекомендуемые практики:

1. **Модульность функций**
   ```bash
   install_package() {
       local package="$1"
       apt-get update && apt-get install -y "$package"
   }

   configure_service() {
       local service="$1"
       systemctl enable "$service" && systemctl start "$service"
   }
   ```

2. **Валидация входных данных**
   ```bash
   validate_input() {
       local input="$1"
       [[ "$input" =~ ^[a-zA-Z0-9_]+$ ]] || {
           echo "Ошибка: Неправильный формат входных данных" >&2
           return 1
       }
   }
   ```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано из раздела документации: `14_стандарты_качества_кода.mdc`
