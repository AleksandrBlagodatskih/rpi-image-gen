---
alwaysApply: ${cursor.file.path.includes("image/")}
---


# Правило 42: Расширения образов дисков (Image Extensions)

## Назначение правила
Расширения образов дисков определяют структуру и содержимое дисковых образов Raspberry Pi, включая таблицы разделов, файловые системы, загрузочные конфигурации и системные настройки.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Расширения образов дисков (Image Extensions)

Расширения образов дисков определяют структуру и содержимое дисковых образов Raspberry Pi, включая таблицы разделов, файловые системы, загрузочные конфигурации и системные настройки.

### Обзор типов расширений образов

#### Простые образы (Simple Images)
- **Назначение**: Минимальные конфигурации с 1-2 разделами
- **Примеры**: `simple_dual` (boot + root), `single_root` (только root)
- **Использование**: Разработка, тестирование, простые сценарии

#### Сложные образы (Complex Images)
- **Назначение**: Продакшн-системы с множественными разделами
- **Примеры**: `ab_userdata` (A/B обновления, шифрование, слоты)
- **Использование**: Критические системы, обновления OTA, безопасность

#### Специализированные образы (Specialized Images)
- **Назначение**: Специфичные сценарии использования
- **Примеры**: RAID-массивы, внешние хранилища, кластеры
- **Использование**: Предприятия, высокая доступность

### Структура каталога image/

```
image/
├── {partition_scheme}/              # Тип таблицы разделов (mbr, gpt)
│   ├── {image_name}/               # Название конфигурации образа
│   │   ├── image.yaml             # Метаданные и конфигурация слоя
│   │   ├── genimage.cfg.in.*      # Шаблоны genimage (по одному на ФС)
│   │   ├── pre-image.sh           # Подготовка перед сборкой
│   │   ├── setup.sh               # Настройка разделов
│   │   ├── post-image.sh          # Финальная обработка (опционально)
│   │   ├── mke2fs.conf            # Конфигурация ext4 (опционально)
│   │   ├── device/                # Специфичные настройки устройств
│   │   │   ├── provisionmap-*.json # Карты предоставления ресурсов
│   │   │   └── *.rules            # Правила udev
│   │   ├── bdebstrap/             # Хуки этапов сборки (опционально)
│   │   └── profile/               # Профили сборки (опционально)
│   └── README.md                  # Документация типа разделов
└── README.md                      # Общая документация расширений образов
```

### Обязательные компоненты расширений образов

#### 1. image.yaml - Метаданные слоя

**Обязательные метки X-Env:**
```yaml
# METABEGIN
# X-Env-Layer-Name: {unique_name}
# X-Env-Layer-Category: image
# X-Env-Layer-Description: Подробное описание функциональности
# X-Env-Layer-Version: 1.0.0
# X-Env-Layer-Requires: essential,image-base,device-base
# X-Env-Layer-Provides: image
# X-Env-VarPrefix: {image_name}
#
# Переменные конфигурации (обязательные)
# X-Env-Var-{variable_name}: {default_value}
# X-Env-Var-{variable_name}-Description: Описание переменной
# X-Env-Var-{variable_name}-Required: y|n
# X-Env-Var-{variable_name}-Valid: validation_rules
# X-Env-Var-{variable_name}-Set: y|n|immediate|force
# METAEND
---
# Конфигурация mmdebstrap (опционально)
mmdebstrap:
  packages:
    - dosfstools
    - e2fsprogs
    - parted
  setup-hooks:
    - echo "Подготовка инструментов создания образов"
```

**Обязательные переменные:**
```yaml
# Тип таблицы разделов (mbr/gpt)
# X-Env-Var-partition_table_type: mbr
# X-Env-Var-partition_table_type-Valid: mbr,gpt

# Размеры разделов
# X-Env-Var-boot_part_size: 100%
# X-Env-Var-root_part_size: 100%

# Тип файловой системы root раздела
# X-Env-Var-rootfs_type: ext4
# X-Env-Var-rootfs_type-Valid: ext4,btrfs,f2fs

# Provisioning map тип
# X-Env-Var-pmap: clear
# X-Env-Var-pmap-Valid: clear,crypt
```

#### 2. Шаблоны genimage.cfg.in

**Обязательные шаблоны:**
- `genimage.cfg.in.ext4` - для ext4 файловой системы
- `genimage.cfg.in.btrfs` - для btrfs (если поддерживается)
- `genimage.cfg.in.f2fs` - для f2fs (если поддерживается)

**Структура шаблона:**
```bash
# Базовый образ с sparse поддержкой
image <IMAGE_NAME>.<IMAGE_SUFFIX>.sparse {
   android-sparse {
      image = <IMAGE_NAME>.<IMAGE_SUFFIX>
   }
}

# Основной образ
image <IMAGE_NAME>.<IMAGE_SUFFIX> {
   hdimage {
      align = 8M
      partition-table-type = "{mbr|gpt}"
      fill = true
   }

   # Определение разделов
   partition boot {
      partition-type = 0xC
      bootable = true
      image = "boot.vfat"
   }

   partition root {
      partition-type = 0x83
      image = "root.{ext4|btrfs|f2fs}"
   }
}

# Определение образов разделов
image boot.vfat {
   vfat {
      label = "BOOT"
      extraargs = "-S <SECTOR_SIZE> -i <BOOT_LABEL>"
   }
   size = <BOOT_SIZE>
   mountpoint = "/boot/firmware"
}

image root.ext4 {
   ext4 {
      label = "ROOT"
      use-mke2fs = true
      mke2fs-conf = <MKE2FS_CONFIG>
      extraargs = "-U <ROOT_UUID>"
   }
   size = <ROOT_SIZE>
   mountpoint = "/"
}
```

#### 3. pre-image.sh - Подготовка образа

**Обязательная функциональность:**
```bash
#!/bin/bash
set -eu

fs=$1
genimg_in=$2

[[ -d "$fs" ]] || exit 0

# Генерация UUID для разделов
BOOT_LABEL=$(uuidgen | sed 's/-.*//' | tr 'a-f' 'A-F')
BOOT_UUID=$(echo "$BOOT_LABEL" | sed 's/^\(....\)\(....\)$/\1-\2/')
ROOT_UUID=$(uuidgen)

# Сохранение UUID для использования в других скриптах
rm -f ${IGconf_image_outputdir}/img_uuids
for v in BOOT_LABEL BOOT_UUID ROOT_UUID; do
    eval "val=\$$v"
    echo "$v=$val" >> "${IGconf_image_outputdir}/img_uuids"
done

# Генерация конфигурации genimage из шаблона
cat genimage.cfg.in.${IGconf_image_rootfs_type} | sed \
   -e "s|<IMAGE_NAME>|$IGconf_image_name|g" \
   -e "s|<IMAGE_SUFFIX>|$IGconf_image_suffix|g" \
   -e "s|<BOOT_SIZE>|$IGconf_image_boot_part_size|g" \
   -e "s|<ROOT_SIZE>|$IGconf_image_root_part_size|g" \
   -e "s|<BOOT_LABEL>|$BOOT_LABEL|g" \
   -e "s|<ROOT_UUID>|$ROOT_UUID|g" \
   > ${genimg_in}/genimage.cfg

# Обработка provisioning map
if [[ -f "$IGconf_image_pmapfile" ]]; then
   cp ${IGconf_image_pmapfile} ${IGconf_image_outputdir}/provisionmap.json
   # Замена UUID в provisioning map
   sed -i "s|<ROOT_UUID>|$ROOT_UUID|g" ${IGconf_image_outputdir}/provisionmap.json
fi
```

#### 4. setup.sh - Настройка разделов

**Обязательная функциональность:**
```bash
#!/bin/bash
set -eu

LABEL="$1"
BOOTUUID="$2"
ROOTUUID="$3"

case $LABEL in
   ROOT)
      # Настройка корневого раздела
      case $IGconf_image_rootfs_type in
         ext4)
            cat << EOF > $IMAGEMOUNTPATH/etc/fstab
UUID=${ROOTUUID} /               ext4 rw,relatime,errors=remount-ro,commit=30 0 1
EOF
            ;;
         btrfs)
            cat << EOF > $IMAGEMOUNTPATH/etc/fstab
UUID=${ROOTUUID} /               btrfs defaults 0 0
EOF
            ;;
      esac

      # Добавление загрузочного раздела
      cat << EOF >> $IMAGEMOUNTPATH/etc/fstab
UUID=${BOOTUUID} /boot/firmware  vfat defaults,rw,noatime,errors=remount-ro 0 2
EOF
      ;;
   BOOT)
      # Настройка загрузочного раздела
      sed -i "s|root=\([^ ]*\)|root=UUID=${ROOTUUID}|" $IMAGEMOUNTPATH/cmdline.txt
      ;;
esac
```

### Опциональные компоненты

#### Provisioning Maps (device/provisionmap-*.json)

**Формат JSON:**
```json
[
   {
      "attributes": {
         "PMAPversion": "1.0.0",
         "system_type": "flat"
      }
   },
   {
      "partitions": [
         {"image": "boot"},
         {"image": "root"}
      ]
   }
]
```

**Шифрование:**
```json
{
   "encrypted": {
      "luks2": {
         "key_size": 512,
         "cipher": "aes-xts-plain64",
         "label": "root",
         "uuid": "<CRYPT_UUID>",
         "hash": "sha256"
      },
      "partitions": [
         {"image": "root"}
      ]
   }
}
```

#### Специфичные настройки устройств (device/*.rules)

**Правила udev:**
```bash
# device/slot.rules
SUBSYSTEM=="block", ENV{ID_FS_UUID}!="", ACTION=="add|change", IMPORT{program}="/usr/bin/rpi-slot -u %E{ID_FS_UUID}", SYMLINK+="$env{SLOT}"
```

### Типы расширений образов

#### 1. Простые образы (Simple Images)

**Характеристики:**
- 1-2 раздела (boot + root)
- Минимальная конфигурация
- Подходят для разработки

**Пример структуры:**
```
image/mbr/simple_dual/
├── image.yaml
├── genimage.cfg.in.ext4
├── genimage.cfg.in.btrfs
├── pre-image.sh
├── setup.sh
└── device/
    ├── provisionmap-clear.json
    └── provisionmap-crypt.json
```

#### 2. Сложные образы (Complex Images)

**Характеристики:**
- Множественные разделы
- A/B обновления
- Шифрование
- Слоты для надежности

**Пример структуры:**
```
image/gpt/ab_userdata/
├── image.yaml
├── genimage.cfg.in
├── pre-image.sh
├── setup.sh
├── mke2fs.conf
└── device/
    ├── provisionmap-clear.json
    ├── provisionmap-crypt.json
    ├── provisionmap-hybrid.json
    ├── slot.rules
    └── emmc-wp.sh
```

#### 3. Специализированные образы (Specialized Images)

**Характеристики:**
- Специфичные сценарии
- Дополнительная функциональность
- Предприятие-ready

### Лучшие практики разработки

#### 1. Безопасность
- Использование uchroot для изоляции операций
- Валидация всех входных параметров
- Безопасное управление ключами шифрования

#### 2. Надежность
- Обработка ошибок на всех этапах
- Верификация результатов операций
- Атомарные операции где возможно

#### 3. Масштабируемость
- Параметризация размеров разделов
- Поддержка различных типов ФС
- Гибкая конфигурация через переменные

#### 4. Тестируемость
- Валидация конфигураций перед сборкой
- Интеграционные тесты для полных образов
- Автоматизированное тестирование

### Примеры расширений

#### Пример простого образа (simple_dual)

**image.yaml:**
```yaml
# METABEGIN
# X-Env-Layer-Name: simple-dual
# X-Env-Layer-Category: image
# X-Env-Layer-Description: Простой образ с двумя разделами (boot + root)
# X-Env-Layer-Version: 1.0.0
# X-Env-Layer-Requires: essential,image-base
# X-Env-Layer-Provides: image
# X-Env-VarPrefix: simple_dual
#
# X-Env-Var-rootfs_type: ext4
# X-Env-Var-rootfs_type-Description: Тип файловой системы root раздела
# X-Env-Var-rootfs_type-Required: false
# X-Env-Var-rootfs_type-Valid: ext4,btrfs
# X-Env-Var-rootfs_type-Set: true
#
# X-Env-Var-boot_part_size: 200M
# X-Env-Var-boot_part_size-Description: Размер загрузочного раздела
# X-Env-Var-boot_part_size-Required: false
# X-Env-Var-boot_part_size-Valid: size
# X-Env-Var-boot_part_size-Set: true
#
# X-Env-Var-root_part_size: 100%
# X-Env-Var-root_part_size-Description: Размер корневого раздела
# X-Env-Var-root_part_size-Required: false
# X-Env-Var-root_part_size-Valid: size
# X-Env-Var-root_part_size-Set: true
# METAEND
---
mmdebstrap:
  packages:
    - dosfstools
    - e2fsprogs
```

#### Пример сложного образа (ab_userdata)

**image.yaml:**
```yaml
# METABEGIN
# X-Env-Layer-Name: ab-userdata
# X-Env-Layer-Category: image
# X-Env-Layer-Description: Образ с поддержкой A/B обновлений и шифрования
# X-Env-Layer-Version: 2.0.0
# X-Env-Layer-Requires: essential,image-base,device-base
# X-Env-Layer-Provides: image
# X-Env-VarPrefix: ab_userdata
#
# X-Env-Var-system_part_size: 100%
# X-Env-Var-system_part_size-Description: Размер системного раздела на слот
# X-Env-Var-system_part_size-Required: false
# X-Env-Var-system_part_size-Valid: size
# X-Env-Var-system_part_size-Set: true
#
# X-Env-Var-pmap: crypt
# X-Env-Var-pmap-Description: Тип provisioning map
# X-Env-Var-pmap-Required: false
# X-Env-Var-pmap-Valid: clear,crypt,hybrid
# X-Env-Var-pmap-Set: force
# METAEND
---
mmdebstrap:
  packages:
    - dosfstools
    - e2fsprogs
    - parted
    - initramfs-tools
    - rpi-blockutils
```

### Процесс разработки расширений образов

#### 1. Планирование
- Определить цель и сценарий использования
- Выбрать тип таблицы разделов (MBR/GPT)
- Определить количество и назначение разделов
- Выбрать поддерживаемые файловые системы

#### 2. Реализация
- Создать структуру каталогов
- Разработать метаданные в image.yaml
- Создать шаблоны genimage.cfg.in для всех ФС
- Реализовать скрипты pre-image.sh и setup.sh
- Добавить provisioning maps при необходимости

#### 3. Тестирование
- Тестирование сборки с различными параметрами
- Валидация созданных образов
- Тестирование загрузки на целевых устройствах
- Проверка функциональности всех компонентов

#### 4. Документирование
- Описание назначения и характеристик
- Инструкции по использованию
- Примеры конфигураций
- Известные ограничения и требования

### Интеграция с другими компонентами

#### Зависимости от слоев устройств
```yaml
# X-Env-Layer-Requires: essential,image-base,device-base
# X-Env-VarRequires: IGconf_device_storage_type
```

#### Совместимость с загрузчиком
- Обновление cmdline.txt для правильной загрузки
- Настройка fstab для монтирования разделов
- Конфигурация initramfs при необходимости

#### Интеграция с provisioning системой
- Поддержка различных типов provisioning maps
- Генерация UUID для разделов
- Управление шифрованием и ключами

### Шаблоны расширений образов

#### Шаблон простого образа (Simple Image Template)

**Структура каталога:**
```
image/mbr/{image_name}/
├── image.yaml                    # Метаданные слоя
├── genimage.cfg.in.ext4         # Шаблон для ext4
├── genimage.cfg.in.btrfs        # Шаблон для btrfs
├── pre-image.sh                 # Подготовка образа
├── setup.sh                     # Настройка разделов
└── device/
    ├── provisionmap-clear.json  # Простая карта
    └── provisionmap-crypt.json  # С шифрованием
```

**Шаблон image.yaml:**
```yaml
# METABEGIN
# X-Env-Layer-Name: {image_name}
# X-Env-Layer-Category: image
# X-Env-Layer-Description: {Описание образа}
# X-Env-Layer-Version: 1.0.0
# X-Env-Layer-Requires: essential,image-base
# X-Env-Layer-Provides: image
# X-Env-VarPrefix: {image_name}
#
# X-Env-Var-rootfs_type: ext4
# X-Env-Var-rootfs_type-Description: Тип файловой системы root раздела
# X-Env-Var-rootfs_type-Required: false
# X-Env-Var-rootfs_type-Valid: ext4,btrfs
# X-Env-Var-rootfs_type-Set: true
#
# X-Env-Var-boot_part_size: 200M
# X-Env-Var-boot_part_size-Description: Размер загрузочного раздела
# X-Env-Var-boot_part_size-Required: false
# X-Env-Var-boot_part_size-Valid: size
# X-Env-Var-boot_part_size-Set: true
#
# X-Env-Var-root_part_size: 100%
# X-Env-Var-root_part_size-Description: Размер корневого раздела
# X-Env-Var-root_part_size-Required: false
# X-Env-Var-root_part_size-Valid: size
# X-Env-Var-root_part_size-Set: true
#
# X-Env-Var-pmap: clear
# X-Env-Var-pmap-Description: Тип provisioning map
# X-Env-Var-pmap-Required: false
# X-Env-Var-pmap-Valid: clear,crypt
# X-Env-Var-pmap-Set: force
# METAEND
---
mmdebstrap:
  packages:
    - dosfstools
    - e2fsprogs
```

**Шаблон genimage.cfg.in.ext4:**
```bash
image <IMAGE_NAME>.<IMAGE_SUFFIX>.sparse {
   android-sparse {
      image = <IMAGE_NAME>.<IMAGE_SUFFIX>
   }
}

image <IMAGE_NAME>.<IMAGE_SUFFIX> {
   hdimage {
      align = 8M
      partition-table-type = "mbr"
      fill = true
   }

   partition boot {
      partition-type = 0xC
      bootable = true
      image = "boot.vfat"
   }

   partition root {
      partition-type = 0x83
      image = "root.ext4"
   }
}

image boot.vfat {
   vfat {
      label = "BOOT"
      extraargs = "-S <SECTOR_SIZE> -i <BOOT_LABEL>"
   }
   size = <BOOT_SIZE>
   mountpoint = "/boot/firmware"
}

image root.ext4 {
   ext4 {
      label = "ROOT"
      use-mke2fs = true
      mke2fs-conf = <MKE2FS_CONFIG>
      extraargs = "-U <ROOT_UUID>"
   }
   size = <ROOT_SIZE>
   mountpoint = "/"
}
```

#### Шаблон сложного образа (Complex Image Template)

**Структура каталога:**
```
image/gpt/{image_name}/
├── image.yaml                    # Метаданные слоя
├── genimage.cfg.in              # Единый шаблон
├── pre-image.sh                 # Подготовка образа
├── setup.sh                     # Настройка разделов
├── mke2fs.conf                  # Конфигурация ext4
└── device/
    ├── provisionmap-clear.json  # Простая карта
    ├── provisionmap-crypt.json  # С шифрованием
    ├── provisionmap-hybrid.json # Гибридная
    ├── slot.rules               # Правила слотов
    └── emmc-wp.sh              # Защита от записи
```

**Шаблон image.yaml для сложного образа:**
```yaml
# METABEGIN
# X-Env-Layer-Name: {image_name}
# X-Env-Layer-Category: image
# X-Env-Layer-Description: {Описание сложного образа с A/B обновлениями}
# X-Env-Layer-Version: 2.0.0
# X-Env-Layer-Requires: essential,image-base,device-base
# X-Env-Layer-Provides: image
# X-Env-VarPrefix: {image_name}
#
# X-Env-Var-system_part_size: 100%
# X-Env-Var-system_part_size-Description: Размер системного раздела на слот
# X-Env-Var-system_part_size-Required: false
# X-Env-Var-system_part_size-Valid: size
# X-Env-Var-system_part_size-Set: true
#
# X-Env-Var-data_part_size: 100%
# X-Env-Var-data_part_size-Description: Размер раздела данных
# X-Env-Var-data_part_size-Required: false
# X-Env-Var-data_part_size-Valid: size
# X-Env-Var-data_part_size-Set: true
#
# X-Env-Var-pmap: crypt
# X-Env-Var-pmap-Description: Тип provisioning map
# X-Env-Var-pmap-Required: false
# X-Env-Var-pmap-Valid: clear,crypt,hybrid
# X-Env-Var-pmap-Set: force
#
# X-Env-Var-ptable_protect: n
# X-Env-Var-ptable_protect-Description: Включить защиту таблицы разделов eMMC
# X-Env-Var-ptable_protect-Required: false
# X-Env-Var-ptable_protect-Valid: y,n
# X-Env-Var-ptable_protect-Set: true
# METAEND
---
mmdebstrap:
  packages:
    - dosfstools
    - e2fsprogs
    - parted
    - initramfs-tools
    - rpi-blockutils
```

#### Шаблон образа с RAID (RAID Image Template)

**Особенности:**
- Внешние диски с RAID-массивами
- SD карта только для загрузки
- Поддержка шифрования RAID

**Структура каталога:**
```
image/mbr/{image_name}/
├── image.yaml                    # Метаданные слоя
├── genimage.cfg.in.ext4         # Шаблон для ext4
├── genimage.cfg.in.btrfs        # Шаблон для btrfs
├── pre-image.sh                 # Подготовка образа
├── setup.sh                     # Настройка разделов
├── key-management.sh            # Управление ключами шифрования
├── integrity-check.sh           # Проверка целостности
├── performance-optimization.sh  # Оптимизация производительности
└── device/
    ├── provisionmap-clear.json  # Простая карта
    └── provisionmap-crypt.json  # С шифрованием
```

**Шаблон image.yaml для RAID:**
```yaml
# METABEGIN
# X-Env-Layer-Name: {raid_image_name}
# X-Env-Layer-Category: image
# X-Env-Layer-Description: Образ с внешними RAID-массивами
# X-Env-Layer-Version: 1.0.0
# X-Env-Layer-Requires: essential,image-base,sys-misc-raid-base
# X-Env-Layer-Provides: image
# X-Env-VarPrefix: {raid_image_name}
#
# X-Env-Var-raid_level: RAID1
# X-Env-Var-raid_level-Description: Уровень RAID массива
# X-Env-Var-raid_level-Required: false
# X-Env-Var-raid_level-Valid: RAID1,RAID5,RAID10
# X-Env-Var-raid_level-Set: true
#
# X-Env-Var-raid_devices: 2
# X-Env-Var-raid_devices-Description: Количество устройств в RAID массиве
# X-Env-Var-raid_devices-Required: false
# X-Env-Var-raid_devices-Valid: int:2-16
# X-Env-Var-raid_devices-Set: true
#
# X-Env-Var-rootfs_type: ext4
# X-Env-Var-rootfs_type-Description: Файловая система RAID массива
# X-Env-Var-rootfs_type-Required: false
# X-Env-Var-rootfs_type-Valid: ext4,btrfs
# X-Env-Var-rootfs_type-Set: true
#
# X-Env-Var-encryption_enabled: y
# X-Env-Var-encryption_enabled-Description: Включить шифрование RAID массива
# X-Env-Var-encryption_enabled-Required: false
# X-Env-Var-encryption_enabled-Valid: y,n
# X-Env-Var-encryption_enabled-Set: true
# METAEND
---
mmdebstrap:
  packages:
    - dosfstools
    - e2fsprogs
    - mdadm
    - cryptsetup
```

### Рекомендации по именованию

#### Соглашения именования расширений образов:

**Простые образы:**
- `{functionality}-{layout}` (например: `minimal-mbr`, `desktop-gpt`)
- `{target}-{type}` (например: `rpi4-simple`, `server-basic`)

**Сложные образы:**
- `{feature}-{layout}-{version}` (например: `ab-gpt-v2`, `ota-hybrid-v1`)
- `{usecase}-{technology}` (например: `enterprise-raid`, `secure-crypt`)

**Специализированные образы:**
- `{domain}-{specialty}` (например: `industrial-ota`, `automotive-ab`)
- `{industry}-{feature}` (например: `healthcare-secure`, `finance-raid`)

### Контроль качества расширений образов

#### Метрики качества:
- **Корректность**: Образ загружается и работает стабильно
- **Безопасность**: Шифрование работает, ключи защищены
- **Производительность**: Время сборки, размер образа
- **Совместимость**: Работает на целевых устройствах

#### Тестирование:
```bash
# Тестирование сборки
rpi-image-gen build -c {config}.yaml --dry-run

# Тестирование образа
qemu-system-aarch64 -M raspi3b -drive file={image}.img,format=raw -serial stdio

# Валидация структуры (рекомендуется использовать внешние инструменты)
# parted -l {image}.img  # Проверка таблицы разделов
# file {image}.img       # Проверка типа файла
```

Эти правила обеспечивают **стандартизированную**, **безопасную** и **масштабируемую** разработку расширений образов для rpi-image-gen.

<a id="layer---библиотека-слоев-системы"></a>

### layer/ - Библиотека слоев системы: 🔴 *ОБЯЗАТЕЛЬНЫЕ*

#### Назначение:
Модульные компоненты для кастомизации образов.

#### Структура каталогов:
```
layer/
├── base/                   # Базовые системные слои
├── device/                 # Специфичные для устройств слои
├── image/                  # Специфичные для образов слои
├── suite/                  # Комплексные решения (desktop, server)
├── app-* /                 # Прикладные слои (app-web, app-database)
├── sys-* /                 # Системные слои (sys-devel, sys-admin)
└── LAYER_BEST_PRACTICES   # Рекомендации по разработке слоев
```

#### Стандарты кодирования для слоев:

**Обязательная структура YAML:**
```yaml
# METABEGIN
# Метаданные слоя (X-Env формат)
# X-Env-Layer-Name: unique-layer-name
# X-Env-Layer-Category: extension|device|image|suite|foundation
# X-Env-Layer-Description: Описание функциональности
# X-Env-Layer-Version: 1.0.0
# X-Env-Layer-Requires: dependency1,dependency2
# X-Env-VarPrefix: layer_name
#
# Переменные конфигурации
# X-Env-Var-variable_name: default_value
# X-Env-Var-variable_name-Description: Описание переменной
# X-Env-Var-variable_name-Required: y|n
# X-Env-Var-variable_name-Valid: validation_rules
# METAEND
---
mmdebstrap:
  packages:
    - package1
    - package2
  setup-hooks:
    - echo "Setup hook"
  customize-hooks:
    - echo "Customize hook"
  cleanup-hooks:
    - echo "Cleanup hook"
```

**Паттерны для хуков в слоях:**
```bash
# layer/{category}/{layer_name}/bdebstrap/customize-layer-setup
#!/bin/bash
set -euo pipefail

# Безопасная установка пакетов
install_layer_packages() {
    local layer_name="$1"
    shift
    local packages=("$@")

    # Проверка доступности пакетов
    for package in "${packages[@]}"; do
        apt-cache show "$package" >/dev/null 2>&1 || {
            warn "Пакет недоступен: $package"
            continue
        }
    done

    # Установка с обработкой ошибок
    if ! apt-get install -y "${packages[@]}" 2>/dev/null; then
        warn "Ошибка установки пакетов в слое $layer_name"
        return 1
    fi

    return 0
}

# Основная логика слоя
main() {
    local layer_packages=(
        "package1"
        "package2"
        "package3"
    )

    install_layer_packages "${IGconf_layer_name:-unknown}" "${layer_packages[@]}"
}

main "$@"
```

<a id="layer-hooks---хуки-для-слоев"></a>

### layer-hooks/ - Хуки для слоев: ⚠️ *ОПЦИОНАЛЬНЫЕ*

#### Назначение:
Общие хуки, используемые несколькими слоями.

#### Структура каталогов:
```
layer-hooks/
├── {category}/             # Категория хуков
│   ├── hook-name.sh       # Конкретный хук
│   └── README.md          # Документация категории
```

#### Стандарты кодирования:

**Шаблон хука:**
```bash
#!/bin/bash
# layer-hooks/{category}/hook-name.sh

set -euo pipefail

# Документация хука
# Назначение: Краткое описание функциональности
# Параметры: $1 - первый параметр, $2 - второй параметр
# Зависимости: список требуемых инструментов
# Возвращает: 0 - успех, 1 - ошибка

# Функция валидации параметров
validate_hook_parameters() {
    [[ $# -ge 1 ]] || die "Требуется минимум один параметр"
    [[ -n "$1" ]] || die "Первый параметр не может быть пустым"
}

# Основная логика хука
execute_hook_logic() {
    local param1="$1"
    local param2="${2:-default_value}"

    # Реализация основной функциональности
    case "$param1" in
        "option1")
            handle_option1 "$param2"
            ;;
        "option2")
            handle_option2 "$param2"
            ;;
        *)
            die "Неизвестная опция: $param1"
            ;;
    esac
}

# Вспомогательные функции
handle_option1() {
    local param="$1"
    # Реализация опции 1
}

handle_option2() {
    local param="$1"
    # Реализация опции 2
}

# Главная функция
main() {
    validate_hook_parameters "$@"
    execute_hook_logic "$@"
}

main "$@"
```

<a id="lib---библиотеки-и-вспомогательные-скрипты"></a>

### lib/ - Библиотеки и вспомогательные скрипты: 🔴 *ОБЯЗАТЕЛЬНЫЕ*

#### Назначение:
Общие библиотеки функций для всего проекта.

#### Правила именования:
- **Основные библиотеки**: `{functionality}.sh` (cli.sh, common.sh)
- **Вспомогательные**: `{utility}-{function}.sh`

#### Стандарты кодирования:

**Шаблон библиотеки:**
```bash
#!/bin/bash
# lib/{functionality}.sh

set -euo pipefail

# Документация модуля
# Назначение: Описание функциональности модуля
# Зависимости: список зависимостей
# Экспортируемые функции: список публичных функций

# Проверка корректности загрузки как библиотеки
if [[ "${BASH_SOURCE[0]}" == "$0" ]]; then
    echo "Этот файл должен загружаться как библиотека, а не выполняться напрямую" >&2
    exit 1
fi

# Глобальные переменные модуля
readonly LIB_NAME="$(basename "${BASH_SOURCE[0]}")"
readonly LIB_VERSION="1.0.0"

# Публичные функции (экспортируемые)
public_function1() {
    # Документация функции
    # Параметры: описание параметров
    # Возвращает: описание возвращаемых значений

    # Реализация функции
}

public_function2() {
    # Реализация второй функции
}

# Приватные функции (начинаются с _)
_private_helper() {
    # Вспомогательная функция
}

# Экспорт публичных функций
export -f public_function1 public_function2

# Инициализация модуля при загрузке
_lib_init() {
    # Код инициализации при загрузке библиотеки
}

_lib_init
```

<a id="scripts---скрипты-этапов-сборки"></a>

### scripts/ - Скрипты этапов сборки: ⚠️ *ОПЦИОНАЛЬНЫЕ*

#### Назначение:
Скрипты для конкретных этапов процесса сборки.

#### Структура:
```
scripts/
└── bdebstrap/             # Скрипты для этапов bdebstrap
    ├── setup*            # Скрипты этапа setup
    ├── essential*        # Скрипты этапа essential
    ├── customize*        # Скрипты этапа customize
    └── cleanup*          # Скрипты этапа cleanup
```

#### Стандарты кодирования:

**Шаблон скрипта этапа:**
```bash
#!/bin/bash
# scripts/bdebstrap/{stage}{number}-{description}

set -euo pipefail

# Документация скрипта
# Этап: Название этапа (setup|essential|customize|cleanup)
# Назначение: Краткое описание
# Выполняется: В контейнере chroot
# Параметры: Стандартные параметры chroot ($1 = путь к chroot)

# Валидация параметров
readonly CHROOT_PATH="$1"
[[ -d "$CHROOT_PATH" ]] || die "Некорректный путь к chroot: $CHROOT_PATH"

# Основная функциональность этапа
execute_stage_logic() {
    # Специфичная логика для этого этапа

    case "$(basename "$0")" in
        "essential"*)
            # Логика для essential этапа
            ;;
        "customize"*)
            # Логика для customize этапа
            ;;
        "cleanup"*)
            # Логика для cleanup этапа
            ;;
    esac
}

# Безопасное выполнение команд в chroot
safe_chroot_command() {
    local command="$1"

    # Валидация команды
    [[ -n "$command" ]] || die "Команда не может быть пустой"

    # Выполнение команды
    chroot "$CHROOT_PATH" sh -c "$command"
}

# Главная функция
main() {
    execute_stage_logic
}

main "$@"
```

<a id="site---python-модули-проекта"></a>

### site/ - Python модули проекта: ⚠️ *ОПЦИОНАЛЬНЫЕ*

#### Назначение:
Python компоненты для обработки метаданных и валидации.

#### Структура:
```
site/
├── validators.py          # Валидаторы метаданных
├── processors.py          # Обработчики конфигураций
└── utils.py              # Вспомогательные утилиты
```

#### Стандарты кодирования (Python):

```python
#!/usr/bin/env python3
"""
Модуль для обработки метаданных слоев.

Назначение: Парсинг и валидация X-Env метаданных
Зависимости: python3-debian, pyyaml
"""

import sys
import yaml
from pathlib import Path
from typing import Dict, List, Optional, Any

class LayerMetadataProcessor:
    """Обработчик метаданных слоев."""

    def __init__(self, layer_path: Path):
        """Инициализация обработчика.

        Args:
            layer_path: Путь к файлу слоя
        """
        self.layer_path = layer_path
        self.metadata: Dict[str, Any] = {}

    def parse_metadata(self) -> Dict[str, Any]:
        """Парсинг метаданных из файла слоя.

        Returns:
            Словарь с метаданными слоя

        Raises:
            MetadataError: При ошибке парсинга
        """
        try:
            content = self.layer_path.read_text(encoding='utf-8')
            return self._extract_metadata(content)
        except (IOError, UnicodeDecodeError) as e:
            raise MetadataError(f"Ошибка чтения файла {self.layer_path}: {e}")

    def _extract_metadata(self, content: str) -> Dict[str, Any]:
        """Извлечение метаданных из содержимого файла."""
        # Реализация парсинга X-Env блоков
        metadata = {}

        # Поиск блока METABEGIN/METAEND
        # Парсинг X-Env директив
        # Валидация структуры

        return metadata

    def validate_metadata(self) -> bool:
        """Валидация метаданных слоя.

        Returns:
            True если метаданные корректны
        """
        required_fields = [
            'X-Env-Layer-Name',
            'X-Env-Layer-Category',
            'X-Env-Layer-Description'
        ]

        for field in required_fields:
            if field not in self.metadata:
                raise ValidationError(f"Отсутствует обязательное поле: {field}")

        return True
```

<a id="docs---документация-проекта"></a>

### docs/ - Документация проекта: ⚠️ *ОПЦИОНАЛЬНЫЕ*

#### Назначение:
Техническая документация проекта в формате AsciiDoc.

#### Структура:
```
docs/
├── index.adoc             # Главная страница документации
├── execution/             # Документация процесса выполнения
│   └── index.adoc
├── config/                # Документация системы конфигурации
│   └── index.adoc
├── layer/                 # Документация системы слоев
│   └── index.adoc
└── provisioning/          # Документация развертывания
    └── index.adoc
```

#### Стандарты документирования:

**Формат AsciiDoc:**
```asciidoc
= Название раздела
:author: rpi-image-gen documentation
:toc: left
:toclevels: 3
:sectlinks:
:sectanchors:

== Подраздел

=== Подподраздел

Описание функциональности с примерами кода.

[IMPORTANT]
====
Важная информация, требующая внимания.
====

[source,bash]
----
#!/bin/bash
# Пример кода
echo "Hello, World!"
----

|===
| Колонка 1 | Колонка 2 | Колонка 3

| Данные 1
| Данные 2
| Данные 3
|===
```

<a id="examples---примеры-использования"></a>

### examples/ - Примеры использования: ⚠️ *ОПЦИОНАЛЬНЫЕ*

#### Назначение:
Готовые примеры расширений и конфигураций.

#### Структура:
```
examples/
├── {example_name}/        # Название примера
│   ├── README.md         # Документация примера
│   ├── config/           # Конфигурационные файлы
│   ├── layer/            # Слои примера
│   └── hooks/            # Хуки примера
```

#### Стандарты для примеров:

**README.md шаблон:**
```markdown
# Название примера

Краткое описание того, что демонстрирует этот пример.

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано из раздела документации: `43_расширения_образов_дисков_image_extensions.mdc`
