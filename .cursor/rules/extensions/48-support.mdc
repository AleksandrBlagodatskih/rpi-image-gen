---
alwaysApply: ${cursor.file.path.includes("support/")}
---


# Правило 48: Поддержка

## Назначение правила
- [Документация](../docs/) - [Issues](../../issues)

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Поддержка

- [Документация](../docs/)
- [Issues](../../issues)
```

<a id="test---тесты-проекта"></a>

### test/ - Тесты проекта: ⚠️ *ОПЦИОНАЛЬНЫЕ*

#### Назначение:
Автоматизированные тесты для проекта.

#### Структура:
```
test/
├── unit/                  # Модульные тесты
│   ├── test_cli.py       # Тесты CLI интерфейса
│   ├── test_layers.py    # Тесты системы слоев
│   └── test_validation.py # Тесты валидации
├── integration/          # Интеграционные тесты
│   ├── test_full_build.py # Тест полной сборки
│   └── test_examples.py  # Тест примеров
└── fixtures/             # Тестовые данные
    ├── configs/          # Тестовые конфигурации
    └── layers/           # Тестовые слои
```

#### Стандарты тестирования:

**Шаблон модульного теста:**
```python
#!/usr/bin/env python3
"""
Модульные тесты для компонента CLI.
"""

import unittest
import tempfile
import shutil
from pathlib import Path
from unittest.mock import patch, MagicMock

class TestCliComponent(unittest.TestCase):
    """Тесты компонента CLI."""

    def setUp(self):
        """Подготовка тестового окружения."""
        self.test_dir = tempfile.mkdtemp()
        self.test_config = Path(self.test_dir) / 'test-config.yaml'

    def tearDown(self):
        """Очистка после тестов."""
        shutil.rmtree(self.test_dir)

    def test_config_parsing(self):
        """Тест парсинга конфигурации."""
        # Создание тестового конфига
        self.test_config.write_text("""
        device:
          layer: test-device
        image:
          layer: test-image
        """)

        # Тестирование парсинга
        config_data = parse_config_file(self.test_config)

        self.assertEqual(config_data['device']['layer'], 'test-device')
        self.assertEqual(config_data['image']['layer'], 'test-image')

    def test_invalid_config(self):
        """Тест обработки некорректной конфигурации."""
        self.test_config.write_text("invalid: yaml: content: [")

        with self.assertRaises(ConfigError):
            parse_config_file(self.test_config)

    @patch('subprocess.run')
    def test_external_command_execution(self, mock_run):
        """Тест выполнения внешних команд."""
        mock_run.return_value = MagicMock(returncode=0)

        result = execute_external_command(['echo', 'test'])

        mock_run.assert_called_once_with(['echo', 'test'], check=True)
        self.assertTrue(result)
```

**Интеграционные тесты:**
```python
#!/usr/bin/env python3
"""
Интеграционные тесты полной сборки.
"""

import subprocess
import tempfile
import shutil
import unittest
from pathlib import Path

class TestFullBuildIntegration(unittest.TestCase):
    """Интеграционные тесты полной сборки."""

    def setUp(self):
        """Подготовка интеграционного теста."""
        self.test_project = Path(tempfile.mkdtemp()) / 'test-project'
        self.test_project.mkdir()

        # Создание минимальной структуры проекта
        (self.test_project / 'config').mkdir()
        (self.test_project / 'layer').mkdir()

    def tearDown(self):
        """Очистка после теста."""
        shutil.rmtree(self.test_project.parent)

    def test_minimal_build(self):
        """Тест минимальной сборки образа."""
        # Создание минимальной конфигурации
        config_content = """
        device:
          layer: pi5
        image:
          layer: image-rpios
        layer:
          base: bookworm-minbase
        """

        config_file = self.test_project / 'config' / 'test.yaml'
        config_file.write_text(config_content)

        # Выполнение сборки
        result = subprocess.run([
            'rpi-image-gen', 'build',
            '-S', str(self.test_project),
            '-c', str(config_file),
            '--dry-run'
        ], capture_output=True, text=True)

        # Проверка успешности
        self.assertEqual(result.returncode, 0)
        self.assertIn('PARAM', result.stdout)
        self.assertIn('LAYER', result.stdout)

    def test_build_with_custom_layer(self):
        """Тест сборки с кастомным слоем."""
        # Создание кастомного слоя
        layer_content = """
        # METABEGIN
        # X-Env-Layer-Name: test-layer
        # X-Env-Layer-Category: extension
        # X-Env-Layer-Description: Тестовый слой
        # X-Env-Layer-Version: 1.0.0
        # METAEND
        ---
        mmdebstrap:
          packages:
            - curl
          customize-hooks:
            - echo "Тестовый хук выполнен"
        """

        layer_file = self.test_project / 'layer' / 'test-layer.yaml'
        layer_file.write_text(layer_content)

        # Тестирование сборки с кастомным слоем
        # Аналогично предыдущему тесту
```

<a id="templates---шаблоны-генерации"></a>

### templates/ - Шаблоны генерации: ⚠️ *ОПЦИОНАЛЬНЫЕ*

#### Назначение:
Шаблоны для генерации документации и конфигураций.

#### Структура:
```
templates/
├── layer-docs/           # Шаблоны документации слоев
│   └── layer.adoc.jinja  # Jinja шаблон для слоя
├── config-docs/          # Шаблоны документации конфигураций
└── example-gen/          # Шаблоны генерации примеров
```

#### Стандарты шаблонов (Jinja2):

```jinja
{# templates/layer-docs/layer.adoc.jinja #}
= {{ layer_name }}
{% if layer_category %}:badge[{{ layer_category }}]{% endif %}
{% if layer_version %}:badge[{{ layer_version }}]{% endif %}

{% if layer_description %}{{ layer_description }}{% endif %}

== Обзор

Этот слой предоставляет {{ layer_purpose }}.

== Требования

{% if layer_requires %}
Требуемые слои:
{% for requirement in layer_requires %}
- {{ requirement }}
{% endfor %}
{% endif %}

== Конфигурация

{% if layer_variables %}
=== Переменные конфигурации

|===
| Переменная | Описание | Значение по умолчанию | Обязательна

{% for var_name, var_info in layer_variables.items() %}
| `IGconf_{{ var_prefix }}_{{ var_name }}` | {{ var_info.description }} | {{ var_info.default | default('Нет') }} | {{ 'Да' if var_info.required else 'Нет' }}
{% endfor %}
|===
{% endif %}

== Примеры использования

[source,yaml]
----
device:
  layer: {{ layer_name }}

layer:
  {{ var_prefix }}: {{ layer_name }}
----
```

<a id="work---рабочая-директория-сборки"></a>

### work/ - Рабочая директория сборки: ⚠️ *ОПЦИОНАЛЬНЫЕ*

#### Назначение:
Временные файлы и результаты сборки.

#### Структура (генерируется автоматически):
```
work/
├── image-{config_name}/      # Директория сборки конкретной конфигурации
│   ├── filesystem/          # Смонтированная файловая система
│   ├── image-{name}.img     # Готовый образ диска
│   ├── genimage.cfg         # Сгенерированная конфигурация genimage
│   └── sbom.json            # Отчет SBOM
└── chroot-{layer_name}/      # Временные chroot окружения
```

#### Правила работы:
- **Автоматическая очистка** при завершении сборки
- **Не включать в git** (добавлено в .gitignore)
- **Безопасное удаление** только через инструменты проекта

Эта система каталогов обеспечивает **четкую организацию**, **масштабируемость** и **сопровождаемость** проекта rpi-image-gen, с ясными правилами для каждого типа компонентов.

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано из раздела документации: `49_поддержка.mdc`
