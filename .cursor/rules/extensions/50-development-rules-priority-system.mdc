---
alwaysApply: ${cursor.file.path.includes(".cursor/rules/") || cursor.file.path.includes("docs/")}
---


# Правило 50: Система приоритетов правил разработки

## Назначение правила
Правила разработки расширений для rpi-image-gen согласованы по приоритетам из следующих источников:

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Система приоритетов правил разработки

### Принципы согласования источников знаний

Правила разработки расширений для rpi-image-gen согласованы по приоритетам из следующих источников:

**[Приоритет 1 - Высший] README.adoc проекта** 📖
- Официальная документация rpi-image-gen
- Архитектурные принципы и дизайн-решений проекта
- Обязательные требования к структуре и поведению

**[Приоритет 2 - Средний] Debian Manual** 📚
- Стандарты системного администрирования Debian
- Лучшие практики пакетного управления
- Безопасность и надежность систем

**[Приоритет 3 - Низший] Lynx CLI Tool** 🔍
- Поиск дополнительной технической информации
- Справочные материалы и документация
- Внешние источники знаний

### Применение приоритетов в практике разработки

#### 1. Архитектурные решения (Приоритет 1 - README.adoc)

```bash
# Официальные требования проекта из README.adoc
# ✅ ОБЯЗАТЕЛЬНО: Модульная архитектура на основе слоев
# ✅ ОБЯЗАТЕЛЬНО: Использование YAML для конфигурации
# ✅ ОБЯЗАТЕЛЬНО: Интеграция с bdebstrap/mmdebstrap/genimage
# ✅ ОБЯЗАТЕЛЬНО: Выполнение без root прав через podman unshare

# Структура слоя ДОЛЖНА соответствовать официальным требованиям
# METABEGIN
# X-Env-Layer-Name: example-layer    # ✅ Из README.adoc
# X-Env-Layer-Category: extension     # ✅ Из README.adoc
# X-Env-Layer-Description: Example     # ✅ Из README.adoc
# METAEND
```

#### 2. Системные стандарты (Приоритет 2 - Debian Manual)

```bash
# Стандарты Debian для системного администрирования
# ✅ РЕКОМЕНДУЕТСЯ: Стандартные пути файловой системы FHS
# ✅ РЕКОМЕНДУЕТСЯ: Правильное использование systemd
# ✅ РЕКОМЕНДУЕТСЯ: Безопасная обработка пакетов

# Структура директорий соответствует Debian FHS
extension/
├── etc/                    # ✅ /etc - конфигурационные файлы
├── usr/bin/               # ✅ /usr/bin - исполняемые файлы
├── usr/share/             # ✅ /usr/share - общие данные
├── var/lib/               # ✅ /var/lib - переменные данные
└── var/log/               # ✅ /var/log - логи системы
```

#### 3. Дополнительная информация (Приоритет 3 - Lynx CLI)

```bash
# Использование lynx для получения справочной информации
# Поиск лучших практик и решений проблем

# Пример использования lynx для поиска информации:
# lynx -dump "https://wiki.debian.org/BestPractices" | grep -i "security"
# lynx -dump "https://manpages.debian.org/stable/debconf/debconf.7" | grep -A 10 -B 10 "priority"

# Получение информации о пакетах:
# lynx -dump "https://packages.debian.org/stable/$(package_name)" | grep -E "(Version|Description)"

# Поиск в документации:
# lynx -dump "https://www.debian.org/doc/" | grep -i "security\|best.practices"
```

### Матрица приоритетов для различных аспектов разработки

| Аспект разработки | Приоритет 1 (README.adoc) | Приоритет 2 (Debian) | Приоритет 3 (Lynx) |
|-------------------|---------------------------|---------------------|-------------------|
| **Архитектура** | Модульная архитектура слоев ✅ | Стандарты FHS ✅ | Доп. архитектуры ⚠️ |
| **Конфигурация** | YAML формат ✅ | Debian конфиги ✅ | Справочники ⚠️ |
| **Безопасность** | podman unshare ✅ | AppArmor/SELinux ✅ | Доп. политики ⚠️ |
| **Документация** | AsciiDoc формат ✅ | man страницы ✅ | Справочники ⚠️ |
| **Тестирование** | Интеграционные тесты ✅ | Debian тесты ✅ | Доп. фреймворки ⚠️ |

### Практическое применение приоритетов

#### Пример разрешения конфликтов:

**Конфликт:** Структура каталогов в расширении
```bash
# Сценарий: Конфликт между желаемой структурой и стандартами

# ❌ НЕПРАВИЛЬНО: Игнорирование официальных требований
extension/
├── my-custom-bin/         # Конфликт с FHS
└── proprietary-config/    # Конфликт с Debian стандартами

# ✅ ПРАВИЛЬНО: Соответствие всем приоритетам
extension/
├── usr/bin/               # ✅ Приоритет 2 (FHS)
├── etc/extension/         # ✅ Приоритет 2 (FHS) + Приоритет 1 (структура)
├── var/lib/extension/     # ✅ Приоритет 2 (FHS)
└── var/log/extension/     # ✅ Приоритет 2 (FHS)
```

#### Пример использования lynx для поиска решений:

```bash
#!/bin/bash
# Скрипт поиска лучших практик с использованием lynx

search_best_practices() {
    local topic="$1"

    echo "Поиск лучших практик для: $topic"

    # Поиск в Debian документации
    lynx -dump "https://wiki.debian.org/$topic" 2>/dev/null | \
        grep -i "best.practice\|recommendation" | head -5

    # Поиск в man страницах
    lynx -dump "https://manpages.debian.org/stable/debconf/debconf.7" | \
        grep -A 3 -B 3 -i "$topic" | head -10

    # Поиск в официальной документации
    lynx -dump "https://www.debian.org/doc/" | \
        grep -l "$topic" | head -3
}

# Использование в разработке
search_best_practices "package-management"
search_best_practices "security-hardening"
search_best_practices "filesystem-hierarchy"
```

### Рекомендации по использованию источников

#### 1. README.adoc (Приоритет 1) - Основа разработки
```bash
# ✅ ВСЕГДА начинать с официальной документации
# Изучение архитектуры, принципов, обязательных требований
# Понимание интеграции с bdebstrap/mmdebstrap/genimage

# Команды для изучения проекта:
git clone https://github.com/raspberrypi/rpi-image-gen.git
cd rpi-image-gen
cat README.adoc | grep -E "(Architecture|Core Concepts|Dependencies)"
```

#### 2. Debian Manual (Приоритет 2) - Системные стандарты
```bash
# ✅ Использовать для понимания Debian-специфичных требований
# Стандарты файловой системы, безопасности, пакетного управления

# Рекомендуемые источники:
man hier                    # Стандарты FHS
man debconf                 # Система конфигурации Debian
man systemd.unit           # Стандарты сервисов
man apt_preferences        # Управление пакетами
```

#### 3. Lynx CLI Tool (Приоритет 3) - Дополнительная информация
```bash
# ✅ Использовать для поиска конкретных решений и справочной информации

# Примеры поиска:
lynx -dump "https://wiki.debian.org/PackageManagement" | grep -A 5 -B 5 "best.practices"
lynx -dump "https://manpages.debian.org/stable/bash/bash.1" | grep -A 10 "set -e"
lynx -dump "https://www.debian.org/security/" | grep -i "advisory\|update"
```

### Согласование с официальными стандартами

#### Соответствие Debian Policy Manual:
- **Раздел 4.2**: Стандарты именования пакетов
- **Раздел 9.1**: Стандарты каталогов (FHS)
- **Раздел 10.4**: Скрипты maintainer scripts
- **Раздел 11.5**: Разрешения и владение файлами

#### Соответствие rpi-image-gen принципам:
- **Модульность**: Каждый слой - независимый компонент
- **Декларативность**: Конфигурация через YAML
- **Безопасность**: Изоляция через podman unshare
- **Автоматизация**: CI/CD интеграция

### Практические рекомендации

#### 1. Разработка с учетом приоритетов:
```bash
#!/bin/bash
# Шаблон разработки с учетом приоритетов источников

develop_with_priorities() {
    local feature="$1"

    echo "=== Разработка функции: $feature ==="

    # 1. Проверка официальных требований (Приоритет 1)
    check_official_requirements "$feature"

    # 2. Проверка Debian стандартов (Приоритет 2)
    validate_debian_standards "$feature"

    # 3. Поиск дополнительной информации (Приоритет 3)
    search_additional_info "$feature"

    # 4. Реализация с учетом всех приоритетов
    implement_feature "$feature"
}

check_official_requirements() {
    local feature="$1"
    # Проверка соответствия README.adoc и архитектуре проекта
    # Валидация метаданных X-Env
    # Проверка интеграции с существующими слоями
}

validate_debian_standards() {
    local feature="$1"
    # Проверка соответствия FHS
    # Валидация скриптов согласно Debian Policy
    # Проверка безопасности и разрешений
}

search_additional_info() {
    local feature="$1"
    # Использование lynx для поиска лучших практик
    # Изучение документации по теме
    # Поиск примеров реализации
}
```

#### 2. Валидация расширений по приоритетам:
```bash
#!/bin/bash
# Валидация расширения по системе приоритетов

validate_extension_priorities() {
    local extension_path="$1"

    echo "=== Валидация по приоритетам ==="

    # Приоритет 1: Официальные требования проекта
    validate_project_requirements "$extension_path"

    # Приоритет 2: Debian стандарты
    validate_debian_standards "$extension_path"

    # Приоритет 3: Дополнительная информация
    validate_additional_info "$extension_path"

    echo "Валидация завершена"
}

validate_project_requirements() {
    local path="$1"
    # Проверка структуры метаданных X-Env
    # Валидация зависимостей между слоями
    # Проверка синтаксиса YAML
    # Соответствие архитектурным принципам
}

validate_debian_standards() {
    local path="$1"
    # Проверка соответствия FHS
    # Валидация скриптов согласно Policy Manual
    # Проверка разрешений файлов
    # Соответствие стандартам безопасности
}
```

Эта система приоритетов обеспечивает **согласованность**, **надежность** и **соответствие стандартам** при разработке расширений для rpi-image-gen.

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано из раздела документации: `51_система_приоритетов_правил_разработки.mdc`
