---
alwaysApply: ${cursor.file.path.includes("service") || cursor.file.content.includes("systemctl")}
---


# Правило 52: Управление сервисами в расширениях

## Назначение правила
Определяет стандарты и best practices для включения, настройки и управления системными сервисами в customize-hooks расширений rpi-image-gen.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Управление сервисами в customize-hooks

### Обзор управления сервисами

В rpi-image-gen сервисы включаются в этапе `customize-hooks`, который выполняется после установки пакетов и перед финализацией образа. Это гарантирует, что:

- Все необходимые пакеты установлены
- Конфигурационные файлы созданы
- Сервисы правильно зарегистрированы в systemd

### Правильные паттерны включения сервисов

#### 1. Базовое включение сервиса
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # Включение одиночного сервиса
      systemctl enable my-service
```

#### 2. Включение с немедленным запуском
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # Включение и запуск сервиса
      systemctl enable my-service
      systemctl start my-service
```

#### 3. Включение нескольких сервисов
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # Включение нескольких сервисов одной командой
      systemctl enable service1 service2 service3
```

#### 4. Условное включение через переменные
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # Включение сервисов на основе переменных конфигурации
      systemctl enable ${IGconf_extension_services:-}

      # Или условное включение
      if [ "${IGconf_extension_enable_web:-false}" = "true" ]; then
        systemctl enable nginx php-fpm
      fi
```

### Важные правила для systemd

#### 1. Всегда выполнять daemon-reload перед включением кастомных сервисов
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # Создание unit файла сервиса
      cat > /etc/systemd/system/my-service.service << 'EOF'
      [Unit]
      Description=My Custom Service
      After=network.target

      [Service]
      ExecStart=/usr/local/bin/my-service
      Restart=always
      User=myuser

      [Install]
      WantedBy=multi-user.target
      EOF

      # ОБЯЗАТЕЛЬНО: перезагрузка конфигурации systemd
      systemctl daemon-reload

      # Теперь можно включать сервис
      systemctl enable my-service
```

#### 2. Правильный порядок операций
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # 1. Установка пакетов (уже выполнено в packages/setup-hooks)

      # 2. Создание конфигурационных файлов
      mkdir -p /etc/my-service
      cat > /etc/my-service/config.yaml << EOF
      setting: value
      EOF

      # 3. Создание пользователя (если нужно)
      useradd --system --shell /bin/false my-service-user

      # 4. Настройка разрешений
      chown -R my-service-user:my-service-user /etc/my-service
      chown -R my-service-user:my-service-user /var/lib/my-service

      # 5. Перезагрузка systemd (если созданы новые unit файлы)
      systemctl daemon-reload

      # 6. Включение сервиса
      systemctl enable my-service

      # 7. Запуск сервиса (опционально, для тестирования)
      systemctl start my-service
```

### Обработка ошибок при включении сервисов

#### Безопасное включение с проверкой
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # Функция безопасного включения сервиса
      enable_service_safely() {
        local service="$1"

        # Проверка существования unit файла
        if [ ! -f "/etc/systemd/system/${service}.service" ]; then
          echo "Warning: Service unit ${service}.service not found"
          return 1
        fi

        # Попытка включения с обработкой ошибок
        if systemctl enable "$service" 2>/dev/null; then
          echo "Service $service enabled successfully"
          return 0
        else
          echo "Failed to enable service $service"
          return 1
        fi
      }

      # Использование функции
      enable_service_safely "my-service" || true  # Не прерывать сборку
```

#### Восстановление после ошибок
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # Попытка включения с fallback
      if ! systemctl enable my-service; then
        echo "Primary service failed, trying alternative configuration"

        # Альтернативная конфигурация
        systemctl enable alternative-service
      fi
```

### Типы сервисов и их особенности

#### 1. System сервисы (работают как система)
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # Системные сервисы (запуск от root, системные ресурсы)
      systemctl enable systemd-networkd systemd-resolved

      # Маскирование конфликтующих сервисов
      systemctl mask networking  # Если используется systemd-networkd
```

#### 2. User сервисы (работают от пользователя)
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # User сервисы (для каждого пользователя)
      # Обычно включаются в пользовательском контексте
      # или через глобальные конфигурации
      mkdir -p /etc/systemd/user
      cp my-user-service.service /etc/systemd/user/
```

#### 3. Timer сервисы (запускаются по расписанию)
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # Timer сервисы для периодических задач
      cat > /etc/systemd/system/my-backup.timer << 'EOF'
      [Unit]
      Description=Run backup every hour
      Requires=my-backup.service

      [Timer]
      OnCalendar=hourly
      Persistent=true

      [Install]
      WantedBy=timers.target
      EOF

      systemctl daemon-reload
      systemctl enable my-backup.timer
```

### Взаимодействие сервисов и зависимости

#### Управление зависимостями между сервисами
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # Сервис A зависит от сервиса B
      cat > /etc/systemd/system/service-a.service << 'EOF'
      [Unit]
      Description=Service A
      After=service-b.service  # Запуск после B
      Requires=service-b.service  # Обязательная зависимость

      [Service]
      ExecStart=/usr/bin/service-a

      [Install]
      WantedBy=multi-user.target
      EOF

      # Включение в правильном порядке
      systemctl daemon-reload
      systemctl enable service-b service-a
```

#### Конфликты сервисов
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # Разрешение конфликтов (например, разные NTP клиенты)
      systemctl disable systemd-timesyncd  # Отключить встроенный
      systemctl enable chrony              # Включить альтернативный

      # Или маскировка
      systemctl mask conflicting-service
```

### Мониторинг и отладка сервисов

#### Проверка статуса после включения
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # Включение с проверкой статуса
      systemctl enable my-service

      # Проверка возможности запуска
      if systemctl is-enabled my-service >/dev/null 2>&1; then
        echo "Service my-service is enabled"

        # Опционально: попытка запуска для проверки
        if timeout 10 systemctl start my-service; then
          echo "Service started successfully"
          systemctl stop my-service  # Остановить после проверки
        else
          echo "Warning: Service failed to start"
        fi
      else
        echo "Error: Failed to enable service my-service"
      fi
```

#### Логирование проблем
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # Ведение лога операций с сервисами
      SERVICE_LOG="/var/log/service-setup.log"

      enable_with_logging() {
        local service="$1"
        echo "$(date): Enabling service $service" >> "$SERVICE_LOG"

        if systemctl enable "$service" 2>> "$SERVICE_LOG"; then
          echo "$(date): Service $service enabled successfully" >> "$SERVICE_LOG"
        else
          echo "$(date): Failed to enable service $service" >> "$SERVICE_LOG"
        fi
      }

      # Использование
      enable_with_logging "my-service"
```

### Best practices для production

#### 1. Масштабируемость
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # Использование переменных для масштабируемости
      for service in ${IGconf_extension_services:-sshd}; do
        systemctl enable "$service"
      done
```

#### 2. Безопасность
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # Безопасное включение только доверенных сервисов
      ALLOWED_SERVICES="sshd nginx postgresql"

      enable_secure_service() {
        local service="$1"

        if echo "$ALLOWED_SERVICES" | grep -qw "$service"; then
          systemctl enable "$service"
          echo "Enabled trusted service: $service"
        else
          echo "Warning: Refusing to enable untrusted service: $service"
        fi
      }
```

#### 3. Производительность
```yaml
mmdebstrap:
  customize-hooks:
    - |
      # Отложенный запуск тяжелых сервисов
      cat > /etc/systemd/system/heavy-service.service.d/override.conf << 'EOF'
      [Unit]
      # Задержка запуска для улучшения boot time
      After=network-online.target
      Wants=network-online.target

      [Service]
      # Ограничения ресурсов
      MemoryLimit=256M
      CPUQuota=50%
      EOF

      systemctl daemon-reload
      systemctl enable heavy-service
```

### Распространенные ошибки и их исправление

#### Ошибка: "Failed to enable unit: Unit file not found"
```yaml
# Решение: Проверить создание unit файла перед включением
mmdebstrap:
  customize-hooks:
    - |
      # Неправильно:
      systemctl enable my-service  # Ошибка!

      # Правильно:
      cat > /etc/systemd/system/my-service.service << 'EOF'
      [Unit]
      Description=My Service

      [Service]
      ExecStart=/usr/bin/my-service

      [Install]
      WantedBy=multi-user.target
      EOF

      systemctl daemon-reload
      systemctl enable my-service
```

#### Ошибка: "Failed to start service: permission denied"
```yaml
# Решение: Проверить разрешения и пользователя
mmdebstrap:
  customize-hooks:
    - |
      # Создание пользователя для сервиса
      useradd --system --shell /bin/false --home /var/lib/my-service my-service

      # Настройка разрешений
      chown -R my-service:my-service /var/lib/my-service
      chmod 755 /usr/bin/my-service

      # Правильный unit файл
      cat > /etc/systemd/system/my-service.service << 'EOF'
      [Service]
      User=my-service
      Group=my-service
      ExecStart=/usr/bin/my-service
      EOF

      systemctl daemon-reload
      systemctl enable my-service
```

#### Ошибка: Сервисы конфликтуют
```yaml
# Решение: Явное разрешение конфликтов
mmdebstrap:
  customize-hooks:
    - |
      # Отключить конфликтующий сервис
      systemctl disable conflicting-service
      systemctl mask conflicting-service  # Или замаскировать

      # Включить нужный сервис
      systemctl enable preferred-service
```

### Тестирование конфигураций сервисов

#### Автоматическое тестирование
```bash
#!/bin/bash
# test-services.sh

# Список ожидаемых сервисов
EXPECTED_SERVICES="sshd nginx postgresql"

echo "Testing service configuration..."

for service in $EXPECTED_SERVICES; do
  if systemctl is-enabled "$service" >/dev/null 2>&1; then
    echo "✓ Service $service is enabled"

    # Проверка возможности запуска
    if timeout 5 systemctl start "$service" 2>/dev/null; then
      echo "✓ Service $service can start"
      systemctl stop "$service" 2>/dev/null || true
    else
      echo "✗ Service $service failed to start"
    fi
  else
    echo "✗ Service $service is not enabled"
  fi
done
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано на основе анализа существующих правил и best practices управления сервисами в rpi-image-gen