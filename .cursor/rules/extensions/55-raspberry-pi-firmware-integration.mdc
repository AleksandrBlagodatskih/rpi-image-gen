---
alwaysApply: ${cursor.file.path.includes("firmware")}
---


# Правило 55: Интеграция Raspberry Pi Firmware

## Назначение правила
Определяет стандарты интеграции firmware Raspberry Pi в расширения rpi-image-gen для обеспечения актуального и совместимого firmware для всех поддерживаемых устройств.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Интеграция Raspberry Pi Firmware

### Управление firmware в процессе сборки

#### Автоматическая установка актуального firmware:
```bash
#!/bin/bash
# install-rpi-firmware.sh

set -euo pipefail

# Конфигурация
FIRMWARE_REPO="https://github.com/raspberrypi/firmware.git"
FIRMWARE_DIR="/opt/rpi-firmware"
BOOT_DIR="/boot/firmware"
BACKUP_DIR="/opt/rpi-firmware-backup"

# Определение архитектуры и модели
detect_system_info() {
    ARCH=$(uname -m)
    case "$ARCH" in
        aarch64)
            FIRMWARE_ARCH="arm64"
            ;;
        armv7l)
            FIRMWARE_ARCH="arm"
            ;;
        *)
            echo "Неподдерживаемая архитектура: $ARCH"
            exit 1
            ;;
    esac

    # Определение модели Raspberry Pi
    if [ -f /proc/device-tree/model ]; then
        PI_MODEL=$(tr -d '\0' < /proc/device-tree/model | sed 's/Raspberry Pi //;s/ Model //;s/Rev.*//')
    else
        PI_MODEL="unknown"
    fi

    echo "Архитектура: $ARCH ($FIRMWARE_ARCH)"
    echo "Модель: $PI_MODEL"
}

# Синхронизация репозитория firmware
sync_firmware_repo() {
    echo "Синхронизация репозитория firmware..."

    if [ ! -d "$FIRMWARE_DIR" ]; then
        git clone --depth 1 --branch stable "$FIRMWARE_REPO" "$FIRMWARE_DIR"
    else
        cd "$FIRMWARE_DIR"
        git fetch origin
        git checkout stable
        git pull --ff-only
    fi
}

# Создание резервной копии текущего firmware
backup_current_firmware() {
    echo "Создание резервной копии текущего firmware..."

    mkdir -p "$BACKUP_DIR"
    local backup_name="firmware-backup-$(date +%Y%m%d-%H%M%S)"

    if [ -d "$BOOT_DIR" ]; then
        cp -r "$BOOT_DIR" "$BACKUP_DIR/$backup_name"
        echo "Резервная копия создана: $BACKUP_DIR/$backup_name"
    fi
}

# Установка firmware
install_firmware() {
    local firmware_source="$FIRMWARE_DIR/boot"

    echo "Установка firmware для архитектуры $FIRMWARE_ARCH..."

    # Создание загрузочной директории
    mkdir -p "$BOOT_DIR"

    # Копирование основных файлов firmware
    cp -r "$firmware_source"/* "$BOOT_DIR/"

    # Установка специфичных для архитектуры файлов
    case "$FIRMWARE_ARCH" in
        "arm64")
            # Raspberry Pi 3, 4, 5 (64-bit)
            cp "$firmware_source/kernel8.img" "$BOOT_DIR/" 2>/dev/null || true
            ;;
        "arm")
            # Raspberry Pi 1, 2, Zero (32-bit)
            cp "$firmware_source/kernel.img" "$BOOT_DIR/" 2>/dev/null || true
            cp "$firmware_source/kernel7.img" "$BOOT_DIR/" 2>/dev/null || true
            ;;
    esac

    # Установка актуального ядра
    install_kernel_firmware

    echo "Firmware установлен успешно"
}

# Установка ядра и модулей
install_kernel_firmware() {
    local kernel_modules="$FIRMWARE_DIR/modules"

    if [ -d "$kernel_modules" ]; then
        echo "Установка модулей ядра..."

        # Определение версии ядра
        local kernel_version
        if [ -f "$BOOT_DIR/kernel.img" ]; then
            kernel_version=$(strings "$BOOT_DIR/kernel.img" | grep "Linux version" | head -1 | awk '{print $3}' | cut -d- -f1)
        else
            kernel_version=$(uname -r | cut -d- -f1)
        fi

        # Копирование модулей
        if [ -d "$kernel_modules/$kernel_version+" ]; then
            cp -r "$kernel_modules/$kernel_version+" /lib/modules/
        elif [ -d "$kernel_modules/$kernel_version" ]; then
            cp -r "$kernel_modules/$kernel_version" /lib/modules/
        fi

        # Обновление зависимостей модулей
        depmod -a
    fi
}

# Настройка config.txt для firmware
configure_firmware_settings() {
    local config_file="$BOOT_DIR/config.txt"

    echo "Настройка config.txt..."

    # Базовая конфигурация для всех моделей
    cat >> "$config_file" << EOF

# Конфигурация Raspberry Pi Firmware
# Автоматически сгенерировано rpi-image-gen

# Включение дополнительных overlays
dtoverlay=
dtparam=

# Настройки производительности
gpu_mem=128
hdmi_force_hotplug=1

# Безопасность
disable_splash=1
avoid_warnings=1
EOF

    # Модель-специфичные настройки
    case "$PI_MODEL" in
        "5"|"4")
            cat >> "$config_file" << EOF
# Настройки для Raspberry Pi 4/5
arm_boost=1
over_voltage=6
EOF
            ;;
        "3"*)
            cat >> "$config_file" << EOF
# Настройки для Raspberry Pi 3
over_voltage=2
EOF
            ;;
    esac
}

# Проверка целостности firmware
verify_firmware_integrity() {
    echo "Проверка целостности firmware..."

    local errors=0

    # Проверка наличия критических файлов
    local critical_files=("bootcode.bin" "start.elf" "fixup.dat")

    for file in "${critical_files[@]}"; do
        if [ ! -f "$BOOT_DIR/$file" ]; then
            echo "ОШИБКА: Отсутствует критический файл: $file"
            ((errors++))
        fi
    done

    # Проверка наличия ядра
    if ! ls "$BOOT_DIR"/kernel*.img >/dev/null 2>&1; then
        echo "ОШИБКА: Отсутствует файл ядра"
        ((errors++))
    fi

    if [ $errors -eq 0 ]; then
        echo "Проверка firmware завершена успешно"
        return 0
    else
        echo "Найдено $errors ошибок в firmware"
        return 1
    fi
}

# Основная функция
main() {
    echo "Установка Raspberry Pi Firmware"

    detect_system_info
    sync_firmware_repo
    backup_current_firmware
    install_firmware
    configure_firmware_settings

    if verify_firmware_integrity; then
        echo "Firmware успешно установлен и настроен"
        return 0
    else
        echo "Обнаружены проблемы с firmware"
        return 1
    fi
}

main "$@"
```

#### Интеграция firmware в слой rpi-image-gen:
```yaml
# METABEGIN
# X-Env-Layer-Name: raspberrypi-firmware
# X-Env-Layer-Category: extension
# X-Env-Layer-Description: Интеграция Raspberry Pi firmware в образ
# X-Env-Layer-Version: 1.0.0
# X-Env-Layer-Requires: essential,bootloader
# X-Env-VarPrefix: firmware
#
# X-Env-Var-firmware_branch: stable
# X-Env-Var-firmware_branch-Description: Ветка firmware для установки
# X-Env-Var-firmware_branch-Required: false
# X-Env-Var-firmware_branch-Valid: stable,master,next
#
# X-Env-Var-firmware_verify: true
# X-Env-Var-firmware_verify-Description: Проверять целостность firmware после установки
# X-Env-Var-firmware_verify-Required: false
# X-Env-Var-firmware_verify-Valid: true,false
# METAEND
---
mmdebstrap:
  packages:
    - git
    - raspberrypi-bootloader
    - raspi-firmware
    - curl
    - wget
  customize-hooks:
    - |
      # Установка и настройка Raspberry Pi firmware
      setup_raspberrypi_firmware() {
          echo "Настройка Raspberry Pi firmware"

          # Клонирование репозитория firmware
          if [ ! -d /opt/rpi-firmware ]; then
              git clone --depth 1 --branch "${IGconf_firmware_firmware_branch:-stable}" \
                  https://github.com/raspberrypi/firmware.git /opt/rpi-firmware
          fi

          cd /opt/rpi-firmware

          # Установка boot файлов
          mkdir -p /boot/firmware
          cp -r boot/* /boot/firmware/

          # Установка модулей ядра (если есть)
          if [ -d modules ]; then
              cp -r modules/* /lib/modules/ 2>/dev/null || true
              depmod -a
          fi

          # Настройка config.txt
          cat >> /boot/firmware/config.txt << EOF

# Raspberry Pi Firmware Integration
# Добавлено rpi-image-gen расширением
dtparam=audio=on
camera_auto_detect=1
display_auto_detect=1
EOF

          # Создание скрипта обновления firmware
          cat > /usr/local/bin/update-rpi-firmware.sh << 'FIRMWARE_EOF'
#!/bin/bash
# Скрипт обновления Raspberry Pi firmware

set -euo pipefail

FIRMWARE_DIR="/opt/rpi-firmware"
BOOT_DIR="/boot/firmware"
LOG_FILE="/var/log/rpi-firmware-update.log"

log() {
    echo "$(date '+%Y-%m-%d %H:%M:%S') $*" | tee -a "$LOG_FILE"
}

update_firmware() {
    log "Проверка обновлений firmware"

    cd "$FIRMWARE_DIR"

    # Получение последних изменений
    git fetch origin
    git checkout "${IGconf_firmware_firmware_branch:-stable}"
    git pull --ff-only

    # Создание резервной копии
    backup_dir="/tmp/firmware-backup-$(date +%Y%m%d-%H%M%S)"
    cp -r "$BOOT_DIR" "$backup_dir"
    log "Резервная копия создана: $backup_dir"

    # Обновление файлов
    cp -r boot/* "$BOOT_DIR/"

    # Проверка целостности
    if [ "${IGconf_firmware_firmware_verify:-true}" = "true" ]; then
        if verify_firmware_files; then
            log "Firmware успешно обновлен"
            return 0
        else
            log "Ошибка верификации firmware, восстановление из резервной копии"
            cp -r "$backup_dir"/* "$BOOT_DIR/"
            return 1
        fi
    fi

    log "Firmware обновлен без верификации"
    return 0
}

verify_firmware_files() {
    # Проверка наличия критических файлов
    local critical_files=("bootcode.bin" "start.elf")
    local missing_files=0

    for file in "${critical_files[@]}"; do
        if [ ! -f "$BOOT_DIR/$file" ]; then
            log "ОШИБКА: Отсутствует файл $file"
            ((missing_files++))
        fi
    done

    [ $missing_files -eq 0 ]
}

update_firmware
FIRMWARE_EOF

          chmod +x /usr/local/bin/update-rpi-firmware.sh

          # Настройка периодического обновления (опционально)
          if [ "${IGconf_firmware_auto_update:-false}" = "true" ]; then
              cat > /etc/cron.weekly/update-rpi-firmware << 'CRON_EOF'
#!/bin/bash
/usr/local/bin/update-rpi-firmware.sh
CRON_EOF
              chmod +x /etc/cron.weekly/update-rpi-firmware
          fi
      }

      setup_raspberrypi_firmware
```

#### Мониторинг и диагностика firmware:
```bash
#!/bin/bash
# firmware-diagnostics.sh

# Диагностика состояния Raspberry Pi firmware

check_firmware_versions() {
    echo "=== Версии firmware компонентов ==="

    # Версия bootloader
    if [ -f /boot/firmware/bootcode.bin ]; then
        echo "Bootloader: $(stat -c%s /boot/firmware/bootcode.bin) bytes"
    fi

    # Версия GPU firmware
    if [ -f /boot/firmware/start.elf ]; then
        echo "GPU firmware: $(stat -c%s /boot/firmware/start.elf) bytes"
    fi

    # Версия ядра
    if ls /boot/firmware/kernel*.img >/dev/null 2>&1; then
        for kernel in /boot/firmware/kernel*.img; do
            echo "Kernel $(basename "$kernel"): $(stat -c%s "$kernel") bytes"
        done
    fi
}

check_firmware_config() {
    echo "=== Конфигурация firmware ==="

    local config_file="/boot/firmware/config.txt"

    if [ -f "$config_file" ]; then
        echo "Основные параметры config.txt:"
        grep -E "^(gpu_mem|arm_freq|over_voltage|dtparam)" "$config_file" || echo "Специфические параметры не найдены"
    else
        echo "Файл config.txt не найден"
    fi
}

check_firmware_health() {
    echo "=== Проверка работоспособности firmware ==="

    # Проверка на поврежденные файлы
    local corrupted=0

    for file in /boot/firmware/*.elf /boot/firmware/*.bin; do
        if [ -f "$file" ]; then
            if ! file "$file" >/dev/null 2>&1; then
                echo "ПОВРЕЖДЕН: $file"
                ((corrupted++))
            fi
        fi
    done

    if [ $corrupted -eq 0 ]; then
        echo "Все файлы firmware в порядке"
    else
        echo "Найдено $corrupted поврежденных файлов"
    fi
}

main() {
    check_firmware_versions
    echo ""
    check_firmware_config
    echo ""
    check_firmware_health
}

main "$@"
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано на основе анализа репозитория: https://github.com/raspberrypi/firmware