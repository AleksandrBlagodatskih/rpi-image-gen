---
alwaysApply: ${cursor.file.path.includes("kernel")}
---


# Правило 56: Интеграция Raspberry Pi Linux Kernel

## Назначение правила
Определяет стандарты интеграции ядра Linux Raspberry Pi в расширения rpi-image-gen для обеспечения совместимости оборудования и оптимальной производительности.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Интеграция Raspberry Pi Linux Kernel

### Управление ядром Linux в процессе сборки

#### Автоматическая компиляция и установка ядра:
```bash
#!/bin/bash
# build-rpi-kernel.sh

set -euo pipefail

# Конфигурация
KERNEL_REPO="https://github.com/raspberrypi/linux.git"
KERNEL_DIR="/opt/rpi-kernel"
BUILD_DIR="/tmp/kernel-build"
INSTALL_DIR="/boot/firmware"
MODULES_DIR="/lib/modules"

# Определение архитектуры и модели
detect_system_info() {
    ARCH=$(uname -m)
    case "$ARCH" in
        aarch64)
            KERNEL_ARCH="arm64"
            DEFCONFIG="bcm2711_defconfig"
            IMAGE_NAME="Image"
            ;;
        armv7l)
            KERNEL_ARCH="arm"
            DEFCONFIG="bcm2709_defconfig"
            IMAGE_NAME="zImage"
            ;;
        *)
            echo "Неподдерживаемая архитектура: $ARCH"
            exit 1
            ;;
    esac

    # Определение модели Raspberry Pi для выбора правильной конфигурации
    if [ -f /proc/device-tree/model ]; then
        PI_MODEL=$(tr -d '\0' < /proc/device-tree/model | sed 's/Raspberry Pi //;s/ Model //;s/Rev.*//')
        case "$PI_MODEL" in
            "5")
                DEFCONFIG="bcm2712_defconfig"
                ;;
            "4")
                DEFCONFIG="bcm2711_defconfig"
                ;;
            "3"*)
                DEFCONFIG="bcm2709_defconfig"
                ;;
            "2"*)
                DEFCONFIG="bcm2709_defconfig"
                ;;
            "Zero"*|"1"*)
                DEFCONFIG="bcmrpi_defconfig"
                ;;
        esac
    fi

    echo "Архитектура: $KERNEL_ARCH"
    echo "Модель: $PI_MODEL"
    echo "Конфигурация: $DEFCONFIG"
    echo "Образ ядра: $IMAGE_NAME"
}

# Установка зависимостей для сборки ядра
install_kernel_dependencies() {
    echo "Установка зависимостей для сборки ядра..."

    apt-get update
    apt-get install -y \
        git \
        bc \
        bison \
        flex \
        libssl-dev \
        make \
        libc6-dev \
        libncurses5-dev \
        crossbuild-essential-arm64 \
        crossbuild-essential-armhf \
        gcc-aarch64-linux-gnu \
        gcc-arm-linux-gnueabihf
}

# Клонирование и подготовка исходного кода ядра
prepare_kernel_sources() {
    echo "Подготовка исходного кода ядра..."

    if [ ! -d "$KERNEL_DIR" ]; then
        git clone --depth 1 --branch rpi-6.1.y "$KERNEL_REPO" "$KERNEL_DIR"
    fi

    cd "$KERNEL_DIR"

    # Очистка предыдущей сборки
    make clean
    make mrproper

    # Загрузка конфигурации
    make ARCH="$KERNEL_ARCH" "$DEFCONFIG"
}

# Настройка ядра (опционально)
configure_kernel() {
    if [ "${INTERACTIVE_CONFIG:-false}" = "true" ]; then
        echo "Запуск интерактивной настройки ядра..."
        make ARCH="$KERNEL_ARCH" menuconfig
    fi
}

# Сборка ядра
build_kernel() {
    echo "Сборка ядра Linux..."

    local jobs=$(nproc)

    # Сборка ядра
    make ARCH="$KERNEL_ARCH" -j"$jobs" "$IMAGE_NAME"

    # Сборка модулей
    make ARCH="$KERNEL_ARCH" -j"$jobs" modules

    # Сборка Device Tree файлов
    make ARCH="$KERNEL_ARCH" -j"$jobs" dtbs
}

# Установка ядра
install_kernel() {
    echo "Установка ядра..."

    # Создание директорий
    mkdir -p "$INSTALL_DIR"
    mkdir -p "$MODULES_DIR"

    # Установка образа ядра
    cp "arch/$KERNEL_ARCH/boot/$IMAGE_NAME" "$INSTALL_DIR/kernel8.img"

    # Установка модулей
    make ARCH="$KERNEL_ARCH" modules_install INSTALL_MOD_PATH="/"

    # Установка Device Tree файлов
    make ARCH="$KERNEL_ARCH" dtbs_install INSTALL_DTBS_PATH="/boot/firmware/overlays"

    # Обновление зависимостей модулей
    depmod -a
}

# Создание initramfs (опционально)
create_initramfs() {
    if [ "${CREATE_INITRAMFS:-false}" = "true" ]; then
        echo "Создание initramfs..."

        # Обновление initramfs
        update-initramfs -c -k "$(make kernelrelease)"

        # Копирование initramfs в boot директорию
        cp "/boot/initrd.img-$(make kernelrelease)" "$INSTALL_DIR/"
    fi
}

# Настройка загрузчика
configure_bootloader() {
    local config_file="$INSTALL_DIR/config.txt"

    echo "Настройка параметров загрузки..."

    # Добавление параметров ядра в config.txt
    cat >> "$config_file" << EOF

# Параметры ядра Raspberry Pi
# Автоматически сгенерировано rpi-image-gen

kernel=kernel8.img
arm_64bit=1
initramfs initrd.img followkernel
EOF

    # Добавление device tree параметров
    if [ -n "${DEVICE_TREE:-}" ]; then
        echo "device_tree=$DEVICE_TREE" >> "$config_file"
    fi
}

# Проверка работоспособности установленного ядра
verify_kernel_installation() {
    echo "Проверка установки ядра..."

    local kernel_version
    kernel_version=$(make kernelrelease)

    # Проверка наличия образа ядра
    if [ ! -f "$INSTALL_DIR/kernel8.img" ]; then
        echo "ОШИБКА: Образ ядра не найден"
        return 1
    fi

    # Проверка модулей
    if [ ! -d "$MODULES_DIR/$kernel_version" ]; then
        echo "ОШИБКА: Модули ядра не установлены"
        return 1
    fi

    # Проверка конфигурации загрузки
    if ! grep -q "kernel=kernel8.img" "$INSTALL_DIR/config.txt"; then
        echo "ОШИБКА: Конфигурация загрузчика не обновлена"
        return 1
    fi

    echo "Ядро версии $kernel_version успешно установлено"
    return 0
}

# Основная функция
main() {
    echo "Сборка и установка Raspberry Pi Linux Kernel"

    detect_system_info
    install_kernel_dependencies
    prepare_kernel_sources
    configure_kernel
    build_kernel
    install_kernel
    create_initramfs
    configure_bootloader

    if verify_kernel_installation; then
        echo "Ядро Linux успешно установлено"
        return 0
    else
        echo "Обнаружены проблемы с установкой ядра"
        return 1
    fi
}

main "$@"
```

#### Интеграция ядра в слой rpi-image-gen:
```yaml
# METABEGIN
# X-Env-Layer-Name: raspberrypi-kernel
# X-Env-Layer-Category: extension
# X-Env-Layer-Description: Интеграция Raspberry Pi Linux kernel в образ
# X-Env-Layer-Version: 1.0.0
# X-Env-Layer-Requires: essential,bootloader
# X-Env-VarPrefix: kernel
#
# X-Env-Var-kernel_branch: rpi-6.1.y
# X-Env-Var-kernel_branch-Description: Ветка ядра для сборки
# X-Env-Var-kernel_branch-Required: false
# X-Env-Var-kernel_branch-Valid: rpi-6.1.y,rpi-6.6.y,rpi-6.8.y
#
# X-Env-Var-kernel_defconfig: auto
# X-Env-Var-kernel_defconfig-Description: Конфигурация ядра (auto для автоматического определения)
# X-Env-Var-kernel_defconfig-Required: false
#
# X-Env-Var-kernel_install_modules: true
# X-Env-Var-kernel_install_modules-Description: Устанавливать модули ядра
# X-Env-Var-kernel_install_modules-Required: false
# X-Env-Var-kernel_install_modules-Valid: true,false
#
# X-Env-Var-kernel_create_initramfs: false
# X-Env-Var-kernel_create_initramfs-Description: Создавать initramfs
# X-Env-Var-kernel_create_initramfs-Required: false
# X-Env-Var-kernel_create_initramfs-Valid: true,false
# METAEND
---
mmdebstrap:
  packages:
    - git
    - build-essential
    - bc
    - bison
    - flex
    - libssl-dev
    - libncurses5-dev
    - crossbuild-essential-arm64
    - raspberrypi-kernel-headers
  customize-hooks:
    - |
      # Сборка и установка Raspberry Pi kernel
      setup_raspberrypi_kernel() {
          echo "Настройка Raspberry Pi kernel"

          # Определение архитектуры и модели
          detect_pi_info() {
              if [ "$(uname -m)" = "aarch64" ]; then
                  ARCH="arm64"
                  IMAGE="Image"
              else
                  ARCH="arm"
                  IMAGE="zImage"
              fi

              if grep -q "Pi 5" /proc/device-tree/model 2>/dev/null; then
                  DEFCONFIG="bcm2712_defconfig"
              elif grep -q "Pi 4" /proc/device-tree/model 2>/dev/null; then
                  DEFCONFIG="bcm2711_defconfig"
              else
                  DEFCONFIG="bcm2709_defconfig"
              fi

              if [ "${IGconf_kernel_kernel_defconfig}" != "auto" ]; then
                  DEFCONFIG="${IGconf_kernel_kernel_defconfig}"
              fi
          }

          detect_pi_info

          # Клонирование исходного кода ядра
          KERNEL_DIR="/opt/rpi-kernel"
          if [ ! -d "$KERNEL_DIR" ]; then
              git clone --depth 1 --branch "${IGconf_kernel_kernel_branch:-rpi-6.1.y}" \
                  https://github.com/raspberrypi/linux.git "$KERNEL_DIR"
          fi

          cd "$KERNEL_DIR"

          # Сборка ядра
          echo "Сборка ядра для архитектуры $ARCH с конфигурацией $DEFCONFIG"

          make ARCH="$ARCH" "$DEFCONFIG"
          make ARCH="$ARCH" -j"$(nproc)" "$IMAGE"

          if [ "${IGconf_kernel_kernel_install_modules:-true}" = "true" ]; then
              make ARCH="$ARCH" -j"$(nproc)" modules
              make ARCH="$ARCH" modules_install
          fi

          # Установка ядра
          mkdir -p /boot/firmware
          cp "arch/$ARCH/boot/$IMAGE" /boot/firmware/kernel8.img

          # Создание initramfs если требуется
          if [ "${IGconf_kernel_kernel_create_initramfs:-false}" = "true" ]; then
              make ARCH="$ARCH" dtbs
              update-initramfs -c -k "$(make kernelrelease)"
          fi

          # Настройка загрузчика
          cat >> /boot/firmware/config.txt << EOF

# Raspberry Pi Kernel Integration
# Добавлено rpi-image-gen расширением
kernel=kernel8.img
arm_64bit=1
EOF

          # Создание скрипта обновления ядра
          cat > /usr/local/bin/update-rpi-kernel.sh << 'KERNEL_EOF'
#!/bin/bash
# Скрипт обновления Raspberry Pi kernel

set -euo pipefail

KERNEL_DIR="/opt/rpi-kernel"
LOG_FILE="/var/log/rpi-kernel-update.log"

log() {
    echo "$(date '+%Y-%m-%d %H:%M:%S') $*" | tee -a "$LOG_FILE"
}

update_kernel() {
    log "Проверка обновлений ядра"

    cd "$KERNEL_DIR"

    # Получение последних изменений
    git fetch origin
    git checkout "${IGconf_kernel_kernel_branch:-rpi-6.1.y}"
    git pull --ff-only

    # Пересборка ядра
    make clean
    make ARCH="$(uname -m | sed 's/aarch64/arm64/;s/armv7l/arm/')" "$(make kernelrelease | cut -d- -f2-)_defconfig"
    make -j"$(nproc)" "$(uname -m | sed 's/aarch64/Image/;s/armv7l/zImage/')"

    if [ "${IGconf_kernel_kernel_install_modules:-true}" = "true" ]; then
        make -j"$(nproc)" modules
        make modules_install
    fi

    # Установка нового ядра
    cp "arch/$(uname -m | sed 's/aarch64/arm64/;s/armv7l/arm/')/boot/$(uname -m | sed 's/aarch64/Image/;s/armv7l/zImage/')" /boot/firmware/kernel8.img

    log "Ядро успешно обновлено до версии $(make kernelrelease)"
}

update_kernel
KERNEL_EOF

          chmod +x /usr/local/bin/update-rpi-kernel.sh

          # Настройка периодического обновления (опционально)
          if [ "${IGconf_kernel_auto_update:-false}" = "true" ]; then
              cat > /etc/cron.monthly/update-rpi-kernel << 'CRON_EOF'
#!/bin/bash
/usr/local/bin/update-rpi-kernel.sh
CRON_EOF
              chmod +x /etc/cron.monthly/update-rpi-kernel
          fi
      }

      setup_raspberrypi_kernel
```

#### Диагностика и мониторинг ядра:
```bash
#!/bin/bash
# kernel-diagnostics.sh

# Диагностика состояния Raspberry Pi kernel

check_kernel_version() {
    echo "=== Информация о ядре ==="

    uname -a

    echo -e "\nВерсия ядра:"
    uname -r

    echo -e "\nАрхитектура:"
    uname -m
}

check_kernel_modules() {
    echo "=== Модули ядра ==="

    echo "Загруженные модули:"
    lsmod | wc -l
    echo "из них специфичных для Raspberry Pi:"
    lsmod | grep -c bcm || echo "0"

    echo -e "\nНе загруженные модули:"
    find /lib/modules/$(uname -r) -name "*.ko" | wc -l
}

check_kernel_config() {
    echo "=== Конфигурация ядра ==="

    local config_file="/boot/firmware/config.txt"

    if [ -f "$config_file" ]; then
        echo "Параметры ядра в config.txt:"
        grep -E "^kernel|^arm_64bit|^initramfs" "$config_file" || echo "Параметры ядра не найдены"
    fi

    echo -e "\nАктивные параметры ядра:"
    cat /proc/cmdline
}

check_kernel_performance() {
    echo "=== Производительность ядра ==="

    echo "Время работы системы:"
    uptime

    echo -e "\nЗагрузка CPU:"
    top -bn1 | head -3

    echo -e "\nИспользование памяти:"
    free -h

    echo -e "\nДиск I/O:"
    iostat -x 1 1 | tail -1 | awk '{print "Read: "$4" KB/s, Write: "$5" KB/s"}'
}

check_kernel_errors() {
    echo "=== Ошибки ядра ==="

    echo "Последние ошибки в dmesg:"
    dmesg | grep -i error | tail -5 || echo "Ошибки не найдены"

    echo -e "\nПредупреждения ядра:"
    dmesg | grep -i warning | tail -3 || echo "Предупреждения не найдены"

    echo -e "\nПроблемы с оборудованием:"
    dmesg | grep -E "(bcm|i2c|spi|gpio)" | grep -i fail | tail -3 || echo "Проблемы не найдены"
}

main() {
    check_kernel_version
    echo ""
    check_kernel_modules
    echo ""
    check_kernel_config
    echo ""
    check_kernel_performance
    echo ""
    check_kernel_errors
}

main "$@"
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано на основе анализа репозитория: https://github.com/raspberrypi/linux