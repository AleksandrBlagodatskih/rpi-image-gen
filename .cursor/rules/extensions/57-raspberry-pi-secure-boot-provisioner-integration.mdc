---
alwaysApply: ${cursor.file.path.includes("secure") || cursor.file.content.includes("boot")}
---


# Правило 57: Интеграция Raspberry Pi Secure Boot Provisioner

## Назначение правила
Определяет стандарты интеграции инструментов secure boot и шифрования файловых систем Raspberry Pi в расширения rpi-image-gen для обеспечения безопасности загрузки и данных.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Интеграция Raspberry Pi Secure Boot Provisioner

### Настройка secure boot в процессе сборки

#### Создание ключей и сертификатов для secure boot:
```bash
#!/bin/bash
# setup-secure-boot.sh

set -euo pipefail

# Конфигурация
SECURE_BOOT_DIR="/opt/rpi-secure-boot"
KEYS_DIR="$SECURE_BOOT_DIR/keys"
CERTS_DIR="$SECURE_BOOT_DIR/certs"
SCRIPTS_DIR="$SECURE_BOOT_DIR/scripts"

# Создание инфраструктуры ключей
create_secure_boot_infrastructure() {
    echo "Создание инфраструктуры secure boot..."

    # Создание директорий
    mkdir -p "$KEYS_DIR" "$CERTS_DIR" "$SCRIPTS_DIR"

    # Генерация корневого ключа (RSA 2048)
    openssl genrsa -out "$KEYS_DIR/root.key" 2048

    # Создание корневого сертификата
    openssl req -new -x509 -key "$KEYS_DIR/root.key" -out "$CERTS_DIR/root.crt" \
        -subj "/C=GB/ST=Cambridge/L=Cambridge/O=Raspberry Pi/OU=Secure Boot/CN=Root CA" \
        -days 3650

    # Генерация ключа для подписи bootloader
    openssl genrsa -out "$KEYS_DIR/bootloader.key" 2048

    # Создание запроса на сертификат для bootloader
    openssl req -new -key "$KEYS_DIR/bootloader.key" -out "$CERTS_DIR/bootloader.csr" \
        -subj "/C=GB/ST=Cambridge/L=Cambridge/O=Raspberry Pi/OU=Secure Boot/CN=Bootloader"

    # Подписание сертификата bootloader корневым ключом
    openssl x509 -req -in "$CERTS_DIR/bootloader.csr" -CA "$CERTS_DIR/root.crt" \
        -CAkey "$KEYS_DIR/root.key" -CAcreateserial -out "$CERTS_DIR/bootloader.crt" \
        -days 3650

    # Генерация ключа для подписи OS
    openssl genrsa -out "$KEYS_DIR/os.key" 2048

    # Создание запроса на сертификат для OS
    openssl req -new -key "$KEYS_DIR/os.key" -out "$CERTS_DIR/os.csr" \
        -subj "/C=GB/ST=Cambridge/L=Cambridge/O=Raspberry Pi/OU=Secure Boot/CN=Operating System"

    # Подписание сертификата OS
    openssl x509 -req -in "$CERTS_DIR/os.csr" -CA "$CERTS_DIR/root.crt" \
        -CAkey "$KEYS_DIR/root.key" -CAcreateserial -out "$CERTS_DIR/os.crt" \
        -days 3650

    echo "Инфраструктура ключей создана"
}

# Настройка OTP (One-Time Programmable) памяти
configure_otp_memory() {
    echo "Настройка OTP памяти для secure boot..."

    # Для Raspberry Pi 4/5 OTP записывается через специальный интерфейс
    # Это делается на реальном устройстве, здесь мы готовим конфигурацию

    cat > "$SCRIPTS_DIR/configure-otp.sh" << 'OTP_EOF'
#!/bin/bash
# Скрипт конфигурации OTP для secure boot

set -euo pipefail

OTP_CONFIG="/boot/firmware/otp-config.txt"
SECURE_BOOT_DIR="/opt/rpi-secure-boot"

# Проверка наличия необходимых инструментов
command -v rpi-otp >/dev/null 2>&1 || {
    echo "Инструмент rpi-otp не найден"
    exit 1
}

# Чтение текущих значений OTP
echo "Текущие значения OTP:"
rpi-otp -r || echo "Не удалось прочитать OTP"

# Запись публичного ключа в OTP (только на реальном устройстве!)
if [ -f "$SECURE_BOOT_DIR/certs/root.crt" ]; then
    echo "OTP конфигурация подготовлена"
    echo "Для применения на реальном устройстве выполните:"
    echo "rpi-otp -w <otp-slot> -f $SECURE_BOOT_DIR/certs/root.crt"
else
    echo "Корневой сертификат не найден"
    exit 1
fi
OTP_EOF

    chmod +x "$SCRIPTS_DIR/configure-otp.sh"
}

# Подготовка bootloader для secure boot
prepare_secure_bootloader() {
    echo "Подготовка bootloader для secure boot..."

    # Создание скрипта подписи bootloader
    cat > "$SCRIPTS_DIR/sign-bootloader.sh" << 'SIGN_EOF'
#!/bin/bash
# Скрипт подписи bootloader

set -euo pipefail

BOOTLOADER_FILE="$1"
SECURE_BOOT_DIR="/opt/rpi-secure-boot"
SIGNED_DIR="/boot/firmware/signed"

mkdir -p "$SIGNED_DIR"

if [ ! -f "$BOOTLOADER_FILE" ]; then
    echo "Файл bootloader не найден: $BOOTLOADER_FILE"
    exit 1
fi

# Вычисление хеша bootloader
BOOT_HASH=$(openssl dgst -sha256 -binary "$BOOTLOADER_FILE" | openssl base64)

# Создание подписи
openssl dgst -sha256 -sign "$SECURE_BOOT_DIR/keys/bootloader.key" \
    -out "$SIGNED_DIR/$(basename "$BOOTLOADER_FILE").sig" "$BOOTLOADER_FILE"

# Копирование оригинального файла
cp "$BOOTLOADER_FILE" "$SIGNED_DIR/"

echo "Bootloader подписан: $(basename "$BOOTLOADER_FILE")"
SIGN_EOF

    chmod +x "$SCRIPTS_DIR/sign-bootloader.sh"
}

# Настройка шифрования файловой системы
setup_encrypted_filesystem() {
    echo "Настройка шифрования файловой системы..."

    # Создание скрипта настройки LUKS
    cat > "$SCRIPTS_DIR/setup-luks.sh" << 'LUKS_EOF'
#!/bin/bash
# Скрипт настройки LUKS шифрования

set -euo pipefail

DEVICE="$1"
MAPPER_NAME="${2:-encrypted}"
SECURE_BOOT_DIR="/opt/rpi-secure-boot"

if [ -z "$DEVICE" ]; then
    echo "Использование: $0 <device> [mapper_name]"
    exit 1
fi

# Генерация ключа шифрования (можно интегрировать с TPM/secure element)
if [ ! -f "$SECURE_BOOT_DIR/luks.key" ]; then
    dd if=/dev/urandom of="$SECURE_BOOT_DIR/luks.key" bs=1 count=32
    chmod 600 "$SECURE_BOOT_DIR/luks.key"
fi

# Создание LUKS контейнера
cryptsetup luksFormat "$DEVICE" "$SECURE_BOOT_DIR/luks.key" \
    --cipher aes-xts-plain64 \
    --key-size 512 \
    --hash sha256 \
    --iter-time 5000 \
    --use-random

# Открытие контейнера
cryptsetup open "$DEVICE" "$MAPPER_NAME" --key-file "$SECURE_BOOT_DIR/luks.key"

echo "LUKS контейнер создан и открыт как /dev/mapper/$MAPPER_NAME"
LUKS_EOF

    chmod +x "$SCRIPTS_DIR/setup-luks.sh"
}

# Основная функция
main() {
    echo "Настройка Raspberry Pi Secure Boot"

    create_secure_boot_infrastructure
    configure_otp_memory
    prepare_secure_bootloader
    setup_encrypted_filesystem

    echo "Secure boot настроен. Ключи и скрипты находятся в $SECURE_BOOT_DIR"
    echo "ВАЖНО: Сохраните резервную копию ключей в безопасном месте!"
}

main "$@"
```

#### Интеграция secure boot в слой rpi-image-gen:
```yaml
# METABEGIN
# X-Env-Layer-Name: raspberrypi-secure-boot
# X-Env-Layer-Category: extension
# X-Env-Layer-Description: Интеграция secure boot и шифрования для Raspberry Pi
# X-Env-Layer-Version: 1.0.0
# X-Env-Layer-Requires: essential,bootloader
# X-Env-VarPrefix: secureboot
#
# X-Env-Var-secureboot_enable: false
# X-Env-Var-secureboot_enable-Description: Включить secure boot
# X-Env-Var-secureboot_enable-Required: false
# X-Env-Var-secureboot_enable-Valid: true,false
#
# X-Env-Var-secureboot_encryption: false
# X-Env-Var-secureboot_encryption-Description: Включить шифрование файловой системы
# X-Env-Var-secureboot_encryption-Required: false
# X-Env-Var-secureboot_encryption-Valid: true,false
#
# X-Env-Var-secureboot_key_size: 2048
# X-Env-Var-secureboot_key_size-Description: Размер ключей RSA
# X-Env-Var-secureboot_key_size-Required: false
# X-Env-Var-secureboot_key_size-Valid: 2048,3072,4096
# METAEND
---
mmdebstrap:
  packages:
    - openssl
    - cryptsetup
    - cryptsetup-initramfs
    - secure-boot-tools
    - raspberrypi-sb-provisioner
  customize-hooks:
    - |
      # Настройка secure boot и шифрования
      setup_secure_boot() {
          echo "Настройка Raspberry Pi secure boot"

          # Создание директории для secure boot
          SECURE_BOOT_DIR="/opt/rpi-secure-boot"
          mkdir -p "$SECURE_BOOT_DIR"/{keys,certs,scripts}

          # Генерация инфраструктуры ключей
          generate_secure_boot_keys() {
              echo "Генерация ключей secure boot"

              # Корневой ключ и сертификат
              openssl genrsa -out "$SECURE_BOOT_DIR/keys/root.key" "${IGconf_secureboot_secureboot_key_size:-2048}"
              openssl req -new -x509 -key "$SECURE_BOOT_DIR/keys/root.key" \
                  -out "$SECURE_BOOT_DIR/certs/root.crt" \
                  -subj "/C=GB/ST=Cambridge/L=Cambridge/O=Raspberry Pi/OU=Secure Boot/CN=Root CA" \
                  -days 3650

              # Ключ для bootloader
              openssl genrsa -out "$SECURE_BOOT_DIR/keys/bootloader.key" "${IGconf_secureboot_secureboot_key_size:-2048}"
              openssl req -new -key "$SECURE_BOOT_DIR/keys/bootloader.key" \
                  -out "$SECURE_BOOT_DIR/certs/bootloader.csr" \
                  -subj "/C=GB/ST=Cambridge/L=Cambridge/O=Raspberry Pi/OU=Secure Boot/CN=Bootloader"
              openssl x509 -req -in "$SECURE_BOOT_DIR/certs/bootloader.csr" \
                  -CA "$SECURE_BOOT_DIR/certs/root.crt" \
                  -CAkey "$SECURE_BOOT_DIR/keys/root.key" \
                  -CAcreateserial -out "$SECURE_BOOT_DIR/certs/bootloader.crt" \
                  -days 3650

              # Ключ для OS
              openssl genrsa -out "$SECURE_BOOT_DIR/keys/os.key" "${IGconf_secureboot_secureboot_key_size:-2048}"
              openssl req -new -key "$SECURE_BOOT_DIR/keys/os.key" \
                  -out "$SECURE_BOOT_DIR/certs/os.csr" \
                  -subj "/C=GB/ST=Cambridge/L=Cambridge/O=Raspberry Pi/OU=Secure Boot/CN=Operating System"
              openssl x509 -req -in "$SECURE_BOOT_DIR/certs/os.csr" \
                  -CA "$SECURE_BOOT_DIR/certs/root.crt" \
                  -CAkey "$SECURE_BOOT_DIR/keys/os.key" \
                  -CAcreateserial -out "$SECURE_BOOT_DIR/certs/os.crt" \
                  -days 3650
          }

          # Настройка шифрования
          setup_disk_encryption() {
              if [ "${IGconf_secureboot_secureboot_encryption:-false}" = "true" ]; then
                  echo "Настройка шифрования диска"

                  # Создание ключа шифрования
                  mkdir -p /etc/keys
                  dd if=/dev/urandom of=/etc/keys/rootfs.key bs=1 count=32

                  # Настройка initramfs для расшифровки
                  echo "cryptsetup" >> /etc/initramfs-tools/modules
                  echo "dm-crypt" >> /etc/initramfs-tools/modules

                  # Обновление initramfs
                  update-initramfs -u
              fi
          }

          # Настройка secure boot в config.txt
          configure_secure_boot() {
              if [ "${IGconf_secureboot_secureboot_enable:-false}" = "true" ]; then
                  echo "Настройка secure boot в config.txt"

                  cat >> /boot/firmware/config.txt << EOF

# Secure Boot Configuration
# Generated by rpi-image-gen secure boot extension
secure_boot=1
secure_boot_key_path=/opt/rpi-secure-boot/certs/root.crt
EOF
              fi
          }

          # Создание утилит управления
          create_management_scripts() {
              # Скрипт проверки состояния secure boot
              cat > /usr/local/bin/check-secure-boot-status.sh << 'STATUS_EOF'
#!/bin/bash
# Проверка состояния secure boot

SECURE_BOOT_DIR="/opt/rpi-secure-boot"

echo "=== Состояние Secure Boot ==="

if [ -d "$SECURE_BOOT_DIR" ]; then
    echo "✓ Директория secure boot существует"

    if [ -f "$SECURE_BOOT_DIR/certs/root.crt" ]; then
        echo "✓ Корневой сертификат найден"
        openssl x509 -in "$SECURE_BOOT_DIR/certs/root.crt" -text -noout | grep -E "(Subject|Not Before|Not After)" | head -3
    else
        echo "✗ Корневой сертификат не найден"
    fi

    if [ -f "$SECURE_BOOT_DIR/certs/bootloader.crt" ]; then
        echo "✓ Сертификат bootloader найден"
    else
        echo "✗ Сертификат bootloader не найден"
    fi

    if [ -f "$SECURE_BOOT_DIR/certs/os.crt" ]; then
        echo "✓ Сертификат OS найден"
    else
        echo "✗ Сертификат OS не найден"
    fi
else
    echo "✗ Директория secure boot не существует"
fi

echo -e "\n=== Шифрование ==="
if [ -f /etc/keys/rootfs.key ]; then
    echo "✓ Ключ шифрования найден"
else
    echo "✗ Ключ шифрования не найден"
fi

echo -e "\n=== OTP Память ==="
if command -v rpi-otp >/dev/null 2>&1; then
    echo "✓ Утилита rpi-otp доступна"
    rpi-otp -r 2>/dev/null || echo "Не удалось прочитать OTP"
else
    echo "✗ Утилита rpi-otp не найдена"
fi
STATUS_EOF

              # Скрипт обновления secure boot
              cat > /usr/local/bin/update-secure-boot.sh << 'UPDATE_EOF'
#!/bin/bash
# Обновление компонентов secure boot

set -euo pipefail

SECURE_BOOT_DIR="/opt/rpi-secure-boot"
LOG_FILE="/var/log/secure-boot-update.log"

log() {
    echo "$(date '+%Y-%m-%d %H:%M:%S') $*" | tee -a "$LOG_FILE"
}

update_secure_boot() {
    log "Обновление secure boot компонентов"

    # Резервное копирование текущих ключей
    if [ -d "$SECURE_BOOT_DIR" ]; then
        backup_dir="/tmp/secure-boot-backup-$(date +%Y%m%d-%H%M%S)"
        cp -r "$SECURE_BOOT_DIR" "$backup_dir"
        log "Резервная копия создана: $backup_dir"
    fi

    # Перегенерация сертификатов (если истек срок)
    if openssl x509 -checkend 86400 -in "$SECURE_BOOT_DIR/certs/root.crt" 2>/dev/null; then
        log "Корневой сертификат истекает, требуется обновление"
        # Логика обновления сертификатов
    else
        log "Сертификаты в порядке"
    fi

    # Обновление bootloader подписей
    if [ -d /boot/firmware ]; then
        log "Обновление подписей bootloader"
        # Здесь должна быть логика подписи новых файлов bootloader
    fi

    log "Обновление secure boot завершено"
}

update_secure_boot
UPDATE_EOF

              chmod +x /usr/local/bin/check-secure-boot-status.sh
              chmod +x /usr/local/bin/update-secure-boot.sh
          }

          # Выполнение настройки
          if [ "${IGconf_secureboot_secureboot_enable:-false}" = "true" ]; then
              generate_secure_boot_keys
              configure_secure_boot
              create_management_scripts
          fi

          if [ "${IGconf_secureboot_secureboot_encryption:-false}" = "true" ]; then
              setup_disk_encryption
          fi

          echo "Secure boot настроен"
      }

      setup_secure_boot
```

#### Мониторинг и аудит безопасности:
```bash
#!/bin/bash
# security-audit.sh

# Аудит состояния безопасности Raspberry Pi

check_secure_boot_status() {
    echo "=== Secure Boot Status ==="

    if [ -f /boot/firmware/config.txt ] && grep -q "secure_boot=1" /boot/firmware/config.txt; then
        echo "✓ Secure boot включен в конфигурации"
    else
        echo "✗ Secure boot не включен"
    fi

    if [ -d /opt/rpi-secure-boot ]; then
        echo "✓ Директория secure boot существует"
        ls -la /opt/rpi-secure-boot/certs/ | grep -E "\.(crt|key)$" | wc -l
        echo "сертификатов/ключей найдено"
    else
        echo "✗ Директория secure boot не существует"
    fi
}

check_encryption_status() {
    echo "=== Encryption Status ==="

    if [ -f /etc/keys/rootfs.key ]; then
        echo "✓ Ключ шифрования rootfs найден"
    else
        echo "✗ Ключ шифрования rootfs не найден"
    fi

    if grep -q "cryptsetup" /etc/initramfs-tools/modules; then
        echo "✓ Модули шифрования добавлены в initramfs"
    else
        echo "✗ Модули шифрования не добавлены в initramfs"
    fi

    # Проверка смонтированных зашифрованных устройств
    encrypted_mounts=$(mount | grep -c "type crypt" || echo "0")
    echo "Зашифрованных разделов смонтировано: $encrypted_mounts"
}

check_security_hardening() {
    echo "=== Security Hardening ==="

    # Проверка прав доступа к ключам
    if [ -f /etc/keys/rootfs.key ]; then
        perms=$(stat -c %a /etc/keys/rootfs.key)
        if [ "$perms" = "600" ]; then
            echo "✓ Правильные права доступа к ключу шифрования"
        else
            echo "✗ Неправильные права доступа к ключу шифрования: $perms"
        fi
    fi

    # Проверка наличия security tools
    security_tools=("openssl" "cryptsetup" "rpi-otp")
    for tool in "${security_tools[@]}"; do
        if command -v "$tool" >/dev/null 2>&1; then
            echo "✓ $tool установлен"
        else
            echo "✗ $tool не установлен"
        fi
    done
}

check_audit_logs() {
    echo "=== Security Audit Logs ==="

    log_files=("/var/log/secure-boot-update.log" "/var/log/auth.log")

    for log_file in "${log_files[@]}"; do
        if [ -f "$log_file" ]; then
            echo "✓ Лог файл существует: $log_file"
            echo "Последние записи:"
            tail -3 "$log_file" 2>/dev/null | head -2 || echo "  (пустой или недоступен)"
        else
            echo "✗ Лог файл не существует: $log_file"
        fi
    done
}

main() {
    check_secure_boot_status
    echo ""
    check_encryption_status
    echo ""
    check_security_hardening
    echo ""
    check_audit_logs
}

main "$@"
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано на основе анализа репозитория: https://github.com/raspberrypi/rpi-sb-provisioner