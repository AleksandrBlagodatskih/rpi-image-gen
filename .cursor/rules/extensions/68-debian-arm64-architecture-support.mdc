---
alwaysApply: ${cursor.file.content.includes("arm64") || cursor.file.content.includes("aarch64")}
---


# Правило 68: Поддержка архитектуры Debian arm64

## Назначение правила
Определяет стандарты поддержки ARM64 архитектуры в Debian, включая проверку совместимости процессора и системных требований.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Поддержка архитектуры Debian arm64

### Проверка ARM64 архитектуры

#### Диагностика архитектурной поддержки:
```bash
#!/bin/bash
# Проверка поддержки архитектуры arm64

check_arm64_support() {
    echo "=== Проверка поддержки ARM64 ==="

    # Определение текущей архитектуры
    ARCH=$(uname -m)
    echo "Текущая архитектура: $ARCH"

    if [ "$ARCH" = "aarch64" ]; then
        echo "✓ ARM64 архитектура поддерживается"
        return 0
    elif [ "$ARCH" = "armv7l" ]; then
        echo "⚠ Текущая архитектура: ARM 32-bit (armhf)"
        echo "  Для полной поддержки ARM64 требуется aarch64"
        return 1
    else
        echo "✗ Архитектура $ARCH не поддерживается"
        return 1
    fi
}

# Проверка поддержки инструкций
check_cpu_features() {
    echo -e "\n=== Проверка CPU возможностей ==="

    if [ -f /proc/cpuinfo ]; then
        # Проверка поддержки 64-bit
        if grep -q "aarch64" /proc/cpuinfo 2>/dev/null; then
            echo "✓ CPU поддерживает 64-bit инструкции"
        fi

        # Проверка специфичных расширений
        FEATURES=$(grep "Features" /proc/cpuinfo | head -1 | cut -d: -f2)
        echo "CPU Features: $FEATURES"

        # Рекомендуемые возможности для Debian arm64
        RECOMMENDED_FEATURES=("crc32" "aes" "pmull" "sha1" "sha2")

        for feature in "${RECOMMENDED_FEATURES[@]}"; do
            if echo "$FEATURES" | grep -q "$feature"; then
                echo "✓ $feature поддерживается"
            else
                echo "⚠ $feature не поддерживается (необязательно)"
            fi
        done
    else
        echo "Информация о CPU недоступна"
    fi
}

# Определение модели процессора
detect_cpu_model() {
    echo -e "\n=== Информация о процессоре ==="

    if [ -f /proc/cpuinfo ]; then
        CPU_MODEL=$(grep -m1 "model name" /proc/cpuinfo | cut -d: -f2 | xargs)
        CPU_CORES=$(grep -c "processor" /proc/cpuinfo)
        CPU_FREQ=$(grep -m1 "cpu MHz" /proc/cpuinfo | cut -d: -f2 | xargs)

        echo "Модель процессора: $CPU_MODEL"
        echo "Количество ядер: $CPU_CORES"
        echo "Частота: ${CPU_FREQ}MHz"

        # Определение типа процессора
        if echo "$CPU_MODEL" | grep -qi "raspberry\|bcm"; then
            echo "Тип: Raspberry Pi (Broadcom)"
        elif echo "$CPU_MODEL" | grep -qi "ampere\|altra"; then
            echo "Тип: Серверный (Ampere)"
        elif echo "$CPU_MODEL" | grep -qi "graviton"; then
            echo "Тип: Облачный (AWS Graviton)"
        else
            echo "Тип: Универсальный ARM64"
        fi
    fi
}

main() {
    check_arm64_support
    check_cpu_features
    detect_cpu_model
}

main "$@"
```

#### Платформо-специфичные требования:
```bash
#!/bin/bash
# Проверка платформо-специфичных требований

check_platform_requirements() {
    echo "=== Проверка платформо-специфичных требований ==="

    # Raspberry Pi detection
    if [ -f /proc/device-tree/model ]; then
        if grep -q "Raspberry Pi" /proc/device-tree/model; then
            echo "Обнаружена платформа: Raspberry Pi"

            # Проверка специфичных для Raspberry Pi требований
            check_raspberry_pi_requirements
        fi
    # Generic ARM64 server
    elif [ -f /sys/class/dmi/id/product_name ]; then
        PRODUCT=$(cat /sys/class/dmi/id/product_name)
        echo "Обнаружена платформа: $PRODUCT"

        # Общие проверки для серверных платформ
        check_server_requirements
    else
        echo "Обнаружена платформа: Универсальная ARM64"
        check_generic_arm64_requirements
    fi
}

check_raspberry_pi_requirements() {
    echo "Проверка требований для Raspberry Pi..."

    # Проверка GPIO (для Raspberry Pi)
    if [ -d /sys/class/gpio ]; then
        echo "✓ GPIO поддерживается"
        GPIO_CHIPS=$(ls /sys/class/gpio | grep -c "gpiochip")
        echo "  GPIO чипов: $GPIO_CHIPS"
    else
        echo "⚠ GPIO не доступен (может быть нормально для headless)"
    fi

    # Проверка I2C (для Raspberry Pi)
    if [ -d /sys/class/i2c-adapter ]; then
        echo "✓ I2C поддерживается"
        I2C_BUSES=$(ls /sys/class/i2c-adapter | wc -l)
        echo "  I2C шин: $I2C_BUSES"
    else
        echo "⚠ I2C не доступен"
    fi

    # Проверка SPI (для Raspberry Pi)
    if [ -d /sys/class/spidev ]; then
        echo "✓ SPI поддерживается"
    else
        echo "⚠ SPI не доступен"
    fi
}

check_server_requirements() {
    echo "Проверка требований для серверной платформы..."

    # Проверка NUMA (для многопроцессорных систем)
    if command -v numactl >/dev/null 2>&1; then
        echo "✓ NUMA поддерживается"
        NUMA_NODES=$(numactl --hardware | grep -c "node [0-9]")
        echo "  NUMA узлов: $NUMA_NODES"
    fi

    # Проверка поддержки виртуализации
    if grep -q "svm\|vmx" /proc/cpuinfo 2>/dev/null; then
        echo "✓ Виртуализация поддерживается"
    fi

    # Проверка большого объема памяти
    TOTAL_MEM=$(free -g | awk 'NR==2{print $2}')
    if [ "$TOTAL_MEM" -gt 64 ]; then
        echo "✓ Большой объем памяти поддерживается (${TOTAL_MEM}GB)"
    fi
}

check_generic_arm64_requirements() {
    echo "Проверка требований для универсальной ARM64..."

    # Проверка базовых возможностей
    if [ -c /dev/random ] && [ -c /dev/urandom ]; then
        echo "✓ Генераторы случайных чисел доступны"
    fi

    # Проверка таймеров высокого разрешения
    if [ -f /sys/devices/system/clocksource/clocksource0/current_clocksource ]; then
        CLOCKSOURCE=$(cat /sys/devices/system/clocksource/clocksource0/current_clocksource)
        echo "✓ Таймер высокого разрешения: $CLOCKSOURCE"
    fi

    # Проверка поддержки energy management
    if [ -d /sys/devices/system/cpu/cpu0/cpufreq ]; then
        echo "✓ Управление энергопотреблением поддерживается"
    fi
}

main() {
    check_platform_requirements
}

main "$@"
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано на основе раздела Debian arm64 system requirements