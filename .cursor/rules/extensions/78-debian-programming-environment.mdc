---
alwaysApply: ${cursor.file.content.includes("python") || cursor.file.content.includes("gcc")}
---


# Правило 78: Программирование среды Debian

## Назначение правила
Определяет стандарты настройки среды программирования в Debian GNU/Linux, включая компиляторы, интерпретаторы и инструменты разработки.

## Обязательные требования 🔴 ОБЯЗАТЕЛЬНЫЕ

## Программирование среды Debian

### Настройка компиляторов

#### Установка и оптимизация GCC/Clang для ARM64:
```bash
#!/bin/bash
# Настройка компиляторов в Debian

compiler_setup() {
    echo "=== Настройка компиляторов Debian ARM64 ==="

    # Установка GCC
    install_gcc() {
        echo "Установка GCC..."

        apt-get install -y \
            gcc \
            g++ \
            gcc-multilib \
            g++-multilib \
            gcc-arm-linux-gnueabihf \
            gcc-aarch64-linux-gnu \
            binutils \
            make \
            cmake \
            ninja-build

        # Проверка установки
        gcc --version | head -1
        g++ --version | head -1

        echo "✓ GCC установлен"
    }

    # Установка Clang/LLVM
    install_clang() {
        echo "Установка Clang/LLVM..."

        apt-get install -y \
            clang \
            clang-tools \
            llvm \
            lldb \
            lld \
            clang-format \
            clang-tidy

        # Проверка установки
        clang --version | head -1
        llvm-config --version

        echo "✓ Clang/LLVM установлен"
    }

    # Настройка переменных окружения
    setup_compiler_environment() {
        echo "Настройка переменных окружения для компиляторов..."

        cat >> /etc/profile.d/compiler-env.sh << 'EOF'
# Compiler environment variables for ARM64
export CC=gcc
export CXX=g++
export CFLAGS="-O2 -march=armv8-a -mtune=cortex-a72"
export CXXFLAGS="${CFLAGS}"
export LDFLAGS="-Wl,--as-needed"

# Alternative compilers
export CLANG_CC=clang
export CLANG_CXX=clang++

# Cross-compilation
export CROSS_COMPILE=aarch64-linux-gnu-
export CROSS_CFLAGS="-O2 -march=armv8-a"

# Include paths
export C_INCLUDE_PATH="/usr/include:/usr/local/include"
export CPLUS_INCLUDE_PATH="/usr/include:/usr/local/include"
export LIBRARY_PATH="/usr/lib/aarch64-linux-gnu:/usr/local/lib"
export LD_LIBRARY_PATH="/usr/lib/aarch64-linux-gnu:/usr/local/lib"

# Go environment (if installed)
export GOPATH="$HOME/go"
export GOROOT="/usr/local/go"
export PATH="$PATH:$GOROOT/bin:$GOPATH/bin"

# Rust environment (if installed)
export RUSTUP_HOME="$HOME/.rustup"
export CARGO_HOME="$HOME/.cargo"
export PATH="$PATH:$CARGO_HOME/bin"
EOF

        # Сделать исполняемым
        chmod +x /etc/profile.d/compiler-env.sh

        echo "✓ Переменные окружения настроены"
    }

    # Оптимизация компиляторов
    optimize_compilers() {
        echo "Оптимизация компиляторов для ARM64..."

        # GCC оптимизации
        cat > /etc/gcc.conf << 'EOF'
# GCC optimization flags for ARM64
*flags:
-O2 -march=armv8-a -mtune=cortex-a72 -fstack-protector-strong -fstack-clash-protection

*cxxflags:
${flags} -std=c++17

*ldflags:
-Wl,--as-needed -Wl,--hash-style=gnu
EOF

        # Настройка ccache для ускорения компиляции
        apt-get install -y ccache

        cat > /etc/profile.d/ccache.sh << 'EOF'
# ccache configuration
export CC="ccache gcc"
export CXX="ccache g++"
export PATH="/usr/lib/ccache:$PATH"

# ccache statistics
alias ccache-stats='ccache -s'
alias ccache-clear='ccache -C'
EOF

        # Настройка distcc для распределенной компиляции
        apt-get install -y distcc

        cat > /etc/distcc/hosts << 'EOF'
# Distcc hosts configuration
localhost/4
EOF

        echo "✓ Компиляторы оптимизированы"
    }

    # Основная логика
    case "$1" in
        "gcc")
            install_gcc
            ;;
        "clang")
            install_clang
            ;;
        "env")
            setup_compiler_environment
            ;;
        "optimize")
            optimize_compilers
            ;;
        "all")
            install_gcc
            echo ""
            install_clang
            echo ""
            setup_compiler_environment
            echo ""
            optimize_compilers
            ;;
        *)
            echo "Использование: $0 <gcc|clang|env|optimize|all>"
            exit 1
            ;;
    esac
}

# Настройка интерпретаторов
interpreter_setup() {
    echo "=== Настройка интерпретаторов Debian ARM64 ==="

    # Установка Python
    install_python() {
        echo "Установка Python..."

        apt-get install -y \
            python3 \
            python3-dev \
            python3-pip \
            python3-venv \
            python-is-python3 \
            python3-setuptools \
            python3-wheel

        # Обновление pip
        python3 -m pip install --upgrade pip setuptools wheel

        # Установка virtualenv
        python3 -m pip install virtualenv

        # Проверка установки
        python3 --version
        pip3 --version

        echo "✓ Python установлен"
    }

    # Установка Node.js
    install_nodejs() {
        echo "Установка Node.js..."

        # Использование NodeSource репозитория для свежей версии
        curl -fsSL https://deb.nodesource.com/setup_lts.x | bash -
        apt-get install -y nodejs

        # Установка npm и yarn
        npm install -g npm@latest
        npm install -g yarn

        # Проверка установки
        node --version
        npm --version
        yarn --version

        echo "✓ Node.js установлен"
    }

    # Установка Java
    install_java() {
        echo "Установка Java..."

        apt-get install -y \
            default-jdk \
            default-jre \
            maven \
            gradle

        # Проверка установки
        java -version
        javac -version
        mvn --version

        echo "✓ Java установлен"
    }

    # Установка Go
    install_go() {
        echo "Установка Go..."

        # Скачивание последней версии
        GO_VERSION="1.21.0"
        wget "https://golang.org/dl/go${GO_VERSION}.linux-arm64.tar.gz" -O /tmp/go.tar.gz

        # Установка
        rm -rf /usr/local/go
        tar -C /usr/local -xzf /tmp/go.tar.gz
        rm /tmp/go.tar.gz

        # Настройка PATH
        cat >> /etc/profile.d/go.sh << 'EOF'
export GOROOT=/usr/local/go
export GOPATH=$HOME/go
export PATH=$PATH:$GOROOT/bin:$GOPATH/bin
EOF

        # Проверка установки
        /usr/local/go/bin/go version

        echo "✓ Go установлен"
    }

    # Установка Rust
    install_rust() {
        echo "Установка Rust..."

        # Установка через rustup
        curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y

        # Настройка PATH
        cat >> /etc/profile.d/rust.sh << 'EOF'
export RUSTUP_HOME=$HOME/.rustup
export CARGO_HOME=$HOME/.cargo
export PATH=$PATH:$CARGO_HOME/bin
EOF

        # Настройка для всех пользователей
        cp /root/.cargo/bin/* /usr/local/bin/ 2>/dev/null || true

        # Проверка установки
        source /etc/profile.d/rust.sh
        cargo --version
        rustc --version

        echo "✓ Rust установлен"
    }

    # Основная логика
    case "$1" in
        "python")
            install_python
            ;;
        "nodejs")
            install_nodejs
            ;;
        "java")
            install_java
            ;;
        "go")
            install_go
            ;;
        "rust")
            install_rust
            ;;
        "all")
            install_python
            echo ""
            install_nodejs
            echo ""
            install_java
            echo ""
            install_go
            echo ""
            install_rust
            ;;
        *)
            echo "Использование: $0 <python|nodejs|java|go|rust|all>"
            exit 1
            ;;
    esac
}

# Настройка инструментов разработки
development_tools_setup() {
    echo "=== Настройка инструментов разработки Debian ARM64 ==="

    # Установка основных инструментов
    install_basic_tools() {
        echo "Установка основных инструментов разработки..."

        apt-get install -y \
            git \
            git-lfs \
            subversion \
            mercurial \
            build-essential \
            autoconf \
            automake \
            libtool \
            pkg-config \
            gettext \
            intltool \
            valgrind \
            gdb \
            strace \
            ltrace \
            perf \
            linux-perf \
            htop \
            iotop \
            ncdu \
            tree \
            tmux \
            screen \
            vim \
            emacs-nox \
            neovim \
            jq \
            curl \
            wget \
            httpie \
            sqlite3 \
            redis-tools \
            postgresql-client \
            mysql-client

        echo "✓ Основные инструменты установлены"
    }

    # Установка IDE и редакторов
    install_editors() {
        echo "Установка редакторов и IDE..."

        apt-get install -y \
            vim \
            neovim \
            emacs-nox \
            nano \
            joe

        # Настройка Vim
        cat > /etc/vim/vimrc.local << 'EOF'
" Vim configuration for ARM64 development
syntax on
set number
set tabstop=4
set shiftwidth=4
set expandtab
set autoindent
set smartindent
set hlsearch
set incsearch
set ignorecase
set smartcase
set encoding=utf-8
set fileencoding=utf-8

" Development shortcuts
nmap <F5> :make<CR>
nmap <F6> :make clean<CR>
nmap <F7> :!./a.out<CR>
EOF

        # Настройка NeoVim
        mkdir -p /etc/xdg/nvim
        cat > /etc/xdg/nvim/init.vim << 'EOF'
" NeoVim configuration
syntax on
set number
set tabstop=4
set shiftwidth=4
set expandtab
set autoindent
set smartindent
set hlsearch
set incsearch
set ignorecase
set smartcase

" Plugins would be configured here for advanced users
EOF

        echo "✓ Редакторы настроены"
    }

    # Установка систем мониторинга
    install_monitoring_tools() {
        echo "Установка инструментов мониторинга..."

        apt-get install -y \
            htop \
            iotop \
            ncdu \
            tree \
            progress \
            pv \
            sysstat \
            dstat \
            atop \
            glances \
            nmon \
            powertop

        # Настройка sysstat
        sed -i 's/ENABLED="false"/ENABLED="true"/' /etc/default/sysstat
        systemctl enable sysstat
        systemctl start sysstat

        echo "✓ Инструменты мониторинга установлены"
    }

    # Настройка Git
    configure_git() {
        echo "Настройка Git..."

        # Глобальные настройки
        git config --global init.defaultBranch main
        git config --global core.editor vim
        git config --global merge.tool vimdiff
        git config --global diff.tool vimdiff
        git config --global color.ui auto

        # Полезные алиасы
        git config --global alias.st status
        git config --global alias.ci commit
        git config --global alias.co checkout
        git config --global alias.br branch
        git config --global alias.lg "log --oneline --graph --decorate"

        # Git LFS
        git lfs install

        echo "✓ Git настроен"
    }

    # Основная логика
    case "$1" in
        "basic")
            install_basic_tools
            ;;
        "editors")
            install_editors
            ;;
        "monitoring")
            install_monitoring_tools
            ;;
        "git")
            configure_git
            ;;
        "all")
            install_basic_tools
            echo ""
            install_editors
            echo ""
            install_monitoring_tools
            echo ""
            configure_git
            ;;
        *)
            echo "Использование: $0 <basic|editors|monitoring|git|all>"
            exit 1
            ;;
    esac
}

main() {
    case "$1" in
        "compiler")
            shift
            compiler_setup "$@"
            ;;
        "interpreter")
            shift
            interpreter_setup "$@"
            ;;
        "tools")
            shift
            development_tools_setup "$@"
            ;;
        *)
            echo "Использование: $0 <compiler|interpreter|tools> [параметры]"
            exit 1
            ;;
    esac
}

main "$@"
```

---

## Метрики соблюдения правила 🔴 ОБЯЗАТЕЛЬНЫЕ

### Обязательные элементы
- [ ] Правило изучено и понято командой разработки
- [ ] Соблюдение требования отслеживается в CI/CD
- [ ] Нарушения правила приводят к блокировке мерджа
- [ ] Документация правила актуальна и доступна
- [ ] Регулярный аудит соблюдения правила

### Качественные показатели
- [ ] 100% соблюдение правила в новом коде
- [ ] Отсутствие нарушений в основных ветках
- [ ] Автоматизированная проверка соблюдения
- [ ] Обратная связь от команды разработки
- [ ] Непрерывное улучшение формулировок правила

### Источник
Создано на основе раздела Debian programming environment