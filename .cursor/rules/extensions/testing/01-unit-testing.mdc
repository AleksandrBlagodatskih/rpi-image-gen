---
alwaysApply: ${cursor.file.path.includes("test/") || cursor.file.path.includes("spec/")}
---


# Правило 03.01: Модульное тестирование расширений

## Назначение правила
Определяет обязательные практики модульного тестирования компонентов расширений rpi-image-gen.

## Обязательные проверки модульного тестирования 🔴 ОВЯЗАТЕЛЬНЫЕ

### Базовые проверки слоев
```bash
# Тестирование отдельного слоя
rpi-image-gen layer --describe your-layer-name

# Проверка зависимостей
rpi-image-gen layer --list --depends your-layer-name

# Валидация переменных
rpi-image-gen metadata --lint layer/your-layer.yaml

# Проверка синтаксиса скриптов
bash -n hook-script.sh

# Валидация YAML структуры
python3 -c "import yaml; yaml.safe_load(open('layer.yaml'))"
```

### Обязательные проверки компонентов:
- [ ] Синтаксис всех скриптов в расширении
- [ ] Валидность YAML метаданных
- [ ] Корректность зависимостей слоев
- [ ] Наличие всех обязательных переменных
- [ ] Правильность путей и разрешений файлов

## Проверки метаданных слоев 🔴 ОВЯЗАТЕЛЬНЫЕ

### Валидация METABEGIN блока
```bash
#!/bin/bash
# validate_layer_metadata.sh

validate_layer_metadata() {
    local layer_file="$1"

    echo "Валидация метаданных слоя: $layer_file"

    # Проверка наличия METABEGIN
    if ! grep -q "^# METABEGIN" "$layer_file"; then
        echo "ОШИБКА: Отсутствует METABEGIN блок"
        return 1
    fi

    # Проверка наличия METAEND
    if ! grep -q "^# METAEND" "$layer_file"; then
        echo "ОШИБКА: Отсутствует METAEND блок"
        return 1
    fi

    # Проверка обязательных полей
    local required_fields=("X-Env-Layer-Name" "X-Env-Layer-Category" "X-Env-Layer-Version")

    for field in "${required_fields[@]}"; do
        if ! grep -q "^# $field:" "$layer_file"; then
            echo "ОШИБКА: Отсутствует обязательное поле $field"
            return 1
        fi
    done

    # Валидация формата версии
    local version
    version=$(grep "^# X-Env-Layer-Version:" "$layer_file" | sed 's/.*: //' | tr -d ' ')

    if [[ ! "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
        echo "ОШИБКА: Неверный формат версии: $version"
        return 1
    fi

    echo "Метаданные слоя корректны"
    return 0
}
```

### Проверка переменных конфигурации
```bash
#!/bin/bash
# validate_layer_variables.sh

validate_layer_variables() {
    local layer_file="$1"

    echo "Валидация переменных слоя: $layer_file"

    # Извлечение переменных из METABEGIN блока
    local variables
    variables=$(sed -n '/^# METABEGIN/,/^# METAEND/p' "$layer_file" | grep "^# X-Env-Var-" | sed 's/# X-Env-Var-//' | cut -d: -f1)

    for var in $variables; do
        echo "Проверка переменной: $var"

        # Проверка описания
        if ! grep -q "^# X-Env-Var-$var-Description:" "$layer_file"; then
            echo "ОШИБКА: Отсутствует описание для переменной $var"
            return 1
        fi

        # Проверка обязательности
        local required
        required=$(grep "^# X-Env-Var-$var-Required:" "$layer_file" | sed 's/.*: //' | tr -d ' ')

        if [[ "$required" != "true" && "$required" != "false" ]]; then
            echo "ОШИБКА: Неверное значение Required для переменной $var: $required"
            return 1
        fi

        # Проверка значения по умолчанию для необязательных переменных
        if [[ "$required" = "false" ]]; then
            if ! grep -q "^# X-Env-Var-$var:" "$layer_file"; then
                echo "ПРЕДУПРЕЖДЕНИЕ: Отсутствует значение по умолчанию для необязательной переменной $var"
            fi
        fi
    done

    echo "Переменные слоя корректны"
    return 0
}
```

## Проверки скриптов и функций 🔴 ОВЯЗАТЕЛЬНЫЕ

### Синтаксическая валидация bash скриптов
```bash
#!/bin/bash
# validate_bash_scripts.sh

validate_bash_scripts() {
    local script_dir="$1"

    echo "Валидация bash скриптов в директории: $script_dir"

    local errors=0

    # Поиск всех bash скриптов
    while IFS= read -r -d '' script; do
        echo "Проверка скрипта: $(basename "$script")"

        # Синтаксическая проверка
        if ! bash -n "$script"; then
            echo "ОШИБКА: Синтаксическая ошибка в $script"
            ((errors++))
        fi

        # Проверка наличия shebang
        if ! head -1 "$script" | grep -q "^#!/.*bash"; then
            echo "ПРЕДУПРЕЖДЕНИЕ: Отсутствует shebang в $script"
        fi

        # Проверка set -euo pipefail
        if ! grep -q "set -euo pipefail" "$script"; then
            echo "ПРЕДУПРЕЖДЕНИЕ: Отсутствует 'set -euo pipefail' в $script"
        fi

        # Проверка документирования функций
        local functions
        functions=$(grep "^[a-zA-Z_][a-zA-Z0-9_]*()" "$script" | sed 's/().*//')

        for func in $functions; do
            if ! grep -q "^# Описание:" "$script" && ! grep -A 2 "^$func()" "$script" | grep -q "^[[:space:]]*#"; then
                echo "ПРЕДУПРЕЖДЕНИЕ: Функция $func не документирована"
            fi
        done

    done < <(find "$script_dir" -name "*.sh" -type f -print0)

    if [[ $errors -gt 0 ]]; then
        echo "Найдено $errors ошибок валидации"
        return 1
    fi

    echo "Все скрипты прошли валидацию"
    return 0
}
```

### Проверка зависимостей функций
```bash
#!/bin/bash
# validate_function_dependencies.sh

validate_function_dependencies() {
    local script_file="$1"

    echo "Проверка зависимостей функций в: $(basename "$script_file")"

    # Извлечение определений функций
    local functions
    functions=$(grep "^[a-zA-Z_][a-zA-Z0-9_]*()" "$script_file" | sed 's/().*//')

    # Извлечение вызовов функций
    local calls
    calls=$(grep -o '\b[a-zA-Z_][a-zA-Z0-9_]*\s*(' "$script_file" | sed 's/(.*/(/' | sort | uniq)

    for call in $calls; do
        call=${call%(*)}  # Удаление скобки

        # Проверка, что вызываемая функция определена
        if ! grep -q "^$call()" "$script_file" && ! is_builtin_command "$call"; then
            echo "ПРЕДУПРЕЖДЕНИЕ: Вызов неопределенной функции: $call"
        fi
    done

    echo "Зависимости функций корректны"
}

is_builtin_command() {
    local cmd="$1"

    # Список встроенных команд bash
    local builtins=("echo" "printf" "read" "test" " [" "return" "exit" "cd" "pwd" "export" "unset" "readonly" "shift" "set" "eval" "exec" "source" "command" "type" "hash" "trap" "kill" "wait" "jobs" "fg" "bg" "disown")

    for builtin in "${builtins[@]}"; do
        if [[ "$cmd" = "$builtin" ]]; then
            return 0
        fi
    done

    return 1
}
```

## Проверки структуры файлов 🔴 ОВЯЗАТЕЛЬНЫЕ

### Валидация структуры расширения
```bash
#!/bin/bash
# validate_extension_structure.sh

validate_extension_structure() {
    local extension_dir="$1"

    echo "Валидация структуры расширения: $(basename "$extension_dir")"

    local errors=0

    # Проверка обязательных файлов
    local required_files=("layer/extension.yaml" "README.md")

    for file in "${required_files[@]}"; do
        if [[ ! -f "$extension_dir/$file" ]]; then
            echo "ОШИБКА: Отсутствует обязательный файл: $file"
            ((errors++))
        fi
    done

    # Проверка рекомендуемых директорий
    local recommended_dirs=("config" "scripts" "tests")

    for dir in "${recommended_dirs[@]}"; do
        if [[ ! -d "$extension_dir/$dir" ]]; then
            echo "ПРЕДУПРЕЖДЕНИЕ: Рекомендуется создать директорию: $dir"
        fi
    done

    # Проверка прав доступа к файлам
    while IFS= read -r -d '' file; do
        local perms
        perms=$(stat -c %a "$file")

        # Скрипты должны быть исполняемыми
        if [[ "$file" == *.sh ]]; then
            if [[ "${perms:2:1}" != [1,3,5,7] ]]; then
                echo "ОШИБКА: Скрипт не исполняемый: $file (права: $perms)"
                ((errors++))
            fi
        fi

        # Конфигурационные файлы не должны быть исполняемыми
        if [[ "$file" == *.yaml || "$file" == *.yml || "$file" == *.json ]]; then
            if [[ "${perms:2:1}" != [0,2,4,6] ]]; then
                echo "ПРЕДУПРЕЖДЕНИЕ: Конфигурационный файл исполняемый: $file (права: $perms)"
            fi
        fi

    done < <(find "$extension_dir" -type f -print0)

    if [[ $errors -gt 0 ]]; then
        echo "Найдено $errors ошибок структуры"
        return 1
    fi

    echo "Структура расширения корректна"
    return 0
}
```

## Инструменты модульного тестирования 🔴 ОВЯЗАТЕЛЬНЫЕ

### Автоматизированный тест-сьют
```bash
#!/bin/bash
# run_unit_tests.sh

run_unit_tests() {
    local extension_dir="$1"
    local report_file="/tmp/unit-test-report-$(date +%Y%m%d-%H%M%S).json"

    echo "Запуск модульных тестов для расширения: $(basename "$extension_dir")"

    local tests_passed=0
    local tests_failed=0
    local test_results=()

    # Тест 1: Валидация метаданных
    if validate_layer_metadata "$extension_dir/layer/extension.yaml" 2>/dev/null; then
        test_results+=('{"name": "metadata_validation", "status": "passed"}')
        ((tests_passed++))
    else
        test_results+=('{"name": "metadata_validation", "status": "failed"}')
        ((tests_failed++))
    fi

    # Тест 2: Валидация переменных
    if validate_layer_variables "$extension_dir/layer/extension.yaml" 2>/dev/null; then
        test_results+=('{"name": "variable_validation", "status": "passed"}')
        ((tests_passed++))
    else
        test_results+=('{"name": "variable_validation", "status": "failed"}')
        ((tests_failed++))
    fi

    # Тест 3: Валидация скриптов
    if validate_bash_scripts "$extension_dir" 2>/dev/null; then
        test_results+=('{"name": "script_validation", "status": "passed"}')
        ((tests_passed++))
    else
        test_results+=('{"name": "script_validation", "status": "failed"}')
        ((tests_failed++))
    fi

    # Тест 4: Валидация структуры
    if validate_extension_structure "$extension_dir" 2>/dev/null; then
        test_results+=('{"name": "structure_validation", "status": "passed"}')
        ((tests_passed++))
    else
        test_results+=('{"name": "structure_validation", "status": "failed"}')
        ((tests_failed++))
    fi

    # Генерация отчета
    local test_summary
    test_summary=$(printf '%s,' "${test_results[@]}" | sed 's/,$//')

    cat > "$report_file" << EOF
    {
        "extension": "$(basename "$extension_dir")",
        "timestamp": "$(date -Iseconds)",
        "test_type": "unit",
        "summary": {
            "total": $((tests_passed + tests_failed)),
            "passed": $tests_passed,
            "failed": $tests_failed
        },
        "tests": [${test_summary}]
    }
EOF

    echo "Отчет модульного тестирования: $report_file"
    echo "Пройдено: $tests_passed, Провалено: $tests_failed"

    return $((tests_failed > 0))
}
```

## Метрики качества модульного тестирования 🔴 ОВЯЗАТЕЛЬНЫЕ

### Показатели покрытия
- [ ] Все файлы расширений проверены на синтаксис
- [ ] Все метаданные слоев валидированы
- [ ] Все переменные конфигурации проверены
- [ ] Все зависимости функций верифицированы
- [ ] Структура файлов соответствует стандартам

### Автоматизация
- [ ] Модульные тесты запускаются автоматически
- [ ] Результаты тестирования сохраняются
- [ ] Отчеты о качестве генерируются регулярно
- [ ] Интеграция с CI/CD системами