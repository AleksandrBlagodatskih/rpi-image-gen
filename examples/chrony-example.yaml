# Пример конфигурации с использованием systemd-chrony-min вместо systemd-timesyncd
---
device:
  layer: rpi5  # или другой класс устройства

image:
  layer: image-base
  name: chrony-example
  compression: zstd

layer:
  base: bookworm-minbase
  system: systemd-chrony-min  # Используем chrony вместо systemd-min
  network: systemd-net-min  # Можно использовать вместе с systemd-chrony-min
